# v0.19.99 stabilization release

Date: 2026-09-08

## Scope

The user explicitly requested a bounded wrap-up of the existing website as **v0.19.99**, using the implemented improvements rather than completing the original v0.20.0 roadmap. Preserve local Scion, the 0.18.7 UI baseline, ten material categories, mature editors and applicable exports. The separate ground-up design remains a future implementation handoff.

This release improves reviewed-task synchronization, stale-write protection, teacher-edit preservation, source-review selection/recovery, student exports, objective fidelity and specific worked examples/scoring. Rejected experimental selectors remain outside production. Hosted/free online inference remains disabled; no new adapter is promoted.

## Verification ledger

- Baseline full `npm run check`: passed on f7c1e565 (58 studio, 941 compatibility, 980 Scion tests, plus benchmark checks, typecheck, lint, build and bundle checks).
- Release source checks: `npm run check` passed (58 studio, 941 compatibility, 980 Scion tests plus benchmark tests, typecheck, lint, build and bundle checks). Public changelog metadata was subsequently updated; final lint passed and the final-commit build is checked separately.
- Formatting: passed; 69 recorded research evidence files verified byte-identical, and the frozen 60-case v3 snapshot verification passed.
- Model distribution: all five pinned GGUF shard URLs returned HTTP 200 to HEAD requests with exactly the expected content lengths; this checks availability, not fresh inference or re-hashing the multi-gigabyte model.
- Single-worker compatibility browser suite: running.
- Built-site production/CSP download recovery: pending.
- Live website release identity and browser workflow: pending.
- Deployment and tag: pending.

The original formatting gate exposed 78 unformatted files, mostly recorded research evidence. Raw model receipts, registered inputs, JSON audit records and byte-bound experiment HTML retain their exact bytes through narrow research-path exclusions. Ordinary documentation and the affected source file are formatted; code checks remain enabled.

## Issues found during stabilization

- Student and teacher Word/PDF controls shared test identifiers and the student controls lacked audience-specific accessible names. Give student controls distinct identifiers and names without changing their visible layout; browser coverage now downloads both versions.
- The historical pooling export fixture omitted the distinct-membership premise now required by the compiler. Retain an assertion that the original input cannot produce an automatic shared task; export an explicitly labeled, adapted fixture with the extra premise. The frozen input remains unchanged, and this is not counted as a fresh or original-packet success.
- Chinese reference pages had localized headings while the browser test still required English. Check the actual English/Chinese headings, retaining the separate-answer-page requirement and page-bound checks.
- A rubric visibility test selected a hidden lesson dropdown option instead of the visible rubric metadata. Target the visible content. Replace an incidental exact aggregate-score assertion with the score-display contract; this is not an educational benchmark threshold change.
- The exploratory browser run also experienced development-page reloads while metadata changed. Preserve its failure log and run the settled source again before release.

## Evidence limits

Software and fixture-based browser checks do not prove natural-model or educational acceptance. Prior actual local runs and reviewed-task edit/save/export evidence remain in `research/scion/evaluation/v0.20.0/` and `research/teaching/v0.20.0/`, with first failures retained. Reviewed proposals still need teacher judgment; general automatic source understanding remains unreliable.

The stabilization release does not certify the unfinished 12-unit fresh-model evaluation, all original frozen-v3 completion thresholds, three newly accepted six-lesson courses, independent teacher validation, measured learning outcomes, all-device inference or live Google conversion. Existing output reviews record remaining repetition, dense reference sections and pagination weaknesses. Do not label all generated output classroom-ready merely because it renders or exports.

## Deployment and recovery

Existing production baseline: v0.19.2, commit `3c97bfc9cf1e090d1b6970677cbe988e299b3515`, confirmed from edutool.dev/release.json before release work. Deployment uses the existing GitHub Pages workflow after Fast verification succeeds. The public release manifest must agree with the final source commit, tag and application version.

Keep the previous commit and release available. If deployment has a material regression, restore the previous verified production commit through the deployment workflow, without overwriting teacher project files. The site keeps the existing `.coursemapper` format and recovery paths; users should retain exported project backups.
