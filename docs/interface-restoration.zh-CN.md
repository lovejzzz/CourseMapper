# 0.18.7 界面恢复记录

## 产品范围

2026-09-05，产品所有者明确要求延续 0.18.7 的 deliverables 和界面，并再次确认“整体 UI”，包括首页，全部回滚。后续工作以提升内容、生成可靠性、编辑联动和导出质量为目标，不沿用实验工作台的视觉或交互重设计。

恢复基线为 `27513a835c5fc9a6bd4a2d98813a04a8afbc55d4`，其 package.json 和应用显示版本均为 0.18.7。此次恢复读取实际 Git 源码，不把历史评估文档当作品质证据。

## 恢复内容

- 首页标题、品牌、布局、主题、附件区、示例、登录和入口。
- 原有材料选择与配置流程、十类标准材料和自定义材料。
- Course Map、专用材料视图、聊天修改、项目保存/恢复、版本和联动控件。
- 原有导出侧栏与材料相关的 Word、PDF、CSV、PPTX、Google 和 ZIP 选项。
- 原版应用入口和样式，以及它们依赖的课程模型和运行代码。

实验工作台与质量研究保存于 `f64f6c62`，其源码当前留在 `src/studio/`，生产构建检查阻止其进入原版应用。没有丢弃本地模型或研究数据。免费 Worker 保留，但恢复后的原版应用尚未接入它；不能把此前工作台的在线模型测试当作原版应用已完成接入的证据。

## 验证

- 原版生产构建成功。
- 30 项原有浏览器测试通过，覆盖材料导出、ZIP 范围与错误显示、320/390/768/1024 宽度的工作区、材料/聊天/导出切换。
- 浏览器实际查看恢复后的首页，并进入材料选择，确认全部原有材料入口存在。
- 新增首页回归通过：附件入口、原有材料列表、全选、自定义材料弹窗、返回后保留输入，全程没有请求模型权重或模型推理。共 31 项浏览器测试通过。
- 129 项原版兼容单元测试、55 项实验引擎/网关测试、类型检查、按原版规则恢复的 lint、格式检查、生产构建与首屏预算检查通过。
- 868 个恢复的原版 src 文件与基线逐字节一致，没有顺手修改首页、组件或原有业务代码。
- 本地生产构建初始 JavaScript 为 271.20 KiB、gzip 88.04 KiB；这不是包含后续懒加载界面的全站体积。
- 本地记录保存于 `.audit-work/rebuild-2026-09-05/`。公开部署仍以实际 GitHub 结果和网站 release.json 为准。

浏览器使用一个测试 worker。此轮不启动浏览器模型，也不生成真实课程，避免将 UI 回滚验证与模型质量验收混淆。

## 仍待完成

此前多份真实课程发现了来源推理错误、重复练习、示例泄露答案等问题。课程质量、免费在线推理与原应用的接入、每类材料的教育价值审计仍需继续。0.19.0 最终版尚未发布。
