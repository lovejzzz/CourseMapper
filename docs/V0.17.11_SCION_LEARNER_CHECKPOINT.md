# V0.17.11 Scion Learner Checkpoint

Date: August 3, 2026

- Baseline production package: `/tmp/edutool-v01710-production-final.zip`
- Baseline SHA-256: `a88671dd5d1da57844fecc7b192d23ea461690eb3cc3cada810bad1e272a2a80`
- Baseline deterministic evidence score: **34/100**

- Pre-audit candidate package: `Applied Civic Data Analysis - Course Materials (1).zip`
- Pre-audit SHA-256: `4a2ed350b78c96a707c17f43236f5c702118c92e7564c73c53ce979eb940b4aa`
- Pre-audit deterministic evidence score: **95/100** (rejected after exact-commit review)

- Repaired local package: `Applied Civic Data Analysis - Course Materials (7).zip`
- Repaired local SHA-256: `d945f8cf52a77a92a489dd63d51413d1f85519316b4a9de76dbe8e5ba75106a9`
- Repaired deterministic evidence score: **87/100** under the stricter ruler

- Rejected production candidate 18: `Applied Civic Data Analysis - Course Materials (18).zip`
- Rejected candidate SHA-256: `8bba43eb19d38674d2007529c8c19ac4b8f84cfc37fa2989b09e2103ddbdaa64`

- Rejected production candidate 19 SHA-256: `ceb68c027735c246c818bcdb10fdcd82f2eeb01fc820ee356c4a835af441b003`
- Rejected production candidate 20 SHA-256: `a07739d21c6b2b22990f3272032a4fb4359a984ae81813b1c63d8b4e21ab157b`
- Rejected production candidate 21 SHA-256: `241a750ed94f325da64b18dc50b0e1d61091e353657ccb6be0302f5244232d29`

- Rejected production package: `Applied Civic Data Analysis - Course Materials (3).zip` (candidate 22)
- Rejected candidate SHA-256: `7979ec520b25426528fa744dc38a4b4548d6e9a8d647d02da98683a978f23485`

- Rejected production package: `Applied Civic Data Analysis - Course Materials (4).zip` (candidate 24)
- Rejected candidate SHA-256: `d9b309bcf17546d2f2ffc7e858e642cf5216aa09e2dc8107729ef9221dc86baf`

- Rejected production package: `Applied Civic Data Analysis - Course Materials (5).zip` (candidate 25)
- Rejected candidate SHA-256: `bcb8e36cd11522d960a79345034e2d7c9e77520879ac407b13ca7ddfd3831fd7`

- Rejected checkpoint package: `Applied Civic Data Analysis - Course Materials (6).zip` (candidate 26; content evidence retained)
- Candidate SHA-256: `9735ac3f44dcf259ba373e993174b0ca8faf458fd0ef414ba46abfdad71cfb4a`
- Audited production code commit: `1102fcab8f4218725be3f89b6343272810756aa8`
- Historical production deterministic evidence score: **91/100**

## Goal

Treat Scion as a learner: improve how it finds, retains, applies, checks, and revises evidence without embedding the facts or vocabulary of one course. The acceptance floor is a fresh-package score of at least 60/100, grounding of at least 15/25, assessment coherence of at least 10/15, and zero P0/P1 findings.

## Lane

Course-neutral source research, provenance-preserving claim memory, lesson-evidence revision, rendered-artifact grounding, assessment coherence, deterministic scoring, physical export verification, and honest release evidence.

## Architectural finding

The 34-point ceiling was not principally a formatting problem and was not proof that Gemma alone was incapable. Scion discarded useful source identity between stages, restarted research instead of learning from its portfolio, and graded package wiring rather than the claims and assessment evidence visible in exported files. This made stronger model prose easy to lose and weak output difficult to diagnose.

V0.17.11 changes the learning loop:

1. **Observe:** build a claim portfolio from retrieved source bytes, preserving canonical source ID, title, URL, locator, exact supported claim, and admission result.
2. **Plan:** compare that portfolio with the course and lesson evidence needs. Research is selected by domain and unresolved concept family, not by a hard-coded course corpus.
3. **Compose:** use admitted claims in lesson kernels while preserving citation identity. Integrative work may reuse already admitted evidence, but cannot manufacture a new support relationship.
4. **Check:** extract visible text from the generated DOCX, PPTX, and text artifacts. A claim earns grounding credit only when the exported wording can be bound back to the exact source receipt.
5. **Revise:** preserve successful lesson evidence, target unresolved lessons, and accept a revision only when the support ledger strictly improves.
6. **Assess:** reconstruct objective → task → student evidence → rubric criterion relationships from the final package. Alignment credit depends on the evidence students are actually asked to produce.

## Course-neutral implementation

- `algiKernelComposer.js` composes exact source-supported statements through general grammatical and claim-boundary rules rather than course-specific facts.
- `algiResearchPlan.js` and `algiResearch.js` select provider order and source-title families from course and lesson context.
- `sourceLedger.js` binds claims to the complete retrieved-snapshot digest, source identity, locator, quote byte offsets, quote hash, and rendered artifact hash, and rejects any mismatched chain.
- `blueprintEnrichmentPass.js`, `scionEvidenceLayer.js`, and `nativeGraphAuthoring.js` preserve canonical citations through enrichment and material assembly.
- `exportRenderedTextAudit.js` reads visible Office text; `packageZipExporter.js` reconstructs support and assessment receipts from the assembled archive.
- `assessmentCoherence.js` evaluates whether objectives, assigned tasks, expected student evidence, and rubric criteria agree.
- `automatedReadinessSignal.js` awards bounded positive evidence points and retains separate curriculum, texture, and integrity dimensions.

The initial exact-commit audit found five audited-course retrieval branches. They were removed before release. Generic noun-phrase canonicalization and coarse domain qualifiers now operate uniformly across unseen programming, linear-algebra, and environmental-policy topics; no course title, lesson sequence, fact, or source quotation from the test course remains embedded as a product rule. The optional adapter remains inactive and the public Gemma weights are unchanged.

## Honest repaired local result

The first 95-point package was not accepted. Roundtable found that its retrieval policy was shaped around the audited course, its source receipt hashed only the copied quote, and deleting an assessment declaration could shrink the denominator. After those loopholes were removed, a new browser origin downloaded the public model and generated a clean six-lesson package from the repaired code:

| Dimension              |      Score | Evidence                                                   |
| ---------------------- | ---------: | ---------------------------------------------------------- |
| Curriculum structure   |      25/25 | Ordered requested lesson sequence                          |
| Evidence grounding     |      13/25 | 3/6 lessons; 49 exact supported claims across 17 artifacts |
| Reader-visible texture |      19/20 | 96/100 texture metric                                      |
| Assessment coherence   |      15/15 | 30/30 reconstructed relationships                          |
| Package integrity      |      15/15 | Structure 100, format 100, identity 100                    |
| **Total**              | **87/100** | Grader 1.15.9 / held-out ruler V53                         |

`PACKAGE_READINESS.json` reports 38/38 export checks and verified download safety. `QUALITY_FINDINGS.json` is empty: zero P0, P1, and P2 findings. The repaired package records six independently derived assessment obligations and 30/30 objective-task-evidence-rubric checks. Each promoted source claim carries the admitted open-source normalized snapshot bytes, complete snapshot digest and byte length, quote offsets and hash, and rendered artifact hash. The independent `audit:package-evidence` verifier binds a caller-supplied immutable pre-export course-contract root, replays 3 source snapshots and 49 claims into visible Office text, and independently recomputes all five assessment predicates across 27 source/assessment Office artifacts directly from ZIP bytes with zero failures.

The first repaired render exposed a long-title masthead collision in LibreOffice that the deterministic score did not detect. The shared DOCX shell now chooses a course-agnostic title-size tier from normalized title length. The exact final ZIP converted all 50 Office files to 225 PDF pages without conversion errors; inspection covered the course map, syllabus boundaries, long and normal lesson mastheads, slide body and footer, assignment boundaries, and landscape rubric matrices. The long title now renders inside its masthead with no collision.

Three of six lessons remained without accepted source evidence after the fresh browser run encountered public-provider limits. The 13/25 score correctly retains those penalties instead of assuming that code capability proves live provider availability. The total score clears the requested 60-point goal, but the release checkpoint still requires a fresh production package to clear the separately declared 15/25 grounding floor.

## Candidate 25 production evidence (checkpoint rejected)

The deployed `cba17825` code generated a fresh production package in a new browser session from the precommitted six-lesson Applied Civic Data Analysis prompt. Earlier candidates exposed malformed prose, masthead overflow, physically portrait rubrics, repeated speaker-note imperatives, stale proof binding, clipped source labels and footers, ambiguous counters, and incomplete accessibility denominators. Candidate 22 reached 91/100 but was rejected after code review proved the PowerPoint accessibility scan was vacuous for shape-only decks and inspected the wrong OOXML picture element. Candidate 24 also reached 91/100 but Roundtable rejected it: native tables and matrices were omitted from the denominator, complex inline mathematics could disappear before OOXML serialization, accessibility findings could remain non-blocking warnings, decorative text could suppress objects, and unreadable inputs could fail open. Candidate 25 repaired those classes under V63 and remains valid content evidence, but the final exact-code audit proved its 1,565-object accessibility denominator was still selected by producer-assigned names. The V64 repair now audits every emitted structural PowerPoint object; candidate 25 is therefore rejected as checkpoint evidence pending a fresh deployment and package.

| Dimension              |      Score | Evidence                                                   |
| ---------------------- | ---------: | ---------------------------------------------------------- |
| Curriculum structure   |      25/25 | Ordered requested six-lesson sequence                      |
| Evidence grounding     |      17/25 | 4/6 lessons; 60 exact supported claims across 20 artifacts |
| Reader-visible texture |      19/20 | 96/100 texture metric                                      |
| Assessment coherence   |      15/15 | 30/30 reconstructed relationships; 6/6 obligations         |
| Package integrity      |      15/15 | Structure 100, format 100, identity 100                    |
| **Total**              | **91/100** | Historical content evidence; checkpoint rejected under V64 |

The production dashboard and physical ZIP agree on readiness 91/100, content conformance 99/A, verified download safety, and zero content P0/P1 findings. The independent verifier replayed five source snapshots, 60 claims, 29 source/assessment Office artifacts, and all six assessment obligations directly from ZIP bytes. It bound package SHA `bcb8e36c…`, evidence-bundle SHA `f5ce9526…`, score-ledger SHA `a8453574…`, immutable course-contract SHA `5182b64b…`, and caller-supplied release-attestation SHA `1de3892a…` with zero failures. Re-running the new structural Office scan against those same bytes correctly fails all six decks with `semantic-object-without-description`, so those otherwise valid receipts do not constitute checkpoint acceptance.

LibreOffice converted every one of the 50 Office files to 221 PDF pages. Representative inspection covered the syllabus, lesson-3 title, evidence table, worked example, and closing slides, plus the lesson-6 rubric cover and matrix. All six rubrics remain physically landscape and readable. The fail-closed package scan audited all 49 DOCX/PPTX artifacts and found 1,565 tracked semantic objects with 1,565 non-empty descriptions across the six decks, including 11 native tables, 4 matrices, 22 connectors, 36 decorative backgrounds, and 78 native slide-counter labels. There were no media pictures in this particular course, so a separate corruption test remains the evidence for the `p:pic` path. The balanced `Expression_(computer_science)` URL, nested source metadata, and repaired coordinated-verb grammar remain intact in final package bytes.

The candidate's 91 remains deliberately below 100. Two lessons did not obtain admissible live-source evidence, so evidence grounding retains an eight-point penalty. No code capability or model fluency was substituted for evidence that was absent from the physical package. The assessment replay is intentionally bounded: each graded obligation must preserve the immutable pre-export contract objective, task identity, student evidence, rubric identity, and observable criteria; it does not claim that every additional lesson objective is assessed.

## Candidate 26 production evidence (checkpoint reopened)

Exact CI run `30847471858` passed all nine lanes for commit `1102fcab8f4218725be3f89b6343272810756aa8`; exact deployment run `30848041236` then published that commit before generation. A fresh browser session generated candidate 26 from the frozen prompt in 37 seconds. The model weights and optional-adapter state remained unchanged.

Candidate 26 independently replayed at **91/100**: curriculum 25/25, grounding 17/25, texture 19/20, assessment coherence 15/15, and export integrity 15/15. The replay verified 5 sources, 60 exact claims, 29 source/assessment artifacts, and all 6 assessment obligations with zero failures. It binds package SHA `9735ac3f…`, evidence-bundle SHA `02e4da53…`, score-ledger SHA `a8453574…`, course-contract SHA `5182b64b…`, and candidate-attestation SHA `77673e62…`.

The fail-closed Office scan audited all 49 DOCX/PPTX artifacts and derived its PowerPoint denominator directly from exported OOXML: 1,891 raw `p:sp`, `p:pic`, `p:cxnSp`, and `p:graphicFrame` objects produced 1,891 audited entries and 1,891 non-empty descriptions, with zero accessibility failures. LibreOffice converted those files plus the course-map XLSX into 50 PDFs and 221 pages. Representative inspection covered the course map, syllabus, lesson-3 title/evidence/worked-example/closing slides, and the lesson-6 rubric cover and landscape matrix; no clipping, collision, orientation, or conversion failure was found.

Candidate 26 clears the declared content floor but is not accepted as the final checkpoint. Final falsification found that removing an entire `cNvPr` metadata node could make a malformed structural object disappear from V64's denominator. V65 preserves one denominator entry for every exported structural subtree even when metadata is absent, adds a direct corruption test, and requires the production validator to cross-check the separate export-boundary digest and file count. Two lessons still lack admissible live-source evidence, which is why grounding remains 17/25 and total readiness remains 91 rather than 100. No deterministic score substitutes for independent instructor judgment, accessibility certification, classroom outcomes, or a sealed external held-out comparison.

## Candidate 27 production checkpoint

Commit `ccbba4d74397140aafc8dbc6a820995aa9582231` is the frozen code boundary. Exact [Fast verification run 30850902593](https://github.com/lovejzzz/CourseMapper/actions/runs/30850902593) passed all nine lanes, including the V65 tracked-ruler, raw-denominator, attestation-root, fixed-proof-path, grader-receipt, and export-boundary checks. Exact [deployment run 30851467662](https://github.com/lovejzzz/CourseMapper/actions/runs/30851467662) then published that commit before generation.

A fresh browser origin generated candidate 27 from the same frozen six-lesson prompt in 29 seconds. The downloaded ZIP hashes to `e07c90e75d12d5978726fe6ed96e8fc50083e42429f375bf9e1fc81e75ec5289`. Independent replay against the caller-supplied production course contract and tracked production attestation passed with zero failures: 5 admitted sources, 60 exact claims, 29 source/assessment artifacts, all 6 assessment obligations, evidence-bundle SHA `02e4da533a4f51d56d0ce0ff6bd369d9f313e315960113bb695ab179f4ba5947`, score-ledger SHA `a8453574a67ac8aba04190a73b9f76802c0d4e176ac6a03a12cfb6e18c9c1cd0`, contract SHA `5182b64ba6314013de3e6b7e473f807d98a2fbcf0fb9dbb6d8df11f896c45d60`, and production-attestation SHA `6d398352e449644dbadc2b2fbae16e0e00a2c8bb6a2e817a9af93d41fbc86dbe`.

The physical Office audit covered all 49 DOCX/PPTX artifacts. It independently counted 1,891 raw `p:sp`, `p:pic`, `p:cxnSp`, and `p:graphicFrame` subtrees, retained 1,891 audited entries, found 1,891 non-empty descriptions, and reported zero accessibility or phrase-repetition failures. LibreOffice converted those files plus the course-map XLSX into 50 PDFs and 221 pages. Representative inspection covered the course map, syllabus, a lesson-4 deck page, and the lesson-6 capstone assignment and rubric; no clipping, collision, orientation, or conversion failure was found.

Candidate 27 is accepted as the deterministic Scion learner checkpoint at **91/100**: curriculum 25/25, grounding 17/25, texture 19/20, assessment coherence 15/15, and export integrity 15/15. The score remains honest and bounded. Two lessons still lack admissible live-source evidence, some prose remains conservative and generic, the optional adapter remains inactive, and the Gemma weights are unchanged. This checkpoint does not claim independent instructor approval, accessibility certification, classroom outcomes, or paid-reference parity.

## Roundtable disposition

Session `681bd065-f174-4971-80f1-1fecb81ed21c` ran six design rounds plus two completion audits against repository code. The room converged on provenance-preserving claim memory, portfolio-aware retrieval, exported-byte verification, assessment evidence chains, a preserve/revise scheduler, and a scorer separated from generation. Both completion audits reported zero material concerns for the implementation plan.

That design session was followed by exact-code review. The first pass found three P1 blockers; a later pass correctly rejected digest-and-offset metadata that omitted the source bytes and found one stale proof note that still treated 95 as accepted. The final code includes replayable snapshot bytes and a standalone ZIP verifier, and the proof note now treats 95 only as rejected history. V51 and V52 remain immutable rejected rulers. V53 binds grader 1.15.9 and the independently replayable implementation without inheriting an earlier score.

Session `d1a89853-bf12-4373-b88b-c05f94426df6` then spent six more rounds trying to falsify commit `50db5720`. It succeeded: a self-consistently re-signed manifest could separate a genuine quote from a fabricated claim, assert five assessment checks without replaying them, relabel an inadmissible source, or omit evidence rows while retaining a pass. The room correctly returned **CHECKPOINT NO**. The post-room implementation now extracts visible Office text independently, requires exact claim/quote identity, replays source admission and all five assessment predicates, pins objectives plus assessment identity in the external contract, and binds the physical ZIP, evidence bundle, score ledger, counts, score, grader version, and contract into one release attestation. Nine adversarial verifier tests cover byte tampering, semantic re-signing, source-policy forgery, assessment forgery, wrong or conflicting external roots, and evidence omission. The later exact-code and physical-output audits described below completed that remediation requirement before checkpoint certification.

Roundtable also recommended decomposing the large exporter into immutable artifact-bundle phases and moving longstanding domain relevance policies into declared adapters. Those are sound P2 architecture follow-ups; they do not replace the fresh production and external-attestation gates for this release.

The first post-remediation audit, session `aa509b74-6574-422d-b5da-463ade0f8b1c`, found one final P1 in the new attestation path: loading an attestation could overwrite a caller-supplied trusted contract root. That room also correctly returned **CHECKPOINT-CODE NO**. The CLI now requires the independently expected attestation and contract digests, preserves the caller roots, rejects conflicts, and includes a ninth adversarial precedence test. The local 13/25 grounding result remains an honest production-gate failure rather than a code-verifier defect.

Session `f4ff3a53-2fd0-46f0-b10c-f6b5791b4f8f` audited commit `a401b174` and physical candidate 17 rather than trusting this report. It reproduced the 91-point evidence, hashes, 29/30 partial-credit boundary, six obligations, and landscape XML, then found the remaining P2 duplicated imperative in the speaker notes. The first code-neutral detector, compiler repair, regression test, and immutable V54 binding were committed as `2d532141`.

Authenticated Claude session `8bb9f539-9edc-4c1b-acf5-d7ce8d294f5d` then rejected candidate 18. It found two agenda-note compiler sites that could still prepend `Use` to an anchor already beginning with `Use`, plus a case-sensitive repeated-imperative pattern that missed `Use use`. The repair centralized every agenda variant behind `agendaSlideNote`, made detection case-insensitive, exercised all six variants with the production-shaped anchor, and bound the 34-file implementation under immutable ruler V55.

The next exact code-and-ZIP review, session `a08b1f70-83c8-474e-8fa7-fb23cd8be71d`, rejected candidate 19. It reproduced the substantive 91-point package evidence but found an uncommitted/stale release-proof graph, the malformed source-label splice `Function computer`, and clipped final rubric footer text. The proof verifier now requires a tracked production attestation, semantic agreement among the deployed commit, ruler, ZIP, contract, ledger, and browser audit, and accepted `production-checkpoint` scope. Generic source-title parsing and footer layout were repaired under later immutable rulers.

Candidate 20 proved the new V56 proof and footer path but remained physically unacceptable because interrupted numbered-list source text still produced `Function computer`. Candidate 21, generated after the V57 parser repair, independently replayed at 91/100 with zero text defects, yet rendered slide inspection found the concept-map source label clipped to `Function (computer` and a stylized counter whose glyphs were ambiguous. The final course-neutral repair preserves up to 52 characters for evidence spokes and replaces the bespoke seven-segment machinery with an accessible native text counter. V58 binds that implementation. Commit `c9ca162a` passed both unit shards, bounded contract evaluation, release receipts, proof smoke, production build, bundle budgets, and exact Pages deployment before candidate 22 was generated.

Session `71990d46-52e2-47a2-9a01-8b95f924affa` then audited candidate 24 from repository code rather than this report and returned **CHECKPOINT NO**. It found the omitted table/matrix denominator, the inline-LaTeX serialization loss, warning-only accessibility disposition, decorative suppression, and fail-open unreadable-input path. Commits `98653532`, `79e1d672`, and `afc02696` repaired those classes; `cba17825` bound the complete V63 test surface. The exact fast-verification run `30843776937` passed all nine lanes, with unit shard 2 at 7:00, contract evaluation at 6:16, and texture evaluation at 5:47; deployment run `30844381289` then published that exact commit. Candidate 25 was generated only after that deployment and independently replayed and rendered as recorded above. Antigravity became unavailable during the room, and Roundtable v0.0.0.47 correctly auto-skipped its remaining turns without another login interruption.

Final audit session `d2db99de-6948-4255-b054-b12dccfad93d` rejected the proposed candidate-25 checkpoint. It found that the successor validator did not enforce its recorded pass statuses, zero failures, score floors, exact counts, or dimension sum; the verified-current contract contradicted its indexed highlights; the 1,565-object denominator was selected by producer-assigned names; and the successor allowlist authorized changes to its own validator while V63 prose exceeded the grader receipt. The working V64 boundary enforces those predicates, instruments every text box, derives the denominator from every emitted `p:sp`, `p:pic`, `p:cxnSp`, and `p:graphicFrame` object, binds the 159-file package-verification graph separately, and moves a five-path successor policy into the deployed code commit without authorizing validator changes. Re-running the structural scan against candidate 25 now fails all six decks, demonstrating that the new check is not tautological.

The next two-round Roundtable falsification rejected candidate 26 and V64 rather than accepting the 91-point score at face value. Codex and Claude found that a complete missing `cNvPr` node could still disappear from the denominator; the validator did not compare the replay attestation digest with the tracked attestation, did not hash the tracked ruler bytes, did not independently cross-check the export-boundary receipt, and allowed mutable proof pointers. Antigravity returned a 503 and Roundtable v0.0.0.47 correctly marked it unavailable and skipped its remaining turns while Codex and Claude continued. Transcript SHA `3858b38b1e7fa74ab73fef635fe6a2159a229513c1badc73bbe60ea86e20b855` preserves that rejection. V65 and commit `ccbba4d7` close each code-level finding, and candidate 27 supplies the required fresh production bytes.

The final three-agent code-and-package audit reproduced the central candidate-27 evidence but reached no precommit consensus. Codex returned checkpoint yes after independently hashing and inspecting the ZIP, converting all 50 Office files to 221 PDF pages, recomputing the implementation receipts, and rerunning the four-family denominator mutation. Claude's focused broker suite passed 65 tests and it returned a conditional yes: the five authorized successor files had to be committed and verified from a clean checkout so the history allowlist could not pass vacuously against an empty committed diff. Antigravity returned no because Roundtable's synthetic Git snapshot omitted the frozen commit object, making the fail-closed ancestry check fail. The owner disposition preserves the stricter EDUTOOL rule instead of trusting disposable-workspace metadata: commit exactly the five policy-authorized paths, require a clean real-repository audit and CI, and repair Roundtable to preserve ancestry and broker dependencies. Producer-recorded structural-total recomputation, `p:grpSp` coverage, and the remaining lesson grounding gap stay explicit P2 hardening. Transcript SHA `4733ae1c223c417e1b3ba82743ce761bf58920aa97de9e2a84698212df27b819` preserves the disagreement and its evidence.

The work also exposed four Roundtable defects. It sent Claude an extra live prompt as an authentication probe before the real turn, JavaScript check commands could lose dependencies inside the disposable workspace, the 1 MB per-file ceiling prevented reviewers from receiving a valid 1.2 MB production ZIP even though the room already allowed 3 MB total, and choosing “continue without” an unavailable participant only skipped one turn before pausing the room again. Roundtable v0.0.0.47 includes the prior authentication, dependency, liveness, and attachment fixes and now marks that participant unavailable for the rest of the room. A two-round regression proves one CLI invocation, no fabricated replies, and automatic later-turn skips.

## Throughput diagnosis

The model is not the release bottleneck. Candidate 26 completed in 37 seconds. The exact nine-lane CI gate completed in about 7 minutes, dominated by unit shard 2 at 6:58, contract evaluation at 6:15, and texture evaluation at 5:39; exact deployment added about 2 minutes. Package replay, Office conversion, and the 50-file/221-page render audit then completed in tens of seconds. Long Roundtable turns are separately reported as live reasoning with elapsed and no-output timers, so a slow participant is no longer mistaken for a dead process.

This cycle reduced avoidable work by cancelling superseded CI runs, separating predeploy code gates from the production-checkpoint successor, freezing the prompt/ruler/code boundary before production generation, and refusing to regenerate after proof-only successor changes. The deployed five-path successor policy permits only the report, browser proof, production attestation, release contract, and current-release record; it does not authorize changes to its own validator. The next throughput work should rebalance the two unit shards and cache the contract and texture evaluators by immutable input hash. Those optimizations may shorten proof latency; they must not weaken the independent ZIP replay or physical-render boundary.

## Release Boundary

V0.17.11 is accepted as the Scion deterministic learner checkpoint on candidate 27. The exact V65 code commit passed all nine CI lanes and deployed before the fresh production run; the resulting 91/100 package passed independent source and assessment replay, raw/audited/described structural equality, all-file Office conversion, and representative visual inspection. The five-file evidence successor changes only the paths authorized by the deployed checkpoint policy.

The checkpoint freezes an evidence-backed engineering result, not a universal quality claim. It does not establish universal factual correctness, independent instructor approval, accessibility certification, classroom outcomes, learned-weight improvement, or parity with a paid reference model. The remaining measurable content gap is grounding: two of six lessons lack admissible live-source evidence, so the honest score remains 17/25 for that dimension and 91/100 overall.
