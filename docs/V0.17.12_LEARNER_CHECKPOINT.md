# V0.17.12 Learner Checkpoint

Date: August 3, 2026

## Goal

Make the generated work teach and assess the capability named by the lesson. A learner assigned automated testing must receive executable testing work; a learner assigned a policy memo must receive a real policy decision architecture. Preserve honest scoring and inspect the exported files, not only internal objects.

## Lane

Course-neutral artifact-genre inference, assignment and rubric alignment, executable learner assets, evidence and anchor projection, statistical-claim repair, Office rendering, wide-map print behavior, deterministic replay, and bounded release evidence.

## Why V0.17.11 was not enough

The V0.17.11 package was a strong evidence-system checkpoint, but independent content review scored its instructor content at 55 and its formatting at 74. The corrected 2:1 content/format composite was 61. Its most important defects were semantic rather than cosmetic:

- the lesson titled “Functions and automated tests” accepted a generic PDF, slide deck, or recording and did not require executable tests;
- the “Integrative policy memo capstone” behaved like a generic analysis project and omitted the decision maker, options, stakeholder and equity effects, tradeoffs, implementation, and monitoring;
- assignment evidence packets and anchor samples existed in structured data but did not reliably reach the learner-facing DOCX;
- confidence-interval copy used an overexact “90 out of 100 samples” shortcut;
- one pitfalls-slide title rendered above the slide canvas, rubric and milestone blocks could begin a page without their labels, and a ten-column Course Map was forced onto one page.

The architectural mistake was allowing broad course mode and generic artifact defaults to outrank the capability explicitly named by a lesson. V0.17.12 reverses that order: the local lesson signal owns the assignment genre, and the selected genre owns its non-negotiable evidence.

## Implemented learner contract

### Automated testing

An automated-testing lesson now compiles as a code lab. The learner must submit executable source or a notebook, record how to run it from a clean start, preserve an initially failing test and the passing result after repair, exercise a typical case plus a boundary or error case, and explain the defect, code change, and verification. The package bundles `starter_policy_analysis.py`, `test_starter_policy_analysis.py`, a synthetic data file, and an automated-testing lab guide. The bundled suite executes three checks and fails closed on a missing required column.

### Policy memo

An explicit policy-memo lesson now compiles as a policy brief. Its brief requires the public problem, affected population, decision maker or authority, at least two feasible options, named evidence, stakeholder and equity effects, costs or constraints, tradeoffs, a recommendation and evidence limit, implementation ownership and timing, and a material risk with mitigation or monitoring.

### Evidence visible to learners

Assignment DOCX files now render their assigned evidence packet and anchor-sample revision contrast. Rubric DOCX files render both array and object forms of anchor examples. Exact lesson identity stays in headings and reverse stamps while repetitive body references compact to local, grammatical aliases.

### Honest concepts and readable files

The content boundary detects and repairs both “At 90% confidence…” and abbreviated “at CL = 90%…” overclaims. Pitfalls titles are shortened and capped for LibreOffice’s Georgia metrics. Wide Course Maps use landscape scaling and repeat the header plus identity columns instead of forcing ten columns onto one page. Anchor examples and milestone checklists remain grouped with their labels across page breaks.

## Local checkpoint evidence

The retained project `Applied Civic Data Analysis - CourseMapper Project (1).coursemapper` was recompiled from its saved CourseGraph through the current compiler and package exporter. This was not a polish-only replay of old deliverables.

- Source project SHA-256: `0b3bcb3b37c66e11d91ce266eeed148435914f3f07e2826806937b663d0f69c9`
- Fresh local ZIP: `/tmp/edutool-v01712-checkpoint-final3.zip`
- ZIP SHA-256: `024789cdd0f3b21c5ed3bdcfc693192ae7268623ad04423567971443797833e3`
- Size: 1,282,887 bytes
- Files: 63
- Reproduction SHA-256: `024789cdd0f3b21c5ed3bdcfc693192ae7268623ad04423567971443797833e3`
- Deterministic package grade: **99/A**
- Findings: **P0 0 · P1 0 · P2 0**

The bundled Python suite passed all three tests. All six slide decks passed the structural slide test. Representative visual inspection covered every lesson’s pitfalls slide, all pages of the Lesson 3 code-lab brief, the Lesson 6 policy-memo brief, and the Lesson 3 and Lesson 6 rubrics. The ten-column Course Map records landscape scale 64, repeated row 1, and repeated columns A:B. The final render has no clipped pitfalls title and no orphaned anchor or milestone continuation.

The local proof was deliberately predeployment-only. It is retained as reproducibility evidence and is superseded for release acceptance by the fresh production generation below.

## Production checkpoint evidence

Exact commit `5848fc775e292e837c82d8a9a536000c4f87feb0` passed every fast-verification lane and deployed through GitHub Pages. The deployed `https://edutool.dev/` identified itself as Scion V0.17.12. A new anonymous workspace was then created from the same course brief; the accepted ZIP was generated from that prompt under the deployed compiler, not exported from the retained project's older saved deliverables.

- CI: `https://github.com/lovejzzz/CourseMapper/actions/runs/30865804670`
- Deployment: `https://github.com/lovejzzz/CourseMapper/actions/runs/30866210217`
- Fresh production ZIP: `/Users/tianxing/Downloads/Applied Civic Data Analysis - Course Materials (3).zip`
- ZIP SHA-256: `d4c29d085c7d9a2dab345ed84fcb2c9b96c5482a03d8866cb59586f8c44ae1d5`
- Size: 1,280,380 bytes
- ZIP entries: 74
- Deterministic package evidence: **91/100**
- Package conformance: **99/A**
- Findings: **P0 0 · P1 0 · P2 0**
- Independently replayed evidence: 5 sources, 64 claims, 29 rendered artifacts, and 6 assessment obligations

The production ZIP's release graph replay passed with no failures: package, course-contract, evidence-bundle, score-ledger, counts, score, and grader identity all match the tracked attestation. The bundled automated-testing suite passed all three tests, including its missing-column fail-closed case. All six slide decks passed the overflow test. Representative rendering covered 16 pages across the code-lab assignment, policy-memo assignment, and both matching rubrics; no clipped text, orphaned label, or split instruction was found. The Course Map preserves landscape scale 64, repeated row 1, and repeated columns A:B.

As a control, exporting the retained project's older saved deliverables produced six historical P2 findings. That exact-restore ZIP is excluded from the checkpoint. Its contrast with the fresh zero-finding package confirms that V0.17.12's result comes from the deployed compiler rather than score suppression or package selection.

## Release Boundary

V0.17.12 is accepted as a learner-readiness engineering checkpoint. The exact deployed commit and fresh production ZIP reproduce the declared gates: course-neutral assignment alignment, executable starter work, deterministic package evidence, zero encoded findings, and inspected Office rendering for the six-lesson course. It does not establish universal factual correctness, independent instructor approval, accessibility certification, classroom outcomes, learned model-weight improvement, or parity with a paid reference model.

## Minimal learner workflow follow-up

On August 4, 2026, the complete learner workflow received a code-first UI/UX audit focused on minimalism, cancellation truth, and continuity across setup, generation, sync, review, and export. The implementation now uses one compact build-status spine, removes the duplicate disabled build action, keeps diagnostics collapsed until requested, respects reduced-motion preferences, and makes the primary Stop action reachable throughout generation, finalization, sync, regrade, and project reset. An intentionally stopped sync is presented neutrally, retains completed work, and is no longer mislabeled as either success or failure. Intentional export cancellation also clears stale warning state instead of showing a red error to the learner.

The workflow hardening underneath that presentation adds operation epochs and abort propagation across generation, deliverable work, Smart Sync, and package finalization. Old async work cannot write into a newer project or release a newer finalizer. New Project stops active work before persistence resets. Sequential multi-lesson sync retains the latest canonical state rather than replaying an older snapshot.

Roundtable reviewed the working code through Codex, Claude, and Antigravity, followed by one mandatory read-only Fable 5 boss audit. The regular panel leaned SHIP. Fable returned FIX after finding that `packageWorkflowEpochRef` and `syncRegradeEpoch` both used zero as their initial sentinel: a restored project could complete its first sync without scheduling the quality regrade. The epoch now starts at one, zero remains reserved for “no regrade pending,” and a regression contract pins the cold-restored-project path. No additional review round was requested after executing Fable's disposition.

Post-fix evidence:

- Vitest: **514 files passed · 6,660 tests passed · 16 files skipped · 162 tests skipped**
- Mobile Playwright: **3/3 journeys passed**, including the 320 px setup flow and a cancelable 375 px build
- ESLint: passed
- Prettier: passed for every changed source, test, and report file
- Production build: passed
- Bundle ratchets: passed for AppFlow and ChatPanel
- Patch integrity: `git diff --check` passed

This follow-up accepts the learner workflow as the v0.17.12 minimalist UI/UX checkpoint. It does not claim that every future browser, course, or long-running model response is defect-free; it establishes that the current critical paths are compact, cancelable, state-safe, and covered by executable release evidence.

## Deployed frame-by-frame acceptance

The deployed learner journey was then repeated from an empty anonymous workspace at desktop (1440 × 900), tablet (768 × 900), and phone widths (375 × 812 and 320 × 812). The inspection covered the brief, example-filled brief, material selection, recommended set, generation review, active generation, completed Content/Agent/Export views, the honest quality modal, and the final export panel. No page-level horizontal overflow appeared at any tested width. Tablet and phone layouts reduced the three-column workspace to one explicit panel at a time, while the wide Course Map remained independently scrollable. The setup, materials, generation, Stop, and workspace-tab controls all retained 44 px phone targets.

That inspection found one remaining low-severity inconsistency: the Agent panel's honest-score chip was 32 px tall on phones even though the surrounding workspace tabs were 44 px. It now uses a 44 px phone target and returns to its compact 32 px treatment at the `sm` breakpoint; a component regression asserts both classes. The production build, bundle ratchets, 3/3 mobile journeys, targeted component tests, lint, formatting, and patch-integrity checks pass after the correction.

A new deployed run produced `/Users/tianxing/Downloads/Applied Civic Data Analysis - Course Materials (4).zip` (SHA-256 `eacd289ffeaa00603644505a4d992f13ad0a5d9761e50227adb6ffc4216c9674`, 1,280,344 bytes, 74 entries). ZIP integrity and all three bundled Python tests pass. Its evidence report records **91/100 deterministic package evidence**, **99/A package conformance**, **0 P0 · 0 P1 · 0 P2 findings**, **30/30 assessment-link checks**, and **38/38 export-verification checks** with no blockers or warnings. The nine-point evidence gap is retained rather than rounded away: eight points reflect two lessons without a byte-bound exact source claim, and one point reflects a 96/100 masked visible-unit texture score. The 99/A conformance score means no encoded defect fired (apart from the independently measured texture deduction); it is not presented as expert validation of every sentence.
