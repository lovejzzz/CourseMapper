# V0.17.01 Output Quality Audit

## Goal

Make generated results more honest when source evidence or model output is
incomplete. V0.17.01 prefers a visible gap and targeted retry to unsupported
source wiring or polished generic fallback content.

## Roundtable Review

Roundtable v0.0.0.26 reviewed the EduTool codebase in two rounds and six turns.
The meeting was steered to inspect implementation and tests rather than infer
behavior from product documentation. Its completion brief recorded no formal
consensus because one proposal—stripping unsupported references after
export—remained disputed. The implementable core nevertheless converged:

- remove first-ledger-row and hard-coded `SL1` source fallbacks;
- fail closed when FAQ or quiz generation cannot meet its evidence-bound
  contract;
- detect repeated prose frames after lesson-specific title substitution; and
- characterize course families outside the tuned sparse-brief panel.

## Independent Audit

The source-fallback finding was valid but the discussion overstated its score
effect. Arbitrary references could make source-coverage manifests look complete
and could affect readiness when trusted concept-linked evidence was also
present. They did not universally produce a fixed readiness-score promotion.
The release therefore claims a coverage-integrity repair, not a universal score
change.

Accepted:

- `fallbackSourceRefsForLesson` and assessment repair must use only validated,
  locally linked references.
- Synthesized concepts must not receive `SL1` when no supporting source exists.
- Deterministic FAQ recovery must remain visibly draft.
- Quiz validation must expose underfill rather than pad with generic prompts.
- The repetition grader must remove the full lesson title before comparing
  semantic frames.
- A disjoint untuned panel is useful report-only evidence of generalization
  limits.

Rejected or deferred:

- Post-export reference stripping was rejected. It would hide an upstream
  ownership defect and could remove legitimate provenance after compilation.
- New distractor rules were not added because the observed padding defect
  generated short-answer questions, not distractors.
- Dynamic lens creation and a larger hand-authored lens table were deferred.
  This release first establishes an untuned measurement lane.
- No claim is made that a present reference proves source correctness, factual
  accuracy, pedagogical effectiveness, accessibility, instructor approval, or
  classroom outcomes.

## Lane

### Evidence integrity

CourseIR repair now leaves unsupported atom references empty. A two-lesson
adversarial fixture proves that evidence for one lesson cannot manufacture
coverage for another, while missing and dangling reference counts remain
visible to validation and compilation.

### Fail-closed learner artifacts

Fallback Course FAQs carry a `draft-review` status that blocks publishability.
Quiz normalization no longer invents questions to reach the configured count;
validation reports the exact underfill and returns the affected lesson index for
retry.

### Semantic repetition and untuned measurement

Deep grader V1.11.1 masks each document's lesson title before cross-lesson line
comparison. The new 12-course untuned panel is disjoint from the frozen thin
panel and records content-fallback telemetry through the same SSR compiler
runtime used by the texture audit.

The first V0.17.01 characterization produced:

- 12 packages and 468 path-free input-mask clusters;
- 7.45% support burden;
- 11.91% reader exposure;
- 8.28% cross-package excess;
- 30 `lens-default` hits across 10 packages; and
- zero unclassified extracted paths.

These values describe the current unrecognized-course boundary. They are not a
before/after quality win.

## Release Boundary

V0.17.01 changes deterministic repair, fallback validation, semantic repetition
measurement, and report-only untuned auditing. It does not change Gemma weights,
activate the optional adapter, certify sources, or establish provider,
instructor, accessibility, learner, or classroom superiority.

## Proof

Focused regression:

```text
npx vitest run src/lib/__tests__/courseIR.test.js \
  src/lib/__tests__/deliverablePostProcess.test.js \
  src/lib/quality/__tests__/deepQualityStructure.test.js \
  tests/v01701-output-quality.test.js
```

Untuned characterization:

```text
npm run audit:texture:cross-package:untuned
```

Release gates:

```text
npm test
npm run test:blueprint:quality:fast
npm run audit:contract:pr
npm run audit:evaluation:pr
npm run format:check
npm run lint
npm run build
npm run bundle:check
npm run audit:release-history
```
