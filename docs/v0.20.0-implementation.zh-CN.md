# v0.20.0 实施记录

基于 [完整路线图](v0.20.0-roadmap.zh-CN.md)，从 v0.19.2 的 `3c97bfc9cf1e090d1b6970677cbe988e299b3515` 开始。工作分支：`codex/v0200-teaching-system`。本文件记录完成证据和剩余工作，不替代路线图的范围。

## 当前状态

M0 进行中，M1 已贯通首个绑定操作的来源/权重审阅、影响预览、确认、教师覆盖冲突、保存恢复和 Word 导出。M3 已进一步支持教师编写的一般表现要求：任务、参考、评分、反馈和新案例练习共同审阅，增删后同步九类派生材料并可持久化撤销。第 14 节补充新任务创建和窄字段 Scion 来源提案；第 15 节补充跨材料、刷新与工程文件恢复的未确认草稿。这仍不等于自动理解要求或完整课程验收。v0.20.0 尚未完成或发布；运行时保持 0.19.2，界面基线保持 0.18.7，当前代码只在本地工作分支。

| 阶段              | 状态     | 当前证据 / 缺口                                                                                |
| ----------------- | -------- | ---------------------------------------------------------------------------------------------- |
| M0 边界与基线     | 进行中   | 主要数据路径、首个迁移方案、v3 规则及两轮 Scion 试验已记录；生产提案、预算和语料清单待冻结     |
| M1 完整任务流程   | 部分完成 | 两个显式操作、要求编辑、新任务创建和草稿恢复已有局部证据；目标对齐、多任务与全部写入者仍缺     |
| M2 操作与教学质量 | 未完成   | 尚未完成通用结构提案、三类操作重构、对照作答和多课进展                                         |
| M3 教学要求同步   | 部分完成 | 已有任务的表现要求增删、动作/评分/练习编辑、影响预览及持久化撤销通过；目标对齐和全部写入者仍缺 |
| M4 材料与改进流程 | 未完成   | 十类输出职责改进、匿名作答审阅和三套六课课程待实现                                             |
| M5 新案例与审阅   | 未完成   | 历史回归不能替代新案例和独立教师审阅                                                           |
| M6 发布           | 未开始   | 版本、README、changelog、部署、最终网页及附件验收待完成                                        |

## 1. 已确认的数据写入与恢复路径

| 路径                                | 当前行为与问题                           | 第一段迁移                                                                  |
| ----------------------------------- | ---------------------------------------- | --------------------------------------------------------------------------- |
| `prepareBlueprintForCompilation`    | 选择来源任务，之后投影九类派生材料       | 从统一读入口读取已保存任务，不再直接信任 map 的散落副本                     |
| `projectTeachingTasksIntoCourseMap` | 接纳任务并写回课程图展示数据             | 同时生成版本化 `teachingProgram`，旧 `teachingTaskSources` 成为只读兼容投影 |
| `applyTeachingTaskSourceEdit`       | 在材料中编辑来源，重建相关任务并三方合并 | 用权威教学数据核对基础版本；接受后通过同一写入口更新程序和兼容投影          |
| `deriveCourseGraphFromCourseMap`    | 课程层额外字段原来全部进入 `course.meta` | 将教学程序提升到 `CourseGraph.teachingProgram`，移除元数据中的第二份权威    |
| `renderCourseMapFromGraph`          | 图渲染为现有编辑器理解的 map             | 教学程序派生兼容来源；旧副本不能覆盖程序中的内容                            |
| `restoreCourseGraphForProject`      | 验证旧图，必要时由 map 重建              | 迁移旧图中的来源；新的程序损坏时不能悄悄回退到旧 ledger                     |
| AppFlow 的 map 写回 effect          | 部分旧编辑仍通过 map 重建图              | 目前由 map 携带同一教学程序；后续目标/要求编辑必须改成显式实体事务          |

旧图、map 和材料副本都曾携带来源。明确课程 map 的优先级后，仍可能因为 JSON 字段排列不同误判“旧版本”。迁移回归实际暴露了该问题，现已改为按 JSON 数据内容比较，保留数组顺序、值类型及真实字段变化的差异。

## 2. 第一段 schema 决策

保留外层 `COURSE_GRAPH_VERSION = 1`，新增可选的 `teachingProgram.version = 1`。没有教学程序的旧图仍可读取。后续增加操作和评分协议时单独版本化，不能把本轮版本号误解为整个 v0.20.0 schema 已冻结。

当前程序包含：

- `objectives`：任务需要学生展示的具体表现，稳定 ID 和文本。
- `sources`：带稳定 ID 的输入陈述；旧来源标为 `legacy-task-input`，不追认其事实真实性。
- `tasks`：通过 `objectiveRef`、`inputs[].sourceRef` 引用目标与来源，保留任务 ID、原输入 ID、课次绑定、时间、作用域和未知的教师字段。
- `revision`：对规范化 JSON 计算摘要，用于检测变动和不完整保存；不是事实认证或教师审批。

任务层表现与旧课程总体 outcomes 的正式对齐关系仍待 M2 实现。首轮存储迁移仍通过旧 solver 派生推理和评分；后续首个操作的绑定与评分权重已写入任务，见第 8 节。所有教学实体的统一尚未完成。

没有根据文本相同自动合并来源。两个独立案例即使陈述相同，也保留各自身份。显式共享引用可表达共同来源；不同任务对同一引用提出相互矛盾的更新时拒绝静默选择其中一个。

`teachingTaskSources` 继续存在于材料快照和 map 的兼容视图中；只要程序存在，读取时以程序为准。新的图不在 `course.meta` 另存这份 ledger。旧文件中用户自定义的来源、输入和任务字段需往返保留。

## 3. 第一轮验证记录

- 新增 9 项存储、迁移和 JSON 比较回归：覆盖图往返、过期副本、身份与 revision、独立案例、悬空引用、重复身份、损坏保存与教师元数据。
- 最初针对迁移和实际 source-edit 的 45 项检查暴露 7 个失败：一个新测试未遵守原 renderer 的资源编号格式；其余涉及字段顺序误判。保留真实合并与来源版本要求后修复，45 项通过；之后增加 JSON 比较回归。
- `npm run check` 通过：58 + 253 + 796 = 1,107 项软件测试；14 项原 benchmark 检查器测试；类型、lint、构建和 bundle 检查通过。日志：`.audit-work/v0200-teaching-system/m0-check.log`。
- 第一轮旧基准实际运行的是 18 个 development 包，0 个自动缺陷。命令向 npm 追加了 `--split exposed`，但旧 runner 取第一个参数，实际被脚本默认的 development 覆盖。已修正为最后一个显式参数覆盖默认值；不能把该轮结果说成 exposed 验收。
- 上述早期 benchmark 使用旧的跟踪文件清单，新建模块尚未加入 git 索引。该轮摘要不足以绑定全部新源码，需要在加入新模块后重新运行，并保留这次记录。

这些结果只证明本轮软件迁移的部分行为。新任务支持、独立教学质量、浏览器编辑、真实导出和 v0.20.0 发布均未因此完成。

## 4. M0 本地模型与设备记录

本机实读：macOS 26.5.1，Apple M4 Max，48 GiB 内存，16 个物理/逻辑 CPU 核心。未据此推断其他访客设备的表现。

复用 Chrome 原有 EduTool 页 `2014663895`，导航到本地 `http://127.0.0.1:17148/?scion-runtime-canary=1`。没有打开第二个推理页。使用应用现有 localhost-only runtime canary，加载同一份 Gemma 4 权重；adapter 保持 base-only。

开发探针包含不同事件日期、志愿问卷比例和中文实验设计三个新输入。两轮真实执行与实现者语义审读已完成，见 [原始输出和审读记录](../research/scion/evaluation/v0.20.0/README.md)。首次完整提案为 16,195 / 16,626 / 17,009 ms；在同一批已暴露输入上的短绑定提案为 7,370 / 5,528 / 8,518 ms。

首轮存在字段类型、总体归属、任务和实验评分遗漏；短提案改善了格式与耗时，但来源角色和必要信息仍不完整。需要继续设计每类任务的窄字段、操作准入与教师修正路径，不能据三次格式通过启用生产自动提案。这些输入不计入路线图要求的十二个新单元最终模型验收。

当前路径首次加载实际进行了模型下载，因此要分别报告下载/加载与推理时间。不能将其记成已有缓存的暖态成绩。

## 5. 来源编辑与恢复的后续验证

- 导入实际 v0.19.2 发布工程 `Records-and-Effective-Dates-Compiler-v0.19.2.coursemapper`，在原 Study Guides 编辑器中将主案例来源的 90 seats 改成 84 seats。
- 首次浏览器运行发现保存的练习问题与可见问题在语言整理后有大小写差异，被三方合并误判为教师冲突。撤销该修改后，修正了内部元数据排除范围并增加回归。材料中的旧 `row.teachingProgram.units` 是需要语言整理的派生练习，不能因为新课程级程序同名就跳过它。
- 修正后再次实际编辑，无上述虚假冲突；主案例解释、推理和评分同步至 84，独立练习 24/36、15 October 保持。
- 刷新并从 Previous session found 的 Resume 恢复后，来源和解释仍为 84，九类派生材料就绪；本轮浏览器捕获的 error/warn 日志为空。这是本地恢复检查，尚非 edutool.dev 的候选发布验证。
- 从 UI 下载新 `.coursemapper` 并检查：map 与 graph 的程序均通过验证且 revision 一致；九类派生材料中旧的 `90 seats` 为零，均包含新的 `84 seats`。下载文件及检查记录保存在 `.audit-work/v0200-teaching-system/m1-source-edit.coursemapper` 和 `m1-source-edit-inspection.json`。
- 恢复时 map/graph 两份程序不一致，或新程序与另一份旧权威 ledger 不一致，会明确拒绝静默覆盖；保留原输入。材料副本不同则保留教师文本并标记需审阅。损坏副本含 null 的回归也已加入，避免恢复异常造成空工程回退。
- 将原有 CourseGraph 测试纳入常规兼容检查后发现一个历史断言要求凭空分配 100% 成绩权重。其期待与基线已有的 `sourceCourseGradeWeight` 规则冲突，相关产品函数本轮没有改动。修正为验证缺失政策保留 null，并新增明确 20%/40% 不被归一化的回归；未为了测试重新引入推断课程成绩。
- 后续 targeted 检查 49 项通过；再增加损坏副本处理后，独立运行的 13 项程序存储/恢复回归通过。完整最终检查及历史基准摘要在完成后追加。

上述验证还不覆盖目标/任务/评分语义事务、完整导出页面和三套多课课程。M1 尚未完成。

## 6. 本轮基础提交的验证结果

- 最终 `npm run check` 通过：58 + 278 + 796 = 1,132 项软件测试；14 项 benchmark 检查器测试；类型、lint、构建及 bundle 检查通过。初始 JS gzip 为 89.769 KiB。日志：`.audit-work/v0200-teaching-system/m0-check-foundation.log`。
- `npm run format:check` 通过，三份路线图/实施/模型记录中的 28 个本地 Markdown 引用均存在。日志：`.audit-work/v0200-teaching-system/m0-format.log`。
- 在新源码全部纳入 git 文件清单后，串行重跑旧 v2 基准：development 18 包、exposed 12 包，均为 0 个自动缺陷、0 次模型调用。它们是历史回归，不是 v3 新输入或教师验证。
- 两轮的 `compilerSourceSha256` 均为 `f0d02484cd0581a96247f0da1ccb0deee1e2a3e8a504acbb82daeb1ed4ed244a`。报告记录基线 HEAD 和 dirty 工作树，并用源码摘要绑定实际代码；未把旧 HEAD 当成未修改源码的成绩。文件：`.audit-work/v0200-teaching-system/m0-development-foundation/report.json`、`m0-exposed-foundation/report.json`。
- 本轮没有运行整个最终浏览器回归、生产检查或发布；模型最终十二单元、v3 案例、完整课程与导出审读仍待完成。公开运行时版本保持 0.19.2，v0.20.0 目标继续执行。

## 7. 后续必须继续的工作

- 基于已保存的两轮 Scion 输出，继续验证按任务族的结构提案协议及失败路径。
- 后续改变 compiler 后继续保留旧基准回归，使用新 v3 输入检验能力扩展。
- 验证新图在旧图、map 和材料不一致时的完整 UI 恢复体验，不能只在底层抛错。
- 设计有明确基础版本和影响范围的来源、目标、任务与评分变更事务。
- 将任务表现与课程 outcomes、推理、评分建立真实引用，减少旧意图/检查分支的重复权威。
- 冻结 v3 开发/保留案例的组织与评分规则，随后建立独立来源家族的语料。
- 继续 M1–M6 的全部路线图要求；本文件的阶段进展不缩减原目标。

## 8. 首个可执行操作：记录修订及适用范围

新增 `teachingOperationPlan.js` 和 `teachingOperationTask.js`。目前只迁移 `record-amendment`，不能据此宣称三个任务族或通用操作组合已完成。

- 操作保存原记录、新记录、各自的数值与单位、生效日期、缺失观察日期的来源绑定。绑定使用稳定输入 ID、UTF-16 片段位置和原文摘要；整句不能充当数值。
- 推导分别记录原状态、新状态、修订关系与尚无法确定的观察适用范围。程序核对定义好的前提，不能认证来源真实性，也不能自动证明自然语言角色正确。
- 保存的 `operationPlan` 成为此任务重新编译的依据。无效或未确认的模型提案不能回退到另一段文本识别后悄悄给出答案。
- 教师确认的显式绑定可以处理旧句式解析器不识别的表达，已有中英文呈现实现；创建任意绑定的产品界面尚未接入，因此不能把单元测试中的结构输入当作已开放给教师的完整功能。
- 旧的有限英文解析器仅负责一次准入。来源中单一数值或已识别日期片段的修改可以移动绑定并更新摘要；更改归属、否定、单位或观察是否已知，需要重新审阅。
- 对负号、小数、指数、比例片段、百分号、过长整数、相同数值的不同写法等边界进行检查；旧解析器遇到超出当前操作范围的数值/日期返回不支持，避免无处理的异常。
- 评分的三个要求及其权重随操作保存，十类材料继续由共同任务派生。目标到课程 outcomes 的对齐、任务要求编辑与一般组合推导仍待实施。

在测试中发现 `readTeachingTaskSources` 返回的嵌套操作对象与权威程序共享引用，修改提案可能提前污染权威内容。现已返回隔离的深拷贝，并校验任务所声明的种类与其操作一致。

### 8.1 题目、答案、评分与对照作答一起改

原评分的 Developing 档只举了错误结论，没有合理覆盖“结论正确但缺少解释”。新描述区分有效范围的解释、只有结论的回答和“新记录证明旧记录从来错误”的误解。

题目、参考解答和最高档都明确要求提出一份能够补足日期缺口的具体佐证，避免只在评分中偷偷增加要求。参考解答把拟寻找的记录与已经取得的事实分开，也接受其他相关佐证。

每个该类核心任务现在产生四种检查用回答：完整推理、正确结论但推理不足、典型误解、合理的替代表达。记录按评分要求关联的判断、理由和精确作答摘录。Rubrics 与 Assignment Briefs 的现有教师参考折叠区展示这些内容；Word 的 rubric 教师参考也包含四类示例。它们是作者构造的检查材料，不是学生数据，也未取得独立评分信度证据。

新要求不能把旧生成文字误判成教师覆盖。为此从真实 v0.19.2 发布工程提取 Study Guides 回归夹具，并保留仅用于三方合并的历史呈现基线；接受修改后写入新的明确操作。这一兼容基线不参与新的内容生产。

### 8.2 实际浏览器与下载验证

继续使用同一 Chrome 页、一个本地开发服务；本轮没有加载模型或执行推理。

1. 将之前恢复的主案例容量 84 改成 104。问题、答案、推理与评分同步；九类派生材料没有旧的 `84 seats`，独立练习的 24/36 保留。
2. 保存 `.coursemapper` 后检查 map/graph：程序均有效、revision 一致，操作片段和来源摘要有效。刷新并 Resume、再通过首页 Attach files 重新导入实际下载文件，均恢复 104 与相应 rubric。
3. 在 Rubrics 的部分回答中加入测试用教师备注并选择 Keep local，再把来源 104 改成 108。程序保留教师的 104 文字，显示一个包含保留/建议版本的冲突；其他相关文字使用 108。带冲突的实际下载文件仍包含该备注、冲突和 stale 状态。
4. 修正了 UI 的矛盾提示：有明确来源冲突或来源待审阅时，不再同时显示旧的“可能只是小改动”提示及通用 Sync now 按钮。
5. 一次保存后尝试撤销意外回到首页。随后重新导入，关闭菜单时独立验证来源修改、撤销和重做均正常。在有冲突的布局中读取实际命中元素，确认展开的 Project 菜单将 New Project 覆盖在撤销按钮中心位置。保存工程现自动收起菜单；同场景复测命中撤销按钮，撤销来源变更保留教师备注，再撤销备注及之前的数值修改，恢复 104。

文件保存在 `.audit-work/v0200-teaching-system/`：

- `m1-bound-source-edit.coursemapper`、`m1-bound-source-edit-inspection.json`：无冲突的绑定操作、九类派生材料与来源一致性。
- `m1-teacher-conflict.coursemapper`、`m1-teacher-conflict-inspection.json`：带真实界面编辑的教师覆盖与来源冲突。
- `m1-bound-rubric-pagination.docx`、`m1-bound-rubric-pagination-render/`：实际界面下载的 Word、PDF 与三页 PNG。

第一次 Word 渲染将教师参考的评分解释拆到只有少量文字的第三页。将教师参考独立起页，并补充所属课次后再次实际下载和渲染，三页均已逐页查看：题目/来源/第一项评分在第 1 页，其他评分在第 2 页，完整教师参考在第 3 页；未见裁切或空白孤页。这只覆盖本份英文 Rubrics，不代替所有材料、语言和导出格式的最终验收。

### 8.3 软件与历史基准

- 新操作共 26 项检查通过，涵盖明确绑定的新表达、未确认提案、数值片段、语义编辑拒绝、来源身份、权重、四类对照、实际旧工程迁移、九类同步及实际 Word OOXML 内容。
- `npm run check` 通过：58 + 304 + 796 = 1,158 项软件测试；14 项 benchmark 检查器测试；类型、lint、构建及 bundle 检查通过。日志：`m1-operation-check-verified.log`。此轮完成于最后两项浏览器提示/菜单修正之前；修正后的检查另行记录。
- 全仓库格式检查通过：`m1-operation-format.log`。
- 旧 v2 development 18 包、exposed 12 包均为 0 个自动缺陷、0 次模型调用。源码摘要均为 `1d8c535faaaf2312428e80265c0dddf91f35394c164e593ddc45b8f032fb6107`；报告明确记录 dirty 工作树。证据：`m1-operation-development/report.json`、`m1-operation-exposed/report.json`。

这是历史兼容与局部工程验收，不是 v3 新案例成绩。生产 Scion 结构提案、教师绑定/要求编辑、跨重开撤销历史、多课课程、全部导出与线上发布仍未完成。

### 8.4 最后内容修正与复验

显式绑定的新输入不一定是一张照片。最终审读发现英文呈现仍沿用旧案例的 photograph/image 假设，已改为 observation；历史三方合并单独保留旧措辞。新增表达测试核对题目参考与对照回答不凭空引入图像，26 项操作检查再次通过。

再次从真实 v0.19.2 工程导入，将 90 改成 104，九类派生材料均没有旧值、冲突或 stale，独立练习 24/36 保留，map/graph 与操作验证通过。最终下载和检查：`m1-final-project.coursemapper`、`m1-final-project-inspection.json`。

再次从 UI 下载 `m1-final-rubric.docx`，用捆绑的 LibreOffice 渲染，并实际查看 `m1-final-rubric-render/` 的全部三页；文字修正后的题目、评分表与教师参考分页完整，未见裁切或孤页。旧导出和初次分页失败仍保留。

最终源码的 `npm run check` 再次通过，仍为 1,158 项软件测试和 14 项 benchmark 检查器测试；类型、lint、构建、bundle 均通过，初始 JS gzip 为 89.777 KiB。日志：`m1-check-final-source.log`。旧 v2 18 个 development 和 12 个 exposed 包再次为 0 个自动缺陷，源码摘要为 `3ab2a9df2e6a0dfe6a3e9e82653ebd291784ddfa4c56bfcd9b27449020f47f98`，报告保存在 `m1-final-development/` 与 `m1-final-exposed/`。

浏览器末轮捕获的 warn/error 日志为空。操作过程中一次浏览器点击命令报告短超时，但随后页面已正确进入 Rubrics；未重复触发操作或据此宣称应用崩溃。全程只有一个测试页，没有模型推理。

## 9. v3 规则已经细化，案例仍待建立

新增 [v3 案例、运行与计分协议](../benchmarks/classroom/v3/PROTOCOL.zh-CN.md)。明确 60 包分组和语言分布、48 个支持包及 12 个限制包、固定分母、自然语言/显式结构/修改事务三条路径、人工协助的独立计数、来源家族隔离、首次失败与曝光记录、严重程度以及检查器故障注入要求。

协议固定计分规则，没有声称语料 manifest 已冻结、v3 已运行或新能力达到 80%。生产提案预算仍需 M0 的后续实际模型试验；下一步继续建立来源与独立参考、实现 runner/checker，并完善教师确认与结构修正入口。

## 10. 教师结构审阅进入实际工作区

原材料工作区新增折叠的 `Review sources and scoring`。目前只处理已有 `record-amendment` 任务：核对三个来源记录、角色及精确片段、修改三个评分要求的权重、预览题目/参考/评分和九类相关材料，再明确确认。相同原文重复出现时必须指定对应位置。任意新任务、目标和要求的语义编辑、其他两类操作及 Scion 自动提案仍未接入，不能据此宣布 M1–M3 完成。

直接来源编辑与结构审阅复用同一 `projectTeachingTaskUpdate` 三方合并及 AppFlow 应用事务。预览不写入工程；实际提交根据审阅意图重新计算，不接纳调用者传来的材料补丁。以下版本条件都需满足：

- 草稿绑定打开时的任务版本，不能借一次新预览重新采纳旧来源。
- 原材料中待审阅的来源文字也被记录；其他材料出现不同的待审阅来源编辑时，不清除该编辑或其状态。
- 预览绑定课程和受影响材料；预览后有新修改或撤销，旧预览被拒绝。
- 应用成功清除已使用草稿；失败保留草稿、撤销确认与预览，允许重新载入当前任务。

教师审阅入口、冲突中的参考答案和已归档教师内容均不在学生视图中显示。确认状态只记录这次操作流程，不代表取得独立教师审阅或来源事实认证。

### 10.1 实机发现并修复的 FAQ 字段错误

第一次把权重 30/35/35 改为 20/50/30 时，Rubrics 更新，但 FAQ 产生 `faqs / qs` 冲突，保留版本显示空白，实际 FAQ 仍为旧权重。保存的真实失败工程为 `m1-review-first-conflict.coursemapper`。

原因是已展开的工程使用 `questions/question/answer`，投影却写入了 `qs/q/an`，三方合并比较的是不存在的字段。现已按现有 FAQ 集合的字段形式投影，避免创建隐藏的第二份答案。回归使用从实际下载工程提取的四问题数据；当旧问题集合与当前六问题投影不同，保留整个真实旧集合供审阅，接受新版本后验证实际 CSV 行使用新权重。没有凭四个相似标题猜测教师是否删过问题。

冲突显示改为可读的列表和字段，明确区分缺失值、空列表及内容；不再把复杂数组压成一行 JSON，也不把未能确定来源的内容差异一律归因于教师编辑。

### 10.2 实际修改、保存与导出证据

继续复用原 Chrome 页 `2014663895` 和开发服务，未打开额外模型页，未加载权重或执行推理。

1. 在原界面将权重改为 20/50/30。预览期间实际 rubric 仍为 30/35/35，确认前应用按钮不可用；确认后 FAQ 和 Rubrics 的可见内容一致。
2. 对来源作出旧解析器不能直接识别的表述：`Replacement entry for this hall: effective 1 July, 112 seats.`，重新绑定修订值为 112。预览明确保留观察日期未知、原记录未被自动推翻及佐证仍待取得的限制。
3. 在一个预览之后撤销课程修改，再尝试应用，实机显示过期提示且没有写入该预览。重新载入最新任务后审阅并应用新来源与权重。
4. 从实际界面下载最终 `.coursemapper`、FAQ CSV 和 Rubrics Word；刷新并 Resume 后，Rubrics 仍使用新来源及 20/50/30。随后从首页 Attach files 实际重新导入最终下载文件，同样恢复 112 来源，实际评分表的权重/分数为 20/20、50/50、30/30。刷新后撤销历史尚未持久化，这仍是后续必须完成的范围。
5. 最终项目检查：map/graph 教学程序有效且 revision 一致；九类派生材料均保存相同的新来源与有效操作，未出现冲突或 stale；FAQ 六问题与 rubric 权重一致。
6. Word 用捆绑 LibreOffice 渲染成三页，三页均逐页查看：来源与第一评分项、其余评分项、独立教师参考完整，未见裁切或空白孤页。教师参考仍明确是构造的审查用回答。该单份英文检查不能替代三套完整课程的全部格式验收。

以上文件保存在 `.audit-work/v0200-teaching-system/`：`m1-review-final.coursemapper`、`m1-review-final-inspection.json`、`m1-review-faq.csv`、`m1-review-rubric.docx` 和 `m1-review-rubric-render/`。FAQ CSV 在 112 来源修改前下载，权重已经为 20/50/30；没有把其下载时间说成最终工程之后。末轮浏览器 warn/error 捕获为空。

### 10.3 检查状态与后续范围

相关来源/操作/事务回归 55 项通过；新增界面检查覆盖确认、草稿变更、失败恢复、结构冲突呈现与学生视图。最终 `npm run check` 通过：58 + 322 + 796 = 1,176 项软件测试，原 benchmark 两组检查器共 14 项；类型、lint、构建和 bundle 检查通过。初始 JS gzip 89.766 KiB。日志：`m1-review-check.log`。

全仓库格式检查通过，日志 `m1-review-format.log`。新文件加入跟踪清单后，串行运行旧 v2 development 18 包及 exposed 12 包，均为 0 个自动缺陷、0 次模型调用。两份报告的源码摘要均为 `d41c790ac85d6a1c474f7cb95b921b66c2593354cfc4fb312edf1b34054bab7e`，报告记录 dirty 工作树，保存在 `m1-review-development/report.json` 和 `m1-review-exposed/report.json`。这些仍是旧案例回归，不计作 v3 新输入成绩。

这些是局部产品路径与软件证据。接下来继续任务新建、正式要求/目标引用、持久化事务历史、按任务族的 Scion 提案协议和其他操作；v3 案例与 runner、三套六课课程、本地作答闭环和发布仍需完成。

## 11. 本地持久化修订历史与恢复一致性

联动修改现在保存双向内容差异。原 Undo/Redo 入口继续使用；一次已确认的来源与评分更新，在课程表、课程图和九类派生材料中作为一条修订。历史随本地完整快照及 `.coursemapper` 文件保存，恢复后保留撤销位置和重做分支。新建项目、打开旧版无历史工程均清除旧栈，防止跨项目撤销。

### 11.1 写入、恢复和大小边界

- 既有编辑回调在写入前捕获相关字段，在该次 React 提交后生成差异；同一提交的批量修复保留最早旧值。没有改变内容的捕获立即结束，不能吸收之后的重新生成。
- 撤销／重做先检查全部相关字段及列表版本，再一次性应用。列表重排、字段被其他写入改变、无效路径、损坏校验或不一致的教学程序均拒绝写入；不部分覆盖当前材料。
- 保存前先去除密钥字段，避免它们藏入补丁的路径／值。恢复只修改所记录的路径，未涉及的后来教师备注保留。
- 历史限制为 30 条及整个序列化历史 8 MiB；修剪最早条目，超大的单次修改建立新的撤销边界并提示。不是每步复制完整材料。唯一的临时旧值在提交后释放；修订之外的普通重渲染不重新序列化整个历史。
- 恢复完整材料条目，包括 stale 证据和教师冲突元数据。原来仅写 status/data/error/stale 的兼容 setter 不承担事务恢复。
- 损坏的可选历史不会阻止有效的当前项目打开；提示放在材料标签栏下方，可关闭，保持原界面结构。

当前范围是完整本地快照、精确保留材料的 IndexedDB 路径及文件备份。旧云端 compact 副本会重新编译材料，不能承诺保留原修订历史，故不把指向旧材料的补丁写入该副本。课程表紧急恢复副本也不含历史。新任务、一般要求、其他操作和所有兼容写入者的统一事务仍属于未完成范围；这不是完整 M3 验收。

### 11.2 实际 Chrome 验证

复用原 Chrome 页 `2014663895`，没有新建其他模型页，没有加载模型或调用推理。

1. 来源从 112 座改为 116 座，权重从 20/50/30 改为 25/45/30；预览并明确确认后，实际 rubric、来源及共享材料更新，历史只有一条。
2. 实际下载 `.coursemapper`，刷新并 Resume，随后一步撤销：页面恢复 112 座及 20/50/30。
3. 下载撤销状态，新建项目，从首页 Attach files 重新导入真实文件；Redo 可用，重做后实际表格恢复 116 座及 25/45/30。
4. 故意破坏下载副本中的历史 revision，再实际导入：页面明确显示历史不可用，当前课程仍能打开。没有把这个故障注入文件替换成正常文件来声称恢复通过。
5. 最终代码下重新导入撤销文件，再重做并下载 `m1-history-redone.coursemapper`。
6. 最终完整刷新并 Resume 后，116 座、25/45/30 及撤销入口仍存在。自 `2026-09-07T00:27:56.727Z` 此次刷新起，捕获的 warn/error 为空。开发过程中改变 Hook 结构曾触发热更新顺序错误，保留该事实；没有将热更新前后日志混称为全程无错误。截图接口本轮超时，功能与提示通过实际操作和 DOM 核对，未计作新增的像素级视觉验收。

修订历史为 304,932 字节，初次完整项目为 1,218,978 字节，均按实际下载 JSON 的紧凑 UTF-8 编码测量。对真实文件进行恢复、撤销和重做，比对课程表、课程图及九份材料全部字段。Node/Vite 中另做 10 次串行差异生成测量，约 103–125 ms；这是修订模块的观测，不是浏览器 compiler 性能或六课课程 benchmark。

### 11.3 逐字段审计发现的旧恢复缺陷

第一次往返比对中，九份材料及课程表一致，但课程图的生成血缘诊断不一致：第一次打开无 lineage 的图，会创建隔离诊断对象；第二次打开将该对象误当成已有 lineage，丢失 `missing-lineage` 原因。

已修复为根据实际证据字段区分真实记录与纯隔离诊断对象，不信任保存的 findings 字符串。重复恢复保持诊断稳定，同时不把无证据的课程提升为可发布。新增回归验证三次重复恢复，以及篡改历史 findings 后重新推导正确诊断。修复后的同一实际文件比对，课程表、课程图、九份材料均一致。

证据位于 `.audit-work/v0200-teaching-system/`：

- `m1-history-applied.coursemapper`、`m1-history-undone.coursemapper`、`m1-history-redone.coursemapper`：实际下载文件的副本。
- `m1-history-invalid.coursemapper`：明确标记的故障注入副本。
- `m1-history-first-inspection.json`、`m1-history-graph-differences.json`：保留的首次不一致及具体路径。
- `m1-history-inspection.json`：修复后的逐字段往返检查及原始计时。
- `m1-history-final-inspection.json`：最终实际下载、完整刷新日志与应用前后全字段一致性。

### 11.4 软件检查与剩余验收

定向修订／恢复回归 28 项通过。最终 `npm run check` 通过：58 + 347 + 796 = 1,201 项软件测试，另有原 benchmark 检查器 14 项，类型、lint、构建及 bundle 均通过；初始 JS gzip 为 89.773 KiB。日志为 `m1-history-final-check.log`。全仓库格式检查通过，日志为 `m1-history-format.log`。未重复运行旧 v2 案例来充当新输出质量成绩。

本次没有新增 Office/PDF 排版改动，也没有将前一阶段的三页 Word 检查当成本阶段新增的全格式验收。仍需执行 v3 60 包、三套六课完整课程、12 个真实 Scion 单元、本地作答闭环和全部发布检查。产品仍为 0.19.2，尚未推送或发布 v0.20.0。

## 12. 显式比例任务与双语实际产物

本阶段实现 `observed-proportion`：教师在原材料页审阅计数记录、分子、分母、所计结果、已观察群体、缺失群体和目标群体的具体来源片段。旧比例任务可以进入此流程，但不会根据两个数字猜出群体归属；初次绑定要求核对实际原文。

### 12.1 操作与教学内容

- 片段绑定来源 ID、原文位置及 revision；验证计数是同一记录中的部分与整体、分母非零、部分不大于整体。精确整数运算产生分数、小数、百分比和反向核验。
- 检查绑定分数后紧接的等式或近似值，拒绝反转角色、错误等式、错误舍入、嵌套分数片段及不支持的数值表达。该检查不是通用数学证明器，也不证明自然语言中的群体归属；这些仍需教师审阅。
- 模型提案状态不能直接执行成确定答案；未确认或无效操作不能由旧模板偷偷兜底成成功。
- 同一已确认操作派生题目、提交物、完整参考、三项评分要求、四类对照回答及具体错误反馈。主任务和独立练习都明确要求反向核验及进一步证据，避免 rubric 评价题目没有要求的表现。
- 进一步证据包含相同结果定义、相关观察时段与目标群体覆盖核查，区分建议收集与已经观测。保持缺失结果未知，接受有依据的抽样或完整记录方案。
- 中英文任务采用对应呈现。主案例计数改变时，独立迁移任务保留自己的来源和数值。全部为实现者构造的开发教学情境，没有真实学生作答或本轮模型调用。

### 12.2 实际操作发现的四个产品缺陷

1. 语言整理器把 rubric 中的完整学习目标替换成材料名称。将 `objective` 和 `objectiveAligned` 纳入原有目标字段保护，保留实际教学要求。
2. 初次 compiler 将独立练习教师说明保存为段落，增量投影却使用换行，产生两处虚假的教师冲突。投影采用同一段落表示，未放宽真实教师文本的合并保护。
3. 新事务派生的课程图缺少恢复时添加的隔离诊断结构，导致刷新后历史失配。事务写入时使用同一 lineage 检查；缺失教学证据仍保持隔离，不据此提升质量状态。
4. 中文 Word 下载调用旧自动修复，改了课程标题、添加不存在的资源说明，并在中文句号后添加英文句点；后续恢复因此关闭撤销。已确认文件恢复函数本身能读取原下载历史，问题发生在导出动作。对携带统一教学程序的课程，导出面板不再在打开或下载时触发旧内容修复；仍显示现有检查问题，明确的准备与教学审阅流程保留。

第 4 项修复有实际前后文件证据：Word 下载前后，课程表、课程图、九份派生材料和修订历史逐字段相同。新增界面回归同时检查自动修复未运行、已有导出问题仍显示，未关闭质量检查。

### 12.3 双语浏览器、历史与 Word 审读

复用一个 Chrome 测试页，未新增模型实例。英文实际修改从 17/40 更新至 19/40，参考随之从 42.5% 变为 47.5%；刷新后撤销，从实际下载文件重开后重做，结果一致。中文完成来源选择、片段绑定、预览、确认、实际 Word 下载、刷新撤销及下载文件重开重做。

最终逐字段检查使用真实下载文件和生产恢复函数：中英文撤销/重做各一组，全部课程表、课程图和九份派生材料一致；主案例数值修改没有改变独立迁移任务。一次浏览器控制点击超时后，通过当前界面确认尚未执行再重试，未把超时当作数据丢失。

实际导出的英文 rubric 为三页，中文 rubric 为三页，全部页面已渲染和审读。计算、未知群体、具体证据方案与对照回答可读，表格没有截断。中文首次渲染虽能提取汉字，页面却没有字形；排查确认为本地检查环境的字体发现问题。用任务级 fontconfig 指向项目已有中文字体后，对同一原始 DOCX 重新渲染成功，没有改写导出内容或全局字体设置。保留两次渲染以区分环境失败和产品缺陷。

这只是单课开发产物审读。中文导出仍有原有英文栏目标签，完整课程的语言一致性、所有材料与全部格式的课堂验收仍属于 M4/M5，不将本次局部检查称为完整发布样本。

证据位于 `.audit-work/v0200-teaching-system/`：

- `m2-proportion-en-final-{corrected,undone,redone}.coursemapper`：英文实际操作下载。
- `m2-proportion-zh-final-{exported,undone,redone}.coursemapper`：中文修复后的实际下载。
- `m2-proportion-download-verification.json`：文件摘要、双语逐字段往返、导出不改工程及独立任务保留检查。
- `m2-proportion-history-initial-inspection.json`、`m2-proportion-history-followup-inspection.json`、`m2-proportion-zh-restore-diff.json`：首轮失败、恢复检查及导出引入的具体差异。
- `m2-proportion-en-rubric.docx`、`m2-proportion-en-rubric-render/`：英文原始 Word 与三页渲染。
- `m2-proportion-zh-rubric.docx`、`m2-proportion-zh-rubric-render/`、`m2-proportion-zh-rubric-cjk-render/`、`render-fonts.conf`：中文原始 Word、首次字体失败、正确渲染与隔离的字体配置。

### 12.4 软件与历史回归

最终 `npm run check` 通过：58 + 381 + 796 = 1,235 项软件测试，另有原 benchmark 检查器 14 项，类型、lint、构建和 bundle 通过；初始 JS gzip 为 89.781 KiB。日志为 `m2-proportion-release-check.log`。新增比例操作回归 31 项包含来源反例、双语任务、预览不提前写入、冲突、独立练习和恢复；导出界面 58 项回归通过。

历史 v2 development 18 包和 exposed 12 包均为 0 项自动检查缺陷，分别记录在 `m2-v2-development/` 与 `m2-v2-exposed/`。它们是旧回归，不能计入 v3 保留案例或独立教师评分。

仍需完成新建任务、通用目标与表现要求编辑、其他数量/来源/实验操作和组合、生产 Scion 提案、本地作答闭环、三套六课课程、v3 与最终性能/线上验收。保持运行时 0.19.2；本阶段不发布 v0.20.0。

## 13. 一般表现要求、稳定材料身份与真实编辑往返

本阶段解决“改任务动作后，旧参考、评分或练习仍留在材料里”的问题。它是 M3 的局部交付，不能替代 M0/M2 的真实模型与操作能力、M4 的完整课程质量或 M5 的独立验收。

### 13.1 同一操作合同中的表现要求

在已有 `operationPlan` 内增加版本 2；外层教学程序和 CourseGraph 继续沿用已有权威入口，没有新建第二套任务库。`teachingPerformanceRequirements.js` 验证并投影每项要求的稳定身份、权重、学生动作、参考作答、推理步骤、四个评分档位、错误反馈、指导问题、构造的对照回答及独立案例任务。独立案例拥有单独的来源记录与来源类别。

要求可以增删，不再局限于某个操作预设的三个评分项目。删除要求时共同重建题目、答案、评分、错误分析、指导练习、独立练习和相关材料；新增要求获得新身份。来源或目标改变后，旧的教师参考必须重新核对，不能因为算术仍能求解就自动确认整段解释。

程序检查字段、身份、引用、权重、来源版本及明确的计算边界；教师编写的开放解释仍记录为“教师审阅内容”，不标成机器已证明。四类构造回答保留对应文字位置，用于审查 rubric；它们不是学生数据，也不证明真实评分区分度。减少评分项目不改变底层事实边界，例如取消“总体限制”评分后，未观察总体的比例仍然未知。

旧版合同继续兼容。版本 1 不接受伪装成旧权重的新版内容，模型提案也不能借兼容规则获得教师确认。

### 13.2 原界面中的审阅与实际负担

原有 `Review sources and scoring` 入口中增加要求编辑。每项要求展开后可以编辑任务、参考、评分和练习；预览列出受影响材料，确认后才写入同一事务，继续使用已有冲突保留和历史恢复。

这里仍有产品缺口：从旧合同开始编写新要求集合，目前得到空白草稿；教师要填写并核对一整套内容，尚未有 Scion 预填或可靠的自然语言修改提案。因此，本阶段证明了编译和修订能力，尚未证明它比教师手工编写更省时间。目标与课程 outcomes 的正式对齐也仍未完成。

### 13.3 实际材料审计暴露的缺陷与修复

1. **幻灯片内容身份依赖位置。** 删除一个要求后，其他要求可能占用原来的 slide role。新版示范、错误分析和指导问题改用要求身份；教师备注不会因为前项被删除而迁移到别的要求。
2. **空活动字段造成假冲突。** 旧编译新增内容页省略 `activity`，重放时写入 `null`，删除未编辑页因此被误判为教师修改。现在只对有生成身份的内容页识别这两种空值等价；真实备注和活动仍保留并产生必要冲突。
3. **题库固定槽位留下已删除要求。** 原来只覆盖前几个题目，余下题目的旧任务与参考继续存在。新版按练习身份重建受管题目，保留匹配题目的身份及分值、非受管教师题和真实覆盖，移除过时生成题；重新计算实际题数与总分。
4. **少分值、多要求可能分配出负数。** 新版 rubric 用整数分配与稳定余数排序处理，确保总分准确且分项非负；旧版兼容路径不变。

题库新增练习默认 1 分，并在分值说明中明确提示审阅；已有题分值保留。这还不是最终测验设计：题量配置、各类题的认知要求、同一题库中主作答和 checkpoint 重复提示的用途需要在 M4 继续审查。不能把“数量与分值一致”写成“测验已适合课堂”。

### 13.4 单页 Chrome 与实际下载证据

全程复用一个 Chrome 页，没有加载或调用模型。案例是开发者编写的比例解释任务，来源包含下午志愿者的 17/40 记录、缺失的上午组及完整总体限制；独立案例为明确虚构的设备测试记录。记录目录为 `.audit-work/v0200-teaching-system/`。

首轮在真实编辑器中把“提出可比的补充记录”改成“比较两种补充收集方案”，同时修改主任务与独立案例的参考、档位及反馈。九类派生材料预览、应用、保存、刷新撤销、文件重新导入后重做均执行。实际 rubric Word 导出渲染为三页并逐页查看，表格可读，未见裁切；该样本内容为英文，不代表新版要求的中文导出已经验收。仍观察到任务编号在导出中合成一段、第二页留白较多，列为 M4 格式改进项，不能据此宣称全部 Word/PDF/课件已验收。

首轮文件为 `m3-performance-applied.coursemapper`、`m3-performance-after-word.coursemapper`、`m3-performance-undone.coursemapper`、`m3-performance-redone.coursemapper` 与 `m3-performance-rubric.docx`。`m3-performance-download-verification.json` 记录下载 Word 前后课程图、材料和历史完全一致，以及实际撤销/重做与保存事务一致。这些是题库生命周期修复前的开发记录，保留原件，不用它们代替最终题库验收。

题库修复后又通过实际界面重新执行删除、添加、刷新撤销和文件重开重做。新的要求由界面生成全新身份，没有复用被删除要求的身份：

| 实际下载状态       | 要求数与权重    | 题数 / 总分 | 历史位置 | 文件                                          |
| ------------------ | --------------- | ----------- | -------- | --------------------------------------------- |
| 删除第二项         | 1 项，100%      | 6 / 22      | 1        | `m3-performance-current-removed.coursemapper` |
| 新增比较任务       | 2 项，60% / 40% | 8 / 24      | 2        | `m3-performance-current-added.coursemapper`   |
| 刷新后撤销新增     | 1 项，100%      | 6 / 22      | 1        | `m3-performance-current-undo.coursemapper`    |
| 导入撤销文件后重做 | 2 项，60% / 40% | 8 / 24      | 2        | `m3-performance-current-redo.coursemapper`    |

`verify-performances-current.mjs` 使用生产恢复和事务函数核对实际下载：两个往返状态的完整课程 map、graph 及九类材料逐字段相同；删除要求的具体旧文本不再存在；题目身份不重复，显示总分等于各题实分。`m3-performance-current-verification.json` 保留四个实际文件的完整 SHA-256 与字节数。验证脚本最初将独立任务中的 `easiest to access` 误写为参考中的 `easy-to-access`，该断言失败后改为核对已保存要求的实际任务字段；没有改动产品内容或实际下载来通过检查。

操作中有几次浏览器控制等待超时，但重新读取页面显示操作已完成，未重复提交。这不能直接归因为产品失败，也不证明交互足够快；后续性能阶段要区分控制通信延迟与实际主线程耗时。

### 13.5 工程检查与下一阶段

最终 `npm run check` 通过：58 studio + 403 compatibility + 796 Scion = **1,257 项软件测试**；另有两个历史 benchmark 检查器共 14 项，类型、lint、构建和 bundle 检查通过。日志：`m3-performance-final-check.log`。初始 JS gzip 为 89.788 KiB。19 项新版表现要求测试覆盖真实编译、预览事务、九类材料删除、教师备注身份、保存往返、陈旧来源/目标和分值边界；界面回归包含真实表单编辑、增删与无效草稿。

这些软件检查与开发样本没有计入 v3 的新案例、12 个最终真实模型单元、独立教师评分或三套六课课程验收。本阶段不提升运行时版本、不发布；接下来仍须完成新任务创建、低负担的 Scion 提案、课程目标对齐和其他写入者收敛，并按完整 roadmap 推进三类操作、材料职责、本地作答闭环及发布。

## 14. 新任务创建、本地来源提案与实际恢复

本阶段让没有共同任务的既有课次进入同一教学结构与修订事务，支持已实现的比例与记录修订操作。它推进 M1 与窄字段 Scion 准入，尚未完成三类操作、一般要求自动预填或完整课程验收。

### 14.1 在原材料界面创建教学任务

`TeachingTaskReview` 增加折叠的新任务入口，可选择未绑定任务的课次、操作、教学目标、课时及来源记录。新任务获得与措辞无关的身份；预览不写入工程，教师确认后通过既有 `projectTeachingTaskUpdate` 和历史事务写入课程图与九类派生材料。

新来源标为教师提供，不冒充公开研究；实际开发案例在来源文字中明确说明虚构。新任务创建与已有任务修改共享旧内容保留、冲突和过期预览规则。目前一个课次只能从此入口创建一个主要共同任务；多任务、正式目标对齐和草稿跨页面持久化仍需实现。

### 14.2 从来源提案到可核对任务

`scionTeachingProposal.js` 只请求两种操作需要的来源角色与精确片段，使用应用已有的本地运行时。输入最多 8 条、合计 6,000 字符，输出预算 1,024 tokens；一次提案最多加一次结构/摘录修复。无效输入不加载模型，重复任务不并行推理，支持取消。未知角色留空，截断不作为完整提案接受；JSON 格式正确仍需前提检查与教师语义核对。

真实开发输入为沿海巢箱的 7/25 记录、未调查内陆巢箱和全部巢箱的总体边界。首次接收器拒绝了模型额外附上的准确整条原文，修复调用又产生坏 JSON。第二版仅放宽可逐字核实的完整 record 引文；缩短或虚构引文仍拒绝。若修复更差，保留较完整的一次提案及所有原始记录，不拼接不同回答冒充一次成功。

同一输入与提示实际重跑后，8 个角色全部定位，1 次推理耗时 4,240 ms，已加载模型复用为 0 ms，总计 4,241 ms；base-only，无 adapter。这不是新模型质量成绩，而是接收协议修正后的一个已暴露开发输入。两份原始收据、第一次失败和摘要见 [Scion 试验记录](../research/scion/evaluation/v0.20.0/README.md)。累计三次真实调用，不计入最终十二单元。

界面不再用不完整提案清空已有有效来源选择。它仅补缺口，保留已有选择，提示不同建议涉及哪些角色；保存提案的采用/保留角色记录。失败和截断收据也可通过开发环境专用按钮下载。该下载按钮不进入生产构建，课堂材料也不含原始模型诊断。此处记录提案不等于认可语义；确认后的任务仍按实际教师选择编译。

### 14.3 实际检查发现的创建与后续编辑缺陷

1. 新任务插入第 2 课时，只有任务的稀疏数组会使旧的位置查找误认成第 1 课。创建投影改为携带完整课次骨架，未绑定课次保留自己的目标。
2. 作业与 rubric 的旧标题带 `Lesson N:`，新来源最初只用裸标题，造成匹配遗漏。新来源统一沿用现有课次标题格式。
3. 新建记录修订任务原本读取缺失的默认权重，现使用原操作兼容权重，不再产生未定义分值。
4. 加强“创建后再更改数值”的回归，发现后续编辑还走稀疏课次路径，给无关第 1 课的教案与活动写入第 2 课的任务 revision。已让创建和后续编辑共同使用完整课次对应；测试同时核对无关课次全部内容与新任务身份。

第 4 项的首次失败保留在 `m1-new-task-later-edit-check.log`，修复后 35 项定向事务、创建和同步检查通过，见 `m1-new-task-later-edit-fixed.log`。已有模型收据不能覆盖这个同步缺陷；它来自后续编辑的实际投影逻辑。

### 14.4 单个 Chrome 页与实际下载核对

在原 Rubrics 界面创建第 2 课任务，预览并确认 7/25 = 0.28 = 28%，明确保留内陆巢箱结果未知和总体外推限制。随后实际保存、刷新恢复、撤销创建，再从撤销文件重新导入并重做。实际 Word 下载后再次保存，验证导出没有修改课程内容或历史。

`verify-new-task.mjs` 通过生产恢复和事务函数核对实际下载：课程 map、graph 与九类材料的重做状态逐字段相同；撤销匹配原事务；提案原始收据与教师确认保留；八类按课材料的第 1 课内容不变，九份材料引用同一个任务。以下文件位于 `.audit-work/v0200-teaching-system/`：

- `m1-new-task-before.coursemapper`：开发者准备的两课基线，用于插入与无关课次保留检查。
- `m1-new-task-{applied,undone,redone,after-word}.coursemapper`：实际界面操作的下载文件。
- `m1-new-task-verification.json`、`verify-new-task.mjs`：逐字段恢复结果、实际文件摘要和可复查核对过程。
- `m1-new-task-rubric.docx`、`m1-new-task-rubric-render/`：实际 Word 与全部五页渲染。

修复后又在真实界面把第 2 课的 7 更正为 9，参考改为 9/25 = 36%，无新模型调用；实际下载为 `m1-new-task-corrected.coursemapper`。八类按课材料的第 1 课全部保持原样，任务身份不变，没有新增假冲突。旧 7/25 模型提案不被记成这次教师更正的来源；原提案仍保留在旧历史版本中。

该次核对脚本起初错误要求 FAQ 也出现 `36%`，因而失败。读到实际 FAQ 后确认它只负责提交、评分与学习支持，数值修改不应在 FAQ 泄露答案；核对改为确认其问题/回答不变、任务版本与来源同步。其他七类按课材料确有新数值。这是核对范围的修正，没有修改实际下载或产品内容来通过检查。

五页均已查看，文字和表格未见裁切，新版任务的来源、计算、评分档位与四类构造作答能够对照。内容审读仍发现英文片段直接拼接的语法生硬，例如 `describes contained an active nest`；参考答案与替代解法重复较长，第 1 课旧模板仍有空泛的示范回答。它们属于 M4 的输出改进，不能将此开发工程交作完整课堂样本。第一课刻意保留作为同步边界，并不代表其质量通过。

测试页全程复用，模型串行；刷新后没有为保存与导出重新加载模型。Chrome 标签曾显示 3.3 GB 占用，刷新后降到数百 MB；这是界面观察，不是正式内存基准。几次 native UI 控制读到旧状态或切到下载/查找面板，均在重新读取当前窗口后继续，没有重建浏览器或把控制延迟算作产品推理延迟。

### 14.5 本阶段边界

最终 `npm run check` 通过：58 studio + 414 compatibility + 808 Scion = **1,280 次软件测试执行**，配置间仍有部分既有回归重叠；另有两个历史 benchmark 检查器共 14 项，类型、lint、构建和 bundle 检查通过。日志为 `m1-new-task-final-check.log`，初始 JS gzip 为 89.775 KiB。新增 Scion 提案测试只通过 Scion 配置执行。组件按 React 检查项复核：模型模块按需加载、取消时保留草稿、异步任务清理、表单标签与显式事务入口。

当前运行时仍为 0.19.2。新任务创建与一次来源提案已经获得局部实机证据，但一般要求仍有手工录入负担；草稿持久化、目标正式对齐、全部权威写入者收敛、其他操作与组合、三套六课课程、本地作答闭环、v3 六十包及最终模型/性能/线上验收均继续按完整路线图推进。

## 15. 未确认任务草稿的项目级保存与恢复

2026-09-07。本阶段修复一个实际生命周期缺口：`TeachingTaskReview` 原先把草稿放在材料组件内，而切换材料会按 feature key 卸载组件，项目保存器因此拿不到未确认的教师输入。它属于 M1/M3 的可靠修订基础，不增加新的生成模式或材料种类。

### 15.1 保存范围和确认边界

`useTeachingReviewDrafts` 管理项目内多份任务草稿，`useProjectPersistence` 将其随同一工程快照保存；不把草稿写入 `teachingProgram`、课程图、正式材料或修订历史。既有任务修改和新任务创建均可暂存，未填写完整的来源、零权重、尚未编完的表现要求仍可保存。恢复先检查是否能安全显示，而不是要求草稿已经满足教学准入。

切换材料保留当前草稿，并可选择其他暂存草稿继续编辑。应用成功或主动放弃只删除该任务的草稿；打开其他工程、旧格式工程或新建项目会更换项目级草稿状态，避免前一工程的输入串入后一工程。来源和课程 revision 保留原值，不在恢复时静默换成最新版，因此过期草稿仍由既有预览检查拒绝。

预览、确认勾选、运行中的模型状态不恢复。刷新、切换材料及文件重开后必须重新预览与确认。仅恢复草稿不调用 Scion，也不把未确认来源变成课堂答案。原材料界面的折叠审阅区增加草稿选择和放弃入口；十类材料与主界面保持原有结构。

### 15.2 持久化与损坏数据处理

完整文件、本地自动保存、历史精简和 IndexedDB 原样快照包含草稿。既有可选云端紧凑格式以 JSON sidecar 携带草稿，避免嵌套数组限制；本轮未登录或实际调用云端服务。最后的课程图恢复格式也保留该 sidecar，不为满足存储预算悄悄删掉草稿。旧文件缺少草稿字段时正常恢复为空。

无法编辑的版本、坏字段、重复任务 ID 和坏 JSON 保留为工程中的恢复数据，界面说明无法打开，不阻断其余课程。它们不会获得课程写入资格。项目保存状态仍由现有保存器负责；普通页面隐藏/刷新会触发尚未到三秒期限的保存。异步大工程的 IndexedDB 写入仍需实际完成，浏览器崩溃、强制退出或完全不可用的存储不能保证最后一次输入已落盘；不能把页面事件覆盖当成无限制的恢复承诺。

### 15.3 实际 Chrome 与工程逐字段核对

继续复用一个本地 Chrome 标签，没有模型加载或推理。用上阶段的 9/25 已确认工程，把草稿来源改为 11/25，完成预览并勾选确认，然后切换到 Lesson Plans：来源和角色选择保留，旧预览/确认消失。刷新恢复和从首页重新打开 `.coursemapper` 后同样需要重新预览。正式课程没有应用 11/25。

首次热更新页面的下载 `m1-draft-saved.coursemapper` 与基线相比，课程 map、graph 和九类材料相同，但历史已为空；开始此试验时界面也没有撤销按钮。因此不能用这一次证明历史保留，也不能据此确定是草稿操作清除了历史。该文件仍保留。随后从首页干净导入原基线，确认两条历史恢复，再重复建立草稿、保存、刷新恢复及文件重开，逐字段检查通过。

以下文件位于 `.audit-work/v0200-teaching-system/`：

- `m1-draft-with-history.coursemapper`：实际从完整基线建立草稿后的下载；课程 map、graph、材料和两条历史与基线逐字段相同。
- `m1-draft-restored.coursemapper`：刷新恢复后的实际下载；前述四项及草稿本身完全一致。
- `m1-draft-discarded.coursemapper`：从草稿工程文件重开后主动放弃草稿的下载；仅草稿清空，已确认课程、材料和历史仍匹配基线。
- `verify-draft-persistence.mjs`、`m1-draft-verification.json`：以上实际下载的独立逐字段断言、文件大小与 SHA-256。

工作区聊天的 Attach files 只用于材料解析；第一次误从该入口选择工程时出现 `Could not parse`，没有完成工程恢复。之后使用首页的工程打开入口。此为本轮控制路径更正，不把该次动作记录为恢复成功。

### 15.4 检查结果和剩余范围

定向检查 80 项通过，涵盖组件切换、多个未完成新任务、过期课程、应用清理、文件/本地/开发快照恢复、新项目清理、普通页面隐藏保存，以及四类保存格式和损坏数据。第一次 hook 测试因测试环境缺少 localStorage 而失败，补入 happy-dom Storage 后通过；不是修复产品后假称原测试已成功。其后补充的一般表现要求草稿与坏推理字段检查亦通过，日志为 `m1-draft-performance-check.log`。

`npm run check` 通过：58 studio + 476 compatibility + 808 Scion = **1,342 次软件测试执行**；本轮也将已有项目保存/恢复测试加入默认兼容配置，数量增加不能全部解释成新覆盖。两个历史 benchmark 检查器、类型、lint、构建和 bundle 检查通过；初始 JS gzip 89.778 KiB。完整日志 `m1-draft-final-check.log`，该全量运行先于最后补充的一项表现要求草稿测试；后者定向单独运行四项草稿检查通过。配置间存在既有回归重叠。

本阶段没有新增真实模型质量证据，也没有完成三套课程或课堂验证。后续仍需正式目标对齐、全部权威写入者收敛、过期草稿的低负担修正流程、其他操作与组合、M4 语法和重复内容改进、v3 六十包、本地作答闭环及最终发布验收。第 14 节列出的英文拼接与空泛旧任务问题仍未解决，不能用保存测试替代教育质量审读。

## 16. 要求与正式目标关联、目标修改事务及恢复缺陷

2026-09-07。本阶段推进 M1/M3：区分任务自身的意图与课程正式学习目标，并使已关联目标的直接编辑进入可恢复的事务。继续沿用现有材料页和折叠审阅入口，无新增 Scion 推理。

### 16.1 关联的是现有目标及其版本

`teachingGoalAlignment.js` 使用现有 `CourseGraph.outcomes`，保存目标 ID、版本摘要以及每项表现要求的目标引用。要求增删、实质修改、任务目标修改、目标删除或移到不适用课次都会要求复核；单纯调权重不改变目标含义。摘要只用于检测变化，不是教学有效性证明。

原 `TeachingTaskReview` 可以编辑任务意图，并为每项要求勾选当前课次或课程目标。首次改变任务意图且没有要求关联时，预览拒绝应用。固定要求显示“Part and whole”“Calculation and checking”“Scope and further evidence”等具体名称；一般表现要求沿用教师命名。未确认内容仍由项目草稿保存，确认勾选和预览不跨恢复重用。

预览和确认都检查当前课程图；即使 map 的任务没有变化，图中目标已经改变也不能应用旧预览。图重建时保留能够唯一匹配的已关联目标，避免插入前面的目标后把旧序号误用于另一条目标。首次确认 native/custom ID 的关联也经过检查。

### 16.2 原课程表里的目标编辑事务

`teachingGoalEdit.js` 处理已关联目标所在的 `learningObjectives` 单元格，直接修改现有目标及关联边，不全量重建课程图。唯一不变的目标保留身份；明确的一项替换保留原 ID 并改变版本。多项同时改写无法确定对应关系时，不按位置猜测身份。共享目标的实质修改、仍受先修关系引用的目标删除，返回具体复核原因，保留当前工程。

这次写入把 map、graph 和受影响材料的 `taskGoalReview` 一起交给原修订历史。没有材料正文变化时，课程结构本身也可形成事务。明确成对提交的图不再马上被旧的 map 重建 effect 覆盖；撤销/重做使用同一对结构。目标变更不会静默重算或改写教师编写的参考答案，也不会让旧任务声称已经支持新目标。

未涉及材料保持原内容。确认下一次任务修订前，受影响材料进入复核，导出准备检查记录 blocker。诊断字段损坏时显示可操作的复核说明，不因为对字符串调用数组方法而崩溃。任务关联的结构检查仍不能证明一项“计算比例”任务满足新设的“设计抽样方案”目标。

### 16.3 实机发现的恢复与显示缺陷

最初在实际开发工程中编辑第 2 课目标时，页面变空白。浏览器控制台的产品错误为 `TypeError: prev is not iterable`，来自课程表追加编辑记录。实际下载文件中的 `userEdits` 是 `{}`；恢复入口直接把它放入本应为数组的状态。

所有项目恢复共用的 sanitizer 现在将空对象/null 编辑记录恢复为空数组，保留有效数组。非空且不支持的格式在写入项目状态前拒绝，避免默默抛弃可能有用的历史；本地恢复失败也给出错误说明。实际原始文件保留，不能将该开发工程的形状推断为所有旧版项目都存在此问题。

首次成功编辑目标时仍走旧重建路径：目标 ID 变为新 ID，材料进入静态的整类待同步状态，目标修改没有成为独立事务。该实际下载保留为 `m1-goal-target-changed.coursemapper`。随后改为上述直接目标事务，实机再次检查，目标 `o2` 保留，形成第二条事务，而不是把旧实现的结果算成最终通过。

最后的截图检查发现深色复核提示过暗、材料标签仍显示绿色完成标记、工作区仍称“ready”。已经修正提示对比度；待目标复核的标签显示警示并移除完成标记，工作区显示具体的目标复核数量，运行中的生成状态继续优先显示。学生视图不显示该内部复核横幅。截图复查可读，原界面布局保留。

### 16.4 实际文件、刷新撤销和文件重开重做

复用一个 Chrome 页和第 14/15 节的两课开发工程。先把任务意图改为明确计算已检查巢箱的比例并说明未观察内陆群体的限制，三项要求关联原第 2 课目标；9/25 = 36% 和来源边界保留。这一步的下载为 `m1-goal-applied.coursemapper`，包含一条历史。

从该实际文件重新打开，将课程表目标改为 `Design a sampling plan to estimate occupancy across all station nesting boxes.`，保存；刷新恢复后撤销并保存；再从撤销文件打开并重做。没有调用模型、应用研究来源或实际云端服务。

`verify-goal-alignment.mjs` 对实际下载执行生产恢复、目标检查、readiness 与历史应用函数，确认：

- 正式目标仍为 `o2`，但旧关联版本失效；任务程序及来源未改。
- 九类材料仅增加目标复核信息，正文、参考和教师内容与修改前逐字段相同；第 1 课没有变化。
- 两条历史保留，原第一条未改；刷新撤销后的 map、graph 和九类材料等于基线。
- 从撤销文件重开并重做后，map、graph、材料及历史等于目标修改后的版本。
- 待复核目标使实际 rubric 的准备检查阻断“已准备好”的判断。

以下本地证据位于 `.audit-work/v0200-teaching-system/`，尚不是发布附件：

| 文件                                       | 来源与用途                            | SHA-256 前 16 位   |
| ------------------------------------------ | ------------------------------------- | ------------------ |
| `m1-goal-applied.coursemapper`             | 实际确认任务意图及关联后的基线        | `8fa49d599b3a215a` |
| `m1-goal-transaction-applied.coursemapper` | 实际修改正式目标，两条事务            | `517628053f33e720` |
| `m1-goal-transaction-undone.coursemapper`  | 刷新恢复后实际撤销，cursor 为 1       | `ba980a352d4b8e44` |
| `m1-goal-transaction-redone.coursemapper`  | 从撤销文件重开后实际重做，cursor 为 2 | `09e115cef99a91e8` |

完整摘要与检查项在 `m1-goal-verification.json`，执行日志为 `m1-goal-artifact-check.log`。最初的目标关联试验是在热更新后的零历史起点进行，不能据此证明保留了第 15 节原有的两条历史；本节历史核对使用上述实际一条历史基线，再增加目标事务。

### 16.5 检查范围与剩余工作

定向恢复检查 53 项、目标/事务/组件检查 104 项通过；后续显示检查 37 项通过。最终 `npm run check` 为 58 studio + 511 compatibility + 808 Scion = **1,377 次软件测试执行**，另有历史 benchmark 检查器；类型、lint、构建和 bundle 检查通过。新增纳入的既有 AgentWorkingSetPanel 测试不能算成全部新增功能覆盖，配置间也仍有既有回归重叠。初始 JS gzip 89.786 KiB。最终日志为 `m1-goal-release-check.log`；名称不表示已完成发布验收。

本阶段没有新模型、真实学生或独立教师效果证据。整版仍需课次重排及其他目标写入入口统一、共享目标编辑、过期草稿修正、三类操作与组合、多课进展、一般要求预填、输出语法与重复问题、本地作答改进、v3 六十包、三套六课课程和全部格式/性能/设备/线上验收。当前运行时仍为 0.19.2，不能把已验证的目标事务当作 v0.20.0 完成。
