# V0.15.151 CS/Python Source Texture Gate Roadmap

## Evidence

Fresh deployed v0.15.150 GPT-5.5-pro Introduction to Computer Science with Python ZIP/log evidence was captured locally at `verification-output/edutool-audit-loop/20260630T100316Z-v015150-gpt55pro-python-fresh/`.

- Model: `gpt-5.5-pro`
- App version: `0.15.150`
- Run ID: `run-1782812227528`
- Embedded report: 99/A, texture 91, 0 P0/P1/P2
- Current-run local regrade before this release: 99/A, texture 91, 0 P0/P1
- Patched source-truth regrade: 98/A, citations 92, texture 91, 1 P1
- Main hidden debt: trusted source-ledger row `Correlation` was promoted as proof for Python `variables`
- Main scored texture debt: Course Map fallback rows repeated generic assigned-materials, LMS/tooling, and evidence-standard phrasing across the Python lessons

## Scope

V0.15.151 keeps the release narrow:

1. Treat correlation/statistical-variable pages as CS/Python false friends unless the source has programming context.
2. Remove bare `variables` as a general CS source anchor while preserving valid programming, Python, data-type, and type-system sources.
3. Mirror the production source rule in the deep quality grader so already-exported false-clean artifacts regrade truthfully.
4. Add a CS/Python Course Map fallback profile with code-tracing, debugging, testing, input/output, runnable-artifact, and edge-case language.
5. Keep the model-comparison conclusion explicit: GPT-5.5-pro improved visible structure, but the remaining source and fallback-texture defects are CourseMapper-side.

## Goal

Stop statistical-variable source bycatch from inflating trusted source proof in CS/Python packages and reduce repeated generic Course Map scaffolding that keeps texture below the long-running clean target.

## Lane

Source relevance, source-ledger trust, quality-grader truth, and deterministic Course Map texture. This is not a model prompt change, score inflation, source-license loosening, or UI redesign.

## Release Boundary

The release may claim the captured `Correlation` false friend is quarantined and CS/Python fallback Course Map rows use domain-specific programming copy after focused source-ledger, source-grader, texture fallback, and normal release gates pass. It may not claim 100/100 GPT-5.5-pro packages until a fresh deployed v0.15.151 ZIP/log audit verifies the new gate in real provider output.

## Verification Target

Ship only after focused source and texture regressions, patched-artifact regrade, normal release gates, and release-history audit pass. The next evidence step is a fresh deployed v0.15.151 GPT-5.5-pro Python ZIP/log audit to confirm trusted sources are programming-relevant and texture improves in provider output.
