# v0.15.78 Texture Caveat Truth Roadmap

## Evidence

A fresh deployed v0.15.77 Project Management ZIP/log audit completed on
June 27, 2026:

- App version: `0.15.77`
- Run ID: `run-1782545502187`
- Finish ID: `finish-mqw1nrvb-gcd9g`
- ZIP: `verification-output/edutool-audit-loop/20260627T073543Z-v01577-fresh/Project-Management-Course-Materials-v01577.zip`
- Console log: `verification-output/edutool-audit-loop/20260627T073543Z-v01577-fresh/edutool.dev-cm-console.log`

The deployed run was a real quality improvement over v0.15.75:

- `12/12` materials ready.
- Readiness `ready`, `0` blockers.
- Export verification passed.
- Enrichment covered all `12/12` lessons.
- Trusted source rows were on-discipline for Project Management.
- No generic Session/Topic label, Course Map double-period seam,
  off-discipline medicine source, or Quiz Bank `X: X` echo chain reproduced.
- Prompt-artifact support phrasing still appeared in Syllabus/Lesson Plan
  material text; it was not the scored loss in this run, but remains a
  product-quality cleanup target.

However, the downloaded package was still not a clean 100/100 package. The
browser digest reported `99/100` with one P2 texture warning, while a local
regrade initially reported `98/100` because the grader misclassified the
texture caveat text as native-authoring fallback:

`Texture score 83/100 indicates repeated prose patterns across deliverables`

The word `prose` in that texture warning is not evidence that native authoring
fell back to prose. The native-fallback P2 should only fire for explicit native
fallback signals such as `fell back to prose`.

## Lane

Grader and report truth. Fix the false-positive caveat classifier before
working on the remaining real texture problem.

## Scope

- Narrow the native-fallback caveat detector to explicit native fallback
  language.
- Preserve the existing native fallback regression that fires on
  `fell back to prose: degenerate-skeleton`.
- Add a regression for the v0.15.77 texture-warning shape so repeated prose
  warnings remain texture findings only.
- Keep the texture P2 intact; do not inflate the package to 100/100.

## Non-Goals

- Do not hide low texture.
- Do not change the texture score or threshold.
- Do not claim the v0.15.77 package is clean.
- Do not modify source-ledger scoring, enrichment gates, export verification,
  or provider behavior.

## Release Boundary

In scope:

- Deep quality grader false-positive cleanup.
- Focused package-quality regression.
- Release metadata for the narrow report-truth patch.

Out of scope:

- Deterministic Course FAQ texture variation.
- UI copy changes.
- Source retrieval architecture changes.
- Another provider generation before deployment.

## Verification

- `npm test -- --run tests/v0143-quality-badge.test.js`
- Regrade the captured v0.15.77 ZIP locally and verify it reports `99/100`
  with only the texture P2.
- Normal release gates before push: format check, lint, full unit test suite,
  build, bundle check, release-history audit, and gold smoke.

## Next

1. Deploy v0.15.78.
2. Run a fresh EduTool.dev ZIP/log audit only after deploy if another proof
   round is needed.
3. Target deterministic Course FAQ texture variation so repeated support
   answer shingles no longer hold texture at `83/100`.
4. Clean up remaining prompt-artifact support phrasing in Lesson Plan and
   Syllabus material text.
