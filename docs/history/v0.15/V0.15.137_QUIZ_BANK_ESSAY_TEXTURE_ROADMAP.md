# V0.15.137 Quiz Bank Essay Texture Roadmap

## Goal

Use the fresh v0.15.136 ZIP/log evidence to remove one remaining CourseMapper-owned Quiz Bank essay texture repeat without weakening the clean 100/A package result, source proof, export verification, or report truth.

## Evidence

- Fresh deployed audit: `verification-output/edutool-audit-loop/20260629T210840Z-v015136-fresh/`
- App version: `0.15.136`
- Run: `run-1782767137586`
- Finish run: `finish-mqzpm7qf-iwtbe`
- ZIP: `User Experience Design Studio - Course Materials (55).zip`
- Console log: `chrome-devtools-log.txt`
- Local regrade: `100/100 (A)`, 0 P0, 0 P1, 0 P2
- Digest gates: export passed, 38 checked, 0 failed, 0 warnings, enrichment coverage 1, retryCallCount 0
- Texture: `95/100 (A)` with advisory repeats, but no score-bearing findings
- v0.15.136 verification: `review distractor choices before the next`, `distractor choices before`, and `It's tempting to think` all had zero hits in the exported package.

## Diagnosis

The package is clean by the current quality gate, and the previous v0.15.136 quiz-bank/pitfall repair held in real provider output. The remaining CourseMapper-side texture target is narrower:

- Ten Quiz Bank essay answers reused the exact counterpoint sentence family `A strong answer also engages the opposing view...`.
- This is deterministic projection scaffolding from `kernelProjection`, not model randomness.
- The answer still needs an opposing-view cue, but the sentence family should vary across lesson-specific kernels.

## Lane

V0.15.137 keeps the release narrow:

- Seed essay counterpoint answer wording from lesson-specific prompt, term, fact, and position text.
- Rotate equivalent opposing-view sentence families while preserving the claim/counterclaim/evidence scoring contract.
- Add a focused kernel-projection regression proving multiple essay projections do not all retain the old counterpoint scaffold.

## Fix

1. `projectionTextSeed` creates a deterministic seed from lesson-specific kernel text.
2. `composeScenarioAnswer` now rotates four counterpoint sentence families when a counterpoint is present.
3. `buildEssayItem` passes the lesson-specific seed into `composeScenarioAnswer`.
4. `src/lib/__tests__/kernelProjection.test.js` proves the old scaffold is not stamped across all essay projections while every answer still names the opposing view.

## Release Boundary

- This release does not claim fresh v0.15.137 provider output.
- The v0.15.136 provider run is the evidence for the fix target; a fresh v0.15.137 ZIP/log audit is still required to prove this counterpoint texture cleanup in real output.
- Texture remains advisory in the current scoring model, but the long-running target still treats texture below 100 as an improvement lane.

## Remaining Target

Fresh deployed ZIP/log evidence still needs to prove:

- overall 100/100
- no hidden source/review/export caveats
- source rows remain real, trusted, on-topic, license-safe, and concept-linked
- no metadata-only, off-topic, or false-friend source-ledger proof
- no prompt-artifact, generic Week/Session/Topic, or stale off-discipline text
- UI, ZIP, manifest, quality report, source report, digest logs, and local regrade agree
- the old Quiz Bank counterpoint scaffold does not dominate exported essay answers
