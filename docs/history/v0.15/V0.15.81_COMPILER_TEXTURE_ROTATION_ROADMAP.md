# v0.15.81 Compiler Texture Rotation Roadmap

## Goal

Reduce the real v0.15.80 texture loss by rotating deterministic compiler
phrases in Discussion Prompts, Slide Deck notes, and Lesson Plans without
weakening the grader or hiding caveats.

## Evidence

A fresh deployed v0.15.80 Project Management audit completed on June 27, 2026
and saved evidence under:

`verification-output/edutool-audit-loop/20260627T103404Z-v01580-fresh`

The package regraded locally at `99/100 (A)` with one P2:

- `texture`: 86/100.
- Repeated shingle evidence included discussion-language tails such as
  `a limitation or revision move tied to [slot] instructor preference feedback tied`.
- Additional texture advisories named Slide Deck and Lesson Plan compiler
  tails repeated across all 12 lessons.

The same audit showed important progress from v0.15.80:

- Course FAQ prepared-response/concrete-evidence repetition no longer scored.
- No `Session N`, `Topic N`, or exact `X:X` echo chain was found.
- Export verification passed with 38 checked files and 0 warnings.
- No app-bundle `QuotaExceededError` reproduced in the current-run log.

## Lane

Compiler texture rotation. This is a deterministic output-quality slice for
repeated compiler-authored tails, not a grader threshold change and not a
broader provider-generation architecture change.

## Scope

v0.15.81 changes only deterministic compiler text:

1. Rotate Discussion Prompt follow-up challenge phrasing.
2. Rotate Discussion Prompt participation review phrasing.
3. Rotate Slide Deck bridge/title speaker-note phrasing.
4. Rotate Lesson Plan warm-up purpose and guided-analysis phrasing.
5. Add a focused compiler regression for the exact v0.15.80 repeated shingles.

## Release Boundary

In scope:

- Discussion Prompt repeated follow-up and participation-review phrasing.
- Slide Deck title/bridge note repeated texture tails.
- Lesson Plan warm-up and guided-analysis repeated texture tails.
- Focused compiler regression for the v0.15.80 artifact shape.
- Release metadata for the v0.15.81 product-quality patch.

Out of scope:

- Changing quality score thresholds or hiding the texture finding.
- Repairing the direct CourseIR `a_sched_lab` fallback.
- Claiming source-ledger, enrichment, export, or UI readiness improvements
  beyond the compiler texture rotation.
- Running another provider generation before v0.15.81 is deployed.

## Out of Scope

- No grader threshold changes.
- No claim of a clean 100/100 package before a deployed v0.15.81 audit.
- No broad source-ledger or CourseIR architecture change in this slice.

## Carry Forward

- Fresh deployed v0.15.81 ZIP/log audit must verify whether package texture
  reaches 100.
- Direct CourseIR fallback from the invalid `a_sched_lab` assessment reference
  remains a separate product-quality target.
- Source proof is still thin in this run: three trusted Wikipedia rows, with
  two concept links using generic Project Management support phrasing.

## Verification Plan

- Focused compiler texture regression:
  `node ./node_modules/vitest/vitest.mjs run src/lib/__tests__/courseBlueprintCompiler.test.js --testNamePattern "varies lecture-exam slide and lesson-plan texture"`
- Normal release gates:
  `npm run format:check`
  `npm run lint`
  `npm test`
  `npm run build`
  `npm run bundle:check`
  `npm run audit:release-history`
  `npm run audit:gold:smoke`
