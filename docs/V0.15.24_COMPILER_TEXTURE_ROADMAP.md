# V0.15.24 - Compiler Texture

## Goal

Make the deployed EduTool.dev package move closer to an honest 100/100 by
removing deterministic compiler prose tails that repeated across every lesson
deck after the v0.15.23 assessment-reconciliation fix.

The fresh v0.15.23 Intro Psychology audit produced a clean package: ZIP
structure passed, the old in-class assessment caveat disappeared, and the local
deep grader found no P0/P1/P2 findings. The remaining 99/A came from texture
93/100. The evidence pointed at repeated slide-deck and lesson-plan language,
not a missing artifact or provider outage.

## Lane

1. Remove repeated default practice-move tails from lesson-scoped slide decks
   without changing the instructional purpose.
2. Vary lecture/exam retrieval-cycle language across lessons so concept-check
   practice does not stamp one long phrase into every deck.
3. Rotate activity, example, and misconception-closure speaker-note anchors
   where the downloaded ZIP showed repeated template shingles.
4. Make the voice-pass self-check disclosure honest by labeling it as
   voice-surface texture, not whole-package texture.
5. Add focused regression coverage that compiles the audited four-lesson
   Psychology lecture/exam shape and rejects the repeated tails.

## Release Boundary

This is a compiler texture and disclosure release. It does not change provider
call routing, CourseIR direct authoring, genome linking, assessment
classification, export packaging, or the texture grader threshold. It should
improve future generated packages by varying deterministic compiler language
before any model voice pass runs, with zero added provider calls.

## Evidence

- Source audit: `verification-output/edutool-audit-loop/20260620T035723Z/`
- App under audit: `0.15.23`
- Run ID: `run-1781927701103`
- Local regrade: `99/100`, texture `93/100`, zero P0/P1/P2 findings
- ZIP structure: `0` issues
- Regression: `src/lib/__tests__/courseBlueprintCompiler.test.js`
