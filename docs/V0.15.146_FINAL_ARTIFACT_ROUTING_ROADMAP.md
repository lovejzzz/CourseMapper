# V0.15.146 Final Artifact Routing Roadmap

## Evidence

Fresh deployed v0.15.145 User Experience Design Studio ZIP evidence was captured at `verification-output/edutool-audit-loop/20260630T050121Z-v015145-fresh/`.

- Exported ZIP: `package.zip`
- App-visible score: Quality 74, Texture 96
- Package report and local regrade: 74/C with 2 P0 identity findings
- Concrete failure: `Final revisions annotation: research detail to design choice.` and `Final prototype studio defense: prototype move and evidence.` were registered as exam-kind rows, but their Quiz & Exam Bank DOCX files did not contain those registered assessment titles.
- Capture caveat: Chrome console-log recovery timed out after the laptop restart/reconnect, so this release uses the downloaded ZIP, `QUALITY_REPORT.md`, `PACKAGE_MANIFEST.json`, local regrade, and the visible digest evidence observed before Chrome control reset.

## Scope

V0.15.146 keeps the release narrow:

1. Tighten assessment-kind derivation so only truly bare `Midterm`/`Final` labels are promoted to exam-kind by the fallback rule.
2. Preserve named final artifacts such as final revision annotations, final prototype defenses, final projects, and final essays as non-exam artifacts unless the title explicitly says exam.
3. Keep true `Midterm`, `Final`, and `Final Exam` behavior unchanged.
4. Make `PACKAGE_MANIFEST.json` readiness agree with self-grading: P0 quality blockers must mark readiness blocked, not ready.

## Goal

Prevent named final artifacts from being routed into fake quiz-bank exams, and keep exported package readiness truthful when self-grading finds blocking quality issues.

## Lane

Registry classification, compiler-routing proof, and package manifest truth accounting. This is not a broad model-prompt, source-retrieval, or texture rewrite.

## Release Boundary

The release may claim the classifier/routing and manifest-readiness fixes after focused tests and normal gates pass. It may not claim clean provider output until a fresh deployed v0.15.146 ZIP/log audit proves the real generated package is clean.

## Verification Target

Ship only after focused registry/compiler and manifest-readiness regressions pass, plus the normal release gates. A fresh deployed v0.15.146 ZIP/log audit is still required before claiming clean provider output.
