# Integration verification on current main

Base: `0e64872aa21543d52077ce7b5fb219cb4f1361ca` (v0.19.99), verified 2026-09-19. Changes were integrated in the isolated `external-authoring-release` worktree and merged through PR #112 as `b125121a`; the original checkout and unrelated edits are preserved.

The current release's recovery, exact autosave, compiler behavior, PDF engine and Pages deployment workflow were retained. Accepted author content bypasses synthetic repair and regeneration. New compiler transaction and snapshot dispatch paths reject late generation over an accepted author layer. Explicit teacher edits retain the author layer and record their override; history restoration retains exact snapshots.

Completed checks:

- `npm run check`: type checks, lint, 58 studio/server tests, 147 authoring tests, 1,044 compatibility tests, 1,074 Scion tests, 45 benchmark checks, build and bundle budgets passed. Six emulator cases were skipped in that offline invocation.
- A follow-up authoring/security run passed 164 tests after adding the existing cloud security suite to the authoring configuration.
- Those six Firestore cases subsequently passed against a separate emulator on port 8089, including concurrent identity binding, unlink/callback ordering, cloud content and revision transactions.
- `npm run format:check` passed.
- Both production-browser PDF/CSP tests passed after installing the missing Playwright browser binary.
- Real Chrome authoring smoke passed: reviewed import, scoped revision, apply, teacher edit, local reopen, project-file reopen, DOCX and student CSV exports, deleting the draft without deleting the applied course. Zero model requests and zero browser errors.
- A fresh allowlisted runtime directory with the deployment lockfile installed passed Node 22 startup, metadata and unauthenticated MCP rejection. Source redaction is a dependency-free shared module, avoiding browser-only restore imports in the backend.

Evidence: `verification-output/external-authoring/current-release/` plus the browser artifacts in its parent directory. All 57 classroom browser regression tests passed. Authored course-map, lesson, assignment and rubric PDF checks passed, including Chinese/math preservation and original 40%/60% criterion weights. DOCX/CSV cross-format assertions and student answer exclusion passed; the rendered rubric PDF was visually inspected without clipping or missing glyphs. These local results do not establish live OAuth, real ChatGPT/Codex integration, public plugin availability or classroom validity.

## Deployment and upgrade verification, 2026-09-20 UTC

- All PR and main-branch checks passed, including the isolated Docker runtime/Firestore workflow. Main fast verification run: `35485004958`.
- Pages deployment `35485331859` succeeded. `https://edutool.dev/release.json` reports commit `b125121a4c983ce3a61ffdf568ded3190d94c1ba`; the landing page rendered in Chrome. Callback HTML and JavaScript return 200, and the deployed CSP includes the exact exchange origin.
- Cloud Run revision `coursemapper-authoring-00004-ntv` serves the new digest-pinned runtime. Protected-resource metadata and missing-auth rejection passed; remote writes and application reservations remain disabled.
- Fifteen existing Firestore rules tests passed separately from the six authoring transaction tests before publishing the default-database rules.
- The old authored project fixture opens on this release. Course-map edits retain accepted materials and mark them stale. Header sync and card retry preserve every accepted material and report review required, in both external-agent and website-generation modes. Both runs recorded zero model requests and zero page errors.
- The cascade smoke script now follows the current save-menu auto-close behavior and terminal sync card/retry flow. The original failures were obsolete test interactions, not lost project content. Evidence: `cascade-external-agent/result.json`, `cascade-site-model/result.json`, and `current-release/public-frontend.json` under the authoring verification output.

## Live authentication and transport follow-up

- The user saved production Google credentials in Auth0 and approved the Google connection for the private client. Real Firebase Google sign-in and the website Auth0 identity-binding flow succeeded on `edutool.dev`.
- PR #114 fixed the production CSP's missing `https://apis.google.com` script origin. All three production browser tests passed, including the Firebase Google bootstrap and rejection of an unlisted script origin. Deployment `35490156749` succeeded; the live frontend reports `1c5bf165e193bde3105d1ac51299ae524e910efa`.
- PR #115 accepts JSON-capable MCP clients without requiring them to advertise unused SSE responses. The endpoint still rejects incompatible media types and enforces authentication on private tools. All CI checks passed, including classroom Chromium tests and isolated runtime/Firestore verification; merge commit: `31057ace7fb5af3b4e0483e75d486e51e227391d`.
- Backend revision `coursemapper-authoring-00005-tzc` serves the tested fix. Direct live JSON-only initialization and tool listing return 200 with 14 tools, while an unauthenticated private read returns `UNAUTHENTICATED`. The offline authoring run passed 165 tests; six emulator cases were covered separately by the passing runtime CI.
- ChatGPT Developer mode and CSP enforcement are enabled. The initial strict third-party Auth0 client completed login and code exchange, but ChatGPT rejected its callback with HTTP 400 `OAUTH_OWNER_PROFILE_ID_MISSING`. Auth0 documents that this client class does not support OIDC scopes, ID tokens or `/userinfo`; removing `offline_access` alone did not fix it.
- A dedicated OIDC-capable first-party Native client now uses authorization code, public token authentication (`none`), the exact ChatGPT callback and the five authoring scopes. Implicit, refresh and password grants are disabled. Google is enabled and database login is disabled. The Auth0 Management API now requires per-app user-delegated authorization; this client has zero Management API permissions and exactly five CourseMapper permissions.
- **Account connection and tool discovery now pass in real ChatGPT.** The private app `CourseMapper OIDC Private` retained the connected account, and Refresh discovered all 14 actions. This fixes the former OAuth blocker. Real ChatGPT calls to public capabilities and authenticated request listing subsequently succeeded without authentication errors. A synthetic request created by ChatGPT appeared in the signed-in website. The complete author/review/apply workflow remains under test.

Draft writes and website application are now enabled on revision `coursemapper-authoring-00007-5d8` for the private test. A real ChatGPT run created a one-lesson request, obtained contracts, submitted the course plan and lesson bundle, validated, previewed, and reached `awaiting_review`. Website review, explicit application and the remote receipt succeeded. A project-file export retained the reviewed lesson, assignment and rubric content fingerprints and all three authored-content layers; its API budget receipt recorded zero website model starts. The existing project was first downloaded as a backup. Cloud save briefly failed while opening that existing project; the acceptance project subsequently reported autosaved to My Projects.

Semantic review found assessment answers in the model-authored `studentEvidenceExpected` and rubric bands. These fields are student-facing, so passing schema/reference validation was insufficient. The generation contract now explicitly identifies student-facing fields, restricts assessment solutions to evaluation/teacher notes, and requires separate worked-example problems. A fresh corrected real ChatGPT draft passed review and was applied with its receipt confirmed. Student prompts, evidence requirements, assignment brief and rubric descriptors omit solved assessment values; full teacher answers remain present. Both versions are retained for audit. The affected authoring suite passed 167 tests (six emulator cases are covered separately by runtime CI).

A live reload initially resumed the older saved project. Application now awaits publication of the exact authored workspace and its main Resume pointer before reporting success. Return Home also awaits its asynchronous save instead of checking a Promise as a boolean. The browser regression seeds an older project and reloads immediately after application success; exact authored content, later teacher edits, project-file reopen, DOCX and student CSV checks pass with zero model requests. PR #116 was merged as `eda4545e9c1483ce2a4417c4e9c3f67668610414`; Pages run `35494245895` succeeded and the public manifest reports that commit. The real recovery check then exposed the underlying browser condition: `QuotaExceededError` while publishing even the small Resume marker. The exact authored snapshot remained in IndexedDB and the new error was surfaced instead of falsely reporting resumability.

The quota follow-up commits the same resume pointer through the existing IndexedDB autosave fallback before removing only the obsolete localStorage resume marker. It leaves unrelated browser data untouched and applies to both initial application and subsequent autosaves. A quota-injected Chrome run passed immediate reload over an older saved project, teacher-edit preservation, Return Home, file reopen and DOCX/student CSV; zero model calls. All 168 authoring tests passed. PR #117 merged as `536b30600b8c2dedcb4b8439e5fc857053470582`; Pages run `35494935041` succeeded. The actual storage-full Chrome profile recovered the corrected application, and immediate reload/Resume reopened the correct course with its student checklist and 40/60 rubric summary intact.

Second-account live isolation, provider revocation/refresh, cross-device interrupted recovery and other target-host acceptance remain pending. The private connection works through application; public plugin publication and universal classroom validity are not established.

Evidence: `verification-output/external-authoring/live-oauth/registration-progress.json`, `json-discovery-deployed.json` and `HANDOFF.md`. No credential values are retained in those artifacts.

## Resilience follow-up

A deterministic interleaving reproduced an additional quota-fallback race: an older pending IndexedDB save removed a newer project's local Resume marker. Marker retirement now checks that the stored value still equals the marker the pending operation replaced. Failed fallback transactions preserve the previous marker.

Website exchange calls now have a 30-second deadline covering token refresh, network response and response parsing. Timeout aborts the request and releases the UI; a late token refresh cannot send a mutation. The message explicitly says a server change may already have committed and directs users to refresh/recover before retrying. Null response bodies fail with an actionable error. Existing durable intent/reservation/receipt recovery remains the mechanism for uncertain writes.

All 175 authoring tests passed locally (six Firestore cases require the separate emulator workflow). New real HTTP tests use cryptographically signed JWTs and the production verifier to check same-token binding revocation, revoked-before cutoffs, newly issued tokens, expiry, cross-account denial and request revocation/list filtering. These automated issuer fixtures do not substitute for real provider or second-Google-account acceptance. ChatGPT's fresh page still stalled loading its connector selector during the follow-up, before a tool call could be submitted.

The follow-up also runs two isolated real Chrome profiles against an HTTP exchange fixture. Lost reservation and receipt response bodies trigger the production client deadline; reloading the first profile recovers its durable intent and latest teacher edits. The second profile remains locked out after a simulated day and after receipt commit, while retrying the first receipt is idempotent. This six-case script is now part of browser CI. Identity and server storage remain fixtures; it does not establish live Google/Auth0 or physical-device cloud-sync behavior.

## Deployed source-sharing follow-up

The signed-in website successfully shared a synthetic source request, listed it remotely, and replaced its source with a second explicitly entered note. The remote editor previously showed only source titles; it now offers a read-only stored-text preview alongside each keep-sharing choice, including extraction/visual limitations and recognized-redaction status. Text is rendered as escaped text, never HTML. The source-selection browser regression verifies stored previews, replacement/removal, credential-like text redaction, cancellation and absence of image/script requests. All 177 authoring tests pass locally, with six emulator cases reserved for runtime CI. The preview itself awaits publication and verification against the server-returned synthetic source.

The privacy page now explains the implemented server-side authoring exchange, explicit source/baseline sharing, Firebase/Auth0 identity binding, revocation versus deletion, separate applied-course/provider copies, 30-day request expiry, daily cleanup and retained security markers. Existing text incorrectly implying a new project erased all local data was corrected. Current Cloud configuration was re-read: daily retention job enabled at `0 3 * * *`, default logs 30 days, required audit logs locked at 400 days. This is a description of implemented behavior, not public-platform approval.

## Initial request choices

The initial website request form now accepts learner profile and language and lets teachers choose lesson plans, assignment briefs and/or rubrics. At least one material is required. Brief-only input remains supported, with omitted learner/language details explicitly recorded as uncertainties. Copied AI tasks include all these requirements rather than silently omitting them. A real-browser regression verifies explicit values, single-material selection, rejection of no materials, exact persistence after reload and disclosure of missing details; this check runs in browser CI.

Single-material application tests exposed a fixed lesson-plan active tab even for assignment-only or rubric-only requests. Application now opens the first requested material. Three submit/validate/preview/apply tests verify each material independently and retain its exact author layer. The Chromium request-choice regression also imports/applies an assignment-only fixture and resumes directly into its editor after reload. All 180 authoring tests pass locally (six emulator tests run in runtime CI). This fixture checks projection and persistence, not the model's language or pedagogical quality.

## Request-choice release and live check — 2026-09-20

PR #120 merged as `feefd1c413281e0629b7fc90bbb693c3c07dcf87`. All source/build, classroom browser, adapter and runtime checks passed; Pages run `35497740058` succeeded and the live release manifest matched. A synthetic local request on the deployed site retained an adult-beginner learner profile, Español and Assignment briefs only after reload. The copied AI task contained all three values. No source was shared and no new course was applied in this check; the previously reviewed ratios course resumed successfully. This verifies request preservation, not Spanish generated-content quality.

The plugin metadata now includes creation, revision and interrupted-draft recovery starters. The package and its skill pass the plugin-creator and skill-creator validators. The [distribution review packet](DISTRIBUTION_READINESS.md) records actual permissions, verified private evidence and unfinished public/host/account acceptance; no installation or publication was performed.

## Provider mode-transition regression — 2026-09-20

Five new failing-before/fixed-after cases exercise a switch into external AI mode while image generation or local lesson-detection module loading is already in progress. GPT Image and DALL-E batches now check before every new request, including fallback models after network/provider errors. Local lesson detection rechecks after its dynamic import and propagates the policy error instead of swallowing it. The already-started image request is not retroactively cancelled; the tests prove no subsequent request starts. No real provider credentials or paid inference were used.

All 185 authoring tests pass (six emulator cases require the runtime job), plus 42 existing image, lesson-detection, grammar and WebLLM cases. This closes these specific transition gaps; the broader large-course/provider acceptance matrix remains separate.

## Two-workspace hot-reload acceptance — 2026-09-20

`scripts/authoring/workspaceHmrSmoke.mjs` imports two independent synthetic project files through the landing-page importer, creates a request in each workspace, and retains old tool callbacks. It verifies actual Vite hot reload of AuthoringPanel, renewed page authorization, stale-epoch rejection, rejection of A mutations through the fresh B grant, and exactly 13 unique registrations. Both stored request base snapshots match their respective course and retain revision 0 with no drafts. Chrome and Chromium runs pass with no page errors. The required browser CI job now runs this regression and retains its evidence.

The browser and Vite lifecycle are real; the tool-registration host is instrumented. This is lifecycle/isolation evidence, not an additional real AI-platform integration or content-quality run. No product code change was needed: the first failures were test locator/timing issues, corrected by selecting the textarea by accessible role and waiting for the actual grant state after hot reload.

## Rollback verification maintenance — 2026-09-20

The disabled-build browser script now selects Current material and the stable Word export control, waits for click/download together, and accepts a Chrome/Chromium channel override. It passes against an isolated production build with all three authoring flags false, preserving the saved authored course, teacher edit and Chinese DOCX text. Eight affected rollback/config tests pass. A zero-traffic Cloud Run revision using the existing production image reports writes disabled while the serving revision reports writes enabled; see [deployment scope and restoration notes](DEPLOYMENT.md#disabled-revision-rehearsal--2026-09-20). This is bounded rollback evidence, not a completed live authenticated cutover.

## Account-boundary follow-up, 2026-09-20

PRs #129–132 preserve the cloud owner of open/local-recovered projects, pause automatic saves after an account switch, and refuse to infer ownership for older snapshots with a cloud project ID but no recorded owner. The notice opens My Projects for an explicit cloud open or Save Current as New Project. Production Pages run `35527493613` serves `e41614d54c70551a640a6663e2a3fa5fc454995f`; PR #132 passed the full source/build and Chromium CI. Real B→A→B switching verified the earlier pause/resume behavior, including local Resume, without copying the course into A. Legacy-owner branches have automated regression coverage; a separate live legacy-snapshot acceptance is not claimed.

The next cache isolation change partitions profile, custom deliverables, developer templates, agent memories and preferences by Firebase UID. The old unowned browser keys remain intact for signed-out use, and are never automatically adopted by a signed-in account. Existing signed-in cloud data repopulates only its matching account cache. Local storage is not an authorization boundary against someone with access to the browser profile; this prevents accidental cross-account merge/upload within the application.

New regressions reproduce the original five cross-account uploads with mocked cloud calls, then verify their rejection, normal per-account CRUD, delayed cloud responses, late template UI responses and a profile edit interrupted by switching accounts. No private production data was copied during these tests. Live OAuth/code replay, reverse account denial and mobile roundtrip evidence resides in `verification-output/external-authoring/live-oauth/`; credentials/tokens are excluded from tracked evidence.

## Reviewer access and cloud Resume verification — 2026-09-20

Production website release `38fe45d6540f0b15304feab898dbad1e157c4701` includes PRs #134–136. PR #134 adopts the fresh Firebase user after same-account Google reauthentication; the live connection-management flow passed without a page reload. PR #135 prepares the stored workspace revision before explicitly opening a cloud or project-file snapshot, preserving ordinary stale-tab compare-and-swap protection. Its 35 focused recovery tests and full CI passed. After deployment, opening the test account's plant-studies cloud course, reloading, and choosing Resume retained that course and its mobile-edited lesson title; it previously resumed the older ratios course.

PR #136 adds Firebase email/password sign-in and current-account reauthentication so reviewers can use the test workspace without a Gmail login. Passwords are submitted to Firebase, cleared from the form after attempts/cancellation, and excluded from application logs and persistent application state. Eleven focused auth/dialog/menu tests, source/build checks and classroom Chromium CI passed. Pages run `35530632188` succeeded and the public release manifest matched. Live checks covered wrong-password rejection, successful email sign-in, email reauthentication, shared-request listing, the existing AI identity binding and reopening the same test cloud course.

A separate Auth0 database identity is linked to this synthetic workspace. Its actual provider token read the workspace's shared request, returned `NOT_FOUND` for an active primary-account request, and returned `FORBIDDEN` for a schema-valid write through the read-only Codex QA client. The installed Codex client's authorization was renewed. An earlier malformed write probe returned `INVALID_INPUT`; it is not the scope-denial evidence. The corrected results are in ignored `reviewer-database-isolation.json` and `reviewer-website-login.json` evidence files. Provider tokens were not persisted there.

The owner explicitly approved database login for the ChatGPT client, domain-level database availability for third-party clients, Firebase email/password activation, and disclosure of the dedicated reviewer credentials in OpenAI's private review form. OAuth and website passwords are distinct from each other and from Gmail. The local inventory is outside the repository with mode `0600`; no credentials are included in this document. OpenAI's scan returned 14 tools, with all 42 annotation explanations retained. Public submission, review approval and ordinary-teacher availability remain unproven; a demo recording and final policy confirmations are still required.

## Reviewer poetry flow and display refinement — 2026-09-20

The dedicated reviewer identity completed an authentic ChatGPT two-lesson poetry draft (8m33s), validation and preview. The live website reviewed and applied it, confirmed the remote receipt, and reported cloud autosave. Student PDF preserves the seven-line poem visually; PDF and DOCX exclude the sampled teacher-only interpretations. The generated lesson-two portfolio explicitly uses lesson-one N1/N2 notes and includes a catch-up route. This is content inspection, not classroom-effectiveness validation.

The same run exposed two interface defects: the shared editable-text renderer collapsed authored newlines, and review links opened the panel without selecting the referenced request/draft. The refinement preserves newlines in editable and read-only fields and loads the linked record only after authentication, with stale-account results discarded. Loading never previews or applies automatically. Focused checks cover sign-in, account switching and unavailable drafts; the exported real course was imported into the fixed production build and its rendered poem retained line breaks. Production deployment verification remains separate. The initial recording contains an OS permission overlay and is diagnostic only; no public submission is claimed.

PR #138 merged as `289b06b6324aa04bc252af361bc7fffeca73a9e1`; all PR and main checks passed, and Pages run `35533627321` succeeded. The live review URL automatically selected its request and draft after authentication. Reload/Resume preserved the exact teacher note, and the production assignment renderer reported `white-space: pre-wrap` with the seven-line poem intact.

## Reviewed-baseline revision gap — 2026-09-20

The real reviewer source case passed: a one-lesson draft cites the exact synthetic excerpt revision, discloses absent measurements and field evidence, and remains unapplied. A subsequent applied-course revision attempt stopped without writing: the saved baseline existed, but context/search did not expose it. The host also reported incomplete write-tool discovery; the server's tools/list still returns all 14 tools, so that host behavior requires a separate retry.

The revision implementation adds explicit `create_draft.fromReviewedBaseline` support for compatible authored courses. It retains stable lesson/objective IDs and complete original bundles. Version-bound material search/read excludes workspace settings and history, redacts recognized credentials, and hides full-baseline access under restricted grants. Lesson contracts identify teacher-edited fields without embedding an unbounded snapshot. Website application compares the original generated projection, reviewed/current content, and the revised projection by stable lesson identity; nested arrays and overlapping edits remain conflicts.

Nine new regressions cover explicit/default modes, baseline absence, scope/owner/revocation boundaries, redaction, teacher-note preservation, overlaps and reordered lessons. The broader authoring suite passed 202 tests with six emulator-dependent skips before the final missing-baseline regression was added; all nine focused regressions then passed. An isolated replay of the actual shared poetry baseline retained lesson one in all three materials and its exact teacher note while changing lesson two's assessment. This replay is not a live-host acceptance result. Runtime/frontend deployment and the authentic revision retry remain pending.
