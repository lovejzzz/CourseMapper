# Public-review icon

`coursemapper-directory.png` is a 1254 × 1254 PNG prepared with the built-in image-generation tool from `public/CMlogo.png`. It preserves the existing navy/blue graphic mark, removes the wordmark, and places the mark on a square white background. The original website asset is unchanged. The same square source can be used for directory and composer uploads; the portal specifies minimum sizes rather than exact sizes.

Prompt used:

> Use case: precise-object-edit. Edit target: the supplied existing EduTool.dev raster logo. Produce one square 1024x1024 PNG app directory icon for CourseMapper, the education app using this exact existing brand. Preserve the existing interlocking dark navy left mark, electric blue right mark, and central blue angular diamond exactly in recognizable geometry, proportions and colors. Extract only that upper graphic mark; remove the bottom EDUTOOL.DEV wordmark entirely. Center the graphic at approximately 78 percent of canvas width, with generous balanced white padding on every side. Solid clean white background, crisp flat edges, no extra text, no border, no shadow, no new symbols, no creative redesign. Output a clean production square PNG.

The returned bitmap is 1254 × 1254, despite the requested 1024 size. PNG format and square dimensions were checked with `sips`; the generated output was visually inspected. This is a generated derivative, not a pixel-identical crop of the source.
