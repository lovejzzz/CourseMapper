# Semi-blind adjudication — v017-cs-replay vs current pipeline (first charter-cadence run)

_2026-07-04 · adjudicator: Claude (judge of record) · procedure: charter
A3, executed for the first time — both packages normalized to plain
text by the humanPacket machinery, assignment randomized, key sealed;
ALL scores and quoted evidence below were written BEFORE the key was
opened. Disclosed limitation: a single adjudicator who built one
pipeline cannot be blinded to prose STYLE, only to assignment; this
protocol removes label bias, not familiarity._

## Blind scores (as written pre-unseal)

**Package A ≈ 5/10.**

- Plan 4.5: real worked example and a genuine misconception poll,
  buried in slot-fill — _"Ask students to explain how functions and
  scope from Autograded quiz: functions and scope changes today's
  modify lists decision"_; _"connect that mark to Autograded quiz:
  lists."_
- Quiz 5.5: Q1/Q2/Q4/Q5 all test zero-indexing recall; Q4 answers
  itself (_"What is the first element of a Python list stored at index
  0?"_); all items Remember-level; the key recycles one explanation
  sentence three times.
- Guide 5: good key-terms table; review questions are template-mangled.

**Package B ≈ 7.8/10.**

- Plan 7.5: clean teach → worked-example → practice → reteach arc; the
  reteach _walks the correction_ (_"Indexes run from 0 to length minus
  1… lst[5] is out of range"_). Deduction: segments total ~32 minutes —
  thin for a college session (see finding below).
- Quiz 7.5: varied cognitive tasks over the same misconception (predict
  / correct-the-claim / explain-why), labeled Review items, one real
  code-reading item (Q6); deductions: 4/6 items orbit the indexing
  family; Q5's distractor D is filler.
- Guide 8.5: precise anchored terms, misconception list, scope
  integration.

**Blind verdict: B teachable with light edits; A needs a rewrite
weekend. Δ ≈ +2.8 for B.**

## Unsealed

A = current-pipeline (July-3 crucible round) · B = trellis
(v017-cs-replay). The blind delta (+2.8) sits inside the band every
open-label instrument has measured (openai +2, deepseek +4, prior read
+3): direction CONFIRMED under assignment-blind conditions.

## New finding (actioned)

The blind read surfaced a contract hole no instrument had: **no
session-duration floor** — B's plan summed to ~32 contract-legal
minutes. Fixed same-day: validateAuthoredLesson requires ≥45 total
minutes, prompts state 50–75 (the prompt/validator-alignment rule).
