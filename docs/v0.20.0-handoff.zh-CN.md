# v0.20.0 收尾与交接

更新：2026-09-08（持续实施）。用户额度已恢复，继续实施原路线图。**开发版未达到 v0.20.0 发布门槛。** 当前应用版本为0.19.2，界面基线0.18.7；开发工作保存在 `codex/v0200-teaching-system`。

## 已落地

- 基础阅读诊断（§59）：12个新短问题×普通/JSON两模式，24次事实含义正确，但最短片段仅7/24。8个带单位/上下文的计数回答可确定性还原为唯一原文计数。下一步实现有界串行短问题提案并测完整包，不把短问答当整课验收。

- 计数ID候选在四个新开发包上完成8次首次提案（§58），两例并集数值正确但语义前提错误，整体0/4自动可用；研究候选不接生产。已修复裸计数充当名称/前提，以及修正候选排序丢失独立错误的问题。下一步先诊断短问题阅读能力，不再在原四包调提示词。

- 数量任务真实Scion固定对照（§57）：基线与改后共8次，结构约束使改后4次均可解析，但两例仍无完整可用候选。完整失败已归档；补早期计数检查和具体冲突角色反馈。停止在同两包反复调提示词，下一步需简化提案协议并用新输入验证。

- 重叠成员界限（§56）：精确四组端点构造、中英文九材料同步、中文浏览器刷新恢复、八份PDF共24页审读；修复旧语法的空/全体活动错误和短作答区跨页。仍是显式结构开发输出，零模型调用。

- 已加入审核后的不等规模合并比例操作（§55）：中英文共同计算/四档评分/补集替代解/独立设备练习，真实九材料修改与刷新恢复通过，八份PDF共24页审读；Word视觉和新Scion提案尚未验收。未知重叠范围的共同任务也已接入（§56），后续仍需真实Scion提案验证。

- 日期关系任务已完成真实来源编辑→九类材料同步→保存重开的开发闭环；两份 PDF 共六页最终审读通过。紧凑候选 Scion 日期提案一次返回完整角色；无约束失败同时保留。开发分支已选择通过机械回归的v3运行时（§52），首页自动任务覆盖仍待接通。证据见实施记录§48–49。
- 三套六课开发课程已有产物，v3 语料现有16/60包，仍未冻结或通过完整验收。不要重新把已做产物当成零进度，也不要把开发稿计为十二个新 Scion 单元。

- M4已加入独立本地作答审阅基础及匿名CSV整批导入。已审阅的自定义要求支持从作答审阅准备公开反馈修订，进入原材料预览/确认事务；旧任务与rubric版本不重新解释。应用结果已关联本地历史，并在真实Chrome完成单任务rubric闭环。仍需其他修订类型、跨会话待应用修订关联及多材料验收；未加入模型评分建议。详见实施记录§28–31。

- 共同教学任务、来源绑定、教师确认、材料投影与修订历史已有实现。
- 比较设计能共享具体方案、答案、分层 rubric 和错误反馈。中文作业/rubric 栏目已改进；替代方案的导出保留完整主方案，只展示确定的差异。
- 完整任务题默认20分，沿用共同 rubric。新的比较练习呈现版本4保留六项：独立迁移、完整任务、三类错误诊断及反馈后修改。新建题目默认分值为12/20/2/2/2/0；迁移题使用自己的三个四档维度，修改练习不重复计分。分步提示保留在教学材料中。
- 已保存的分值不被默认值覆盖。版本3任务仍按原内容重建；升级经教师确认，可撤销/重做；被教师编辑的待移除题目保留并产生冲突。
- 运行时语法状态缺陷已修复；adapter应用/清除/恢复、协议错配实测通过后，开发分支已选择不可变v3配对。同一日期开发案例一次提案角色完整；比较设计等泛化语义覆盖仍待独立验收。失败的句子编号方案已撤回，证据保留。

最新§60：四个新完整来源包中，并集范围中英文各9次提案成功并接入原审阅入口；合并比例仍失败，不接入候选。两份真实模型任务已完成四类PDF逐页审读、实际中文编辑/保存恢复。不要重复首轮输入调提示词；下一步继续合并比例定位及完整路线图验收。

最新§61：计数句上下文让一个新英文合并比例包全部角色正确，已完成实际四类导出12页审读及16→20浏览器同步/重开。中文部分整体混用、未知成员关系误选仍存在；增加了独立诊断及明确矛盾拦截，原子合并提案尚不接入。下一步用有界语义修复处理已定位字段冲突，勿重复同批案例试提示词。

最新§62：已接入有界原子合并比例来源提案；联合计数修复保留缺失证据，真实中文入口12次成功采纳“三十”。新增中文模型产物12页PDF审读及实际回执，所有原始失败保留。58/643/897检查通过。后续应推进自然生成覆盖、正式语料/完整课程与M4剩余验收，不继续在相同输入上反复调提示词。

## 接下来按顺序做

1. **自然生成入口**：联合类型＋角色候选也因零可用候选而不接入（§54），19次真实调用已保留；不要再以同一16包做无界提示词试错。当前目标变化时沿用旧确认的底层漏洞已修复。先补操作覆盖和减少模型必须生成的冗余元数据，再独立验证自动入口。自动类型分类器已因13/16错误接受被撤回（§53）；不能用分类标签当支持证明。应先验证候选来源角色、目标覆盖和可执行条件。来源块传递已修复，但自然生成仍可能只有泛化 source-ledger 任务；当前角色提案只从审阅面板调用；新建草稿已自动承接明确标签来源和单课目标（§51），无需重复粘贴。本地首答仍有两处角色错误，独立日期检查已修复，不能把人工确认后的成功当自动提案成功。优先将可检查任务连接生成与审阅，明确不支持的语义边界；再完成其余课程产物审读。已解决中文标签、短答案重复和书写空间的具体范围见§35–40，勿重复从头实施。
2. **独立证据**：完成路线图的60包（36开发、24保留）、三套六课课程、12个新 Scion 单元。已有中英文比较案例均已暴露，只能作为开发回归。
3. **Scion**：当前开发版选用 `public/scion/runtime/v3/`；候选路径保留。adapter与协议实机检查已完成（§52），剩余独立提案、设备/性能与完整语义验收；重复引用仍不可猜选。原始证据在 `research/scion/evaluation/v0.20.0/grammar/`。
4. **完整闭环**：完成真实作答到教师确认修订、项目保存重开、所有编辑器/导出格式和设备验证。工作区 `.coursemapper` 附件导入与首页返回问题已记录，仍待复查修复。
5. **发布**：核对 README、changelog、版本号、全部发布门槛；再合并部署，实测 edutool.dev，最后创建正式标签。不能将本分支的代码检查通过表述为线上已部署或教育质量已证明。

## 恢复工作

以实际仓库与 git 状态为准。详细范围见 [路线图](v0.20.0-roadmap.zh-CN.md)，逐轮证据见 [实施记录](v0.20.0-implementation.zh-CN.md)。最新状态见实施记录第49–61节；第27节仅为早期阶段。

开发回归：`npm run check`。比较练习检查：`npx vitest run --config vitest.compatibility.config.js src/lib/__tests__/teachingOperationComparison.test.js`。

实际导出检查：`npx vite-node scripts/benchmarks/reviewProportionPresentation.mjs <新的输出目录> paired-condition-confound quizBank,rubrics,studyGuides`。该命令使用实际创建、投影与导出路径，零模型调用；不是独立 benchmark 或生产部署。

保留用户约束：本地 Scion、不租服务器、不开免费在线模式；沿用0.18.7界面与十类材料；界面和提示简约；一个浏览器/模型、串行推理、低并发构建。
