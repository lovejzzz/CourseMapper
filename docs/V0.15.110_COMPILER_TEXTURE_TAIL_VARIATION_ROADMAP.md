# V0.15.110 Compiler Texture Tail Variation Roadmap

## Goal

Move the freshly verified v0.15.109 User Experience Design Studio package closer
to the long-running strict-clean target. The package now grades honestly at
100/A with no P0/P1/P2 findings, but it is not texture-clean: the current
texture subscore is 94/100 because deterministic compiler tails still repeat
across all 12 lessons.

This is CourseMapper-side compiler work. A stronger provider model cannot fix
phrases that CourseMapper adds after provider generation.

## Evidence

- Fresh deployed audit: `verification-output/edutool-audit-loop/20260628T214620Z-v015109-ux-fresh/`
- App version: `0.15.109`
- Run ID: `run-1782682839558`
- ZIP: `package.zip`
- Console log: `edutool.dev-current-run-only.log`
- Visible UI: `Quality 100`, `Texture 94`, `Genome 0/12`, `Materials 12/12`, `Ready with notes`
- Exported report: `100/100 (A)`, `0 P0`, `0 P1`, `0 P2`
- Local current-run-only regrade: `100/100 (A)`, texture `94/100`

## What Improved

The v0.15.109 audit proved the native recovery retry hardening worked in real
provider output:

- Native Pass B authored all `12/12` lessons.
- Enrichment coverage reached `1`.
- Export verification passed with `38` checks, `0` failures, and `0` warnings.
- The package carried `3` trusted, URL-backed, CC BY-SA source-ledger rows.
- SourceRef coverage was complete across outcomes, activities, examples,
  assessments, rubric criteria, and factual claims.

## Remaining Failure

The package is still not strict-clean because texture remains `94/100`. The
current local texture evidence named repeated shingles in:

- Slide Deck visual accessibility / decision-matrix descriptors.
- Discussion evidence requirements.
- Rubric strong/partial anchor sample language.
- Lesson Plan closing checks.
- Assignment anchor self-check language.

These are deterministic compiler phrases, not provider randomness.

## Lane

1. Vary assessment anchor examples.
   - Rotate partial-anchor language by lesson.
   - Rotate student-facing self-check instructions.
   - Rotate instructor anchor-sharing cues.

2. Vary discussion, lesson-plan, and slide descriptor tails.
   - Keep source-use and evidence requirements intact.
   - Rotate discussion evidence requirement phrasing.
   - Rotate lesson-plan closing checks.
   - Rotate discussion visual kind, purpose, evidence use, and accessibility
     description phrasing.

3. Preserve truth and source standards.
   - Do not change quality thresholds.
   - Do not hide texture evidence.
   - Do not weaken source, export, or readiness gates.

## Release Boundary

- This release changes only deterministic compiler language variation.
- This release does not change source-ledger trust, source relevance gates,
  sourceRef coverage, genome linkage, quality scoring thresholds, export
  verification, or package readiness truth.
- This release does not claim strict-clean provider output. A fresh deployed
  v0.15.110 ZIP/log audit must prove the texture improvement in real output.

## Verification

- `npx vitest run src/lib/__tests__/courseBlueprintCompiler.test.js --testNamePattern "varies lecture-exam slide and lesson-plan texture"`

## Carry-Forward

- Fresh deployed v0.15.110 ZIP/log audit must prove texture improves beyond the
  v0.15.109 `94/100` baseline.
- Genome linkage remains `0/12`; richer source/genome bridge work remains a
  separate objective.
- The source ledger is clean but sparse for this UX run: `3` trusted open
  resources for `12` lessons.
