# V0.15.116 UX Texture Tail Cleanup Roadmap

## Goal

Move the latest real User Experience Design Studio package from score-clean to
strict-clean by removing the next deterministic repeated compiler stamps.

The fresh deployed v0.15.115 ZIP/log audit showed real progress:

- App version: `0.15.115`
- Run ID: `run-1782699345047`
- Exported and local regrade: `100/100 (A)`
- Embedded findings: `0` P0/P1/P2
- Source proof: nine trusted, licensed, URL-backed, concept-linked rows, with no
  repeat of the v0.15.113 UX false-friend rows
- Texture: `95/100`, still below the strict target

The remaining texture evidence was compiler-owned rather than a source or model
truth failure:

- Slide decks repeated summary visual wording around readiness checks and
  feedback actions.
- Activity slide notes repeated the same pair-circulation evidence line.
- Study guides repeated the same cite-evidence-and-explain hint.

## Lane

1. Vary slide summary frames without changing the teaching contract.
   - Rotate summary slide titles by lesson.
   - Rotate visual kinds and evidence-use descriptions by lesson.
   - Preserve accessibility, transfer, evidence-use, and artifact-revision
     expectations.

2. Vary activity evidence notes.
   - Keep the instructor cue to circulate/check visible work.
   - Rotate whether the note asks for exact evidence moves, concept details,
     evidence cues, or draft spots.

3. Vary Study Guide first-question hints.
   - Keep source-grounded answer expectations.
   - Rotate source detail, decision impact, artifact support, and defensible
     revision language.

4. Pin the latest failure shape.
   - Extend the UX texture regression with the exact v0.15.115 repeated
     shingles so these stamps cannot silently return.

## Release Boundary

- This release changes deterministic compiler prose only.
- This release does not lower the texture metric, hide texture advisories,
  weaken source/citation standards, or inflate package scores.
- This release does not change provider prompts or source retrieval logic.
- This release does not claim strict-clean provider output until a fresh
  deployed v0.15.116 ZIP/log audit proves texture reaches `100/100`.

## Verification

- `npm test -- src/lib/__tests__/courseBlueprintCompiler.test.js -t "varies UX lesson-plan, discussion, and slide texture frames"`
- `npm run format:check`
- `npm run lint`
- `npm test`
- `npm run build`
- `npm run bundle:check`
- `npm run audit:release-history`
- `npm run audit:gold:smoke`

## Next Audit

After v0.15.116 is deployed, run a fresh User Experience Design Studio provider
audit and verify:

- Texture reaches the strict target or identifies the next concrete repeated
  shingle.
- No v0.15.113 false-friend UX source rows return to trusted sourceLedger proof.
- `QUALITY_REPORT.md`, `SOURCE_REPORT.md`, `PACKAGE_MANIFEST.json`, digest logs,
  UI state, ZIP contents, and local regrade agree.
- No generic lesson labels, prompt artifacts, stale off-domain kernels, export
  warnings, retry-truth mismatches, or premature green UI states appear.
