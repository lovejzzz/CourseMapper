# V0.15.1 — Client of the Brain: the app consumes the facade it proved

_Written June 12, 2026, at v0.15.0. Sources: the S1 boundary now standing (the headless proof grades 99/A with zero browser APIs), the two workstreams v0.15 deliberately re-scoped (S2/S3 diet + inversion, T2 depth slice), the calendar-gated default flips (native day-three, voice day-two — both now one clean different-day round from flipping), and the browser-coupled corner the boundary work surfaced (PPTX). Theme: v0.15 built the wall and proved the brain lives behind it — v0.15.1 makes the app walk through the same door, and cashes the gated flips._

**Bars are defined before work starts, per house law.**

---

## Lane 1 — THE APP BECOMES A CLIENT (the re-scoped S2/S3, now unblocked)

### C1. The AppFlow diet (S2)

AppFlow is ~5,100 lines and its chunk sits at 250 KiB against a budget deliberately raised (248→252) with a dated note that this release deletes. Extract by ownership, against the now-existing facade:

- The review-queue ownership block (B1's home) → `useReviewQueueOwner` hook.
- The save/restore/autosave block (which just grew the pruned tier and the graph-aware compact restore) → `useProjectPersistence`.
- The tab drag/drop machinery → its own component module.
- **Bar:** AppFlow < 4,000 lines AND the chunk budget back at ≤248 KiB with the dated comment deleted; zero behavior change (battery + the sync-edit proof script both green).

### C2. Machine ownership inversion (S3)

`derivePipelineState` becomes the only reader of `packageQualityPass.status` — every surface consumes machine selectors.

- **Bar:** a source-scan test pins zero direct `.status ===` reads outside the machine and the state owner; the 12-state matrix extends to the new selectors; behavior unchanged.

### C3. The PPTX corner

The headless proof named the one browser-coupled spot: pptxgenjs needs `document`. Either inject a DOM shim at the facade boundary (explicitly, as IO) or swap the slide builder's DOM dependency out.

- **Bar:** `npm run curriculumos:proof` grades ALL NINE features headless — the slideDecks exception comment in the proof script gets deleted, which is the point of named exceptions.

---

## Lane 2 — CASH THE FLIPS (both are one clean day away)

### F1. Native authoring, day three

The placeholder fix shipped (93 P1s → honest "Class notes and assigned materials" copy, pinned). Run `--authoring both` on cs-python + geology + mandarin on a DIFFERENT day from June 12.

- **Bar (standing, third attempt):** 100/A, zero P1s, cost ≤ prose on all three → flip the default, keep prose as the loud fallback, delete the ~212-line legacy path in its own commit. Not met → named failure class, native stays opt-in.

### F2. Voice, day two of the fair protocol

`--voice ab` on 4 courses on a different day.

- **Bar (standing):** voiced wins the judged majority with structural 100/A held, ≤$0.15/course → flag default ON (the texture self-check keeps reverting bad passes). Then tiered widening (briefs + study-guide intros next), each tier on its own fair trial.

### F3. T2 — the substance depth slice (re-scoped from v0.15, now trivial to instrument)

`compileCourse` is deterministic and headless: the depth A/B is two facade calls and two judge passes — no browser, no generation cost.

- Flag: lesson-plan activities carry the kernel's worked example + misconception corrective + citation INSIDE the activity steps.
- **Bar:** judge prefers the deep arm on a judged majority at zero generation-cost delta (the compile is free); no structural regression; texture sub-scores don't collapse.

---

## Lane 3 — THE TRAJECTORY BECOMES THE INSTRUMENT

- **T1. Judge variance characterization:** three release rounds now carry judge columns in the trajectory table (j3–j6 spread already visible). After the third, compute the per-course judge variance and decide — with data — whether the judge can ever gate or stays advisory forever. **Bar:** a one-page variance note in the round report; no gating change without it.
- **T2. Second contribution round-trip:** one more live extraction (any uncovered subject) → contribute → review → ship, proving the pipeline twice end-to-end. **Bar:** a second contributed shard (or a deepening of lang) in the manifest with the same review trail.

**Deliberately out (v0.16+):** the CurriculumOS repo lift-out (C1–C3 finish the in-repo separation first); provider proof rounds (key-gated, unchanged); the durability backend (product decisions about accounts pending); judge-bar gating before T1's variance note exists.

## The release in one sentence

_V0.15.1 walks the app through the door the brain built — the diet, the inversion, the last browser-coupled corner — and cashes the two default flips the bar system has been honestly holding._
