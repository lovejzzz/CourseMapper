# V0.17.10 Roundtable Output-Quality Audit

Date: August 1, 2026

Audited base: `46671c99` (`v0.17.09`)

Evidence package: `Python for Public Policy - Course Materials (6).zip`
Method: six Roundtable rounds / 18 participant turns, direct repository inspection, full representative DOCX/PPTX rendering, package-manifest inspection, and targeted executable regressions.

## Goal

Make EDUTOOL's generated course materials materially more coherent while making every automated score disclose the exact kind of evidence that earned it.

## Lane

Compiler composition, assessment-modality classification, rubric evidence alignment, deterministic defect detection, evidence-score decomposition, and score-ledger verification.

## Release Boundary

V0.17.10 fixes the reproduced deterministic defects and the audited score-reporting integrity gaps. It does not activate semantic grounding or assessment-coherence credit, certify pixel-perfect rendering, claim instructor approval, or change the Scion model weights.

## Verdict

V0.17.09's fixed 100-point ledger was arithmetically honest, but its presentation still overstated what the 59 points meant. The production package was not publication-ready even though it reported 89/B encoded-defect conformance and passed the existing slide checks.

The observed 59/100 decomposed into:

- 44/45 from narrow positive metrics: ordered lesson-title token matching and masked visible-unit variation.
- 15/15 from negative-evidence-only conformance: no registered structure, format, or identity defect fired.
- 40/100 unobserved: rendered-claim grounding and objective-task-rubric coherence were not evaluated.

V0.17.10 keeps the fixed 100-point potential and keeps both semantic constructs unobserved. It corrects the package-specific attainable ceiling, publishes the evidence polarity, derives the qualitative band from narrow positive metrics, and makes the score-ledger verifier reject forged ceilings, bands, decomposition fields, and rule polarity.

The first pushed V0.17.10 implementation was then subjected to a separate three-round / nine-turn adversarial Roundtable code audit at exact commit `a1226931`. That audit correctly rejected the initial description of the ledger as tamper-resistant: its arithmetic checks were sound, but an attacker could rewrite the rule schema, component projection, or cached quality block while preserving balanced totals. The release was reopened and hardened.

A third three-round / nine-turn room then audited exact commit `5756a04a`. It also refused certification. It reproduced a coherent deletion of a real P1 deduction, showed that texture findings made an honest shipped ledger fail replay, and found remaining modality, concept-map, article, DOCX, and readiness-receipt boundaries. Those findings define the final predeploy repair below. No audited commit is called a checkpoint merely because CI is green.

## Production defects reproduced

1. A policy-memo assessment titled `Evidence explanation: Python and pandas for public datasets` was classified as a code lab solely because its topic contained `Python`. The assignment then demanded a computing environment, computation, and a verification milestone, while the rubric's generic performance bands graded policy organization and format.
2. Native authoring composed `Audit a practical the pandas ...` by inserting a determiner-led lesson title into an article-bearing template.
3. Compact source labels removed internal conjunctions, producing `Python pandas for public evidence brief`.
4. Concept-map hubs used a character-bound prose truncator, producing the incomplete identity `Python and pandas for public`.
5. The package manifest contained zero admitted sources, zero trusted concept-linked sources, and no rendered claim receipts. Grounding therefore remained genuinely unobserved.
6. Rectangle/OOXML checks passed while the rendered deck still showed an awkward final-slide footer and semantically broken labels. Existing fit checks are structural evidence, not pixel-render or teachability evidence.

## Changes implemented

- Narrowed code-lab classification. `Python` alone is now a topic, not a modality signal; genuine computational, notebook, script, code, or contextual Python-lab titles still take the lab path.
- Added code-lab-specific performance bands for computation correctness, code clarity, verification evidence, and feedback-informed revision.
- Rephrased native authoring slots so determiner-led titles cannot collide with template articles.
- Preserved internal `and` / `or` in compact evidence labels.
- Restricted concept-map hubs to complete admitted candidates that fit the visual budget; character-truncated prefixes are not identities.
- Added a score-bearing narrow grammar detector for framing-adjective/determiner collisions such as `a practical the`.
- Added explicit user-facing construct names: ordered lesson-title sequence match, masked visible-unit variation, rendered claim support, objective-task-rubric coherence, and encoded package-defect conformance.
- Added polarity-aware evidence totals and package-specific evaluated coverage. For the audited shape, the expected disclosure is `44/45 narrow positive metrics · 15/15 negative-evidence-only conformance · 40/100 unobserved`.
- Updated score-ledger verification to recompute coverage, polarity totals, attainable ceiling, evidence ceiling, and band from the rule rows.

## Post-push adversarial hardening

The second Roundtable room inspected code rather than release prose and found four consensus blockers plus several classifier and language boundaries. The accepted repairs are:

- Pin the exact five-rule deterministic evidence protocol: version, rule IDs, constructs, operators, maxima, dependencies, evidence paths, statuses, and evidence polarity. Missing, extra, renamed, reweighted, or demoted rules now fail verification.
- Replay each rule from its independently bound observations. Predicate results, points, statuses, evidence fingerprints, and anti-gaming fingerprints must match the canonical replay; balanced arithmetic is not sufficient.
- Validate the five user-facing component rows as an exact bidirectional projection of the canonical rule ledger.
- Pin and replay the conformance ledger's nine dimensions, weights, statuses, negative deductions, texture metric, severity caps, and aggregate metadata.
- Replace the cached-quality prose regex with a structured, canonical package-readiness binding. Current status, blocker/warning counts, issue IDs, affected surfaces, and relevant values must match before cached grading is reused.
- Classify explicit programming labs, Python labs, Jupyter notebooks, scripts, and debugging work as computational; reject memo, brief, report, essay, proposal, plan, reflection, and presentation genres unless they also require executable artifacts.
- Reject dangling concept-map hubs and fail over to an honest `concept definition panel` instead of emitting an incomplete map identity.
- Detect generic adjacent article collisions, not only the originally observed `a practical the` shape.
- Remove a vacuous band assertion and replace it with exact producer-reachable band expectations.
- Strip a duplicated answer-key prefix at the DOCX rendering boundary, preventing `ANSWER — A A ...` and the article-corrected `ANSWER — A An ...` without weakening answer-position admission checks.

Adversarial regression coverage now includes forged predicates, dropped and unknown rules, reweighting, integrity-rule demotion, component divergence, conformance-schema mutations, mutually rewritten cached blocks, policy/computing modality boundaries, dangling hubs, adjacent articles, and answer-key rendering.

## Exact-commit checkpoint repair

The `5756a04a` room found that the preceding hardening was still not sufficient. The accepted final repair adds these constraints:

- Export `QUALITY_FINDINGS.json` as the independent, reason-bearing receipt for every P0/P1/P2 result. Its exact bytes, count, path, and SHA-256 digest are bound into `SCORE_LEDGER.json`.
- Require every negative conformance deduction to match one canonical finding by rule/version, finding identity, points, severity, evidence tier, reason, action, file, and quote. A deleted deduction can no longer be hidden by coherently rewriting the score and display.
- Keep the texture metric as a positive-and-negative metric row while projecting texture finding severities from the independent receipt. Honest texture P1/P2 findings now drive the displayed counts and severity cap without masquerading as one-to-one negative deductions.
- Cross-check structure, format, and identity scores between the deterministic package-integrity rule and the conformance ledger.
- Export `PACKAGE_READINESS.json`, bind its exact bytes in the ledger, and make the public verifier validate the receipt directly. Readiness is no longer only a same-process cache comparison.
- Replay the exact predicate operator, expected value, and actual value for both evaluated and unobserved rule states. This closes the thin-package curriculum/texture variants missed by the hand-built fixture.
- Give documentary genres precedence over incidental programming-topic language; a `Python assignment: policy memo` is not a code lab without an executable artifact.
- Fall back to a concept-definition panel whenever fewer than two valid concept-map spokes survive.
- Detect every adjacent `a` / `an` / `the` pair in either position.
- Strip a DOCX explanation prefix after bare whitespace only for the single-letter A–D answer-key form. Phrase answers such as `Elastic` remain intact unless followed by an explicit key delimiter.

Real-producer regressions now exercise a genuine `deepQualityGrader.grade()` result containing both P1 and P2 texture findings and require its emitted ledger to self-verify. Extracted DOCX text proves phrase-answer explanations retain their opening words.

The guarantee remains intentionally bounded. These receipts make the unsigned package internally replayable and make each score explainable. They do not establish origin or resist an attacker who rewrites every artifact, receipt, ledger, and hash together. That stronger guarantee requires trusted regrading from artifact bytes or an external signature/transparency service.

## Fresh production-package audit and final repair

The deployed package from commit `8a5a86ca` was downloaded and audited as bytes, not inferred from release notes. Its ZIP SHA-256 was `f8a243b694ed0ba1eb63a7c977271a8c6561079834c43801fb6f25c6294b75a9`. The package reported 89/B encoded-defect conformance and 33/100 deterministic evidence earned with 65 points explicitly unobserved. Direct DOCX/PPTX extraction and rendering reproduced:

- four real framing-adjective/article collisions (`a practical the …`);
- one false adjacent-article finding at the structural answer-key boundary `ANSWER — A The …`;
- one real 11-word instructional phrase repeated 39 times across 11 assessment artifacts;
- two learner-facing ImageJ2/Molecule Archive facts in a Python public-policy course, exposing a cross-course evidence-admission failure rather than a cosmetic subtitle defect;
- six honest source-support review findings that remain in Agent/Quality and were not reclassified as malformed citations;
- a readiness receipt that described pre-grade archive structure while the later quality ledger retained findings, leaving its claim boundary unclear.

A three-round, nine-turn Roundtable room inspected the code and the extracted package. It reached consensus that the package was not yet a checkpoint. The accepted repair:

- fixes determiner ownership at the compiler slot and repairs the exact legacy saved-project form during preparation;
- replaces the sentence-shaped fallback assessment identity with a noun-shaped canonical identity and compacts only the exact compiler-owned legacy direction outside identity fields;
- normalizes structural answer labels before prose-format checks while preserving phrase-answer text;
- sends Scion lesson topics, objectives, and assessment context into evidence discovery, rejects known-offender kernels without explicit or strong topical support, repairs already-saved learner-facing leaks, and adds a P0 defense-in-depth grader check;
- emits complete title-slide framing sentences and omits legacy concept dumps at both PPTX and slide-PDF boundaries;
- introduces `coursemapper-package-readiness-receipt-v2`, which separately records `contentReadiness`, `exportVerification`, and blockers-only `downloadSafety` after grading;
- makes Export a prepare/verify/download surface with no red quality state, score, grade, or finding reasons. Agent/Quality remains the honest review surface.

The grader boundary is now v1.15.1. The frozen Scion held-out fixture boundary advances to V42 with a new transitive implementation receipt; no earlier benchmark result or model-quality claim is inherited.

## Pre-commit code audit corrections

A separate three-round, nine-turn Roundtable room then challenged the live working tree rather than this report. It found additional release-blocking trust defects, all reproduced before repair:

- verified-download recovery accepted a passed label with zero completed checks and did not enforce receipt-v2 `downloadSafety`;
- manifest export warnings used `warnings` while receipt v2 read `warningCount`, so the Export handoff erased warning evidence that belonged in Agent;
- Export rebuilt warning/blocker domains without the canonical schema version, causing the trust reader to ignore them;
- the source-leak blacklist could delete legitimate in-discipline PRISMA, Alzheimer, CES-D, and similar material because its topical escape excluded the course's own scope tokens;
- a rejected cached score could survive in the returned readiness receipt when fresh grading timed out;
- the legacy assessment repair changed prose references while preserving the old sentence-shaped definition, creating an identity mismatch;
- whole-sentence source cleanup could empty a required title, note, objective, or single-item list;
- the adjacent-article detector misread `a priori`, uppercase answer/grade `A`, and `A/B`, while the initial predicate-based fix was incompatible with the release gate's regex-tuple contract;
- the first subtitle rule dropped valid instructional clauses containing serial lists.

The accepted corrections require positive export-check counts, zero failures, zero download blockers, and receipt-v2 verification; preserve canonical Agent warning domains; replace rejected cached scores with `not-graded`; use immutable request/course-map scope plus explicit offender-to-discipline hints across repair, grading, kernel admission, and reading attachment; normalize legacy assessment identity definitions and references together; preserve non-empty review placeholders when an entire required field is unsafe; and share one tuple-compatible article regex across the release gate and live grader. Complete instructional subtitle sentences are retained even when they contain serial commas.

The content-grade gate remains intentionally fail-closed. `downloadSafety` proves structural preparation and file-export verification; it does not authorize bypassing a missing or timed-out quality proof. Export and `downloadCourseMaterialsZip()` therefore keep the same release policy.

## Release-pipeline correction

The exact-hash release pipeline then rejected the candidate on the gold cross-package texture panel even though the thin panel passed. One generic slide-agenda frame had grown from two to three packages after input masking: `Frame [concept] through [source] guide.` The compiler now selects among lesson-specific agenda actions—frame, inspect, trace, or anchor—while retaining the actual concept and source cue. The gold panel moved from 551 to 550 repeated clusters, reduced support burden from 9.13% to 9.11%, reduced reader exposure from 18.63% to 18.61%, and passed its no-growth ratchet. The thin panel remains at 468 clusters, 4.82% support burden, 7.80% reader exposure, and a passing ratchet. The untuned release panel also improved from 469 to 455 clusters, with support burden moving from 6.88% to 6.73%, reader exposure from 10.77% to 10.54%, and cross-package excess from 7.43% to 7.29%; its exact release receipt was regenerated rather than bypassed. These failures were treated as evidence changes, never as baselines to relax.

The exact-commit Roundtable review also found that the first subtitle rule still inferred language from ASCII characters, so valid Indonesian comma-separated subtitles could disappear. Omission now requires positive legacy provenance: the first comma segment must exactly repeat the normalized lesson title. Ambiguous authored text is retained, including ASCII-only non-English subtitles. The same review exposed two recovery-boundary risks. A failed final save now returns a typed quality-versus-package-safety reason, sends the correct blocker domain to Agent, and returns Export to its calm blue preparation state without a misleading warning banner. A fresh finalizer blocker snapshot is retained until a verified receipt or explicit user action supersedes it; an older clean prop can no longer erase the preparation card. Quarantining multiple unsafe list items now creates numbered source-review placeholders, preserving cardinality without manufacturing a new repeated-phrase defect.

## Deployed receipt-migration correction

The exact `e09f0f4b` build passed every clean-checkout CI lane and deployed successfully. Its first fresh production download, ZIP SHA-256 `b7196d90a09b544eaa16535e9ff07636222d46bc6c1e99374df1ffc599d8d62f`, proved the new Export presentation was calm and the package receipt was structurally verified (`38` checked, `0` failed, `downloadSafety.status = verified`). It also exposed that a terminal quality receipt created before the latest deterministic repair contract was still reusable after deployment. Download therefore skipped preparation and preserved nine ImageJ2/Molecule Archive P0 leaks plus four framing-adjective/article collisions that the current repair code knows how to remove.

Finalizer receipts are now revision-bound. A current receipt is reusable even when its honest quality result remains under review. A legacy receipt with P0 findings is not: it must pass through the current deterministic finalizer once before Download can reuse its verified file map. This is a migration boundary, not a suppression rule; the refreshed grade and every remaining reason still stay in Agent and in the exported ledgers.

The first deployed revision-bound refresh at exact commit `8245d501` was also rejected after byte-level inspection. ZIP SHA-256 `ac58620eb6cca176568bf44e33fe6c001cd8967f1bb9d9a10bf9ee00c13a464d` proved that revision 1 ran and removed four of the nine P0 leaks, but five learner-facing ImageJ2/Molecule Archive facts remained. One 15-word compiler-owned assessment direction also repeated 33 times across exported artifacts. The finalizer had two narrower defects:

- a known offender could escape quarantine when the contaminated sentence also repeated generic lesson language such as “reproducible analysis” and “pipeline”;
- the offender registry recognized `ImageJ2` but not a `Molecule Archive` source fact after the ImageJ token had already been removed;
- the legacy assessment compactor handled an identity only at the end of a string, leaving the same full direction embedded in notes, review actions, accessibility cues, and teaching prompts.

Revision 2 makes explicit request text and calibrated discipline hints the only ways a known offender can remain, recognizes Molecule Archive independently, and compacts the exact compiler-owned code-review direction wherever it occurs in saved learner-facing copy. Replaying the new finalizer against the exported production `.coursemapper` snapshot reduced non-provenance offender-bearing strings from 71 to 0 and the exact repeated assessment direction from 100 to 0. A new production ZIP and independent exact-hash audit remain mandatory before checkpoint approval.

The same Roundtable room also identified a panel-level migration trap reproduced in production: Export correctly labeled the obsolete P0 receipt “Prepare package” but disabled the button, leaving Agent's `Finish package` command as the only escape. It also found that revisionless receipts with legacy `p0: 0` counts could bypass migration, and that independent reader/writer revision literals could drift. The final boundary now treats every missing or mismatched revision as obsolete, enables Export preparation from that obsolete-receipt predicate even when the old quality result is terminal, and uses one shared revision constant for receipt production and consumption. A mounted regression requires exactly one finalizer call and binds that call's repaired deliverables and refreshed grade into the same ZIP click; current-revision reviewed packages remain directly downloadable without looping.

## Revision 3 production repetition correction

The next exact production export at `ec27fb5c` proved that the panel migration and calm Export presentation worked: one Export click prepared and downloaded the package, the panel showed `Ready to download` and `10/10 materials checked` without a red quality warning, and Agent retained the honest grade and reasons. The ZIP SHA-256 was `1ee72c42a5c8c5778ab6924bd24355d2b73e40e9a931cc9962180a218fb73ecd`. Its structural receipt reported `38/38` successful export checks and verified download safety. Its honest quality result was still only 89/B with `0 P0`, `1 P1`, and `6 P2`; deterministic evidence remained `34/100`, including `65` explicitly unobserved points.

The P1 was real: the 12-word compiler-owned phrase `Statistical Modeling and Uncertainty Quantification evidence check: state one supported, bounded conclusion` repeated 20 times across six exported artifacts. The six P2 source-review findings were valid unverified-bibliography receipts and remain visible. This production package was rejected rather than relabeled as a checkpoint.

The three-round code-and-package Roundtable audit also found two independent migration hazards: the assessment compactor normalized whitespace in every visited string even when no compiler signature matched, and legitimate uses of `Data clustering` or `R: A Language` could be quarantined because those broad offender labels lacked calibrated in-discipline scope routes.

Revision 3 closes all three defects:

- New generic course-map generation emits the compact `topic evidence check` identity, and saved legacy course-map cells plus learner-facing deliverable copies compact only the exact compiler-owned evidence-check direction.
- One shared compiler-owned formative matcher now drives graph classification and rubric eligibility. Both the full legacy direction and compact identity remain in-class checks rather than accidental graded artifacts.
- Unmatched strings are returned byte-for-byte; multiline paragraphs, list indentation, and repair counts are unchanged unless a recognized signature is actually replaced.
- `Data clustering` and `R: A Language` now have calibrated discipline hints, with preservation regressions for clustering and R-programming courses.
- Finalizer revision 3 forces already-prepared saved projects through this repair exactly once.
- The package-level grader now has an explicit regression for the observed 20 occurrences across six files, preserving the honest P1 defense if a future generator reintroduces the phrase.

Before commit, the candidate passed 6,188 tests, formatting, lint, production build, bundle budgets, the core blueprint matrix, deliverable and hybrid-pipeline audits, all Scion evidence/lineage/readiness receipts used by CI, and the thin, gold, and untuned cross-package texture panels. Exact-hash CI, deployment, a fresh production ZIP, and representative DOCX/PPTX rendering remain mandatory gates before checkpoint certification.

The first exact candidate, `9e178fc7`, was rejected during its new Roundtable audit before production certification. The room proved that the scope matcher compared stemmed course tokens such as `programm` with unstemmed hint terms such as `programming`; the initial `R Programming for Statistical Computing` regression passed only because `statistical` independently matched. The shared matcher now normalizes both incoming scope tokens and calibrated hints through the same semantic tokenizer, and the regression is reduced to the exact minimal course name `R Programming`.

The follow-up exact candidate, `a27d8d41`, was also rejected before production certification. Normalizing the hint vocabulary exposed a deeper contract error: the multiword hint `meta-analysis` was flattened into two independently sufficient tokens, so the generic word `analysis` could falsely preserve PRISMA in `Python for Public Policy Analysis`. The same audit reproduced overly broad rescues for `R: A Language` in `Computing Ethics` and MNIST in `Machine Learning Policy`; because the matcher is shared by repair and grading, these false fits could both retain learner-facing contamination and suppress the honest P0 finding.

The final scope boundary precomputes normalized hints once, treats a multiword hint as an all-token phrase rather than a bag of independently sufficient words, and rejects any single-token hint that collides with the normalized generic vocabulary. Broad single-token hints were removed where they could establish discipline fit by themselves; offender-specific compound routes such as `biomedical image`, `mental health`, `blood pressure`, and `machine learning foundation` preserve legitimate domain material without restoring bag-of-words rescue. Paired regressions now require PRISMA to survive in both `Systematic Review Methods` and `Meta-Analysis Methods` but not in the policy-analysis course; require `R: A Language` to survive in minimal `R Programming` but not `Computing Ethics`; require MNIST to be quarantined in `Machine Learning Policy`; and preserve ImageJ, CES-D, and XGBoost in their compound discipline contexts. This prevents both false preservation and false deletion.

The exact follow-up audit also enforced the user's presentation boundary literally. Although the Export card itself was calm, a workspace-crown chip still exposed the quality score and opened a report modal owned by the Export component. The score/report control now lives in the Agent header, and opening it on mobile no longer switches the workspace to Export. Export presents preparation and verified-download state only; Agent presents the candid evidence score, conformance grade, severity counts, reasons, and improvement actions.

Cross-examination then found an irregular-plural tokenizer asymmetry: `analysis` and `analyses`, and `synthesis` and `syntheses`, produced different identity stems. That could falsely delete PRISMA from legitimate `Meta-Analyses Methods` or evidence-synthesis courses. The audit rejected changing the shared tokenizer because its stem identities are consumed by unrelated semantic and genome-key paths. Instead, the offender contract now names the two plural surfaces explicitly, closing the release blocker without migrating a shared token contract. The scope-table initializer also rejects any hint composed entirely of generic tokens, rather than checking single-token hints only. Sparse `Biomedical Image Analysis` plus plural meta-analysis and evidence-syntheses regressions exercise the repair path directly.

## Revision 4 production source-fact compaction

The exact `21f443ce` release passed every GitHub verification lane and deployed successfully. Roundtable's three-round code audit found no concrete code blocker, and the deployed UI met the presentation boundary: Export showed only `Prepare package`, then `Ready to download`, `10/10 materials checked`, and `10 safe repairs applied`; the 34/100 evidence score and quality report remained in Agent.

The fresh production ZIP, SHA-256 `db7c44524eae0270059d8037fdde385c56aa77c62e2a2b52ee2255584ecb166d`, was nevertheless rejected. Its receipt proved 60 files, 38/38 successful export checks, zero export failures, and verified download safety, but its honest grade remained 89/B with one P1 and six P2 findings. The P1 was a second real fan-out defect: `Functions in Python allow for the creation of reusable blocks ...` repeated 43 times across seven exported artifacts. Extracted lesson-plan and study-guide text showed the same valid source fact copied into the evidence ledger, mini-lesson, catch-up direction, instructor notes, comparison claims, review questions, and practice prompts. This is not a weak-model fact error; it is an architecture error in how one verbose fact is projected through many compiler-owned teaching frames.

Revision 4 adds a deliberately exact, meaning-preserving migration repair: the verbose fact becomes `Python functions create reusable code for analysis`. The shortened seven-word proposition retains the instructional meaning while preventing a ten-word verbatim shingle from dominating the package. The repair runs across saved learner-facing deliverables without altering provenance, increments repair counts only where bytes change, and is guarded by a production-shaped repetition regression. Finalizer revision 4 forces every revision-3 receipt through this repair once. A new exact-hash deployment, fresh production ZIP, and rendered DOCX/PPTX inspection remain mandatory before checkpoint certification.

## Revision 5 architectural source-fact fan-out correction

The exact `caa6391c` build passed every CI lane and deployed successfully, but the fresh revision-4 ZIP, SHA-256 `85773646a3f4ef826c751914f19bd63a4c544ff83998cb62758fd6dd2c21f974`, was rejected. Its 62 files again passed all 38 export checks with verified download safety. The targeted functions repair held, yet the package remained 89/B with one P1 and six P2 findings: `Conditional branching logic allows programs to execute different blocks of` repeated 42 times across seven artifacts. This proved that another exact sentence repair would only expose the next long source proposition.

Revision 5 changes the owning architecture. The finalizer now inventories only explicit fact-ledger claims (`sourceEvidenceBrief.claims` and kernel facts), never arbitrary instructor prose. Within each exported lesson artifact it preserves every standalone fact because that string may be a definition, quiz option, claim card, or visible slide assertion; when no visible standalone fact exists, it retains two prioritized full embedded uses. Excess compiler projections become short references to the cited claim. Questions, prompts, bullets, and summaries win preservation priority over explanations and speaker notes, and sentence-boundary references retain correct capitalization. Each artifact therefore remains self-contained while the same fact can no longer be stamped into every model, catch-up note, claim comparison, question, answer, and slide annotation. If standalone facts alone exceed the grader threshold, the repair leaves the honest P1 in place for a compiler fix rather than changing instructional meaning. Provenance mirrors remain byte-for-byte unchanged, changed-string counts are path-deduplicated, and the pass is idempotent.

A package-shaped JSON regression reproduces 42 compiler-projected copies across seven artifacts and reduces them to 22 while retaining a local full proposition for each independently consumed question or slide, including explanation-only units. A second regression crosses the real DOCX serialization and package-extraction boundary: the actual package grader sees the production shingle 42 times in seven rendered lesson plans before repair, seven times after repair, and no remaining repeated-instructional-phrase P1. Its companion renders 24 standalone source claims and proves the honest P1 remains. The matcher now follows the grader's NFKC word stream across punctuation and whitespace variants, globally protects strings equal to any inventoried fact so overlapping prefix facts cannot corrupt a longer standalone statement, selects only exporter-declared multi-item feature collections, treats object-rooted payloads such as a syllabus as one document even when metadata arrays precede the content, and keeps references grammatical after `whether`, `if`, and `when`. A replay against the saved production project inventories 24 explicit long facts; the worst surviving exact fact remains below the package limit. Finalizer revision 5 invalidates revision-4 receipts so the correction runs exactly once.

The first three-round code audit of revision 5 correctly held exact commit `dc037696`: its JSON-only proof did not establish the rendered grader result, literal matching diverged from the grader, first-array selection was ambiguous, root-wide antecedent logic could orphan a question or slide, and a focused overlap case demonstrated corruption of a longer standalone quiz option. Those are not waived findings; the corrections and rendered regressions above are direct responses. Exact-hash CI, deployment, a fresh production ZIP with zero P1 findings, a new Roundtable audit of the corrected exact commit, and visual DOCX/PPTX inspection remain mandatory before checkpoint certification.

A second exact-hash audit held `d6b53823` after reproducing the remaining object-rooted syllabus variant: a preceding metadata array captured generic fallback traversal and left five syllabus projections unrepaired, raising the package fixture from 22 to 27. The final root contract disables generic array fallback for this repair pass; declared lesson, deck, quiz, rubric, discussion, assignment, study-guide, and FAQ collections remain item-scoped, a wrapped syllabus scopes exactly to `data.syllabus`, and unwrapped or unknown/custom object payloads remain document-scoped. One regression now places repeated fact-like metadata before the syllabus content and requires that metadata to remain byte-identical; the package fixture separately requires 22 rendered-content survivors with no hidden standalone rewrite.

A third exact-hash audit held `4c8ce43f` because the repair and export paths still resolved those declared collections independently. A partial schema migration could therefore repair `lessonPlans` while a DOCX exporter emitted `plans`, or repair one alias while PPTX, CSV, PDF, the cover count, required-asset discovery, and package verification consumed another. One shared canonical-first resolver now owns that boundary across every renderer and verifier. It accepts only arrays, falls back to a valid legacy alias when the canonical value is absent or malformed, includes the canonical `quizBank` and `courseFaq` roots, and never selects an incidental metadata array for syllabus or custom payloads. Dual-root, alias-only, malformed-truthy, quiz/FAQ, and unknown-root regressions pin the choice. Real serialized proofs now reduce the production source fact from 42 to 7 occurrences through both canonical and legacy lesson-plan DOCX roots, from 42 to 7 through the legacy study-guide DOCX root, and from 42 to 21 through the legacy slide-deck PPTX root with no P1. The PPTX proof also exposed that fallback speaker notes copied projected bullets verbatim. Notes now guide teaching without echoing slide text, using bounded slide-indexed variants so the fallback itself does not become a package-wide ten-word template.

The next exact-hash audit held `b6aab403` after reproducing a source-authority mismatch outside the renderers themselves. The package-wide fact inventory still traversed the entire saved deliverables object, so a claim present only in a stale alias, failed feature, or unselected feature could rewrite canonical rendered prose into `the cited source claim` even though the antecedent would never enter the ZIP. The shared root contract now also exposes the exact content authority used for inspection: the chosen canonical-first array for collection-backed features, the inner object for a wrapped syllabus, and the full object only for unwrapped syllabus or custom payloads. Fact collection accepts only selected, completed features and retains one package-wide union across those valid rendered roots. A finalizer-level regression combines a stale alias ledger, a failed study-guide ledger, and an unselected assignment ledger, then serializes the canonical lesson-plan DOCX and proves all four legitimate fact uses remain present with no dangling replacement. A separate collector regression preserves legitimate cross-feature union behavior while excluding all three invalid authority sources.

The focused verification room then held `f51c1748` on one remaining fallthrough. A declared collection-backed feature marked complete but lacking every valid canonical or alias array rendered zero items, yet the inspection helper treated its metadata object like a custom document root. That metadata could still donate a fact and recreate the same dangling-reference defect in another valid feature. Declared collection features now have an empty authority root when none of their declared arrays is valid, and the repair pass likewise leaves that non-rendered payload byte-identical. Only syllabus and truly custom feature IDs retain whole-object fallback. Resolver, collector, repair, and finalizer regressions now cover this missing-root case alongside stale aliases, failed features, unselected features, and the canonical serialized DOCX result.

The next exact-hash room held `12a3b813` after following the shared aliases downstream into ZIP preprocessing. Renderers accepted legacy `lessonPlans.lessons` and `courseFaq.faq`/`courseFAQ`, but the per-lesson scoper maintained a separate alias table that omitted all three; every lesson slice could therefore retain and render the whole two-lesson collection. Readiness also used an older truthy-key resolver, so a malformed canonical value could hide a valid alias and report zero items while export rendered content. Both duplicate tables are removed. Readiness now uses the shared canonical-first valid-array key, and lesson scoping uses the shared declared-key list to scope every competing array in the ephemeral slice, preventing stale cross-lesson data from reaching whole-object analyzers while retaining canonical render precedence. Unit regressions cover all three legacy aliases, dual canonical/alias roots, and malformed-canonical fallback. Real ZIP-to-DOCX extraction proves lesson isolation for `lessons`, `faq`, and `courseFAQ` and proves stale FAQ aliases do not render when canonical data coexists.

The whole-repository resolver trace then held `965742d1`: classroom readiness, deterministic post-processing, pedagogical validation, pre-export checks, sync, and edit/retry helpers still reached the older `getArrayKey`, whose truthy canonical test, reversed FAQ/quiz precedence, and first-array fallback disagreed with render authority. The reproduced malformed-canonical lesson-plan package rendered one valid `lessons` item but the finalizer returned `needs_retry` with `Lesson Plans has no generated lesson items.` The legacy sync helper now delegates every declared collection feature to the shared canonical-first array-valid resolver and retains generic first-array discovery only for unknown/custom features. Classroom readiness and finalizer-reachable post-processing import that resolver directly. Regressions pin malformed-canonical alias fallback through finalization and real ZIP/DOCX output, canonical FAQ/quiz precedence, no undeclared-array guessing for known features, canonical-only post-processing when aliases coexist, and lesson-plan support normalization through a valid `lessons` alias.

The first production bundle of that repair also exposed a module-boundary defect: importing the broader rendered-root helper pushed the strict course-map continuation chunk above its 9.25 KiB raw budget. Collection identity now lives in a dedicated authority kernel shared by rendering, readiness, post-processing, and sync; the continuation chunk is 9.2 KiB raw and 3.5 KiB gzip, with no duplicated alias policy or relaxed budget.

The three-round code-only audit of exact `3c008c4a` then held the release on the first user-visible consequence outside those back-end paths. The production Lesson Plans view could call `.map` on a malformed canonical object even while finalization and export correctly chose the valid `lessons` array. FAQ and quiz views preferred stale legacy arrays over coexisting canonical content. The same trace reproduced finalizer observation repair mutating all three lesson-plan aliases, found proposal and finished-overview first-array selection, and found forced `decks` keys in image and agent helpers. The remediation routes all aliased deliverable views, edit paths, LaTeX scanning, saved-scope recovery, quality sampling, substance checks, sync diffs, title compaction, observation repair, agent tools, and package counts through the shared array-valid authority. Generic first-array discovery remains only inside explicitly custom-deliverable branches. Mounted regressions cover malformed canonical plus valid alias, canonical plus stale alias across all five aliased views, canonical edit paths, finalizer mutation with byte-identical stale aliases, proposal confirmation lookup, honest quality sampling, saved-scope inference, and sync diff parity.

A final whole-repository selector trace found one more user-visible duplicate in the Configure-page preview. Its lesson, slide, quiz, guide, and FAQ branches still chose aliases independently, and real FAQ data was omitted from the initial preview parse. Configure now uses the same canonical-first collection for both compact and expanded previews, including valid-alias recovery after a malformed canonical field. A mounted FAQ regression proves canonical content wins over stale aliases and malformed canonical data recovers through the declared legacy alias. After this correction the complete suite passed `6,275` tests (`162` skipped), and formatting, lint, production build, bundle budgets, hybrid pipeline, core blueprint, deliverable audit, and thin/gold/untuned texture gates all remained green. Exact-hash Roundtable review, CI/deployment, and a fresh rendered production package remain mandatory.

The next three-round Roundtable room unanimously held exact `0d001b1b` after Codex reproduced a split-authority repair path that the green suite had missed. Source-fact fan-out already selected only the rendered collection, but the preceding mechanical repair recursively visited the entire deliverable object. A double period in canonical `lessonPlans` and stale `plans` was therefore normalized in both roots; the same path could quarantine text in a stale alias or malformed non-rendered field. Mechanical and source-fact repair now share the rendered authority boundary. Declared collection features repair only the valid canonical-first array, do nothing when no declared array can render, and leave stale aliases plus adjacent metadata byte-identical. Wrapped syllabus and custom object roots retain their explicit document behavior, and repetition diagnostics inspect only the rendered content root. Direct and finalizer-level regressions cover repairable dual roots, malformed canonical plus valid alias, and missing roots; the focused repair/finalizer set contains `73` passing tests. The amended full suite passes `6,280` tests (`162` skipped), with formatting, lint, build, bundle, hybrid pipeline, blueprint, deliverable, and all three texture gates green.

Roundtable then reran that reproduction successfully against exact `9ccdd557`, including `147/147` authority, repair, finalizer, and real DOCX/PPTX serialization checks, but held the release again on public Agent paths outside the earlier trace. A canonical `quizBank` containing one question plus a stale `quizzes` alias containing two made the local Agent report two; the independent Scion course-answer reader could likewise answer from stale quiz content. The direct Course FAQ cloud-export repair mutated `faqs` even when canonical `courseFaq` content rendered. All three paths now use the shared canonical-first valid-array resolver. Public-path regressions require canonical precedence, malformed-canonical alias recovery, canonical-only FAQ mutation, and byte-identical stale FAQ data. The amended full suite passes `6,285` tests (`162` skipped); every build, bundle, pipeline, blueprint, deliverable, and texture gate remains green.

The third three-round room held exact `74dd1ded` on the last demonstrated mutation bypass. The public Agent schema still recommended legacy-root quiz edit paths, while the generic executor and proposal preview redirected only when the supplied root was absent. In a dual-root payload the action therefore reported success after editing stale `quizzes`; a malformed-but-present canonical root also prevented recovery through a valid alias. Generic edits and previews now normalize the first path segment unconditionally through rendered authority for every declared collection feature, reject a missing rendered root, and retain raw or first-array behavior only for custom deliverables. The public `edit_deliverables` regression proves canonical-only mutation with a byte-identical stale alias, and direct executor plus preview regressions cover dual-root precedence and malformed-canonical recovery. The amended full suite passes `6,290` tests (`162` skipped); all release gates remain green.

The next exact-code room held `f671783f` after executing the proposal rather than trusting the green suite. A `replaceItem` confirmation still previewed the first array inside a lesson object while acceptance used the feature-specific content array. With `metadata: ['receipt']` before canonical quiz questions, the card showed `receipt` but replaced the actual question. The executor, prevalidator, and proposal preview now share one read-only feature-aware sub-array locator, fail closed when the declared content array is absent, and preserve canonical-root precedence plus stale-alias byte identity. A combined regression proves the previewed object is exactly the canonical object subsequently replaced. The adjacent removal preview was migrated in the same pass; it now follows execution for nested quiz/slide/FAQ/rubric content and flat assignments instead of independently guessing `.items`. The focused action and proposal suite passes `368` tests. The complete suite passes `6,296` tests (`162` skipped), and formatting, lint, build, bundle budgets, hybrid pipeline, the 24-case blueprint matrix, deliverable audit, and thin/gold/untuned texture panels are all green. A new independent exact-candidate room remains mandatory before release.

That new room held exact `75516cf4` in its final round. Assessment addressing deliberately supplies both `lessonIndex` and `itemIndex` for a flat assignment so the generic remove and replace actions can locate the same artifact. Replacement preview and execution nevertheless interpreted any integer `itemIndex` as a nested-content request; assignments have no nested content array, so the card previewed nothing and acceptance returned an error without changing the assignment. One shared replacement-target resolver now owns both preview and execution. It treats assignments as a flat root collection, accepts either root coordinate, requires equal coordinates when both are supplied, and retains nested sub-item behavior for quiz, slide, FAQ, and rubric content. Prevalidation uses that same target and fails closed on missing, out-of-range, or contradictory coordinates. Regressions cover an explicit dual-coordinate replacement, preserved compiler metadata, contradictory-coordinate rejection, and a real assessment-ID-derived replacement of the intended assignment with its neighbor untouched. The focused action, proposal, and assessment-link suite passes `203` tests. The complete suite passes `6,299` tests (`162` skipped); formatting, lint, build, bundle budgets, hybrid pipeline, blueprint, deliverable, and all texture gates are green. Another independent exact-hash room remains mandatory.

## Prepared-package snapshot and calm Export boundary

The final browser audit did not stop at mounted component tests. It exercised the complete prepare, review, and ZIP-download flow across clean, repaired, and structurally invalid packages. The first full run exposed stale UI expectations and two real architecture defects: Export could regenerate from live state after preparation, and Agent could lose the exact structural reasons that made a package unsafe. Both defects are now closed.

- Preparation freezes immutable copies of every ZIP input: course map, selected deliverables, course graph, columns, lesson filter, feature IDs, theme, pipeline state, quality context, readiness result, quality result, and receipt. A later state change cannot silently substitute different bytes beneath an earlier verified status.
- `coursemapper-package-readiness-receipt-v2` is built by one shared receipt kernel and embedded in the finalizer summary. Agent, Export, and the ZIP therefore read the same structural and download-safety contract without forcing the UI through the full ZIP importer.
- Every package without a current receipt presents `Prepare package` first. Preparation never downloads automatically; only the separately enabled `Download package` action can export the frozen snapshot.
- Export remains blue and operational. It does not show an evidence score, grade, red quality warning, or duplicate blocker explanation. Agent carries the candid evidence score, exact reasons, affected surfaces, and improvement actions.
- A structurally invalid quiz package is not relabeled or softened: Export stays calm but cannot advance to Download, while Agent reports `31/100` and the exact answer-key and source-reference defects.
- Readiness summaries prefer canonical issue arrays over stale scalar counters, and user-facing issue text now distinguishes one issue from multiple issues.
- Honest review artifacts remain in the ZIP when the package is structurally safe but quality still needs review. Preparation is a safety gate, not a device for hiding an unfavorable grade.

The first exact-commit room unanimously held `384df862`. It reproduced a lesson-scope change that cleared the prepared reference but left the receipt reusable; Download then fell back to live state. It also showed that columns, feature IDs, lesson filter, theme, pipeline state, quality context, and referenced objects were not frozen. The replacement contract is fail-closed: Download requires a complete snapshot whose receipt key matches the current verified receipt, never falls back to live package state, and returns visibly to `Prepare package` after explicit scope invalidation. A browser regression prepares two lessons, deselects one, proves the next click cannot download, prepares again, and verifies that the ZIP contains only the newly selected lesson. A separate mutation regression changes every frozen input after preparation and proves the downloaded arguments remain the prepared values.

The completed replacement-candidate evidence is `153/153` browser tests and `6,301` unit/integration tests (`162` skipped), plus formatting, lint, production build, bundle budgets, hybrid-pipeline audit, the 24-case blueprint matrix, deliverable audit, teacher-ready constitution, and the thin cross-package texture gate. The thin texture receipt reports 12 packages, 468 clusters, 4.82% support burden, 7.80% reader exposure, 5.40% excess, zero default-lens hits, and SHA-256 `4109c8c931c2307ad160140b3879b1164d6f5e56a295b91e60ced2e6a8edc625`. A new exact-commit Roundtable review, exact-hash CI/deployment, and a fresh production ZIP with representative DOCX/PPTX rendering remain mandatory.

The next exact-commit room unanimously held `46d9f55c` on a narrower receipt-handoff race. When finalization returned corrected content and receipt B while the parent still exposed verified receipt A, the render-time mismatch guard discarded the exact B snapshot and could later reconstruct a B-keyed snapshot from stale live props. The original A→B regression masked this because it installed the finalizer result into live props before download. The corrected contract records the parent receipt visible at preparation: the finalizer snapshot is retained—but cannot download—while the parent remains at that receipt, and becomes downloadable only when the parent advances to the snapshot's receipt. A new regression deliberately leaves stale live props installed through the A→B transition and requires the ZIP to contain the exact finalizer-only marker. An unrelated A→C transition still clears the snapshot and fails closed. The focused Export readiness suite passes `31/31`, the complete unit/integration corpus passes `6,302` tests (`162` skipped), and all `153/153` browser tests executed successfully. A fresh exact-hash room remains mandatory.

That fresh room unanimously held `328cb5f6` after expanding the transition table. The direct A→B handoff was correct, but three fail-open paths survived: a scope invalidation between A and B could permit B to hydrate from changed live props; an A→undefined→B sequence retained the old B snapshot; and an unrelated C receipt cleared B but immediately reconstructed C from live props in the same render. Receipt-local invalidation is therefore replaced by a lifecycle tombstone. A saved verified project may hydrate its matching snapshot once, but any explicit scope change, missing receipt, or unexpected receipt transition blocks all automatic hydration until a successful finalizer explicitly installs a new immutable snapshot. The canonical receipt key now type-tags primitives and uniquely handles `NaN`, infinities, negative zero, and Date values rather than colliding `NaN` with `null` or Date with an empty object; unsupported bigint and circular receipts fail closed. Mounted regressions cover direct A→B with reversed object-key order, A→invalidation→B, A→undefined→B, and A→non-JSON-value-change, while the broad mutation proof now mutates receipt, readiness, finalizer quality, and the existing package inputs after preparation. The first full browser replay exposed one legitimate successor transition: a completed ZIP adds its own exact verification receipt, which initially tripped the tombstone and returned Export to Prepare. The panel now advances only the frozen snapshot's receipt key from that ZIP result before the parent publishes it; no content input is hydrated from live state, and safe re-download remains available. The focused suite passes `34/34`, the complete unit/integration corpus passes `6,305` tests (`162` skipped), and all `153/153` browser tests pass. Formatting, lint, production build, bundle budgets (`ExportSidePanel` 51.9 KiB raw), hybrid-pipeline audit, 24/24 blueprint matrix, deliverable audit, teacher-ready constitution, and the 12-package thin texture panel are green. Another exact-hash room remains mandatory.

The next room held exact `7bbd0943` because the report's unsupported-receipt claim exceeded the implementation. BigInt reached `JSON.stringify` and a circular object recursed during render, before the tombstone guard, so either structure could crash Export instead of returning to Prepare. Canonical receipt derivation now lives in the shared package-trust kernel and returns an explicit invalid sentinel after guarded cycle detection or an unsupported primitive. Export tombstones that sentinel before hydration, snapshot construction rejects it, and a later valid receipt remains blocked until explicit finalization. Mounted BigInt and circular regressions prove no render exception, no ZIP call, continued Prepare state after valid B returns, and recovery only after finalization. A new two-download proof publishes the ZIP-derived successor receipt, replaces live course state, downloads again, and requires every ZIP argument to be byte-for-byte equal to the first frozen call. The shared-kernel and Export focused suites pass `53/53`. The complete replay passes `6,309` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.7 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel. Another exact-hash room remains mandatory.

That exact-hash room held `ecd1baac` on two final trust-boundary gaps. Agent trust status did not interpret the invalid receipt sentinel, so it could call a BigInt- or cycle-bearing receipt clean and downloadable while Export correctly returned to Prepare. The canonicalizer also treated every non-array object like a plain record, allowing a `Map`, `Set`, regular expression, typed array, invalid Date, or class instance to collide with an ordinary record key. The shared trust status now emits an actionable receipt-integrity blocker, sets `canDownload` false, and drives Agent to Refine whenever a present receipt is invalid. Canonicalization accepts only primitives, valid Dates, arrays without custom properties, and plain or null-prototype records; every unsupported structure fails closed. Shared-kernel, mounted Agent, and mounted Export regressions cover BigInt, cycles, every reproduced object collision, persistent tombstoning, no ZIP call, and recovery only through explicit finalization. The amended focused set passes `73/73`. The complete replay passes `6,319` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.7 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel. Another exact-hash room remains mandatory.

The next room held exact `e66a2b45` after proving that a plain-prototype object was not necessarily a data record. The canonicalizer read enumerable accessors, so a stateful getter could produce one value for the authorization key and another during snapshot cloning. It also ignored non-enumerable record state and collapsed a sparse array hole with explicit `undefined`. Receipt identity now reads own property descriptors without invoking accessors. Records containing accessors, symbols, or non-enumerable state fail closed; arrays must be dense, contain only canonical indices, and expose each index as an enumerable data property; Dates must have the exact Date prototype, valid time value, and no custom state. Explicit `undefined` remains representable, while a hole is invalid. Shared-kernel and mounted Agent/Export regressions cover each descriptor case, persistent tombstoning, zero ZIP calls, and explicit-finalizer recovery. The focused set passes `82/82`. The complete replay passes `6,328` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.7 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel. Another exact-hash room remains mandatory.

The following room held exact `6de6786c` on the remaining difference between keyability and snapshotability. A transparent Proxy could report ordinary descriptors and receive a valid key, leaving Agent clean, while `structuredClone` rejected it and Export returned to Prepare. Receipt admission is now one shared operation: descriptor-safe canonicalization runs first, then the receipt must clone successfully, and the clone must produce the identical canonical key. Export stores that admitted clone rather than independently cloning the source, so Agent authorization and frozen snapshot construction cannot disagree. Array length is captured from its data descriptor and bounded before allocation. Regressions cover transparent and descriptor-mutating proxies, oversized arrays, forced clone/key divergence, one non-duplicated receipt-integrity issue alongside existing blockers, calm Export tombstoning, zero ZIP calls, and explicit-finalizer recovery. The focused set passes `91/91`. The complete replay passes `6,337` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.8 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel. Another exact-hash room remains mandatory.

The next room held exact `7f9fa007` after tracing rejected receipts beyond admission. Trust status built the correct invalid sentinel, then its readiness, export, and source readers inspected the original object; Export helpers likewise dereferenced raw receipt fields before trust evaluation. A throwing `exportFailed` getter could therefore crash Agent or Export even though admission rejected it without execution. Trust status now admits once and supplies only the admitted clone to every downstream reader. Export downloadability, finished-receipt, export-failure, hydration, snapshot, failure-update, and ZIP-successor paths either consume admitted clones or fail closed without reading the source. Chat receipt summaries and the finished-package overview use the same boundary. Direct and mounted regressions place throwing getters on every consumed receipt field and require zero getter reads, one exact integrity blocker, Agent Refine, calm Export Prepare, no ZIP call, persistent tombstoning, and explicit-finalizer recovery. The focused set passes `97/97`. The complete replay passes `6,342` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel. Another exact-hash room remains mandatory.

The next exact room held `c95d11f0` on the persistence path. Terminal package evidence still entered the shared project snapshot by reference before admission, and recursive snapshot sanitization could execute an invalid receipt getter during autosave or `.coursemapper` save. The persistence selector now admits a present receipt before returning any terminal judgment, persists only structured clones, and omits the entire terminal package state when receipt admission fails. Restoration therefore returns honestly to idle and Export Prepare rather than retaining a receiptless ready state. Direct persistence and snapshot-sanitizer regressions cover every top-level consumed field plus nested `exportVerification` and `downloadSafety`; all require zero getter reads, no thrown save, omitted terminal evidence, and idle restoration. The exact named six-file receipt, Export, Agent, chat, persistence, and restore set passes `131/131`. The complete replay passes `6,360` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the 12-package thin texture panel with unchanged SHA-256 `4109c8c931c2307ad160140b3879b1164d6f5e56a295b91e60ced2e6a8edc625`. Another exact-hash room remains mandatory.

That room held exact `880ce38b` after executing beyond the receipt-local tests. The selector still read its wrapper, `packageQualityPass.status/quality/receipt`, and `lastRunDigest.finishRunId` before admission; throwing accessors and a digest proxy therefore escaped during persistence. A receiptless `{ status: 'ready' }` also survived selection and restoration when paired with an unrelated old `finishRunId`, producing dishonest Agent state even though Export still failed closed to Prepare. The repaired selector admits the complete wrapper, package judgment, receipt, and digest before any semantic read, rejects accessor-bearing, proxy-wrapped, and revoked-proxy envelopes without executing their value getters, and requires an admitted receipt unconditionally for persisted `ready` or `blocked` state. Restore reads only root data descriptors before passing a plain envelope through the same admission. Regressions cover wrapper, judgment, digest, and snapshot-root accessors; wrapper, digest, and revoked snapshot proxies; and stale-digest attempts to authenticate receiptless terminal state. The authoritative named six-file command—`packageTrustStatus`, `ExportSidePanel.readiness`, `AgentWorkingSetPanel`, `ChatPanel.trustCounts`, `packageQualityPersistence`, and `projectSnapshotSanitizer`—passes `146/146`. The complete replay passes `6,375` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt. Another exact-hash room remains mandatory.

The next room held exact `8fb57be9` on the separate project-restore entry point. Direct persistence and restoration passed every former reproduction, including legitimate terminal compatibility and clone isolation, but `prepareProjectSnapshotForRestore` still reflected on a revoked root proxy outside a guard. Evidence cleanup was also gated only on `packageQualityPass`, so a digest-only throwing accessor bypassed admission and executed during recursive sanitation. Root reflection is now fail closed, and evidence normalization runs whenever either the package judgment or digest descriptor is present. Invalid or accessor-backed evidence is removed from a rebuilt snapshot before recursive sanitation; a revoked project root restores a safe versioned default. New revoked-root, digest-only accessor, and digest-proxy regressions pass with zero semantic reads. The authoritative named six-file set now passes `149/149`. Another exact-hash room remains mandatory.

That exact-hash room held `9f1938a6` on the complete project-root boundary. The narrow revoked-root and digest-only repairs were valid, but unrelated enumerable getters were copied unchanged and then executed by `Object.entries` outside the guard. The room reproduced the escape with absent, legitimate `ready`, and legitimate `blocked` evidence. It also showed that transparent and trap-mutating root proxies could run traps and admit synthetic project state. The replacement boundary validates the complete graph through property descriptors before any semantic traversal, rejects accessor-bearing or cyclic graphs, then requires a platform structured clone. Because structured cloning rejects Proxy objects, neither transparent nor trap-mutating roots can authorize reconstructed state; every rejected root returns only `{ formatVersion: 1 }`. Downstream migrations consume the detached clone, while the general redaction helper now recursively reads data descriptors and drops accessors without executing them. Regressions cover root accessors in all three evidence states, nested object and array accessors, direct revoked sanitation, transparent and mutating proxies, valid legacy data, legitimate ready/blocked receipt preservation, and post-restore source mutation. The authoritative named six-file set passes `158/158`; the complete replay passes `6,387` unit/integration tests (`162` skipped) and all `153/153` browser tests. Formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt are green. A new exact-commit Roundtable audit remains mandatory.

That room held exact `00651db7` on cloud compatibility. The root boundary closed the hostile-object cases, but an actual Firebase `Timestamp` silently became plain `{ seconds, nanoseconds }` after structured cloning and lost its `toDate()` semantics. The room also corrected an overbroad security claim: descriptor inspection can run Proxy reflection traps and a malicious trap can mutate its own source target; the proved guarantee is that structured cloning rejects the Proxy before any trap-created state is hydrated. The repaired admission contract accepts only arrays, plain records, exact unmodified Dates, and the exact enumerable two-field Firestore timestamp data shape. Other cloneable objects—including Map, Set, RegExp, typed arrays, and arbitrary class instances—return the versioned safe default. After clone admission succeeds, SDK timestamp instances are recognized without calling `toDate()` or any user method and normalized recursively to detached Date values; plain timestamp-shaped records remain ordinary project data. Restore-level regressions use Firebase's actual `Timestamp`, cover root and nested cloud metadata, verify native Date isolation, and exercise every newly rejected cloneable type plus hidden root state. The authoritative named six-file set passes `166/166`; the complete local replay passes `6,395` unit/integration tests (`162` skipped) and all `153/153` browser tests. Formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt are green. A fresh exact-commit room remains mandatory.

That fresh room held exact `16883664` on two timestamp-admission gaps. Descriptor shape alone admitted an unrelated custom-prototype object and promoted it to Date, while safe-integer validation admitted seconds immediately outside Firebase's legal `[-62135596800, 253402300799]` range. The boundary now requires exact identity with the installed SDK's `Timestamp.prototype`, retains the strict two-field enumerable data-descriptor contract, and enforces both seconds and nanoseconds domains before admission. Forged unrelated prototypes, adjacent invalid seconds, invalid nanoseconds, and Dates with custom state all return the versioned safe default; exact minimum and maximum Firebase timestamps remain compatible and normalize to the nearest representable millisecond without calling source methods. New regressions cover every boundary and the authoritative named six-file set passes `174/174`. The complete replay passes `6,403` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw and 14.4 KiB gzip, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt with SHA-256 `4109c8c931c2307ad160140b3879b1164d6f5e56a295b91e60ced2e6a8edc625`. A fresh exact-commit Roundtable room remains mandatory.

The three-round audit of exact `b747c0b7` ended without consensus, and the checkpoint owner applies the stricter HOLD. Codex reproduced a valid `Object.create(Timestamp.prototype)` forgery that the generic restore boundary promoted to Date; Antigravity correctly established that the detached conversion created no code-execution or prototype-pollution path, but public prototype identity still could not prove Firebase provenance. The same room proved the floor formula diverged from the installed SDK at the maximum timestamp and at `(-1, 999999999)`, while project-list conversion already used SDK semantics. The architectural repair removes Firebase knowledge from generic project admission entirely. Values returned directly by `DocumentSnapshot.data()` now pass through a dedicated cloud-ingress normalizer, where exact SDK timestamps are converted with the installed Firebase `toMillis` contract before secret sanitation. Generic restore accepts only detached Dates, arrays, and plain/null-prototype records, so both actual SDK timestamps and valid public-prototype forgeries fail closed outside that controlled ingress. Regressions cover root and nested SDK conversion, plain timestamp-shaped records, the valid public-prototype forgery, SDK-relative negative and maximum conversion semantics, cloud-list/load consistency, detachment, and accessor non-execution. The expanded eight-file trust, cloud, restore, Export, Agent, and chat set passes `186/186`. The complete replay passes `6,406` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw and 14.4 KiB gzip, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt. A new exact-commit Roundtable room remains mandatory.

That new room reached consensus HOLD on exact `136f4297`. It confirmed the timestamp repair, all nine direct Firestore read routes, generic-forgery rejection, SDK-relative time conversion, and the earlier receipt and presentation guarantees. It then reproduced actual Firebase `GeoPoint` and `Bytes` instances crossing the cloud ingress by reference because unknown non-plain values were returned unchanged; project listing also retained a legacy post-ingress `toDate()` invocation. The strengthened ingress now validates the complete descriptor graph before normalization, rejects cycles, accessors, sparse/custom arrays, functions, symbols, unsupported prototypes, and a final platform clone failure, and thereby rejects Proxy graphs before normalized state is used. `GeoPoint` becomes a detached tagged latitude/longitude record and `Bytes` a detached tagged base64 record. `DocumentReference` and every unknown scalar reject the complete snapshot instead of retaining a live capability. Project listing accepts only an exact finite native Date and never invokes source methods. Actual-SDK, identity, mutation, DocumentReference, unknown-class, transparent and revoked Proxy, cycle, nested unsupported value, accessor non-execution, custom-state, and custom timestamp-method regressions expand the focused eight-file set to `192/192`. The complete replay passes `6,412` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets with `ExportSidePanel` at 51.9 KiB raw and 14.4 KiB gzip, the hybrid-pipeline audit, all `132` blueprint-quality cases, the deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt. Another exact-commit Roundtable room remains mandatory.

## Cloud-ingress and hostile-key checkpoint closure

The exact `148955f0` cloud-ingress candidate passed its room review, but checkpoint-owner replay found three unproved scalar-state boundaries: adjacent invalid Firestore timestamp ranges, `BigInt`, and a forged Bytes inner value. Exact `0d7a166b` closed those scalar gaps by validating Timestamp, GeoPoint, and Bytes state without invoking source scalar methods, rejecting unsupported primitives, and constructing detached values. Its next code-only room then found a different defect: assigning a Firestore document ID such as `__proto__` with bracket syntax changed the per-result map prototype and lost the document entry. This was result-object prototype injection and entry loss, not global `Object.prototype` pollution.

Exact `7a832ef3` replaced every external document and chunk feature-ID assignment with an own enumerable data write. Its three-round audit correctly held the checkpoint again: direct snapshot normalization and JSON chunk sanitation still rebuilt nested records with bracket assignment, so a nested own `__proto__` field could become the rebuilt record's prototype and disappear as data. Exact `b1865166` centralizes one `Object.defineProperty` writer and uses it across the outer maps and both recursive record reconstructors. `__proto__`, `constructor`, and `prototype` therefore remain ordinary own data properties with writable, enumerable, and configurable flags, while ordinary record prototypes remain unchanged.

The first three-round audit of `b1865166` found no remaining runtime defect or sibling reconstruction sink, but reached consensus HOLD for proof completion. The integration regression covered only nested `__proto__`; lower-level tests for `constructor` and `prototype`, partial descriptor assertions, and general detachment tests did not directly prove the complete direct/chunk seam. Exact `2e240268` closes that evidence gap with one production-path matrix covering direct Firestore and JSON chunk restoration, all three hostile keys, exact full descriptors, unchanged prototypes, source/output/sibling mutation isolation, and a neighboring direct `BigInt` fail-closed control.

The final exact-commit confirmation room unanimously **CERTIFIED `2e240268b4087ebb2590df39d349e4deeb87079b`** with zero material concerns. Its broker independently passed the focused boundary and integration suite at `41/41`; the local three-file security set passed `89/89`. The complete local replay remains green at `6,432` unit/integration tests (`162` skipped), all `153/153` browser tests, formatting, lint, production build, bundle budgets (`ExportSidePanel` 51.9 KiB raw / 14.4 KiB gzip; `AppFlow` 281.6 KiB raw / 84.6 KiB gzip), the hybrid-pipeline audit, all `132/132` blueprint-quality cases, deliverable and teacher-ready constitution audits, and the unchanged 12-package thin texture receipt SHA-256 `4109c8c931c2307ad160140b3879b1164d6f5e56a295b91e60ced2e6a8edc625`. Exact-hash CI, deployment, a fresh production ZIP, and representative DOCX/PPTX rendering remain required before the release itself is certified.

## What was deliberately not done

- Grounding points were not activated from source-ledger booleans. The current producer runs before rendering, supplies no rendered locations, and uses order-insensitive lexical overlap. Those receipts cannot prove rendered-claim entailment.
- Assessment-coherence points were not activated from broad conformance checks. The new regression fixes the reproduced contradiction, but one fixed classifier does not establish a calibrated objective-task-evidence-rubric measurement protocol.
- The score was not rewritten as `59/60 = 98%`. That headline would mix narrow positive metrics with absence-of-defect credit and recreate inflation.
- A global article-deletion rewriter was rejected. It could damage legitimate reduced relative clauses. The repair stays at the owning composition slots with a conservative detector as a safety net.

## Remaining work

1. Build typed objective → task requirement → student evidence → rubric-criterion links and verify them bidirectionally after artifact serialization.
2. Build a post-render grounding verifier bound to artifact bytes, rendered location, source snapshot hash, passage locator, verifier version, and an order- and role-sensitive entailment result.
3. Add stable pixel-render / OCR regressions for slide counters and footer regions. Treat these as render-fit evidence only.
4. Obtain independent instructor review before any classroom-readiness claim.

## Roundtable disposition

The original six-round completion brief was audited and revised once after four material concerns. Its transcript is retained locally as `roundtable-2026-08-01 (2).md`. The first post-push three-round adversarial brief was also audited and revised once after four material concerns; its transcript is retained as `roundtable-2026-08-01 (3).md`. The exact-`5756a04a` three-round checkpoint brief was revised after three material concerns and is retained as `roundtable-2026-08-01 (4).md`. The production-package audit of `8a5a86ca` reached consensus after three rounds and rejected that output as a checkpoint. Verified code and package findings were implemented; hypotheses without a confirmed producer were not promoted into release claims. A second fresh production package and exact-commit Roundtable audit remain required after this repair deploys.

## Revision 6 production replay and lesson-scoped trust boundary

The deployed `437e06e6` production package, SHA-256 `629691ec8d98e5de11b66799b607b991d56bb837fd23e3104714cdf6f6ba0928`, passed all 38 export checks across 60 files, but honest grading still rejected it at 89/B with one P1 and six P2 findings. The P1 was no longer the earlier source-fact fan-out. One compiler-owned source-boundary correction appeared 38 times across 12 files. The six P2 source-review findings were legitimate unobserved-evidence disclosures and remain intentionally visible in Agent rather than being hidden in Export.

Exact `44e74f37` attempted to compact that correction, but its first code-only Roundtable room held the release for two concrete defects: lexical replacement could mutate byte-identical instructor prose, and a nine-word replacement escaped the then ten-word repetition detector while retaining 38 directive copies. Exact `8ed4cd0a` added explicit compiler-lineage authorization, a seven-word directive detector with an instructional-verb gate, deterministic per-artifact compaction, punctuation and idempotency proofs, manifest-only objective masking, and a retained real-package replay. Its next Roundtable room still reached unanimous HOLD. Authorization was package-global, so a researched lesson could authorize mutation of byte-identical instructor content in another lesson; the executable replay was also not yet retained inside the repository.

Exact code candidate `8472a459` removes that package-global authority. Source-boundary corrections and compiler scenario materials are collected as `Map<lessonId, Map<exactText, term>>`. Repair receives only the nested authorization for the current stable lesson key. Scope can enter through structured `lessonId`, positive `lessonNumber`, or an exact Course Map lesson-title mapping; arbitrary node titles, prose parsing, and flat compatibility fallbacks are excluded. A production-shaped adversarial regression gives a researched lesson and an instructor-authored lesson byte-identical correction and scenario-material text, then proves that only the researched lesson changes, instructor content and provenance mirrors remain byte-for-byte, and a second replay is idempotent.

The sanitized retained replay is now executable without account or chat state:

```text
npx vite-node scripts/v01710OutputQualityReplay.mjs --input evaluation/output-quality/fixtures/python-public-policy-v01710.json.gz
```

Its original saved-project SHA-256 is `bae529b6669cc02ce5693576be87f58169820d1b3a4de209e7afafef5f980f71`; the compressed fixture SHA-256 is `dd2750a4e6c678dc1e5fe51b866ddd394a88908a7b82b5436244db2a2708f618`; and the canonical retained fixture JSON SHA-256 is `47d7de6ca38bb97cf810138448ee71a467b4cf0b7a180fb19659cc2e782a2d3c`. The replay moves the real package from 89/B with one P1 and six P2 findings to 97/A with zero P0/P1 and the same six honest P2 findings. It reduces the 38 repeated directions to 12 complete local corrections plus 26 short references; 16 legacy strings remain only in provenance paths that the migration deliberately never rewrites. The frozen 22-file Scion implementation receipt remains unchanged because this repair is outside its transitive grader boundary.

Before push, the exact candidate passed 6,446 unit/integration tests (`162` skipped), formatting, lint, production build, all bundle ratchets, all 24 fast blueprint cases, the deliverable audit, the hybrid pipeline audit, held-out evaluation across all course families, the direct retained replay, and all 22 Scion adapter checks. Exact-hash Roundtable review, GitHub CI, deployment, a fresh production ZIP, honest package grading, and representative DOCX/PPTX rendering remain mandatory before checkpoint certification.

The exact-`8472a459` three-round room unanimously held the release despite confirming the lesson-scoped trust repair. Its retained-fixture probe proved that the replay script's former one-pass result did not establish idempotence. Pass two changed `slideDecks.data.decks.1.slides.4.notes` from `The cited source claim. The cited source claim.` into a readiness scaffold containing the malformed seam `Emphasize the cited source claim. in concrete language`. Pass three was byte-stable but still reported `changed:true` because Course Map repair diagnostics could survive an identical output. The room correctly traced both defects to repair ordering and incomplete mutation evidence; its completion brief contains zero material audit concerns.

The corrected candidate closes all four requirements from that HOLD. Course Map readiness now runs to a bounded, explicitly checked fixed point and derives `changed` from serialized output bytes; a cycle or failure to converge is rejected instead of being reported as ready. After source-fact compaction, a same-invocation readiness pass repairs newly underfilled artifacts, then assignment identity/texture and mechanical content normalization run over only the newly introduced bytes. Slide-note anchors remove terminal punctuation and normalize initial case before joining a connective. The retained replay now executes a second complete finalizer invocation, compares the complete Course Map plus deliverables snapshot, records both SHA-256 values, requires `secondChanged:false`, and exits nonzero on any mismatch.

The corrected replay's first and second snapshot SHA-256 values are both `43116d3d830bca79181dfad254115d7d529ff34ad2feab1d0e1d55c7473fc61d`; the second invocation reports no repair messages. The score remains 97/A with zero P0/P1 and the same six P2 findings. Focused fixed-point and punctuation regressions pass, including a production-shaped slide-note case and the previous assignment-texture idempotence case. The complete suite now passes 6,448 tests (`162` skipped), formatting, lint, production build, bundle budgets, all 24 fast blueprint cases, the deliverable audit, and the held-out PR evaluation across every course family. The ALGI/Scion hybrid protocol remains honestly non-promotable because its separate paired evidence is not recorded; this release makes no promotion claim. The generic adapter-package command also requires an explicit adapter directory and is not a release gate; its frozen 22-file implementation receipt remains the applicable unchanged evidence.

## Production-package classifier failure and repaired physical candidate

The first deployed `448f9867` package disproved the retained replay's checkpoint claim. Its ZIP, SHA-256 `ba9b460b08e38bb4412effe156c26f91ab419dc4042d5a91bd86645885a2f7ac`, was structurally healthy and reported 97/A with zero P0/P1, yet physical Office XML contained at least 169 literal `the cited source claim` placeholders across 24 files plus a real `claim. in concrete language` speaker-note seam. Rendering also reproduced a tautological agenda, a repeated objective, a positional bridge error, a truncated activity question, and a generic closing. Pixel crops disproved the initially suspected title, counter, and DOCX-footer clipping; those claims were removed from the audit.

The six-round production room reached consensus HOLD and located the architectural failure. The grader already opened the serialized Office containers; it simply did not classify prose that the deterministic repair layer deliberately generated and its tests positively required. The repaired candidate therefore changes both sides of that boundary:

- Source-fact compaction now emits subject-bearing source-statement references, strips a reproduced leading `See also` parenthetical from the reference subject, preserves complete standalone source facts, and never traverses provenance mirrors.
- The same preparation invocation replays narrowly scoped sentence-shell repair after fact compaction, reaching a byte-stable fixed point without a second click.
- Saved projects receive exact-signature migrations for the shipped agenda tautology, repeated objective, truncated policy activity, generic closing, opaque claim shell, and legacy admitted-fact language. Current compiler producers emit the corrected copy directly.
- Three confirmed seams are P1 classifier rules in deep-quality grader `1.15.2` and hard Office-export boundaries: the opaque source placeholder, the punctuation seam, and the admitted-claim shell.
- Bridge rendering routes `Last time`, `Today`, and `Next` by semantic prefix rather than midpoint position while preserving every unlabeled imported bullet exactly once. Real PPTX XML regressions cover fully mixed, recap-only-labeled, and today-only-labeled bridge lists and prove every input survives in the intended panel.
- The retained replay can now write the real ZIP it grades. The unrepaired fixture fails closed before grading because the Office exporter detects its admitted-claim shell; the prepared candidate grades 97/A with zero P0/P1 and the same six honest P2 source-review findings.
- Scion held-out benchmark V43 preserves the first `1.15.2` classifier boundary. The current V44 boundary binds grader `1.15.3` and implementation receipt `935e9be39968ae317bc1a54197a044a4999aef6cfbd400ec1dbea106c08335d4`; it adds malformed compacted-source-reference defects and inherits no earlier score, adapter win, or promotion claim.

The first corrected-room preparation exposed a second architectural weakness during direct Office-text inspection: replacing repeated facts with a subject-only `source statement` could create clause-shaped fragments, retain source-owned parentheses, or replace a short fact embedded as the prefix of richer prose. It also stamped one sentence-opening directive throughout the package. The final repair therefore refuses prefix/appositive matches, consumes only source-owned delimiters, derives named topics for otherwise generic subjects, and selects among deterministic path-seeded sentence forms. The Office boundary and grader now reject reproduced malformed-reference shapes. This is a substantive correction to the system, not a score-preserving wording pass.

Roundtable independently reproduced the compaction corruption and also rejected the saved Lesson 2 activity as disciplinary-operation erasure: its generic policy comparison never made learners trace or test control flow. The lesson-aware migration now requires mapping policy options as `if/elif/else` branches, defining each selection condition, testing a threshold boundary, recording the branch and boundary input, tracing the chosen path, and revising the rule when evidence changes. The real rendered activity slide shows the complete operation without clipping.

The current retained physical candidate is the read-only content-addressed archive `/tmp/edutool-v01710-62b363d4880f4ec4310bae7d719ec4571e1d2813f2553dd7ca9e14d44b21bf0f.zip`, SHA-256 `62b363d4880f4ec4310bae7d719ec4571e1d2813f2553dd7ca9e14d44b21bf0f`, 1,083,859 bytes, with 61 regular files. A separate fail-closed verifier reopens the retained ZIP and requires its hash, byte size, regular-file count, embedded grader version `1.15.3`, score, grade, P0/P1/P2 counts, and byte-reproduction proof to match the replay receipt; the current artifact passes at 97/A with zero P0/P1 and six disclosed P2 source-review findings. Raw inspection of all 50 Office containers reports zero instances of every confirmed old seam, migration signature, malformed clause-shaped reference, unmatched compacted-reference parenthesis, low-information `source claim on components` fallback, or truncated `frame the public?` activity. It also confirms the `if/elif/else`, threshold-boundary, and branch-input requirements in physical Office text. Representative Lesson 2 PPTX slides and all 31 pages across the six lesson-plan DOCX files were rendered again; every page's first and last content pixels remain inside the printable margins, with no clipping or overlap. The finalizer replay is idempotent with first/second canonical snapshot SHA-256 `374ed321072dfdd92ba410c4ea14004b676724d825fb24c7e921ff5f91e42852` and `secondChanged:false`.

The final local candidate passes 6,474 unit/integration tests (`162` skipped) across 518 files with zero failures, including the new DOCX pagination contract, formatting, lint, production build, and every frozen bundle/repository ratchet. The compiler remains within its 28,222/28,224-line budget; the compiler family remains within 31,222/31,224; `ExportSidePanel` is 51.9 KiB raw / 14.4 KiB gzip; and `courseBlueprintCompiler` is 849.9 KiB raw / 238.7 KiB gzip. The relevant browser export smoke test, all 24 fast blueprint cases, the deliverable audit, the hybrid-pipeline audit, and the held-out PR evaluation across every course family pass. Exact working-tree Roundtable review, commit/push, exact-hash CI and deployment, a new production prepare/download, physical ZIP inspection, and fresh PPTX/DOCX rendering remain mandatory before certification.

The fresh six-round working-tree room then unanimously **CERTIFIED** this exact candidate and sealed ZIP after 18 turns, two independent completion-brief audits, and zero material brief concerns. Both available agents independently reproduced the ZIP identity and quality receipt, scanned the physical Office containers, verified the Lesson 2 control-flow protocol, challenged apparent repetition through the production paragraph-aware grader, and found no release-blocking defect. Claude was unavailable and Roundtable skipped it automatically without interrupting the audit or requesting another login. The room correctly limited its decision to the audited working tree and content-addressed package: the complete commit, clean-tree replay, exact-hash CI/deployment, and fresh production-package audit remain required before release certification.

The room's clean-tree open question then exposed a separate attestation defect: identical package inputs produced different ZIP hashes because the outer archive, every nested Office entry, Office core properties, the manifest, and grading evidence carried wall-clock timestamps. Package assembly now normalizes archive and Office-core timestamps while preserving the caller's explicit logical `generatedAt`; ordinary downloads still use their actual export time, while the retained replay binds the saved run timestamp. The replay builds the complete graded package twice and exits nonzero unless the byte streams match. Its receipt records the second SHA-256 and byte size, and the independent verifier rejects missing or inconsistent reproduction proof. Two separate replay processes each produced the exact current hash `a7773bfd9847fd2903f31f1c88494b263e2bdde9c515a4b10eb5d8e750b54407`; all outer and nested archive entries use the deterministic timestamp and no live Office-core timestamp survives. This change invalidates the earlier working-tree room as certification of the current code boundary, so a new exact-tree Roundtable audit remains mandatory.

## DOCX pagination blocker found by physical audit

The next six-round exact-tree room produced one valid HOLD before later turns tried to dismiss it by inspecting the wrong page. The retained Lesson 3 DOCX had a repeated session-outline header rendered at the physical top edge on page 3 and a footer collision on the same page. Page 4 was clean; proving that page 4 had a white top band did not refute the page-3 defect. The checkpoint owner reproduced the failure from the exact content-addressed ZIP and kept the release held.

The exporter no longer emits a single flowing session-outline table. Each teaching move is now a bounded `cantSplit` table, with a zero-height `keepNext` paragraph between adjacent tables so LibreOffice cannot coalesce them back into a margin-violating continuation. Only the first block owns the semantic column header; alternating bands and the shared fixed grid remain intact. A direct OOXML regression requires one bounded table per move, one header, complete row text, and every keep-with-next boundary. The repaired retained replay remains byte-reproducible at SHA-256 `62b363d4880f4ec4310bae7d719ec4571e1d2813f2553dd7ca9e14d44b21bf0f`, and the complete six-DOCX, 31-page render confirms normal top and bottom margins on every page. A fresh Roundtable audit of the repaired exact commit remains mandatory before push.
