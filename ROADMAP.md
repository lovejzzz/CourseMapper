# CourseMapper Roadmap

## Product Direction

CourseMapper is moving toward a "consider it done" course-production agent. The user should describe the course, choose scope/materials, and receive a finished draft package. The agent owns generation, QA, repair, alignment, and export readiness. Human involvement should be final approval only, not QA labor.

## v0.7 - God-Mode Delivery Release

Goal: make the website feel like an autonomous course package producer, not a prompt-and-review tool.

1. One primary course-package flow: describe the course, choose scope, generate, receive package.
2. Agent-owned quality loop after generation: completeness check, readiness check, cross-deliverable validation, safe repair, recheck.
3. Safe auto-repair before presentation: missing metadata, inconsistent quiz points, unsupported FAQ categories, short slide notes, rubric coverage, publishability placeholders.
4. Targeted retry without asking: regenerate only weak lessons or deliverable sections when validation identifies local failure.
5. Final "Done Package" handoff: generated materials, agent fixes, remaining assumptions, and export actions in one simple view.
6. Internal scores hidden by default: user sees confidence states like Excellent, Good with assumptions, Needs attention.
7. Model auto-routing: use cost-efficient models first and escalate only after failed quality/repair passes.
8. Export auto-test: verify ZIP/PPTX/DOCX/CSV paths before claiming the package is ready.
9. No fake chat labor: background agent work must not appear as user-authored prompts.
10. Simpler UI: fewer expert controls in the main path; advanced controls remain available but out of the way.

## v0.8 - Autonomous Classroom Package Release

Goal: make CourseMapper credible for real classroom pilots while keeping the agent in charge.

1. Institution profile memory for school policies, grading language, accessibility defaults, AI policy, and course logistics.
2. Agent-filled classroom policies using the institution profile and safe defaults.
3. Reading and citation agent that finds, cites, and formats readings instead of emitting vague reading lists.
4. Canvas-oriented LMS package structure: modules, pages, assignments, quizzes, rubrics, and files.
5. Full-course consistency agent across syllabus, assignments, rubrics, quizzes, slides, study guides, and FAQ.
6. Multi-pass autonomous improvement runs until target quality is reached or a true blocker remains.
7. Finished package report: generated, repaired, verified, assumptions.
8. Minimal escalation: ask only for official dates, official policies, copyrighted readings, or instructor-specific constraints.
9. Versioned package handoff so users can recover prior generated packages.
10. Public demo packages showing polished complete outputs across multiple subjects.

## Perfect System Gap

Goal: close the gap between the current audited v0.8 hybrid compiler and a near-perfect course artifact system.

1. Compile nearly everything. All audited v0.8 package deliverables now compile from the course blueprint; the next cost frontier is optional enrichment, custom deliverables, image generation, and adaptive repairs.
2. Real quality proof, not just validators. Compare generated packages against expert-reviewed gold samples and real instructor edits, not only structure, readiness, heuristics, sparse inputs, and regressions.
3. A true enrichment pass. A source-grounded compact enrichment pass now exists and can be selected adaptively; remaining work is proving when it improves expert-reviewed output enough to justify the extra call.
4. Learning from edits. Learn repeated user preferences for rubrics, slide wording, quiz difficulty, lesson-plan pacing, and apply them automatically on later runs.
5. Adaptive generation. The compiler now chooses deterministic vs enriched compile when asked to run adaptively; remaining work is extending that policy to local repair and rare model-generated fallbacks.
6. Better user trust. Show what was compiled, what was model-generated, what was repaired, what quality gates passed, and where human review is still recommended.
7. Broader edge-case coverage. Handle unusual formats, nontraditional courses, labs, clinical placements, studio courses, multi-section syllabi, and messy imported documents.

Hourly execution rule: pick one small vertical slice from this section per run, implement it, run the narrowest meaningful verification, and update roadmap notes or audit evidence before moving to the next slice.

Latest slice evidence:

- 2026-05-26: `audit:pipeline` now emits a per-case feature source matrix in `verification-output/hybrid-pipeline-audit/latest.md`, explicitly naming compiled versus still model-generated deliverables for each audited release fixture.
- 2026-05-26: Discussion Prompts moved onto the deterministic blueprint compiler path; `audit:pipeline` now reports 8 compiled deliverables and only `lessonPlans` remaining on the model path (156 baseline calls -> 18 hybrid calls, 88.5% saved). Remaining warning: compiled discussions still repeat boilerplate across fixtures, so the next slice should make their lesson-specific guidance more varied.
- 2026-05-26: Compiled Discussion Prompts now vary follow-up probes, facilitation guidance, response stems, criteria, and participation guidance by lesson context; scoped `audit:pipeline` rerun (`research-methods`, scope 5) cleared the repeated-boilerplate warning with 0 release warnings and 0 stress findings at the same 33 baseline calls -> 3 hybrid calls cost profile.
- 2026-05-26: Lesson Plans moved onto the deterministic blueprint compiler path with lesson-specific outlines, student-facing summaries, formative checks, UDL notes, and ready-to-teach support. Scoped `audit:pipeline` rerun (`--scopes 5 --no-stress`) now shows all 9 audited deliverables compiled, 33 baseline calls -> 0 hybrid calls, and 0 release warnings in `verification-output/hybrid-pipeline-audit/latest.md`.
- 2026-05-26: `audit:pipeline` now emits a per-case quality gate matrix with validator/readiness pass states plus an explicit human-review recommendation. Scoped rerun (`--scopes 5 --no-stress`) shows all three release fixtures passing validators, quality floor, workspace readiness, and classroom readiness while still recommending a spot-check for institution-specific facts, official dates, and copyrighted readings before handoff.
- 2026-05-26: `audit:pipeline` now emits a Trust Evidence Matrix that pairs repaired course-map field counts, compact repair evidence, delivery path (`compiled` vs `model-generated`), and the handoff review recommendation per release fixture. Scoped rerun (`--scopes 5 --no-stress`) shows all three audited fixtures at `9 compiled / 0 model-generated`, `0` repaired input fields, and the same targeted spot-check recommendation in `verification-output/hybrid-pipeline-audit/latest.md`.
- 2026-05-26: `audit:pipeline` now includes a Stress Case Matrix for atypical course maps, covering sparse assessments plus a messy imported clinical-studio fixture. Scoped rerun (`--scopes 5`) still passes release fixtures at `33 -> 0` calls while exposing one remaining stress warning: compiled slide decks repeat boilerplate across 3 items for the messy-import case, so the next slice should make those edge-case decks more lesson-specific.
- 2026-05-26: Compiled slide decks now vary speaker notes, accessibility guidance, assessment carry-forward cues, and fallback concept selection by lesson context instead of reusing shared boilerplate. Scoped `audit:pipeline` rerun (`--scopes 5`) cleared the slide-deck specificity warning across all three release fixtures plus both stress fixtures, leaving `0` release warnings, `0` stress findings, and the same `33 -> 0` hybrid cost profile in `verification-output/hybrid-pipeline-audit/latest.md`.
- 2026-05-26: The messy-import stress fixture now spans all audited scopes instead of collapsing `8` and `14` down to the same `5`-lesson sample. Full `audit:pipeline --scopes 5,8,14` rerun keeps `0` release warnings and `0` stress findings while the Stress Case Matrix now shows distinct messy-import repair counts at `5`/`8`/`14` lessons (`12`, `18`, `32`) in `verification-output/hybrid-pipeline-audit/latest.md`.
- 2026-05-26: Package handoff receipts now surface shared trust evidence from the same helper logic used by `audit:pipeline`, adding repair evidence plus an explicit human-review recommendation to both embedded and agent-generated quality receipts. Focused coverage passed in `src/lib/__tests__/packageTrust.test.js`, `src/lib/__tests__/packageFinalizerSummary.test.js`, `src/components/chat/__tests__/PackageSummaryCard.test.jsx`, and `scripts/__tests__/hybridPipelineAudit.test.js`; full `npm run audit:pipeline` rerun also passed at `156 -> 0` calls and refreshed `verification-output/hybrid-pipeline-audit/latest.md` with the same `0` release warnings / `0` stress findings baseline.
- 2026-05-26: Live Spanish for Healthcare Professionals ZIP audit passed package readiness (`11/11`, `0` blockers, `0` warnings, export verification passed) at `$0.15 / 24k tokens`, with 9 compiled core deliverables and only the custom Weekly Reflection using model calls. Follow-up fixes should keep custom deliverable names user-facing in receipts/ZIP manifests and make the next cron slices target custom-deliverable cost plus slide-deck phrase polish.
- 2026-05-26: Agent chat history and retry-failure prompts now resolve custom deliverable names instead of leaking internal `custom_*` IDs into serialized agent copy. Focused coverage passed in `src/lib/__tests__/chatHistory.test.js` and `src/components/chat/__tests__/useChatRouter.test.js`; next priority remains reducing custom-deliverable model-call cost for common patterns like Weekly Reflection.
- 2026-05-26: Safe custom Weekly Reflection / check-in templates now compile directly from the course blueprint with zero provider calls while unknown custom deliverables stay on the model path. Focused coverage passed in `src/lib/__tests__/courseBlueprintCompiler.test.js`, `src/lib/__tests__/courseBlueprintCostComparison.test.js`, and `src/lib/__tests__/packageExportVerifier.test.js`; next priority is broadening deterministic custom coverage beyond reflection/check-in patterns or returning to slide-deck phrase polish.
- 2026-05-26: Safe per-lesson custom Reading Response templates now compile directly from the course blueprint with zero provider calls while unknown custom deliverables still stay on the model path. Focused coverage passed in `src/lib/__tests__/courseBlueprintCompiler.test.js`, `src/lib/__tests__/courseBlueprintCostComparison.test.js`, and `src/lib/__tests__/packageExportVerifier.test.js`; next priority is broadening deterministic custom coverage to another common pattern like feedback forms or returning to phrase polish where compiled copy still sounds generic.
- 2026-05-27: Compiled slide decks now use cleaner lesson titles, contextual agenda bullets, varied instructor-note transitions, and placeholder-resistant concept selection for messy imports. Focused coverage in `src/lib/__tests__/courseBlueprintCompiler.test.js` confirms the messy clinical-studio deck no longer leaks `TBD` or note scaffolds like `Anchor the explanation in`, while classroom readiness stays clear of repeated slide boilerplate.
- 2026-05-27: Added `audit:gold`, a curated gold-sample quality gate that compares compiled package output against classroom-quality expectations beyond validators, plus an optional compact blueprint enrichment pass that uses one model call before deterministic compilation when explicitly enabled. Focused coverage passed in `scripts/__tests__/goldSampleQualityAudit.test.js`, `src/lib/__tests__/blueprintEnrichmentPass.test.js`, and `src/lib/__tests__/apiCallBudget.test.js`.
- 2026-05-27: Raised the blueprint toward A-quality semantics by adding lesson-level source confidence, source anchors, missing-input signals, workload estimates, difficulty profiles, evidence plans, misconception maps, pacing bridges, course arc, course workload, and blueprint quality signals. The compiler now surfaces those controls in syllabus receipts, lesson plans, slide decks, assignments, rubrics, discussions, quiz bank, and study guides, and `audit:gold` now includes a Blueprint Maturity Matrix.
- 2026-05-27: Expanded `audit:gold` from 1 curated research-methods fixture to 5 fixtures: research methods, AI-supported course design, community health evaluation, sparse-assessment resilience, and messy clinical-import resilience. The blueprint now distinguishes fully source-grounded lessons from lessons with review flags, reports evaluation-derived assessment anchors, strips unfinished assessment fragments from publishable output, and derives cleaner titles from messy multi-section imports. Latest `audit:gold` passes with `5` gold samples, minimum quality `8`, `0` blockers, and `0` warnings.
- 2026-05-27: Added explicit instructional alignment proof to the blueprint and syllabus. Each lesson now carries objectives, in-class practice, assessment artifact, rubric criteria, success criteria, evidence requirement, feedback use, misconception check, review flags, and an alignment status. `audit:gold` now includes an Instructional Alignment Matrix and fails if compiled lesson plans, slides, assignments, rubrics, discussions, quizzes, or study guides lose the lesson concept, assessment artifact, or success-criteria trace. Latest focused run passes with `6` gold samples and `0` compiled alignment findings.
- 2026-05-27: Added a Classroom Excellence Matrix to `audit:gold` with A-quality dimensions for instructional alignment, teachability, assessment authenticity, feedback/revision loops, cognitive progression, and accessibility/trust. The first run exposed a real cognitive-progression gap, so lesson plans now keep a broader Bloom-level sequence and the course arc stage list covers every lesson. Latest `audit:gold` passes all `5` samples with minimum quality `8`, minimum classroom excellence `10`, and `0` blockers/warnings.
- 2026-05-27: Added `audit:expert`, an expert/instructor-review quality harness that can ingest external reviewer fixtures via `--fixtures` and compare compiled packages against reviewer expectations and instructor-edit pressure patterns. Default fixtures are clearly marked internal provisional, and the report separates `proofStatus` from pass/fail so we do not confuse internal checks with external quality proof. The first run exposed reviewer-surface gaps around formative checks, mini-rubrics, scoring guidance, and trust language, so the compiler now makes those labels explicit. Latest `audit:expert` passes `3` internal provisional fixtures with `0` blockers/warnings and reports `proofStatus: internal-provisional-only`.
- 2026-05-27: Added deterministic instructor preference profiles from accepted/rejected edit patterns, giving the blueprint a zero-call learning layer for rubric feedback style, quiz difficulty, slide-note compactness, lesson pacing, assignment scaffolding, discussion evidence norms, and instructor-edited naming. The compiler now surfaces those learned preferences in the syllabus receipt plus lesson plans, slide decks, assignments, rubrics, discussions, and quiz bank while leaving the no-history default path unchanged.
- 2026-05-27: Extended `audit:expert` so external fixtures can include real instructor edit histories. The audit now builds the same deterministic preference profile used by production, compiles with it, and fails if the learned preference does not appear in the blueprint receipt and affected deliverables. Reports now distinguish external review proof from external edit-history proof, including the stronger `external-review-and-edit-evidence-present` state.
- 2026-05-27: Hardened the compact blueprint enrichment pass so a model-enriched blueprint must remain source-grounded before deterministic compilation can use it. The parser now rejects generic enrichment, publishability-risk details, and responses with too little course-map vocabulary overlap; accepted enrichment carries quality evidence into the syllabus receipt, while rejected enrichment falls back to the deterministic blueprint path.
- 2026-05-27: Closed the remaining compiled-output traceability gap by adding source grounding to Course FAQ and deterministic custom Reflection/Reading Response outputs. `audit:gold` now requires every compiled lesson-facing feature to expose source confidence, evidence grounding, and local-review trace signals, not only the major core package deliverables.
- 2026-05-27: Added a reusable blueprint compiler contract that blocks missing course arc, workload, source anchors, evidence plans, misconception maps, assessment anchors, and alignment loops before compiled output can be treated as A-quality. The compile entry point now refuses blocker-level contract failures by default, the syllabus receipt exposes the compact contract, and `audit:gold` fails if the contract is missing or not passing.
- 2026-05-27: Added a structured blueprint-fidelity gate to `audit:gold` and `audit:expert`. The audits now check that every lesson-facing compiled artifact preserves the blueprint's confidence, evidence requirement, assessment artifact, misconception check, success criteria, and weak-input review flags, so A-quality is measured as traceable instructional consistency rather than only valid output shapes.
- 2026-05-27: Tightened external quality proof intake for `audit:expert`. External fixtures now need reviewer role, review evidence, reviewed package version, reviewed artifacts, and concrete expectations or edit patterns before they count toward external proof status; added `docs/EXTERNAL_QUALITY_PROOF.md` and a fixture template so real expert reviews and instructor edit histories can be collected consistently.
- 2026-05-27: Extended `audit:expert` so external proof fixtures can include their own `project.courseMap`, not only built-in curated gold samples. This lets real reviewed course maps become audit evidence while still running the same compiler contract, blueprint fidelity, reviewer expectation, and instructor edit-history checks.
- 2026-05-27: Added reviewer scorecards to `audit:expert`. External reviewers can now submit scored quality dimensions, normalized to a 10-point scale with an A-quality floor of `8/10`; low scores block the audit and appear in a Reviewer Scorecard Matrix alongside fidelity and expectation gates.
- 2026-05-27: Tightened reviewer scorecards so A-quality proof needs coverage across the same six classroom-excellence dimensions as `audit:gold`: instructional alignment, teachability, assessment authenticity, feedback/revision, cognitive progression, and accessibility/trust. Narrow scorecards now block `audit:expert` even when their single score is high.
- 2026-05-27: Added `audit:expert:external`, a stricter A-quality proof gate that intentionally fails unless proof-eligible external reviewer evidence, a broad external scorecard, and external instructor edit-history evidence are present together. This keeps normal CI usable with internal provisional fixtures while making the remaining external-proof gap enforceable for release claims.
- 2026-05-27: Added an adaptive blueprint enrichment path selector for production generation. The compiler now stays deterministic by default, honors explicit enrichment requests, and can choose one source-grounded enrichment call only when there is a connected model, call budget, enough compiled deliverables, enough lessons, and enough course-map vocabulary to improve subject-specific phrasing safely.
- 2026-05-27: Added compiler-path evidence to the blueprint and syllabus quality receipt. Compiled packages now state whether they used deterministic or enriched blueprint compilation, how many enrichment calls were used, the safe repair policy, and the handoff review policy; `audit:gold` now blocks if that compiler-path evidence is missing.
- 2026-05-27: Added adaptive safety classification to compiler-path evidence. Receipts now distinguish ready-with-spot-check from review-required packages, count source-inferred local repairs and synthesized/derived assessment anchors, and state that model fallback was not used for blueprint-compiled deliverables. `audit:gold` blocks if this safety classification is missing.
- 2026-05-27: Expanded `audit:gold` to a sixth curated fixture for a nontraditional interaction-design studio/lab course. The new fixture verifies studio critique, prototype evidence, usability testing, accessibility audit, design-system, and portfolio-rationale language across the compiler output, broadening A-quality coverage beyond lecture/seminar-style course maps.
- 2026-05-27: Tightened the external A-quality proof gate so a reviewer scorecard only counts for release certification when the reviewer saw the full core package, not just a few artifacts. `audit:expert:external` now reports external full-package review scorecards and blocks partial-package proof even when scores and edit-history evidence are otherwise present.
- 2026-05-27: Added enrichment-impact proof to `audit:gold`. The audit now compiles each curated sample with and without compact enrichment, verifies that enrichment adds measurable course-specific phrase lift, and blocks if enrichment lowers heuristic quality or blueprint-fidelity traceability.
- 2026-05-27: Expanded `audit:gold` to a seventh curated fixture for Spanish healthcare communication. The new sample stresses oral role-play evidence, patient interviews, symptom descriptions, interpreter protocol, cultural humility, discharge instructions, and final simulation performance across the blueprint compiler output.
- 2026-05-27: Added `audit:expert:packet`, a reviewer-packet generator for external A-quality proof. It compiles the curated gold packages, exposes lesson evidence and artifact excerpts, and emits full-package reviewer and instructor-edit fixture templates so external evidence can be collected without hand-building audit inputs.
- 2026-05-28: Tightened external proof intake so copied templates cannot masquerade as A-quality evidence. External fixtures now block placeholder review metadata such as `YYYY-MM-DD` or `Replace with...`, and external reviewer scorecard dimensions need concrete notes instead of blank or template text.
- 2026-05-28: Hardened external proof fixtures again so removing `templateOnly` is not enough. Real-review fixtures now block placeholder course-map content and placeholder positive reviewer expectations before they can count toward external A-quality proof.
- 2026-05-28: Added lesson-level exemplar/non-exemplar contrast to the blueprint compiler contract. Each lesson now carries a strong-work move, weak-work contrast, contrast question, and transfer prompt; lesson plans and assignments surface it, and `audit:gold` blocks if the blueprint or compiled fidelity trace loses it.
- 2026-05-28: Added lesson-level diagnostic support planning to the blueprint compiler contract. Each lesson now carries a readiness diagnostic, ready-work evidence, targeted support move, extension move, and grouping cue; lesson plans and assignments surface the plan, and `audit:gold` blocks if the compiler loses the support trace.
- 2026-05-28: Added assessment-validity evidence to the compiler contract. Each assessment now states its target construct, authentic performance rationale, validity threat, and calibration check; assignments and rubrics surface the evidence, and `audit:gold` blocks if the blueprint or compiled trace loses it.
- 2026-05-28: Added lesson-level instructional design rationale to the compiler contract. Each lesson now states why the sequence, practice, assessment, and support choices fit the learning target; lesson plans, assignments, rubrics, source grounding, and the syllabus alignment matrix surface the rationale, and `audit:gold` blocks if the rationale is missing or lost in compiled traces.
- 2026-05-28: Promoted accessibility and participation support into the blueprint contract. Each lesson now carries representation, engagement, expression, participation, support-bridge, and accommodation-review cues; lesson plans, slide decks, assignments, rubrics, discussions, source grounding, and the syllabus alignment matrix preserve the plan, and `audit:gold` blocks if accessibility becomes generic downstream text.
- 2026-05-28: Promoted feedback and revision from a loose sentence into a structured blueprint cycle. Each lesson now carries formative evidence, feedback method, student revision action, next-use transfer, and closure check; lesson plans, assignments, rubrics, discussions, source grounding, and the syllabus alignment matrix preserve the cycle, and `audit:gold` blocks if the compiler loses the revision trace.
- 2026-05-28: Added lesson-level retrieval and transfer planning to the blueprint contract. Each lesson now carries a retrieval cue, spaced-practice cue, transfer task, cumulative connection, and metacognitive prompt; lesson plans, slide decks, quiz banks, study guides, source grounding, and the syllabus alignment matrix preserve the plan, and `audit:gold` blocks if cognitive progression loses spaced practice or transfer evidence.
- 2026-05-28: Added structured grading calibration to assessment anchors. Each assessment now carries anchor comparison, scorer norming, bias check, student transparency, and post-score review evidence; lesson plans, assignments, rubrics, source grounding, and the syllabus alignment matrix preserve the calibration plan, and `audit:gold` blocks if scoring-consistency evidence is missing or lost.
- 2026-05-28: Expanded the external A-quality proof packet so reviewers can inspect the same evidence the compiler contract now guarantees: target construct, scorer norming, bias check, student transparency, feedback/revision, transfer, accessibility/participation, and local-review flags per lesson. Scorecard templates now include dimension-specific reviewer prompts, and `audit:expert:packet` regenerates enriched reviewer packets for all curated gold samples.
- 2026-05-28: Added criterion-level evidence guidance to assessment anchors. Each rubric criterion now carries evidence-needed, strong-signal, partial-signal, feedback-move, and calibration-question fields; assignments, rubrics, lesson-plan support, source grounding, syllabus alignment, and external reviewer packets preserve the criterion evidence, and `audit:gold` blocks if it is missing.
- 2026-05-28: Added source-use and citation-integrity planning to the blueprint contract. Each lesson now carries approved source cues, attribution expectations, no-invented-source rules, source-evaluation prompts, local replacement cues, and copyright-review guidance; syllabus, lesson plans, assignments, rubrics, source grounding, alignment, and external reviewer packets preserve the guidance, and `audit:gold` blocks if it is missing.
- 2026-05-28: Added course-level learner-context profiling to the blueprint contract. The compiler now records the inferred learner role, expected prior knowledge, support assumptions, participation modes, and local-review needs; syllabus receipts, lesson-plan teaching support, assignment briefs, source grounding, gold audits, and external proof packets preserve this context so A-quality output is judged against the intended learners instead of only artifact structure.
- 2026-05-28: Promoted learner-context traceability from a few major artifacts to every lesson-facing compiled output. Lesson plans, slide decks, assignment briefs, rubrics, discussions, quiz bank, study guides, Course FAQ, and deterministic custom reflection/reading-response templates now carry learner-context cues in their grounding; `audit:gold` fails if any compiled lesson-facing artifact drops that trace.
- 2026-05-28: Added a structured classroom handoff plan to the blueprint contract. Compiled syllabus receipts and external proof packets now expose handoff status, local-confirmation checklist, cross-artifact coherence checks, lesson review order, and publish-boundary guidance; `audit:gold` blocks if the plan is missing or the syllabus drops it.
- 2026-05-28: Added a package coherence matrix to the blueprint contract. The matrix records per-lesson invariants for assessment artifact, weight, success criteria, evidence, source-use, learner context, feedback, transfer, and handoff review focus across the full compiled artifact set; syllabus receipts and external proof packets now render the inspectable per-lesson rows, and `audit:gold` blocks if it is missing or incoherent.
- 2026-05-28: Raised the internal gold-sample quality floor from passable to A-level. The heuristic scorer now recognizes rich blueprint trace evidence across progression, specificity, actionability, trust, accessibility, source integrity, and feedback/revision, while capping internal-only heuristic proof at `9/10`; `audit:gold` now requires both feature quality and classroom excellence to meet a `9/10` floor.
- 2026-05-28: Made external proof packets reviewable as full packages instead of excerpt-only summaries. `audit:expert:packet` now writes per-sample full-package Markdown and JSON files under `verification-output/external-quality-proof-packet/full-package/`, so external scorecards can inspect every compiled core artifact before claiming full-package review.
- 2026-05-28: Aligned the external expert-review scorecard with the internal A-quality floor. External proof now requires all six classroom-quality scorecard dimensions to normalize to at least `9/10`, while lower reviewer scores block `audit:expert:external` even if the reviewed artifact list is complete.
- 2026-05-28: Improved the generated full-package review files from flattened text dumps to structured review artifacts. Each full-package Markdown file is now organized by artifact and item with reviewer-facing labels, and the paired JSON preserves structured review data so external reviewers can inspect context without losing audit traceability.
- 2026-05-28: Added reviewer intake artifacts to the external proof packet. `audit:expert:packet` now writes per-sample intake forms and standalone review/edit-history fixture JSON templates, reducing the chance that external reviewers have to hand-author or hand-extract audit fixtures.
- 2026-05-28: Added combined external proof fixture bundles to the review packet. Each curated sample now gets a single review-plus-edit-history template wired to the strict `audit:expert:external` command, reducing reviewer handoff friction for the remaining real external A-quality proof step.
- 2026-05-28: Added `audit:expert:preflight`, a strict external-proof readiness check that reports whether external review, edit-history evidence, full-package scorecard coverage, 9/10 reviewer floors, and normal expert-audit blockers are all clear before claiming A-quality.
- 2026-05-28: Added source-input review files to the external proof packet. Reviewers now receive the original source course map beside the compiled full package, so external A-quality reviews can judge source fidelity and unsupported invention risk instead of only polished output quality.
- 2026-05-28: Made source fidelity a strict external A-quality proof requirement. `audit:expert:external` now blocks release proof unless at least one proof-eligible external reviewer confirms they compared the source course map and compiled package, with concrete notes and no high unsupported-invention risk.
- 2026-05-28: Added an internal source-fidelity gate to `audit:gold`. The gold audit now checks that source course-map topic and assessment signals survive into the blueprint and compiled lesson-facing outputs before external review begins.
- 2026-05-28: Added explicit teaching-intent sequencing to the blueprint contract. Each lesson now carries diagnostic, modeling, guided-practice, evidence-of-learning, feedback, revision, transfer, and local-review intent; lesson-facing compiled outputs preserve the trace, `audit:gold` now includes a Teaching Intent Matrix, and external proof packets expose the same evidence to reviewers.
- 2026-05-28: Added course-modality fit to the blueprint contract and gold proof. The compiler now infers whether a course behaves like clinical simulation, studio/lab, applied lab, field-applied work, online hybrid, or weekly applied seminar; every lesson carries a modality cue, compiled outputs preserve that trace, `audit:gold` now includes a Modality Fit Matrix, and external proof packets expose modality evidence to reviewers.
- 2026-05-28: Promoted modality from a label into a compiler decode pattern. Each course modality now carries signature practice, evidence routine, feedback routine, instructor move, and student-product expectations; lesson plans and slide decks use those routines in the actual teaching sequence, compiled outputs preserve the structured `modalityDecode`, and `audit:gold` blocks if the modality teaching pattern is missing or lost.
- 2026-05-28: Promoted prerequisite readiness into the blueprint compiler contract. Each lesson now carries assumed prerequisite knowledge, prerequisite evidence, a diagnostic check, reteach move, acceleration move, and local assumption review; syllabus alignment, package coherence, lesson plans, slide decks, assignments, rubrics, discussions, quizzes, study guides, Course FAQ, gold audits, and external proof packets preserve the prerequisite trace so classroom readiness is checked before new instruction begins.
- 2026-05-28: Added structured assessment anchor examples to the blueprint compiler contract. Each assessment now carries strong and partial anchor samples, scoring rationale, revision prompt, scorer calibration use, and student-facing anchor guidance; lesson plans, slide decks, assignments, rubrics, discussions, quizzes, study guides, Course FAQ, gold audits, and external proof packets preserve the anchor-example trace so rubric quality can be judged against concrete sample evidence instead of abstract criteria alone.
- 2026-05-28: Added raw source-provenance traces to the blueprint compiler contract. Each lesson now records the source course-map field, sanitized raw evidence, compiled value, confidence, compiler-use purpose, and inferred-field review policy for lesson identity, objectives, topics, activities, resources, and assessment artifacts; lesson-facing compiled outputs preserve the trace through grounding, `audit:gold` blocks if provenance is missing, and external proof packets now include a Source Provenance table for reviewer source-fidelity checks.
- 2026-05-28: Added a source-risk register on top of the provenance trace. The blueprint now classifies high/medium source risks from inferred fields, sparse assessments, unfinished source text, and local-review flags; syllabus receipts, package coherence rows, compiled grounding, gold audits, and external proof packets expose the review-required items so instructors and external reviewers can distinguish polished output from source assumptions before classroom handoff.
- 2026-05-28: Added class-session feasibility planning to the blueprint compiler contract. Each lesson now carries a six-phase timing plan with planned/session minutes, live-session feasibility, workload-fit cues, and adjustment guidance; lesson plans preserve it through outline timing, slide decks expose slide timing fit, `audit:gold` now includes a Session Feasibility Matrix, and external proof packets show timing evidence for reviewers before classroom handoff.
- 2026-05-28: Added course-level assessment architecture to the blueprint compiler contract. Assessment anchors now carry role, stakes, weight, grading mode, cadence, feedback window, and revision use; assignments and rubrics preserve that architecture, syllabus/package coherence expose the grading cadence, `audit:gold` now includes an Assessment Architecture Matrix, and external proof packets show role/weight/feedback evidence for reviewers.
- 2026-05-28: Added criterion-level weighting to the assessment compiler contract. Each assessment now carries a 100% criterion-weight plan with instructional priority, rationale, evidence signal, calibration use, feedback use, and student transparency; assignments and rubrics preserve the weighting instead of defaulting to equal 25-point criteria, `audit:gold` now includes a Criterion Weighting Matrix, and external proof packets expose criterion weights for reviewer calibration.
- 2026-05-28: Replaced generic rubric performance-band prose with criterion-specific observable descriptors. Rubric bands now reference the lesson concept, artifact, source cue, decision logic, revision target, and common scoring pitfall for each criterion; `audit:gold` blocks if rubrics fall back to stock exemplary/proficient/developing/beginning language.
- 2026-05-28: Replaced generic assignment brief typing with artifact-genre and workload decoding. Assignment briefs now derive assignment type, submission format, evidence standard, review protocol, workload fit, and local adaptation cues from the lesson artifact genre instead of rotating through generic case-study/reflection/project labels; `audit:gold` blocks if assignments fall back to generic type/time text or lose the submission profile.
- 2026-05-28: Replaced rotating discussion-prompt formats with modality/artifact protocol decoding. Discussions now choose formats such as Claim-Evidence Critique, Studio Critique, Method Clinic, Source Synthesis Seminar, or Role Play / Simulation from the lesson modality and artifact genre, and preserve participation pattern, evidence move, artifact use, review focus, modality fit, and local adaptation cues; `audit:gold` blocks if discussions fall back to a generic rotating format without the protocol.
- 2026-05-28: Replaced rotating slide visual kinds with purpose-aware visual plans. Slide visuals now decode from slide purpose, evidence source, artifact connection, modality practice, artifact genre, student action, and accessibility needs instead of cycling through diagram/table/chart/image; `audit:gold` blocks if decks lose the visual plan or fall back to legacy rotating visual kinds.
- 2026-05-28: Replaced lesson-position Bloom rotation with source-text cognitive-demand inference. Lesson Bloom levels now come from prioritized source signals such as objectives, assessment text, activities, goals, topics, and title; sparse assessment synthesis uses that inferred demand, the compiler contract requires the inference trace, and `audit:gold` blocks legacy index-rotation Bloom assignments.
- 2026-05-28: Replaced position-based rubric objective alignment with source-text criterion-to-objective matching. Assessment anchors now store criterion objective matches with strategy, keywords, cognitive signal, and rationale; rubrics expose that evidence per criterion, and `audit:gold` blocks legacy index-rotation objective alignment.
- 2026-05-28: Replaced fixed quiz-bank Bloom sequencing with source-grounded quiz planning. Each quiz question now records the source signal, Bloom rationale, role, and objective-alignment strategy used to choose the item; `audit:gold` blocks quiz banks that fall back to fixed Bloom sequences without that plan.
- 2026-05-28: Added per-lesson compiler decisions and publish gates to the blueprint contract. Each lesson now states the cheapest safe path (`deterministic-compile`, local-review compile, or source-repair review), why that path is safe, the model-use policy, the repair policy, and the review focus; compiled lesson artifacts preserve the decision trace, and `audit:gold` blocks if compiler decisions or publish gates disappear.
- 2026-05-28: Expanded `audit:gold` to an eighth curated fixture for a human-services field placement seminar. The new sample stresses field-applied modality, site evidence, stakeholder interviews, supervision notes, implementation constraints, case handoff, professional boundaries, and field-evidence artifacts so the compiler is tested against a nontraditional practicum-style course, not only classroom, studio, clinical, and messy-import fixtures.
- 2026-05-28: Tightened learner-context lens consistency across the blueprint compiler. Learner context now uses the same normalized enrichment lens as compiled artifacts, records domain/evidence/decision terms directly on the profile, and lesson/package-coherence cues now say field practitioners, studio designers, healthcare communicators, course designers, or student researchers instead of falling back to generic roles; `audit:gold` blocks if learner context drops the expected lens.
- 2026-05-28: Tightened modality/lens conflict handling for A-quality compiler output. Clinical-simulation courses that reuse a community-health lens now blend implementation evidence with role-play evidence and program decisions with clinical communication decisions, `audit:gold` blocks if learner context drops the course-modality practice lens, and the discipline classifier no longer treats ordinary program logic models as AI course-design evidence. The latest `audit:pipeline` stress matrix is back to pass for messy clinical imports with no stress findings.
- 2026-05-28: Raised quiz-bank cognitive progression to full internal A-quality. The quiz planner now reserves a source-grounded `Create` transfer-synthesis item in addition to retrieval, application, analysis, and evaluation items, and the stored quiz-plan trace now includes Bloom level, difficulty, intended use, and item index. Latest `audit:gold` passes all 8 curated samples with minimum classroom excellence `10/10`, including `11/11` assessment-authenticity checks and `9/9` cognitive-progression checks for every sample.
- 2026-05-28: Made quiz-bank cognitive progression inspectable in the external proof packet. `audit:expert:packet` now renders a per-lesson Quiz Progression Evidence table with Bloom coverage, source-grounded question roles, explicit `transfer-synthesis -> Create` evidence, transfer use, and retrieval-to-synthesis status so external reviewers can verify the quiz/exam compiler instead of digging through raw package JSON.
- 2026-05-28: Promoted course sequencing from loose prerequisites into an explicit concept dependency graph. The blueprint now records nodes, prerequisite/transfer edges, and per-lesson practice progression; syllabus receipts, lesson plans, source grounding, `audit:gold`, and the external proof packet expose the graph so reviewers can inspect whether each lesson builds from prior concepts, makes practice visible, and transfers into the next artifact. Latest `audit:gold` shows all 8 curated samples passing the Concept Dependency Graph Matrix with `8` nodes, `14` edges, `9` checked features, and `0` findings.
- 2026-05-29: Added first-class mastery-evidence planning to the blueprint contract. Each lesson now carries diagnostic, guided-practice, independent-performance, feedback-revision, transfer, misconception-repair, and mastery-threshold evidence; syllabus receipts, package coherence, lesson plans, source grounding, `audit:gold`, and the external proof packet preserve the trace. Latest `audit:gold` passes all 8 curated samples and now reports a Mastery Evidence Matrix with `8` lessons, `6` checked stages, `9` checked features, and `0` findings per sample.
- 2026-05-29: Added evidence-response decisions to the blueprint contract. Each lesson now records ready, partial, and needs-support signals; matching instructor moves; student next steps; recheck cues; and escalation cues so compiled lessons tell instructors what to do when evidence is strong, incomplete, or weak. Syllabus receipts, package coherence, lesson plans, source grounding, `audit:gold`, and the external proof packet preserve the trace. Latest `audit:gold` passes all 8 curated samples and now reports an Evidence Response Matrix with `8` lessons, `3` checked states, `9` checked features, and `0` findings per sample.
- 2026-05-29: Added laboratory-course edge coverage to the compiler and gold audit. The artifact decoder now has a `lab-report` path with protocol, notebook, data-table, safety, variable-control, and conclusion-limit review guidance; discussion protocols now include a Lab Evidence Clinic; and `audit:gold` includes a ninth Biology Laboratory Methods fixture. Latest full `audit:gold` passes all `9` curated samples, with the new biology lab sample showing applied-lab modality, `8/8` lab-report artifact matches, minimum quality `9`, minimum classroom excellence `10`, and `0` findings.
- 2026-05-29: Added multi-section lesson source coverage to the blueprint contract and reviewer packet. Source provenance now records section-by-section coverage with source columns, preserved signals, and reviewer cues; compiled grounding must preserve the coverage for multi-section lessons; the external proof packet renders a Section Coverage table; and `audit:gold` now passes all 10 curated samples, including a Comparative Literature Seminar fixture with two source sections per lesson.
- 2026-05-29: Added online/hybrid course coverage to the compiler and gold audit. Online-hybrid lessons now get asynchronous module phases, no-live-facilitation assumptions, LMS-centered revision checkpoints, and asynchronous discussion protocols instead of live-class defaults; the learner-context audit now checks online modality language; and `audit:gold` now passes all 11 curated samples, including an Online Academic Writing Workshop fixture.
- 2026-05-29: Added quantitative problem-set coverage to the compiler and gold audit. Student artifacts can now decode as `problem-set` with worked-solution traces, equation/representation setup, answer checks, and error analysis; assignments use a Worked Problem Set profile; discussions use a Problem-Solving Clinic protocol; and `audit:gold` now passes all 12 curated samples, including a College Algebra Problem Solving fixture.
- 2026-05-29: Added capstone/project-based course coverage to the compiler and gold audit. Capstone courses now infer a `capstone-project` modality with milestone design reviews, sponsor-constraint checks, decision logs, feasibility-risk review, portfolio defense, and showcase/handoff support; student artifacts decode as `capstone-project` instead of generic briefs or design prototypes; and `audit:gold` now passes all 13 curated samples, including a Product Innovation Capstone fixture.
- 2026-05-29: Added competency-based/accreditation-style course coverage to the compiler and gold audit. Competency courses now infer a `competency-based` modality with standards-aligned performance review, benchmark descriptors, assessor calibration, remediation planning, reassessment commitments, and accreditation-ready evidence defense; student artifacts decode as `competency-evidence`; and `audit:gold` now passes all 14 curated samples, including a Teacher Education Competency Assessment fixture.
- 2026-05-29: Added creative/studio arts course coverage to the compiler and gold audit. Creative writing courses now infer a `creative-studio` modality with workshop critique, craft evidence, audience-effect checks, visible revision decisions, artist statements, final readings, and portfolio curation; student artifacts decode as `creative-portfolio` instead of generic reflections; and `audit:gold` now passes all 15 curated samples, including a Creative Writing Workshop fixture.
- 2026-05-29: Added business case-method course coverage to the compiler and gold audit. Business strategy courses now infer a `case-method` modality with case preparation, exhibit checks, decision criteria, stakeholder tradeoff challenges, strategic recommendation defense, and implementation-risk review; student artifacts decode as `case-analysis` instead of generic memos; and `audit:gold` now passes all 16 curated samples, including a Business Strategy Case Method fixture.
- 2026-05-29: Added legal/doctrinal course coverage to the compiler and gold audit. Constitutional law courses now infer a `legal-doctrinal` modality with case briefing, Socratic questioning, holding/rationale extraction, rule synthesis, issue spotting, hypothetical application, and IRAC revision; student artifacts decode as `legal-analysis` instead of generic memos or business case analyses; and `audit:gold` now passes all 17 curated samples, including a Constitutional Law fixture.
- 2026-05-29: Upgraded humanities and film-style interpretive courses from generic seminar/memo handling to a discipline-specific compiler path. Comparative literature, film, history, and media-studies evidence now infer an `interpretive-humanities` modality with close reading, context/source checks, competing-interpretation challenges, and claim revision; artifacts decode as `close-reading-analysis`; and the existing Comparative Literature gold fixture now proves the more specific path.
- 2026-05-29: Added lecture/exam-heavy survey course coverage to the compiler and gold audit. Introductory psychology-style courses now infer a `lecture-exam` modality with retrieval warm-ups, focused concept modeling, guided concept checks, misconception repair, exam-style transfer practice, and study-guide handoff; exam artifacts decode as `checkpoint-response`; and `audit:gold` now covers an Intro Psychology lecture fixture.
- 2026-05-29: Added communicative world-language course coverage to the compiler and gold audit. Beginning Spanish-style courses now infer a `world-language` modality with comprehensible input, language-pattern noticing, interpersonal rehearsal, feedback/recast cycles, presentational or interpretive transfer, and proficiency reflection; language artifacts decode as `language-performance`; and the compiler no longer treats ordinary Spanish dialogue practice as clinical simulation unless healthcare evidence is present.
- 2026-05-29: Added performing-arts rehearsal coverage to the compiler and gold audit. Acting, theatre, dance, and music-style performance courses now infer a `performing-arts` modality with body/voice/instrument readiness, technique modeling, guided rehearsal, critique/note uptake, performance run-through, and reflection; rehearsal artifacts decode as `performance-rehearsal` instead of generic simulation or written reflection.
- 2026-05-29: Added software programming lab coverage to the compiler and gold audit. Programming, computer-science, and software-engineering courses now infer a `programming-lab` modality with environment/test setup, live coding, guided implementation, debugging/test loops, code review/refactor work, and commit handoff; student artifacts decode as `code-lab` with repository commits, tests, debugging traces, edge-case checks, pull-request notes, and implementation rationale instead of generic applied-lab analysis logs.
- 2026-05-29: Added data-science analytics lab coverage to the compiler and gold audit. Data science, analytics, and machine-learning lab courses now infer a `data-science-lab` modality with dataset provenance checks, notebook analysis, visualization/model work, validation, bias/limitation review, and insight handoff; student artifacts decode as `data-science-notebook` with cleaning logs, validation metrics, dashboards, model cards, bias audits, and data-story evidence instead of generic coding or analysis logs.
- 2026-05-29: Added engineering design-build-test coverage to the compiler and gold audit. Engineering design, fabrication, CAD, robotics, and mechatronics-style courses now infer an `engineering-design-lab` modality with requirement checks, prototype/model builds, test data, failure analysis, redesign review, safety/tolerance reasoning, and verification handoff; student artifacts decode as `engineering-design-test` instead of generic studio prototypes or capstone milestones.
- 2026-05-29: Added proof-based mathematics coverage to the compiler and gold audit. Real analysis, abstract algebra, topology, and theorem-proof courses now infer a `proof-seminar` modality with definition checks, theorem modeling, proof-strategy comparison, counterexample testing, proof critique, and portfolio handoff; student artifacts decode as `proof-portfolio` instead of generic worked problem sets.
- 2026-05-29: Added statistical inference coverage to the compiler and gold audit. Introductory statistics, biostatistics, business statistics, and inferential-statistics courses now infer a `statistics-inference` modality with question framing, variable/sample checks, assumptions, confidence intervals, hypothesis tests, p-value/effect-size interpretation, limitation review, and inference handoff; student artifacts decode as `statistical-inference-report` instead of generic analysis logs or data-science notebooks.
- 2026-05-29: Added accounting and finance analysis coverage to the compiler and gold audit. Financial accounting, managerial accounting, corporate finance, statement-analysis, budgeting, and valuation courses now infer an `accounting-finance-analysis` modality with source-document checks, account classification, statement linkage, ratio or variance calculation, assumption/control review, and financial decision handoff; student artifacts decode as `financial-analysis-report` instead of generic business cases or quantitative worksheets.
- 2026-05-29: Added public policy analysis coverage to the compiler and gold audit. Public policy, public administration, governance, urban policy, social policy, education policy, health policy, environmental policy, and regulatory-policy courses now infer a `policy-analysis` modality with problem framing, policy option comparison, stakeholder/equity analysis, feasibility review, implementation-risk checks, and policy memo handoff; student artifacts decode as `policy-brief` instead of generic business cases, field reflections, or broad civic memos.
- 2026-05-29: Added economics analysis coverage to the compiler and gold audit. Microeconomics, macroeconomics, managerial economics, and applied market-analysis courses now infer an `economics-analysis` modality with market definition, assumptions, supply-demand or macro-model work, elasticity/surplus calculation, comparative-statics explanation, welfare/incentive review, and economic-decision handoff; student artifacts decode as `economic-analysis-brief` instead of generic lecture checkpoints, policy memos, business cases, or problem sets.
- 2026-05-29: Added ethics and philosophy argumentation coverage to the compiler and gold audit. Ethics, applied ethics, bioethics, technology ethics, professional ethics, and moral-philosophy courses now infer an `ethics-argumentation` modality with ethical issue framing, normative-framework modeling, argument mapping, objection/reply testing, case application, and moral-decision handoff; student artifacts decode as `ethical-argument-brief` instead of generic reflection, legal doctrine, policy analysis, or broad humanities memos.
- 2026-05-29: Hardened the compact model-enrichment pass against lens drift. Model-produced enrichment now needs not only overall source vocabulary overlap, but also source-grounded lens terms and lesson-local phrase grounding before it can feed the deterministic compiler; focused coverage blocks source-vocabulary bait that tries to push a research-methods course into an unrelated studio/design lens.
- 2026-05-29: Promoted deterministic-baseline quality into a required gold-audit gate. The Enrichment Impact Matrix now fails if the no-enrichment compiler baseline falls below the gold quality floor, loses blueprint fidelity, or violates the compiler contract, so compact enrichment can polish course-specific phrasing but cannot hide a compiler that only works after enrichment.
- 2026-05-29: Added clinical judgment and nursing care-planning coverage to the compiler and gold audit. Nursing care-plan courses now infer a `clinical-judgment-simulation` modality with patient cue checks, care-plan building, safety/monitoring review, and SBAR handoff/debrief phases; student artifacts decode as `clinical-care-plan` instead of generic healthcare role-play; and the new `gold-clinical-judgment-8` fixture passes with minimum quality `9`, classroom excellence `10`, and `8/8` care-plan artifact matches.
- 2026-05-29: Added clinical placement/practicum coverage to the compiler and gold audit. Supervised nursing placement courses now infer a `clinical-placement-practicum` modality with site-readiness, confidentiality, preceptor-feedback, competency-log, scope-of-practice, handoff, and transfer phases; student artifacts decode as `clinical-placement-evidence` instead of generic competency evidence or simulation; and the new `gold-clinical-placement-8` fixture proves the distinction.
- 2026-05-29: Added source-conflict resilience to the blueprint contract. Imported course maps now carry an explicit source-conflict report, duplicate or contradictory lesson rows become high/medium source-risk review items instead of being silently smoothed over, compiler decisions hold affected lessons for local review, and the external proof packet exposes a Source Conflict Report for expert reviewers.
- 2026-05-29: Added assessment-weight provenance to the blueprint contract. Source-map percentages are preserved when they form a usable grading plan, compiler-distributed weights are labeled as draft planning weights requiring instructor confirmation, and assignments, rubrics, syllabus receipts, gold audits, and external reviewer packets now expose weight source and review status.
- 2026-05-29: Added a human-reviewable blueprint assumption ledger. The compiler now summarizes learner-context, modality, inferred source fields, source risks, source conflicts, assessment weights, compiler decisions, and publication boundaries as explicit review rows; syllabus receipts, gold audits, and external proof packets expose the ledger so reviewers can judge the blueprint's assumptions instead of only final artifact polish.
- 2026-05-29: Made assumption-ledger review part of strict external A-quality proof. `audit:expert:external` and preflight now require proof-eligible external `assumptionLedgerReview` evidence confirming the reviewer inspected ledger categories and review-required rows, and generated review fixtures/intake forms now include that field.
- 2026-05-29: Tightened assumption-ledger proof from "reviewed" to reviewer decisions. External A-quality proof now requires `assumptionLedgerReview.reviewedRows` decisions for review-required blueprint assumptions, generated proof packets include per-row decision templates, and reviewer intake forms ask reviewers to confirm, revise, or hold each assumption for local confirmation.
- 2026-05-29: Tightened source-fidelity proof from package-level confirmation to artifact-level comparison. External A-quality proof now requires `sourceFidelityReview.artifactReviews` rows for every core compiled artifact, each with source/package comparison flags, preserved-source confirmation, and concrete reviewer notes.
- 2026-05-29: Tightened instructor edit-history proof so accepted external edits must include concrete evidence. `audit:expert` now requires external edit patterns to carry feature, field, action, and before/after wording or evidence notes before they count as edit-history proof, and proof-packet templates now ask instructors for that evidence directly.
- 2026-05-29: Tightened external reviewer scorecards so high scores must be evidence-anchored. Each external scorecard dimension now needs concrete notes, reviewed artifact IDs, and evidence examples before it can support A-quality proof, and the proof packet intake table now asks reviewers for those anchors.
- 2026-05-29: Tightened strict external A-quality proof so evidence cannot be stitched across unrelated courses. `audit:expert:external` now reports complete external proof samples and blocks release proof unless at least one reviewed sample has the full-package scorecard, source-fidelity review, assumption-ledger decisions, and concrete instructor edit-history evidence tied to that same sample.
- 2026-05-29: Tightened strict external A-quality proof from one complete sample to robust coverage. `audit:expert:external` now requires at least two complete external proof samples across at least two teaching modalities, and proof-packet templates carry course-modality and proof-scope tags so reviewers can prove the compiler works beyond a single polished course.
- 2026-05-29: Tightened strict external A-quality proof so curated samples alone cannot certify release quality. At least one complete external proof sample must now compile from a real externally supplied `project.courseMap`, while the proof packet and docs explain how to include those real course maps.
- 2026-05-29: Made the real-course proof requirement operational in the generated proof packet. `audit:expert:packet` now writes `fixtures/external-project.combined-fixtures.template.json`, paired review/edit-history templates, and an external-project intake note so reviewers have a concrete starting point for the required `project.courseMap` evidence.
- 2026-05-29: Extended `audit:expert:packet` so filled external-project fixtures can generate their own source-input, full-package, review-intake, and combined fixture artifacts with `--fixtures ... --external-only`. This closes the reviewer workflow gap between "supply a real course map" and "inspect the compiled package before scoring it."
- 2026-05-29: Hardened external-project packet generation so `--external-only` refuses empty fixture sets and course maps that still contain template placeholders. This prevents fake `Replace with...` source data from becoming reviewer packets and keeps external A-quality proof tied to real reviewed courses.
- 2026-05-29: Added a proof collection plan to `audit:expert:packet`. The generated packet now states whether strict external collection is ready, lists missing proof requirements, recommends two modality-distinct proof samples, points reviewers to source/full-package/combined-fixture files, and shows the exact preflight/external gate commands before anyone claims A-quality proof.
- 2026-05-29: Tightened compact model enrichment from phrase polish to lesson-slice coverage. Accepted enrichment now needs a complete course-decode lens plus exactly one grounded `lessonPhrases` entry for every lesson id in the compact source payload, and it rejects omitted or invented lesson ids so one partial model response cannot masquerade as a full enriched blueprint.
- 2026-05-29: Added strict scope coverage to `audit:gold` so internal A-quality proof must include short-module, standard, and full-semester course lengths (`5`, `8`, and `14` lessons). The new full-semester research-methods fixture exposed a modality bug where later test/portfolio wording could overpower stronger applied-lab evidence; the classifier now requires lecture-exam or competency-based signals to dominate applied practice before they override applied-lab mode.
- 2026-05-29: Extended the external proof packet's collection plan with the same `5`/`8`/`14` scope coverage used by `audit:gold`. Reviewer packets now list available course scopes, missing recommended scopes, and suggested short/standard/full-semester proof samples so external A-quality review does not only test one course length.
- 2026-05-29: Promoted scope coverage from packet guidance into the strict external proof gate. `audit:expert:external` now blocks A-quality release proof unless complete external proof samples cover `5`, `8`, and `14` lesson scopes, so external reviewer evidence must prove the compiler across short modules, standard courses, and full-semester packages.
- 2026-05-29: Aligned reviewer-packet recommendations with the stricter external proof gate. The proof collection plan now recommends one strict proof bundle candidate set that covers required `5`/`8`/`14` scopes first, then adds modality-diverse and real-course samples, so reviewers are not pointed at a two-sample set that cannot satisfy the release gate.
- 2026-05-29: Added an explicit Recommended Bundle Coverage table to `audit:expert:packet`. Reviewer packets now state whether the recommended strict-proof bundle covers sample count, modality diversity, real external `project.courseMap`, and `5`/`8`/`14` scopes before reviewers start filling scorecards and edit-history evidence.
- 2026-05-29: Made external reviewer packets practical to inspect by bounding reviewer-facing full-package Markdown while preserving complete structured artifact data in the paired JSON files. The packet now records when artifact sections are truncated and points reviewers to `fullPackageArtifacts[].reviewData` for the full audit trace.
- 2026-05-29: Reduced the main external proof packet from an all-sample evidence dump to an index plus detailed sections for the recommended strict-proof bundle. Every curated sample still gets its own source, full-package, intake, and fixture files, but `latest.md` now stays focused on the samples reviewers actually need for the A-quality gate.
- 2026-05-29: Slimmed `audit:expert:packet`'s `latest.json` into a true manifest. Heavy source maps, package structures, handoff matrices, and fixture templates now stay in per-sample JSON/fixture files, while the manifest keeps compact quality summaries and file paths for review navigation.
- 2026-05-30: Added a Blueprint Decode Losslessness Matrix to `audit:gold`. The gate now checks that each compact blueprint lesson preserves enough source title/topic/assessment signal and that every lesson-facing compiled feature visibly decodes that signal, so A-quality proof measures source-to-blueprint-to-output retention instead of only artifact shape.
- 2026-05-30: Added external blueprint-quality review as a strict A-quality proof requirement. `audit:expert:external` now requires proof-eligible reviewers to compare each source lesson against the compact blueprint before scoring compiled artifacts, and `audit:expert:packet` now generates blueprint-quality lesson-review templates and intake prompts for that source-to-blueprint proof.
- 2026-05-30: Made blueprint-quality review operational by adding standalone `compact-blueprints/` reviewer artifacts to `audit:expert:packet`. Reviewers now receive source-input, compact-blueprint, and full-package files for every sample, so strict proof can inspect source-to-blueprint compression directly instead of inferring it from compiled artifacts.
- 2026-05-30: Added `fixtures/recommended-strict-proof-bundle.template.json` to `audit:expert:packet`. The packet now gives reviewers a one-file strict-proof starting point that combines the recommended scope/modality samples plus the required external-project template when no real external course map is already present.
- 2026-05-30: Hardened strict-proof preflight for reviewer bundles. `audit:expert:preflight` now blocks fixture files that still have top-level `templateOnly` with a `bundleTemplateOnly` finding, so a recommended strict-proof bundle cannot be mistaken for completed A-quality evidence until reviewers fill it and remove the bundle marker.
- 2026-05-30: Added information-literacy/library-research coverage to the blueprint compiler and gold audit. Courses with database search, source evaluation, citation trails, annotated bibliographies, and synthesis matrices now infer an `information-literacy` modality and decode student artifacts as `source-evaluation-dossier` work instead of generic research-methods or literature-synthesis output.
- 2026-05-30: Added teacher-preparation / instructional-methods coverage to the blueprint compiler and gold audit. Teaching methods courses with lesson planning, microteaching, student-work analysis, formative assessment, differentiation, classroom routines, and reteach decisions now infer a `teacher-preparation` modality and decode student artifacts as `teaching-plan-portfolio` work instead of generic education essays or competency portfolios.
- 2026-05-30: Added counseling / social-work helping-skills coverage to the blueprint compiler and gold audit. Counseling skills and social-work practice courses with client intake, active listening, process recordings, case conceptualization, ethics and boundaries, risk/safety planning, referral rationale, and supervision feedback now infer a `counseling-practice` modality and decode artifacts as `case-conceptualization` work instead of generic reflection, field evidence, or healthcare role-play.
- 2026-05-31: Added a human-readable Blueprint Review Surface to the compiler contract, syllabus receipt, gold audit, and external proof packet. The dense compact blueprint now exposes an instructor-facing decode of course modality, learner role, lesson source confidence, artifact genre, compiler decision, publish gate, local-confirmation counts, and per-lesson reviewer questions before the package is expanded, so A-quality proof can reject opaque high-density blueprints even when generated artifacts look structurally valid.
- 2026-05-31: Tightened the Blueprint Review Surface from "present" to source-traceable. Every lesson row now carries source-field provenance, evidence requirement, compiler reason, local-confirmation cue, assumption references, and an answerability status; the compiler contract and gold audit block untraceable reviewer questions, and the external proof packet exposes the same traceability/answerability evidence for reviewers.
- 2026-05-31: Promoted compact enrichment from vocabulary-only phrase lift into reusable teaching-move guidance. Accepted model enrichment now needs source-grounded opening, practice, feedback, assessment, and review moves; the blueprint contract and gold audit require those moves, and the lesson-plan and slide-deck compilers preserve lesson-specific versions without triggering repeated-boilerplate readiness warnings.
- 2026-05-31: Made those teaching moves reviewable at the compact-blueprint layer. The Blueprint Review Surface now exposes the reusable instructional move decode plus per-lesson opening/practice/feedback/assessment/review traces; the compiler contract, gold audit, and external proof packet fail or surface the issue when a dense blueprint cannot explain the teaching moves the lesson-plan and slide-deck compilers will expand.
- 2026-05-31: Added an Instructional Move Propagation Matrix to `audit:gold`. The gate now verifies that every review-surface opening, practice, feedback, assessment, and review move survives into compiled lesson plans and slide decks for each gold-sample lesson, so A-quality proof checks the blueprint-to-compiler handoff rather than only the compact blueprint surface.
- 2026-05-31: Tightened blueprint-fidelity proof for user trust. `audit:gold` now blocks if compiled lesson-facing artifacts drop the compiler decision path, publish gate, model-use policy, or handoff review focus from their grounding/text traces, so package receipts cannot claim review safety while downstream materials hide the publication boundary.
- 2026-05-31: Extended the same user-trust proof into external review intake. `audit:expert` now blocks source-fidelity artifact rows that do not explicitly confirm compiler-decision visibility, publish-gate visibility, model-use-policy visibility, and handoff-review-focus visibility, and `audit:expert:packet` prompts reviewers for those confirmations per core artifact.

## Current v0.7 Focus

Complete the autonomous package loop in production:

- Built-in finalizer repairs safe issues, reruns readiness, runs export smoke tests, validates course health, and returns a simple confidence state.
- Targeted retry regenerates only localized weak generated sections when the finalizer finds a concrete lesson-level problem.
- Package handoff card shows delivery confidence, safe repairs, export readiness, and remaining assumptions without exposing internal judge scores.
- Agent model routing advice keeps low-cost models first and escalates only after targeted retry cannot clear concrete blockers.
- Main agent starter now supports a single "finish and verify my course package" path while advanced controls remain available.
