// Stub — course sections / module grouping (not yet implemented)

/**
 * Returns section/module definitions for a course map.
 * Used by deliverablePrompts to group lessons into thematic sections.
 */
export function getCourseSections(courseMap) {
  return [];
}

export function getSections(courseMap) {
  return [];
}

export function buildSectionsContext(sections) {
  return '';
}

export function buildSectionPreamble(sections) {
  return '';
}
