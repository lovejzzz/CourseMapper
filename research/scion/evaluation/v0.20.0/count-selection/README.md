# Count-selection proposal experiment

Research only; not used by the application generation path. The frozen plan precedes the first model call. Four new English/Chinese inputs are separate from their references; the harness sends only the input records and objective. Eight actual calls and all raw outputs are retained.

The candidate deterministically enumerates supported source counts, preserving distinct occurrences, and lets Scion select their IDs. Other semantic phrases still require exact quotation and human review. It derives whole-record references only from selected child roles. No source meaning or model approval is inferred from a catalog ID.

Two union cases passed the then-current structural checks but failed semantic review (bare counts as population names, and one as stable-population evidence). Both pooling cases failed source binding checks. The candidate is not promoted. Product changes reject these obvious invalid witnesses and preserve individual diagnostics when choosing a repaired proposal. Replay tests use this raw run; they do not count as additional inference or independent acceptance.

See `review.json` for exact findings, timings, hashes and limits. Open `screening.html` only through the local dev server. Do not rerun these exposed inputs to manufacture a passing score.
