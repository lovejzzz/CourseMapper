# Early joint count repair — development evidence

Four new source packets were frozen before one serial local Scion pass: 47 calls, maximum 48. Both Chinese complete packets produced correct bindings (12/11 calls). The English complete packet repaired a wrong denominator but failed exact citation matching because the model capitalized “The” after a semicolon; the raw failure is retained. A bounded sentence-initial capitalization resolver subsequently passes its recorded response replay, not a new inference run. The missing-total packet remains incomplete and must not compile.

The public reviewed pooling entry now uses the bounded atomic protocol on the verified grammar runtime. `browser-public-entry.json` records an actual Chrome run on the first Chinese packet: 12 calls, 14,235 ms including 3,506 ms load, successful joint second-group repair, no field issues. An existing second-outcome field was set to 待定位 and observed before inference; the UI adopted 三十 and requested teacher review. No semantic field correction was supplied by the implementer.

Only the first Chinese complete packet received four PDF/DOCX output pairs and twelve-page visual PDF review in this batch; see `research/teaching/v0.20.0/atomic-pooling-zh-review.json`. These exposed development tasks are not held-out evidence, independent classroom acceptance, or the complete v0.20.0 release.
