# v3 案例建设状态

2026-09-08。协议仍为 `edutool-classroom-v3-protocol-1`。当前共60包：三族各12个开发包和8个保留包，中英文各半。**manifest已冻结，完整产品运行尚未开始**。输入与参考分开保存，作者与审读者均为实现者，没有独立教师或作者未见的盲测声明。

| 包                | 语言 | 来源家族                        | 范围      | 设计重点                                                                 |
| ----------------- | ---- | ------------------------------- | --------- | ------------------------------------------------------------------------ |
| experiment-dev-01 | en   | workshop-stamp-drying           | supported | 墨水配方与温度同时改变；独立卡片、可执行测量和新试验结果边界             |
| experiment-dev-02 | zh   | cup-sleeve-temperature-loss     | supported | 保温套与起始水温同时改变；杯子与重复读数的区别、温降测量和随机分配       |
| quantity-dev-01   | en   | library-tablet-return-pooling   | supported | 自然表达的不等规模计数、明确互斥身份、对象权重与组权重、无关记录         |
| quantity-dev-02   | zh   | campus-observation-club-overlap | supported | 共同名册、组内去重与跨组重复、可实现的范围、答案未变但推理变化的来源修改 |

本次先写输入与参考，再修改新版显式操作。`43ccc416` 基线的现有 `buildSharedTeachingTask` 对两个输入均返回 null；这是直接调用现有确定性任务构建器的结果，**不是自然语言 Scion 或完整管线的失败率**。原始结果保存于 `.audit-work/v0200-teaching-system/m2-comparison/baseline.json`。

参考使用 [NIST 完全随机设计](https://www.itl.nist.gov/div898/handbook/pri/section3/pri331.htm) 的单位分配与运行顺序原则，以及 [NIST 随机区组设计](https://www.itl.nist.gov/div898/handbook/pri/section3/pri332.htm) 对可控干扰因素和区组内比较的说明。网页于本次检索核对；这些方法来源不证明虚构案例的结果，也不构成教学效果证据。具体的资源数量和操作步骤是本测试的设计约束或参考方案，不能当作样本量充分性证明。

后续仍按固定协议完成各族、语言与限制类型的分布，审查来源家族隔离，并在完整案例审读后冻结 manifest。本文不改变固定分母或准入标准。

两个quantity包的分数/范围及修改结果已独立用有理数和穷举重叠检查。直接确定性任务构建器对它们均返回null，体现自然表达和中文数量操作的覆盖缺口；该诊断不是完整生成流程或Scion实测。并行检查发现旧合并路径可能在未证明组间对象互斥、甚至存在重复成员记录时仍断言精确合并比例，已增加显式成员范围检查。旧v2原包与历史收据不修改；补充了互斥条件的回归变体单独标为development，不计旧包通过。

## 来源类开发包补齐（2026-09-08）

source-dev-01至12覆盖事件/记录日期、单日例外、观察/转述/评价、同事件冲突、局部勘误、不同感受、时间精度、对象身份、引文否定范围及规划/实施。十个支持包中英文各五个，另有英文缺少附件的精确引文请求、中文要求实际化学鉴定的范围外请求。每包原始输入与评审参考分文件，包含四个具体对照回答、允许解释、禁止推断和修改预期。全部为实现者编写的虚构教学案例，不是外部独立评审或模型成绩。

新增`npm run benchmark:v3:inventory`检查文件配对、输入字段隔离、来源ID、参考基本结构、家族复用与完全相同来源、固定分布和摘要。它不会调用产品，不会自动冻结manifest，也不能判断语义来源家族是否独立或教学参考是否正确。当前结果incomplete、16包、0产品运行；原两experiment没有四类作答对照、两quantity只有两类，明确记为缺项。原参考字节未改；已有experiment参考的acceptableProcedure字段按其既有含义接收，不要求通过重命名伪装新增推理。

输入与参考由本轮作者审读，缺少独立教师复核；其余44包、全套语义审读、不可变manifest、Scion完整运行与逐项输出验收均未完成。


## 三类开发分布完成（2026-09-08）

新增quantity-dev-03至12与experiment-dev-03至12，共20包。开发集现有36包，各族中英文各5个支持任务和1个限制包，范围标签分别为10 supported、1 needs-input、1 out-of-scope。没有新增保留包，没有运行完整自然语言产品流程，manifest仍未冻结。

数量案例检查零计数、全部覆盖导致的确定交并人数、相同比例但不同组规模、志愿回应与可达样本的范围、缺失分母和不可识别的未来结果。实验案例检查奇数资源、不可改变的材质属性、区组方案、单位与重复读数、明确测量端点、缺失与零的区别，以及无可分配资源和历史因果识别限制。不得因现有操作不支持固定织法或奇数资源而事后把支持包改成限制包来提高成绩。

四个早期quantity/experiment参考在冻结前补充缺失对照回答，原判断、原输入和已有回答未改。补充前文件及前后摘要保存在research/teaching/v0.20.0/corpus-development-36/，便于区分旧结果和后续审阅；这不是修改旧答案以取得通过。现在每个开发参考均至少有四个具体对照回答。

独立于产品compiler的有理数计算与整数分配穷举复核全部10个数量支持包及其修改结果；另核算10个实验支持包的主要/替代资源分配。语义与教学判断由实现者审读，不是独立教师验收。记录见同目录arithmetic-review.json；它不运行模型、材料生成或导出。

最终inventory无结构错误，只报告24个保留包及60包总量未完成。检查器5项回归通过。接下来补保留来源家族并审读，然后按协议冻结完整manifest；产品运行、三套课程和12个新Scion单元仍单独执行与报告。


## 保留集及冻结（2026-09-08）

新增三族各8个保留包，共24包。每族6 supported、1 needs-input、1 out-of-scope，每种语言3支持和1限制，保持协议原分母。新材料涵盖时间/事件、否定范围、对象身份、狭义更正、观察与感受、数量加权与去重、零与缺失，以及配对的软件比较、异常测量和缺失方法等条件。

来源家族审读在产品运行前撤换两份过近初稿，原文和参考保存于research/teaching/v0.20.0/corpus-reserved-prefreeze/；这些不是额外样本，也不是根据模型结果挑换案例。当前保留材料未经产品诊断或提示词调试。它们由实现者编写并审读，不能声称外部独立盲测。

manifest.json固定60包分类及输入/参考SHA-256、协议、作者、基线提交与审读文件摘要；exposures.jsonl从空记录开始。后续不得覆盖冻结字节，勘误须追加或另立语料版本，保留初始结果和分母。读取保留内容用于产品/检查器缺陷诊断时按协议追加曝光记录。

`npm run benchmark:v3:verify`验证实际文件、分组、数量及审读摘要与冻结快照一致。`benchmark:v3:inventory`只做结构盘点；发现manifest时明确不自行认证冻结。验收检查器共15项测试通过，包括字节修改、删除/重复案例、审读文件更改检测。完整应用未因这次语料冻结而被判为通过。

冻结摘要：f84e51998f42ca29dd778e60d43bf48c74f0382038c99d368aa0a9b31f23335c。审读与核算、验证记录见research/teaching/v0.20.0/v3-freeze-verification.json及corpus-reserved-prefreeze/。下一步是固定完整运行合同并执行开发基线、三套课程和12个新Scion单元；保留包依冻结顺序作首次验收，不从已调试成功结果中挑选。
