{"task":"blend","mode":null,"source":"A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.","output":"The while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs."}
{"task":"blend","mode":null,"source":"The issue is that each recursive call must move toward the base case by shrinking the problem (e.g. n - 1). As the corrective states: “Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.”","output":"The issue is that each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task":"blend","mode":null,"source":"Without a base case, the self-calls never stop. That is exactly why recursion must have a simplest input solved directly without another call. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.","output":"The self-calls must stop before the base case is reached. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task":"blend","mode":null,"source":"Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set — that is exactly why elimination is a valid solving method.","output":"The system is inconsistent because every point on the line through the two solutions is not a solution. The only solution is the zero vector, so elimination is not a valid method."}
{"task":"blend","mode":null,"source":"Dictionaries use keys, and assigning to an existing key overwrites the previous value. They cannot hold duplicate keys, so the old `95` is replaced rather than preserved. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].","output":"Dictionaries use keys, and assigning to an existing key overwrites the previous value. They cannot hold duplicate keys, so the old `95` is replaced rather than kept. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0] to get the value stored under the key '0'."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.","output":"Defining a function only stores it; the body runs when the function is called by name, such as greet()."}
{"task":"blend","mode":null,"source":"Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set — that is exactly why elimination is a valid solving method.","output":"The system is inconsistent because every point on the line through the two solutions is not a solution. The only solution is the zero vector, so elimination is not a valid method."}
{"task":"blend","mode":null,"source":"A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.","output":"The while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs."}
{"task":"blend","mode":null,"source":"Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"The while loop is the core idea: it runs the body condition only at the top of each iteration. The condition is checked at the top using the body condition, so the body must change something the condition depends on."}
{"task":"blend","mode":null,"source":"Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.","output":"A comparison using == (two equals signs) is not exactly the same as assignment. A single = is assignment, not comparison. A single = is the equality test, not the comparison."}
{"task":"blend","mode":null,"source":"This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.","output":"This overwrites the key definition: grades['Ava'] = 97 replaces the previous value, so grades['Ava'] = 97 is the correct entry. The key is unique, so grades['Ava'] = 97 is the correct entry."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.","output":"Defining a function only stores it; the body runs when the function is called by name (e.g. greet()) so \"greet() explicitly invokes the function.\""}
{"task":"blend","mode":null,"source":"Distinct eigenvalues give eigenvectors that are automatically linearly independent, so two of them in a 2×2 matrix give the needed two independent eigenvectors. The zero vector is not an eigenvector at all; it is excluded because eigenvectors must be nonzero. Eigenvectors are required to be nonzero — A0 = λ0 holds trivially and identifies nothing about the matrix. A zero eigenvalue only means some nonzero vector is sent to zero — the matrix is singular, not necessarily zero.","output":"Distinct eigenvalues give eigenvectors that are automatically linearly independent, so two of them in a 2×2 matrix give the needed two independent eigenvectors. The zero vector is not an eigenvector at all; it is excluded because eigenvectors must be nonzero — A0 = λ0 holds trivially and identifies nothing about the matrix."}
{"task":"blend","mode":null,"source":"Not every case is erasure. The key idea is: “Much forgetting is encoding failure (never stored) or retrieval failure (blocking, interference) — the tip-of-the-tongue state proves information can remain stored yet be temporarily unreachable.” That is why a tip-of-the-tongue moment points to blocking, not deletion.","output":"Not every case is erasure. The key idea is: “Much forgetting is encoding failure (never stored) or retrieval failure (blocking, interference).” That is why a tip-of-the-tongue moment points to blocking, not deletion."}
{"task":"blend","mode":null,"source":"Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.","output":"`Calling read() consumes the file contents as it reads, so the current position is reset when you reopen the file or reset the position. The opening idea is that read() is a lazy action and does not start from scratch. Also, a second read() on the same file could raise an IndexError, so you should reopen the file or reset the position before again."}
{"task":"blend","mode":null,"source":"`a` and `b` are separate instances, so each carries its own copy of the attributes. The tempting class-level idea is the misconception: a class is the blueprint; per-object data lives on each instance, not on the class.","output":"The class-level idea is wrong because a class is a blueprint that defines the blueprint; per-object data lives on each instance, not on the class."}
{"task":"blend","mode":null,"source":"readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.","output":"Reads a single line, and then reads the next single-line string."}
{"task":"blend","mode":null,"source":"Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"If a while loop uses a while expression, the condition must be checked at some point before the body is executed. A while-loop expression is used only at the top of its current pass, so the condition depends on that. The body must change something the condition depends on; an += 1 inside the body is the correct way to increment the counter."}
{"task":"blend","mode":null,"source":"A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.","output":"The while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs."}
{"task":"blend","mode":null,"source":"Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. Runny basaltic magma flows easily like syrup, matching gentle lava flows.","output":"Runny basaltic magma flows easily like syrup, matching gentle lava flows. The viscosity of basaltic magma is determined by its viscosity, not its viscosity of the fluid."}
{"task":"blend","mode":null,"source":"Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.","output":"The read() behavior is checked by the function, so the file object is at the end, so the second read() returns '' until the file is reopened or the position is reset."}
{"task":"blend","mode":null,"source":"Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"Every while loop must change the variable n inside the body so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task":"blend","mode":null,"source":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.","output":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Zero-based indexing starts at 0, so lst[0] is the first item and lst[1] is the second."}
{"task":"blend","mode":null,"source":"Defining a function stores it. The body runs only when the function is called by name, such as `greet()`.","output":"A function must be defined by providing a name (or a block of code) and a body (or a function call). The body runs only when the function is called by name, such as `greet()` in the example."}
{"task":"blend","mode":null,"source":"The passage frames senses as “natural” powers through which we receive information, and says reading processes words into mental images involving them. That wording matters: the senses are part of understanding, not a decorative metaphor.","output":"The passage frames the idea that “reading is a natural process that uses the senses to understand something.” The idea is that “reading is a natural process that uses the senses to understand something.” The key is that “reading is a natural process that uses the senses to understand something.”"}
{"task":"blend","mode":null,"source":"Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.","output":"Every comparison needs its own operands joined by and, so age > 13 and age < 19 can be compared by their exact values."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.","output":"Defining a function only stores it; the body runs when the function is called by name, so def greet(): sets up the function, but it does not print anything until you call it."}
{"task":"blend","mode":null,"source":"Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"The while-loop runs n times; the condition is checked once inside the body. So, the body changes something the condition depends on (like += 1 in the counter example), so the loop eventually stops and the condition is never checked again."}
{"task":"blend","mode":null,"source":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.","output":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Zero-based indexing starts at 0, so lst[0] is the first item and lst[1] is the second."}
{"task":"blend","mode":null,"source":"x = x + 1 is the assignment form Python uses here. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one.","output":"In Python, x = x + 1 is the assignment form that defines the assignment behavior. The right side is evaluated first, so x = x + 1 is the assignment that binds the name on the left."}
{"task":"blend","mode":null,"source":"range(5) starts at 0 and stops before 5, so it yields exactly five values: 0 through 4.","output":"Range(5) starts at 0 and stops before 5, so it yields exactly five values: 0 through 4."}
{"task":"blend","mode":null,"source":"`text` is local to `make_message()`, so it does not exist outside that call. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.","output":"To use the local variable in a returned value, the caller must capture it and return it. That is why you can't assign to an unchanged local variable; instead, the caller must return it and the function must capture the returned value."}
{"task":"blend","mode":null,"source":"The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.","output":"The condition is tested only at the top of each iteration, not in the middle of the body. So the loop expression is not tested again until it reaches the top."}
{"task":"blend","mode":null,"source":"Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"Every while loop must change the variable n inside the body so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task":"blend","mode":null,"source":"Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. Runny basaltic magma flows easily like syrup, matching gentle lava flows.","output":"Runny basaltic magma flows easily like syrup, matching gentle lava flows. The viscosity of basaltic magma is determined by its viscosity, not its viscosity of the fluid."}
{"task":"blend","mode":null,"source":"Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"A while loop is executed one at a time, so its condition must be checked after each iteration. If the condition changes, the loop must move toward the same state—either before the condition succeeds, or after the loop exits, so the condition eventually becomes false. The body must change the condition so the loop eventually returns to zero and the condition remains true."}
{"task":"blend","mode":null,"source":"When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.","output":"When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In Python, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared."}
{"task":"blend","mode":null,"source":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.","output":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Zero-based indexing starts at 0, so lst[0] is the first item and lst[1] is the second."}
{"task":"blend","mode":null,"source":"`range(3)` yields exactly three values: 0, 1, 2. That matches the worked example: starting at 0 and stopping before 3 gives three lines, not 1 through 3.","output":"`range(3)` yields exactly three values: 0-3, 1-2, and 2-3."}
{"task":"blend","mode":null,"source":"Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set — that is exactly why elimination is a valid solving method.","output":"The system is inconsistent because every point on the line through the two solutions is not a solution. The only solution is the zero vector, so elimination is not a valid method."}
{"task":"blend","mode":null,"source":"Each comparison must be written with its own operands joined by `and`—for example, `age > 13 and age < 19`. Leaving a fragment like bare `< 19` doesn’t make sense because it has nothing on its left. Finally, in Python 3 `/` always produces floats (`7 / 2` is 3.5), so use `//` for whole-number floor division.","output":"`/` is a close-brackets operator in Python 3. A full-bracket expression consists of single-character words, the first character of which is the opening tag, and the rest of the words are the body of the expression."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.","output":"Defining a function only stores it; the body runs when the function is called by name, so def greet(): sets up the function, but it does not print anything until you call it."}
{"task":"blend","mode":null,"source":"When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.","output":"When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In Python, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.","output":"Defining a function only stores it; the body runs when the function is called by name."}
{"task":"blend","mode":null,"source":"Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.","output":"The while loop expression only runs on each iteration, not at every other. The body must change something that the condition depends on to eventually run without repeating."}
{"task":"blend","mode":null,"source":"Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left. Repeating `age` makes both parts complete.","output":"The comparison is done with two operators: `>` and `<`, with the operands joined by an = sign. The example correctly says age > 13 and age < 19."}
{"task":"blend","mode":null,"source":"According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.","output":"\"A classic example is the story of Romeo and Juliet, which follows a romance that is not the novel. The core idea is that the story is a romance, not a novel. The core idea is that the story is a romance, not a novel.\""}
{"task":"blend","mode":null,"source":"No. The (i,j) entry of AB is the dot product of row i of A with column j of B, which is different from an entrywise product.","output":"The entrywise product is the entry of AB, not the entry of A with B."}
{"task":"blend","mode":null,"source":"The passage frames senses as “natural” powers through which we receive information, and says reading processes words into mental images involving them. That wording matters: the senses are part of understanding, not a decorative metaphor.","output":"The passage frames the idea that “reading is a natural process that uses the senses to understand something.” The idea is that “reading is a natural process that uses the senses to understand something.” The key is that “reading is a natural process that uses the senses to understand something.”"}
{"task":"blend","mode":null,"source":"Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.","output":"A comparison using == (two equals signs) is not exactly the same as assignment. A single = is assignment, not comparison. A single = is the equality test, not the comparison."}
{"task":"blend","mode":null,"source":"`read()` returns the entire contents as a single string, and the `with` block closes the file automatically. `readline()` gives only one line, and `readlines()` returns a list of lines, not one string. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.","output":"`read()` returns the entire contents as a single string, so `readline()` returns a single line. `readlines()` returns a list of all remaining lines, not a single string."}
{"task":"blend","mode":null,"source":"The text notes the Odyssey is only a continuation in the loosest sense. Its heart lies beyond the war narrative, in homecoming, adaptability, and wisdom.","output":"The text is a continuation of the story, so its heart is not in the war. The idea is that Odysseus returns home to Ithusa, not to the epic’s ending."}
{"task":"blend","mode":null,"source":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.","output":"The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Zero-based indexing starts at 0, so lst[0] is the first item and lst[1] is the second."}
{"task":"blend","mode":null,"source":"Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.","output":"The read() behavior is checked by the function, so the file object is at the end, so the second read() returns '' until the file is reopened or the position is reset."}
{"task":"blend","mode":null,"source":"readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.","output":"Reads a single line, and then reads the next single-line string."}
{"task":"blend","mode":null,"source":"A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.","output":"The while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs."}
{"task":"blend","mode":null,"source":"Variables created inside a function have local scope. The corrective rule is: “Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.”","output":"Variables created inside a function have local scope, so the “variable scope” rule is the key to local scope. The “function call creates a local variable” is the core idea."}
{"task":"blend","mode":null,"source":"Which `range` loop prints exactly three lines: `0`, `1`, `2`?\n`for i in range(3): print(i)`\n`for i in range(1, 3): print(i)`\n`for i in range(3, 0): print(i)`\n`for i in range(1, 4): print(i)`\n`range(3)` yields 0, 1, 2, so the loop runs three times and starts at 0. The common mistake is thinking `range(3)` means 1 through 3, but it stops before the endpoint.","output":"Range(3) prints exactly three lines: `0`, `1`, `2`; the loop runs three times and stops at 0."}
{"task":"blend","mode":null,"source":"Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.","output":"Defining a function only stores it; the body runs when the function is called by name, such as greet()."}
{"task":"blend","mode":null,"source":"A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.","output":"The while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs."}