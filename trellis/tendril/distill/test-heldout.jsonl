{"task": "skin", "mode": "reteach", "source": "Reteach reading core concept using the same worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d. Then explicitly connect it to the position marker idea:\n- First f.read() consumes the file contents and returns a string.\n- A second call to read() at that point returns '' because the position has advanced to the end.\n- To see the file again, you\u2019d need to reopen the file or reset the position.\nAnchor corrective explicitly with the reading/position marker explanation.", "reference": "Reteach the core concept of reading a file using the same worked example: with open('data.txt') as f: text = f.read(). Then explicitly demonstrate/describe the \u201cposition advances\u201d idea as a repair: reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Tie it back to the original example: the first f.read() gives the whole file; after that, there\u2019s nothing left to read from that same file object at that current position."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (string + number): Put the anchor text on screen and walk the expression step-by-step: 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added. Emphasize that Python won\u2019t silently convert an int to a string inside + when the other operand is a str.", "reference": "Worked example focus: string + number. Use the anchor: \u201c'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added.\u201d Walk through what changes when we use str(19), and connect it to explicit type conversion to move a value from one type to another."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example for Week 3: if temp > 100: print('boiling') else: print('not boiling'). The else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "blend", "mode": null, "source": "Use the boundary definition: \u201cA transform boundary is where the lithospheric plates slide past each other in the horizontal plane.\u201d The key idea is horizontal sliding past.", "reference": "A transform boundary is where the lithospheric plates slide past each other in the horizontal plane."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration, but the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "teach", "source": "Lists overview: show lists as ordered collections written with square brackets. Emphasize the key syntax: square brackets [] with comma-separated values, and that each element has an index that begins at 0. Use the anchor quote: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d. Briefly preview: we\u2019ll do direct indexing, then iteration with a for loop.", "reference": "Hook + list anatomy: define what a list is (ordered collection in square brackets), emphasize indices start at 0, and show direct element access with the anchor quotes: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d and \u201cIndividual list elements can be accessed directly using an index.\u201d"}
{"task": "skin", "mode": "reteach", "source": "Reteach reading core concept using the same worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d. Then explicitly connect it to the position marker idea:\n- First f.read() consumes the file contents and returns a string.\n- A second call to read() at that point returns '' because the position has advanced to the end.\n- To see the file again, you\u2019d need to reopen the file or reset the position.\nAnchor corrective explicitly with the reading/position marker explanation.", "reference": "Reteach worked example (the same anchor worked example) but focus on a common failure: reading twice. Use: with open('data.txt') as f: text = f.read(). Step 1: f.read() consumes the file contents and puts everything into text. Step 2: if you did f.read() again right after, you would get '' because the position is already at the end. Step 3: to read again, you must reopen the file or reset the position. End by reminding students that the with block closes the file automatically even if something goes wrong."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the big idea: a class defines a type of object with attributes (data) and methods (behavior). Anchor on the class-as-blueprint framing: \u201cA class is a blueprint for creating objects.\u201d Then connect to instances: \u201cIndividual objects created of the class type are called instances\u201d and \u201cEach instance carries its own copy of the attributes.\u201d"}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "readlines() returns a list with every line of the file, so it matches a loop over separate lines. read() gives one string, and readline() gives only the next single line."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The body changes `n`, so the condition `n > 0` eventually becomes false. A while loop keeps going only while its condition stays true; it is not re-checked in the middle of the body. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A system like x + y = 1 and x + y = 3 is inconsistent, because the equations describe parallel lines that never meet. Over the real numbers, two distinct solutions force infinitely many, and each reversible elementary row operation preserves the solution set\u2014so Gaussian elimination works."}
{"task": "blend", "mode": null, "source": "Variables created inside a function have local scope. The corrective rule is: \u201cLocal variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.\u201d", "reference": "Defining a function only stores it; the body runs when the function is called by name, such as square(5). The other choices confuse definition with execution or confuse local scope with global scope. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python uses zero-based indexing, the first element is at index 0, so lst[0] is the first item and lst[1] is the second. If you\u2019re tempted to think in 1-based terms, examples like 'cat'[0] == 'c' help show that index 0 means the first character."}
{"task": "blend", "mode": null, "source": "Yes. A square matrix with determinant 0 can be nonzero yet have no inverse, such as a matrix with two equal rows.", "reference": "A square matrix can be nonzero yet still have no inverse if its determinant is zero. For instance, a matrix with two equal rows has determinant 0, so it is singular even though not every entry is zero."}
{"task": "skin", "mode": "teach", "source": "Warm start: functions as \u201cnamed, reusable block of code\u201d (anchor quote). Show how execution differs between (1) defining with def and (2) calling by name. Emphasize that code inside a function runs only when you call it.", "reference": "Warm-up: What\u2019s a function? Introduce the mental model: a named, reusable block defined with def; it runs only when called. Show the syntax quickly: def name(...): then an indented body. Tie it to scope: variables created inside a function are local to that call; variables created outside have global scope. Anchor quote: \u201cA function is a named, reusable block of code that performs a task when called\u201d."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every `while` loop must change something the condition depends on; for a counter loop, decrement (e.g. `n = n - 1` or `+= 1`) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration, before the rest of the pass runs."}
{"task": "blend", "mode": null, "source": "Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. Runny basaltic magma flows easily like syrup, matching gentle lava flows.", "reference": "That statement is the misconception in a direct way. The correction is: \u201cEruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions.\u201d So the disaster footage model does not apply to every volcano."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left. Repeating `age` makes both parts complete.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the second comparison must repeat age."}
{"task": "blend", "mode": null, "source": "Without a base case, the self-calls never stop. That is exactly why recursion must have a simplest input solved directly without another call. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.", "reference": "A base case is essential: every recursion must include a simplest input solved directly, so the chain of self-calls eventually terminates. Without shrinking toward that stopping condition, the base case is never reached."}
{"task": "blend", "mode": null, "source": "`text` is local to `make_message()`, so it does not exist outside that call. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.", "reference": "Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. Since `note` was created inside `make_message()`, `print(note)` outside that call cannot access it."}
{"task": "blend", "mode": null, "source": "Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Use a with statement to ensure the file gets closed when you\u2019re done (even if an error happens). This is the correction to relying on manual closing\u2014Python will automatically close the file for you."}
{"task": "blend", "mode": null, "source": "Every recursion needs a base case\u2014a simplest input solved directly without another call\u2014so the chain of self-calls eventually stops. A recursive function should call itself on a smaller input until it reaches that stop point. Without that shrinking toward a base case, the function can\u2019t terminate properly.", "reference": "Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops. Without that stopping point, the function keeps recursing."}
{"task": "blend", "mode": null, "source": "The text notes the Odyssey is only a continuation in the loosest sense. Its heart lies beyond the war narrative, in homecoming, adaptability, and wisdom.", "reference": "Correct the misconception: the text notes that the Odyssey is a continuation only in \u201cthe loosest sense,\u201d and that the poem\u2019s heart lies beyond the war narrative. It is not simply the Trojan War story continued."}
{"task": "skin", "mode": "teach", "source": "This is absolute (numerical) dating because \"Absolute dating assigns specific time units, in this case years, to mineral grains within a rock.\" Relative dating only answers older vs younger, without knowing their specific ages.", "reference": "Start with the big picture: geologic time is Earth\u2019s ~4.5-billion-year history, and it\u2019s so vast that we must use methods that work on rocks (not memory). Distinguish the two dating goals: relative dating tells which event is older/younger without giving a year; absolute (numerical) dating assigns specific ages in years to mineral grains within a rock. Anchor to the reading wording: \u201cRelative dating is the process of determining if one rock or geologic event is older or younger than another, without knowing their specific ages,\u201d and \u201cAbsolute dating assigns specific time units, in this case years, to mineral grains within a rock.\u201d Emphasize the key contrast students often mix up: ordering vs numerical ages."}
{"task": "blend", "mode": null, "source": "This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.", "reference": "Because dictionary keys are unique, using grades['Ava'] = 97 on an existing key replaces the old value instead of creating a second entry. That overwrite is what updates Ava\u2019s stored grade."}
{"task": "blend", "mode": null, "source": "The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.", "reference": "The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again. That means a change inside the body affects the next check, not the current one."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A system like x + y = 1 and x + y = 3 is inconsistent because the equations represent parallel lines that never meet. Over the real numbers, two distinct solutions imply infinitely many, since the entire line through those solutions satisfies the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method. Pivot positions are determined by echelon structure during elimination, not by the size of the entries."}
{"task": "blend", "mode": null, "source": "`text` is local to `make_message()`, so it does not exist outside that call. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.", "reference": "`message` has local scope, so it exists only inside `make_message()`. Returning a value is how you pass data out; the variable name itself is not available outside the function. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value."}
{"task": "blend", "mode": null, "source": "Indexes run from 0 to length minus 1, so in a 5-item list the last index is 4. That means lst[4] is valid, while lst[5] is out of range.", "reference": "With 0-based indexing, the third element is at index 2, so names[3] would be beyond the valid range for that list. More generally, indexes run from 0 to length minus 1, so for a 5-item list the last valid index is 4 and lst[5] is out of range."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left. Repeating `age` makes both parts complete.", "reference": "Each comparison needs its own operands, joined by `and`, so the correct form is `age > 13 and age < 19`. The bare `< 19` has nothing on its left, and the duplicate pattern here comes from treating the second comparison as if it could stand alone."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must eventually change something the condition depends on, or it can run forever. The loop expression is checked only at the top of each iteration, after the current pass finishes."}
{"task": "blend", "mode": null, "source": "Which `range` loop prints exactly three lines: `0`, `1`, `2`?\n`for i in range(3): print(i)`\n`for i in range(1, 3): print(i)`\n`for i in range(3, 0): print(i)`\n`for i in range(1, 4): print(i)`\n`range(3)` yields 0, 1, 2, so the loop runs three times and starts at 0. The common mistake is thinking `range(3)` means 1 through 3, but it stops before the endpoint.", "reference": "The worked example matches this exactly: `range(3)` gives 0, 1, 2. `range(n)` starts at 0 and stops before `n`, so the loop runs three times and prints three lines."}
{"task": "blend", "mode": null, "source": "Use s[0:3]. The slice starts at index 0 and stops before index 3, so it returns the characters at indices 0, 1, and 2. That is exactly three characters, not four.", "reference": "Slicing starts at index 1 and stops before index 4, so it returns the characters at positions 1, 2, and 3. That makes the result `\"yth\"`."}
{"task": "blend", "mode": null, "source": "A while loop runs its body as long as the loop expression stays true, and the condition is re-checked only at the top of each iteration. If the loop body never updates the variable (or state) that the loop expression depends on, the condition may never become false, which creates an infinite loop.", "reference": "To avoid infinite loops, a while loop must change something its condition depends on, like a counter decreasing each pass. Additionally, the condition is only tested at the top of each iteration, so the rest of the body still runs even if the condition becomes false mid-loop."}
{"task": "blend", "mode": null, "source": "The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.", "reference": "Correct: after each pass through the body, the loop expression is evaluated again to decide whether to continue. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example from the reading: `if temp > 100: print('boiling') else: print('not boiling')` \u2014 walk through two runs: (1) when `temp > 100` is True, the `else` branch is skipped; (2) when `temp > 100` is False, the `else` branch executes. Emphasize: one and only one branch is taken."}
{"task": "blend", "mode": null, "source": "Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.", "reference": "read() consumes the file contents from the current position, and the example opens the file, reads all of it, and closes it automatically. A second read() at the end of the file returns an empty string, not the same content again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (functions): use the provided example def square(n): return n * n, then connect to calls: square(5) runs the body and returns 25. Reinforce: defining stores the function; calling produces the output. Mention what happens if you try to use an internal variable name outside\u2014scope prevents access.", "reference": "Worked example (functions + return): use the provided example\ndef square(n): return n * n\nExplain: defining with def stores the function; calling square(5) runs the body and returns 25. Then connect the output to scope: variables created inside the function are local to that call, so to use results outside, the function must return them and the caller captures the returned value."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "A loop variable is just a fresh binding to each element in turn, so assigning to it does not edit the container; to change list contents, index into the list directly. To keep a while loop from running forever, it must change something the condition depends on, such as n = n - 1 or += 1 inside the body so the condition eventually becomes false, and the loop expression is checked only at the top of each iteration."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop needs an update that moves `count > 0` toward false, so `count = count - 1` works. The other choices rely on the wrong belief that the condition is re-checked in the middle of the body the instant the variable changes, but the loop expression is tested only at the top of each iteration. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "teach", "source": "Warm start: functions as \u201cnamed, reusable block of code\u201d (anchor quote). Show how execution differs between (1) defining with def and (2) calling by name. Emphasize that code inside a function runs only when you call it.", "reference": "Warm start: what a function is and when it runs. Read the anchor idea aloud: \u201cA function is a named, reusable block of code that performs a task when called.\u201d Build the mental model: definition happens with def, execution happens only at a call like greet() or square(5). Tie this to scope: variables created inside are local to that call."}
{"task": "blend", "mode": null, "source": "range(5) starts at 0 and stops before 5, so it yields exactly five values: 0 through 4.", "reference": "range(n) starts at 0 and stops before n. So range(5) gives exactly five values: 0 through 4."}
{"task": "blend", "mode": null, "source": "Least squares finds the best approximate solution when Ax=b has no exact solution. It minimizes the sum of squared vertical gaps, and the line can miss every single data point.", "reference": "Least squares minimizes the residual (sum of squared vertical gaps), so the least-squares line need not pass through any data point. It\u2019s a common misconception to think closest implies equal: Ax\u0302 generally differs from b. Also, the product AB is matrix multiplication, combining rows of A with columns of B via dot products; the entrywise product is a different, rarely used operation."}
{"task": "blend", "mode": null, "source": "x = x + 1 is the assignment form Python uses here. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one.", "reference": "Confronting the belief \u201cx = x + 1 is a mathematical equation, so it should be impossible\u201d: In Python = is assignment, not equality. The right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "In a while loop, the loop expression is tested at the top of each iteration, then the rest of that pass runs before the condition is checked again. The loop variable is just a binding to each element; changing that variable doesn\u2019t edit the container, so you must index into the list to update contents."}
{"task": "blend", "mode": null, "source": "`with open('data.txt') as f: text = f.read()` reads the whole file into one string and closes it automatically. Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Use the `with` statement, as in with open('data.txt') as f: text = f.read(). It opens the file, reads the contents, and then closes it automatically. This avoids having to manually close the file and still guarantees cleanup even if something goes wrong."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A `while` loop must eventually make the condition become false; otherwise it can run forever. For a counter loop, update the counter inside the body (e.g., `n = n - 1` or `n += 1`) so the condition changes over time. Also note that the loop condition is checked only at the top of each iteration, after the current pass body runs."}
{"task": "blend", "mode": null, "source": "Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.", "reference": "`read()` advances through the file until the end, so a second `read()` has nothing left to return. Reopening the file would reset the position; the file object itself does not start over."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "readline() returns the next single line, while readlines() returns a list with every line of the file."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "While loops must change something the condition depends on; a counter loop needs n = n - 1, or += 1, inside the body so the condition eventually becomes false. The loop expression is checked only at the top of each iteration, though the rest of the current pass still runs before the next check."}
{"task": "blend", "mode": null, "source": "`with open('data.txt') as f: text = f.read()` reads the whole file into one string and closes it automatically. Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "`with open('input.txt') as f: text = f.read()` reads all contents and then closes the file automatically. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The condition has to change inside the body so it can become false later. The wrong belief is to leave the condition variable untouched, which makes the loop run forever if the condition never changes. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, so a counter loop needs `n = n - 1` or `+= 1` inside the body to make the condition eventually false. The loop expression is checked only at the top of each iteration, after the current pass finishes."}
{"task": "skin", "mode": "worked-example", "source": "Worked example walkthrough (Lists + indexing): Use the provided names list example. Write: names = ['Ava', 'Ben', 'Cy'] then evaluate: names[0] is 'Ava', names[2] is 'Cy', and names[3] raises an IndexError. Emphasize the rule: indexes run from 0 to length minus 1, so the last valid index for a 3-item list is 2. Then extend to a 5-item list mentally (last index 4) and practice choosing the right index for the \u201cfirst\u201d element.", "reference": "Then, a worked example walkthrough (Lists + indexing) using the provided names list example: Write: names = ['Ava', 'Ben', 'Cy'] then evaluate: names[0] is 'Ava', names[2] is 'Cy', and names[3] raises an IndexError. Emphasize the rule: indexes run from 0 to length minus 1, so the last valid index for a 3-item list is 2. Then extend to a 5-item list mentally (last index 4) and practice choosing the right index for the \u201cfirst\u201d element."}
{"task": "blend", "mode": null, "source": "Indexes begin at 0, so the third item is at index 2. `names[3]` is out of range, and list items are not accessed by key the way dictionary values are. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0]. Indexes run from 0 to length minus 1, so a 5-item list's last index is 4; lst[5] is out of range.", "reference": "List indexes start at 0 and go up to one less than the length. For three elements, the valid indexes are 0, 1, and 2, so names[3] is out of range. The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0]."}
{"task": "blend", "mode": null, "source": "Hangul is alphabetic, but it is written in stacked syllable blocks rather than a linear string of letters. Each block combines consonant and vowel symbols.", "reference": "Hangul is not a simple line of letters; it is alphabetic, but it is written in stacked syllable blocks rather than a linear string of letters. Learners first master individual consonants and vowels, then combine them to read syllables and words."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python lists are zero-based, the first element is at index 0, the second is at index 1, and so on. So `lst[0]` is the first item and `lst[1]` is the second\u2014there\u2019s no \u201cposition 1\u201d indexing for the first element in Python."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "`readlines()` returns a list with every line in order. The single-string result belongs to `read()`, while `readline()` gives just the next line."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): the Dog class. Show: class Dog: defines the blueprint; then create a = Dog() and b = Dog(). Connect it directly to attributes: each instance has its own name attribute. Clarify with a quick mental trace: if you change a.name, b.name does not change.", "reference": "Worked example (classes): use the reading\u2019s example exactly\u2014class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Walk through: set a.name = 'Ada', set b.name = 'Ben', and compare what changes. Emphasize: methods are shared by the class, but attributes are per-instance."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. The tempting \u201cit changes the underlying list\u201d belief is exactly the wrong idea here. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "`read()` returns the whole file as one string. The list result belongs to `readlines()`, not `read()`."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "A class is the blueprint; `a = Dog()` and `b = Dog()` are the objects built from it. The class-is-an-object trap confuses the blueprint with the things built from it; per-object data belongs on each instance, not on the class. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared. Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task": "skin", "mode": "worked-example", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Walk the worked example: temp threshold.\nCode: if temp > 100: print('boiling') else: print('not boiling')\nFocus question: Which branch runs, and why? Tie to the anchor quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d"}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "`n` is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, you would need to index into the list directly. range(5) yields 0, 1, 2, 3, 4 \u2014 it starts at 0 and stops before the endpoint, giving exactly five values. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The condition is checked before each iteration, so the body runs three times and prints the updated value each time. The \u201cinstant re-check\u201d belief is wrong: the rest of the current pass still runs before the condition is checked again. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Lists are zero-based in Python, so the first element is at index 0. Therefore lst[0] is the first item and lst[1] is the second, not position 1 being the first element."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested at the top of each iteration, so the rest of the current pass finishes before the next check."}
{"task": "skin", "mode": "reteach", "source": "Reteach one worked example: the two-Dog scenario again, but focus on the misconception trap. Start with the wrong thought: \u201cThe class itself is an object, so storing data on the class should affect all Dogs.\u201d Then correct it by using the proper model: create instances from the class (e.g., `d = Dog()`), and put per-dog data on each instance. Finish by showing the second trap: if you change one instance\u2019s attribute (like its name), only that instance should reflect the change, while the other remains independent.", "reference": "Reteach one worked example\u2014two Dogs again\u2014but focus on the misconception trap. Start with the wrong thought: \u201cThe class itself is an object, so storing data on the class should affect all Dogs.\u201d Then correct it by using the proper model: create instances from the class (e.g., `d = Dog()`), and put per-dog data on each instance. Finish by showing the second trap: if you change one instance\u2019s attribute (like its name), only that instance should reflect the change, while the other remains independent."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Reteach using the same worked example again, but focus on the branching rule: if temp > 100: print('boiling') else: print('not boiling'). Ask students: which branch runs when temp = 100? (Condition temp > 100 is false \u2192 else runs.) Then when temp = 101? (Condition true \u2192 if runs.) Reinforce the reading\u2019s key idea: \u201cone and only one of the branches is taken,\u201d and that else runs only when the if condition is false."}
{"task": "blend", "mode": null, "source": "Strings are immutable, so you cannot change a character in place with `s[0] = 'b'`. Use string slicing/concatenation to create a new string, such as `s = 'b' + s[1:]`, or build a new value from pieces.", "reference": "Strings are immutable, so `s[0] = 'C'` and `s[1] = 'C'` do not work. The correct approach is to create a new string from pieces, such as `s = 'C' + s[1:]`. This directly confronts the mistake that you can change a character in a string with assignment like `s[0] = 'A'`; instead, use slicing/concatenation to build a new value."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example (from the reading): the Dog blueprint. Work through: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Then walk the scenario: set a.name = 'Rex' and b.name = 'Mina'. Conclude: changing one Dog\u2019s attribute affects only that instance because \u201cEach instance carries its own copy of the attributes,\u201d while the methods defined in the class are the same across instances."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Even when a linear system is consistent, it can have only one solution or infinitely many, and over the real numbers two distinct solutions force infinitely many because the whole line through them also solves the system. Each elementary row operation is reversible and preserves the solution set, which is why elimination is a valid solving method. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet."}
{"task": "skin", "mode": "reteach", "source": "Reteach reading core concept using the same worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d. Then explicitly connect it to the position marker idea:\n- First f.read() consumes the file contents and returns a string.\n- A second call to read() at that point returns '' because the position has advanced to the end.\n- To see the file again, you\u2019d need to reopen the file or reset the position.\nAnchor corrective explicitly with the reading/position marker explanation.", "reference": "Reteach with one worked example: re-open/with + read() once vs twice. Use this concrete scenario: you do with open('data.txt') as f: first = f.read(); second = f.read(). Walk students through the file-position marker: after first reads the entire contents, the file is at end-of-file, so second becomes '' (empty string). If you need the contents again, you must reopen the file (or reset the position), then read() again. End by restating: \"Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset.\""}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "During iteration, the loop expression is checked only at the top of each pass, and the body runs fully before rechecking. Also, the loop variable is a fresh binding to each element\u2014assigning to it doesn\u2019t change the list, so you must index into the list to modify contents."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example re-teach (Conditionals + else branch): Use the reading\u2019s example: \"if temp > 100: print('boiling') else: print('not boiling')\". Walk through two cases: (a) temp = 101 \u2192 condition temp > 100 is true \u2192 the if-body runs and the else-body is skipped; (b) temp = 100 \u2192 condition is false \u2192 the if-body is skipped and the else-body runs. Reinforce the anchor idea: \"When an if-else statement is executed, one and only one of the branches is taken\"."}
{"task": "skin", "mode": "reteach", "source": "Which code correctly builds the string \"Age: 19\"?\n'Age: ' + 19\n'Age: ' + str(19)\nstr('Age: ') + int('19')\n19 + 'Age: '\nConvert the number with `str()` before joining it to text, or use formatting. Python will not silently turn an int into a string inside a `+` with a `str`, so `'Age: ' + 19` raises a type error.", "reference": "Reteach one worked example scenario\u2014string-building with types: start from the reading\u2019s idea 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError. Explain why: 'Age: ' is a str and + requires compatible types; Python will not silently turn an int into a string inside a + with a str. Then connect to expressions/type conversion: use str(number) (explicit conversion) before concatenation."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "A class is the blueprint, and you create instances from it, such as `d = Dog()`, with per-object data living on each instance rather than the class. Each instance has its own copy of the attributes, so changing one object\u2019s name does not change another\u2019s, and each recursive call still must shrink the problem toward the base case."}
{"task": "blend", "mode": null, "source": "A heuristic fits because it is a general framework used when information is too much or time is limited. It is not a sloppy version of an algorithm; the corrective matters here: \u201cHeuristics are adaptive general frameworks deliberately used when information or time is limited; an algorithm guarantees a result only where a step-by-step formula exists and there is time to run it.\u201d", "reference": "A heuristic is a general problem-solving framework, not an exact recipe. I\u2019m being careful with the wording because the reading contrasts it with an algorithm, which is the step-by-step formula. Again, the corrective is: \u201cHeuristics are adaptive general frameworks deliberately used when information or time is limited; an algorithm guarantees a result only where a step-by-step formula exists and there is time to run it.\u201d"}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop keeps running only while its condition stays true, so the loop body must change something the condition depends on. For a counter loop, decrement/increment inside the body (e.g., n = n - 1 or += 1) so the condition eventually becomes false. The loop expression is checked at the top of each iteration, and the rest of that pass runs before it\u2019s checked again."}
{"task": "blend", "mode": null, "source": "x = x + 1 is the assignment form Python uses here. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one.", "reference": "This is assignment, not algebra. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one."}
{"task": "blend", "mode": null, "source": "The issue is that each recursive call must move toward the base case by shrinking the problem (e.g. n - 1). As the corrective states: \u201cEach recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.\u201d", "reference": "To keep recursion moving toward the base case, the problem should shrink each time, so `n` becomes `n - 1`. Separately, iterative development means implementing small pieces, testing them immediately with simple and edge-case inputs, and then integrating them, which reduces debugging time and isolates failures. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword creates the reusable function block, but it doesn\u2019t execute the indented body immediately. The body runs only when you later call the function by name, such as greet()."}
{"task": "blend", "mode": null, "source": "A square matrix is invertible exactly when its determinant is nonzero, and -7 is nonzero. The sign does not block invertibility; only a zero determinant does. The determinant measures invertibility and volume scaling, not entry size, so a matrix of large numbers can have determinant zero.", "reference": "A square matrix is invertible exactly when its determinant is nonzero, and -3 is nonzero. The sign does not block invertibility; only a zero determinant does."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. So reassigning the loop variable inside a for loop does not change the underlying list it came from. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "A while loop needs its condition to eventually change, so Every while loop must update something it depends on; for counters, use n = n - 1 (or += 1) in the body. The loop expression is checked only at the top of each iteration. Also, the loop variable is a fresh binding to each element\u2014assigning it won\u2019t modify the container, so update the list via indexing instead."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The condition is evaluated at the start of each pass, not in the middle of the loop body, so the body runs before the next condition check. In addition, the loop variable is a temporary binding to each element in turn; changing the binding won\u2019t edit the container, so update list contents by indexing into the list. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "A function definition needs the def keyword, the name, parentheses, a colon, and an indented body. The other choices miss one of those parts, so Python would not read them as a valid function definition.", "reference": "A function definition needs `def`, a name, parentheses, a colon, and an indented body. The other choices are missing required syntax pieces or use a keyword Python does not use."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "For example, the system x + y = 1 and x + y = 3 is inconsistent, since the equations are parallel lines that never meet. Also, over the real numbers, two distinct solutions force infinitely many solutions because the whole line through them satisfies the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "Defining a function stores it. The body runs only when the function is called by name, such as `greet()`.", "reference": "Defining a function stores the code; it does not run the body yet. The body runs when the function is called by name, such as greet()."}
{"task": "blend", "mode": null, "source": "Correct: \u201cEach instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.\u201d Attribute data is per instance; methods are shared across instances.", "reference": "Direct correction: \u201cEach instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.\u201d Sharing applies to the methods defined in the class, not to each instance\u2019s attribute data."}
{"task": "skin", "mode": "reteach", "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "If you\u2019re coming in cold, focus on the direction of the data. read() gives the entire file as one string. readline() gives the next single line. readlines() gives a list containing every line. The file keeps a position marker, so repeated reads keep moving forward through the file."}
{"task": "skin", "mode": "worked-example", "source": "Use s[0:3]. The slice starts at index 0 and stops before index 3, so it returns the characters at indices 0, 1, and 2. That is exactly three characters, not four.", "reference": "Work through a few string examples on the board. Show how s[0:3] gives the first three characters, not four, because the slice stops before index 3. Then build a new string from parts instead of trying to change one character in place."}
{"task": "skin", "mode": "teach", "source": "The law of supply says higher price means higher quantity supplied, so the curve slopes upward. The phrase to keep in mind is that 'a higher price raises the quantity that producers are willing to supply.'", "reference": "Present the law of supply and the upward slope. Use the anchor quote: \u201ca higher price raises the quantity that producers are willing to supply, so the supply curve slopes upward.\u201d Emphasize what is plotted: price of the good (on the vertical axis) vs. quantity supplied (per period)."}
{"task": "skin", "mode": "worked-example", "source": "Worked example: determinant of a product. Start with \u201cdet(AB) = det(A)det(B)\u201d and walk through the full computation workflow: (a) compute det(A); (b) compute det(B); (c) multiply the results to get det(AB). Then interpret: if det(A)=0 or det(B)=0, det(AB)=0 so AB is not invertible. Finish by checking consistency with the invertibility rule: a matrix is invertible iff its determinant is nonzero.", "reference": "Worked example: determinant of a product. Start with \u201cdet(AB) = det(A)det(B)\u201d and use this workflow: (a) compute det(A); (b) compute det(B); (c) multiply to get det(AB). Then interpret: if det(A)=0 or det(B)=0, det(AB)=0, so AB is not invertible. Finish by matching the rule: a matrix is invertible iff its determinant is nonzero."}
{"task": "blend", "mode": null, "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "A class is the blueprint for creating objects; \u201cIn a Python program, a class defines a type of object with attributes (fields) and methods (procedures).\u201d Instances created from the class each carry their own copy of the attributes."}
{"task": "blend", "mode": null, "source": "The passage frames senses as \u201cnatural\u201d powers through which we receive information, and says reading processes words into mental images involving them. That wording matters: the senses are part of understanding, not a decorative metaphor.", "reference": "The passage frames senses as real cognitive pathways: \u201cOne of the five natural powers (touch, taste, smell, sight, and hearing) through which you receive information about the world around you.\u201d It also says reading processes words into mental images with those senses. So the senses are not merely metaphorical; they are natural powers used in understanding."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the first option is not valid Python.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the shortened version is not valid Python."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.", "reference": "Defining a function just stores it; nothing in the function body runs until you call it (for example, `greet()`). If there is no call to `greet()`, you won\u2019t execute its prints\u2014so only `done` appears. Any variables defined inside the function exist only during the call and must be returned to be used outside."}
{"task": "blend", "mode": null, "source": "Orthogonality is defined by the inner product being zero, not by how the vectors look in a picture. For nonzero vectors, that also gives linear independence. The \u201cperpendicular lines in a picture\u201d idea is too narrow; it misses higher-dimensional and non-graph settings.", "reference": "Orthogonality is defined by the inner product being zero, not by how the vectors look in a picture. That definition extends to functions and high-dimensional data where no picture exists. For nonzero vectors, orthogonality also gives linear independence."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop condition is checked only at the top of each iteration, so the rest of the current pass runs before the next test. The loop variable acts like a fresh binding to each element, so assigning to it doesn\u2019t modify the container; to change the list, write to the list by index."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop expression is tested only at the top of each iteration, so the rest of that pass still runs before the next check. The \u201cinstant the variable changes\u201d idea is wrong; `count` must be rechecked at the top. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example walkthrough (from the reading): if temp > 100: print('boiling') else: print('not boiling'). Ask: What happens when temp is 105? (Condition is true \u2192 run the if-branch.) What happens when temp is 99? (Condition is false \u2192 run the else-branch.) Reinforce the quotes: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed\u201d and \u201cAn else statement is used with an if statement and contains a body of statements that is executed when the if statement's condition is false.\u201d"}
{"task": "blend", "mode": null, "source": "That description misses the key structure. Recursion is not mindless repetition; it is a problem solving technique that uses the solution to a simpler version of the problem to solve the bigger problem.", "reference": "This is the course\u2019s core idea: recursion solves a problem by reducing it to a simpler version of the same problem. As the reading puts it, recursion is \u201ca problem solving technique that uses the solution to a simpler version of the problem to solve the bigger problem.\u201d"}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must change something the condition depends on, so a counter loop needs `n = n - 1` or `+= 1` inside the body to make the condition eventually false. The loop expression is checked only at the top of each iteration, but the rest of the current pass still runs before the condition is tested again; Big-O describes growth, not how to fix an infinite loop."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 then walk through two test temps (e.g., temp = 120 and temp = 80) and explicitly connect the chosen branch to whether the condition is true/false. Close by tying it to the anchors: If the condition is true, the body is executed; if the condition is false, the body is not executed."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Classes: define what a class is (blueprint for objects) and what an instance is (a specific object created from the class). Anchor the key behavior/data idea: each instance carries its own copy of attributes, while the methods defined by the class are shared."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "Every while loop must change something the condition depends on; for a counter loop, update n inside the body (like n = n - 1) so the condition eventually becomes false. Separately, in a loop over elements the loop variable is a fresh binding each time, so assigning to it doesn\u2019t edit the list; to change list contents, index into the list directly. The loop expression is tested only at the top of each iteration."}
{"task": "blend", "mode": null, "source": "Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. Runny basaltic magma flows easily like syrup, matching gentle lava flows.", "reference": "Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. This directly links silica to viscosity and eruptive style."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the \u201cblueprint \u2192 object\u201d idea. Quote and connect the anchor lines: \u201cA class is a blueprint for creating objects\u201d and \u201cIn a Python program, a class defines a type of object with attributes (fields) and methods (procedures).\u201d Explain that a class defines a type, and when you call the class you create an instance (object). Emphasize: instances carry their own attribute data, while the methods are the class\u2019s shared behavior."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example: use the provided Dog anchor. \u201cclass Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute.\u201d We\u2019ll walk through: (1) create two instances a and b, (2) set a.name and observe b.name stays unchanged, and (3) connect to debugging/testing by adding an assertion that checks the intended invariant: only the instance you modify should change. This also helps catch the bug where someone mistakenly stores data on the class instead of on instances."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system. Connect to the key idea that row reduction can reveal inconsistent vs consistent once we encode the system as an augmented matrix.", "reference": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system; then note that row reduction can reveal this once we encode the system as an augmented matrix."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example (Dog): Start with the provided code idea: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Explain what \u201cown copy of the attributes\u201d means: if a.name and b.name exist as attributes on the instances, then changing a.name does not change b.name. Emphasize what is shared: the methods defined by the class are shared by every instance."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly, so the list stays [10, 20, 30]. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.", "reference": "Defining a function only stores the code; it doesn\u2019t execute the indented body yet. The body runs later when you call it by name, such as greet()."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "At the top of each iteration, the loop expression is tested again, but the rest of the current pass still runs before that check. Also, the loop variable is just a fresh binding to each element in turn, so assigning to it does not edit the container; to change list contents, index into the list directly."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. Likewise, each comparison needs its own operands joined by and: age > 13 and age < 19, since the bare < 19 has nothing on its left. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "teach", "source": "That mixes up price with cost. The concept says the opportunity cost of a choice is the value of the next best alternative that must be given up, and a decision that looks free still has an opportunity cost if it uses time or resources that could have produced something else. Always ask what the next best use of the time or money was; that foregone value is the real cost.", "reference": "Opportunity cost and trade-offs: define opportunity cost as \u201cthe value of the next best alternative that must be given up.\u201d Walk through the rule: rational decision-makers weigh opportunity cost, not just out-of-pocket price. Use the second anchor logic: even \u201ca decision that looks free still has an opportunity cost if it uses time or resources that could have produced something else.\u201d"}
{"task": "skin", "mode": "teach", "source": "Warm-up + goals for Week 10: file input/output essentials (open \u2192 read/write \u2192 close). Re-anchor the idea that open() returns a file object, and that you either close the file explicitly or use a with statement.", "reference": "Warm-up + goals: today is File Input/Output plus a midterm review. We\u2019ll focus on the exact workflow\u2014open a file, read from it (read/readline/readlines), then close it. Reminder: open() returns a file object you read from or write to."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, like n = n - 1 or += 1, so the test eventually becomes false. The loop expression is checked only at the top of each iteration, not again in the middle."}
{"task": "skin", "mode": "activity", "source": "Scope triage mini-lab: give 3 short code snippets (one with a local variable read outside; one with a global variable read inside; one that returns a computed value so it can be used outside). Students predict outcomes (value vs NameError) and justify using \u201cvariable\u2019s scope is the part of the program where it can be accessed.\u201d", "reference": "Scope debugging mini-lab (pairs): give students short code snippets where a variable is created inside a function, and another attempt reads it outside. Students predict what happens (e.g., NameError vs correct behavior) and then modify by returning the value and capturing it in the caller. Require students to also classify any outside variable as global scope."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "The rows reduce to a contradiction, so there is no solution. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. That is why not every system of equations has a solution. Over the real numbers, once a linear system has two distinct solutions it has infinitely many, because the whole line through the two solutions solves the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method. Pivot positions are determined by echelon structure during elimination, not by the size of the entries."}
{"task": "blend", "mode": null, "source": "A list is written with square brackets [] holding comma-separated values. The other forms are a tuple, a set, or just a comma-separated expression.", "reference": "A list is written with square brackets holding comma-separated values. Parentheses and braces are different forms."}
{"task": "blend", "mode": null, "source": "`read()` reads the entire contents of a file and returns a single string. `readline()` gives one line, `readlines()` gives a list of lines, and `open()` returns a file object, not the contents themselves. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.", "reference": "The `read()` function reads the entire file and returns a single string. The `with open(...)` pattern also handles closing automatically, so `text` won\u2019t require the file to stay open. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the first option is not valid Python.", "reference": "Each comparison needs its own operands, joined with and. The bare < 19 has nothing on its left, so the chained version is not valid Python."}
{"task": "blend", "mode": null, "source": "Strings are immutable; you cannot assign to s[0]. Build a new string instead, e.g. 'H' + s[1:]. Here, the key idea is that changing one character in place is not allowed.", "reference": "Strings are immutable; you cannot assign to `s[0]`. That is why the belief that modifying `s[0] = 'H'` will change a character in place is incorrect. Build a new string instead, e.g. `'H' + s[1:]`."}
{"task": "blend", "mode": null, "source": "Not every case is erasure. The key idea is: \u201cMuch forgetting is encoding failure (never stored) or retrieval failure (blocking, interference) \u2014 the tip-of-the-tongue state proves information can remain stored yet be temporarily unreachable.\u201d That is why a tip-of-the-tongue moment points to blocking, not deletion.", "reference": "Not all forgetting is erased storage. The lesson says, \u201cMuch forgetting is encoding failure (never stored) or retrieval failure (blocking, interference) \u2014 the tip-of-the-tongue state proves information can remain stored yet be temporarily unreachable.\u201d That is the key correction here."}
{"task": "blend", "mode": null, "source": "The passage frames senses as \u201cnatural\u201d powers through which we receive information, and says reading processes words into mental images involving them. That wording matters: the senses are part of understanding, not a decorative metaphor.", "reference": "The corrective is built into the passage\u2019s language: it \u201cframes senses as \u2018natural\u2019 powers through which we receive information\u201d and \u201csays reading processes words into mental images with our five senses\u2014sight, smell, taste, touch, and hearing.\u201d That means senses are involved in reading, not merely metaphorical."}
{"task": "skin", "mode": "activity", "source": "Scope triage mini-lab: give 3 short code snippets (one with a local variable read outside; one with a global variable read inside; one that returns a computed value so it can be used outside). Students predict outcomes (value vs NameError) and justify using \u201cvariable\u2019s scope is the part of the program where it can be accessed.\u201d", "reference": "Scope prediction mini-lab (quick pairs). Students read short code snippets and predict what they can access and what errors appear. Include cases like: variable created inside a function and then referenced outside; and variable created at top level referenced inside. Students write short answers: \u201cNameError?\u201d and \u201caccessible or not?\u201d Tie back to: \u201cA variable created within a function has local scope and only exists within the function\u201d and \u201cA variable created outside of a function has global scope and can be accessed anywhere in the program\u201d."}
{"task": "blend", "mode": null, "source": "A square matrix with nonzero determinant is invertible, and the triangular matrix shown has determinant 6. The nonzero and \u201cevery square matrix\u201d choices are wrong: a square matrix with a zero determinant \u2014 for example one with two equal rows \u2014 is nonzero yet has no inverse.", "reference": "A square matrix with nonzero determinant is invertible, and a triangular matrix has determinant equal to the product of its diagonal entries (here it is 6). So the option claiming \u201cnonzero and every square matrix\u201d is wrong: a square matrix with determinant 0 (e.g., with two equal rows) can be nonzero yet have no inverse."}
{"task": "skin", "mode": "teach", "source": "Vectors as elements of a vector space: start with the standard example R^n, then show how the axioms force closure. Quick check: if you take a subset of R^n, you only get a vector space if (i) the subset contains the zero vector and (ii) scaling and adding stay inside the subset. Use this to anticipate the common trap: \u201ca line not through the origin\u201d fails the closure/zero requirements.", "reference": "Vectors as elements of a vector space: start with the standard example R^n, then show how the axioms force closure. Quick check: if you take a subset of R^n, you only get a vector space if (i) the subset contains the zero vector and (ii) scaling and adding stay inside the subset\u2014so expect the common trap: \u201ca line not through the origin\u201d fails the closure/zero requirements."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "For a `while` loop to end, it must change something the condition depends on, such as `n = n - 1` or `+= 1` inside the body. Also, the loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Walk through the provided worked example explicitly using only what the example says: \u201cclass Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute.\u201d Make the two-instance point concrete: if a.name and b.name can be different, then each instance carries its own copy of the attributes; the methods defined by the class are what can be shared. Use a quick chalk-style reasoning: changing a.name should not change b.name because per-object data lives on each instance."}
{"task": "skin", "mode": "reteach", "source": "Reteach reading core concept using the same worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d. Then explicitly connect it to the position marker idea:\n- First f.read() consumes the file contents and returns a string.\n- A second call to read() at that point returns '' because the position has advanced to the end.\n- To see the file again, you\u2019d need to reopen the file or reset the position.\nAnchor corrective explicitly with the reading/position marker explanation.", "reference": "Reteach one worked example from reading + contrast: start with with open('data.txt') as f: text = f.read(). Then answer: what happens if you immediately do text2 = f.read() right after? Walk through the corrective: reading advances a position marker; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Finish by revisiting the anchor quote about read() returning a string."}
{"task": "skin", "mode": "worked-example", "source": "This opens with the worked example itself: the penny details were not encoded well, so there was nothing to forget. The point is: \u201cWe can't remember something if we never stored it in our memory in the first place.\u201d", "reference": "Work through the penny example step by step. Ask: what would it mean if the person \u2018forgot\u2019 the penny? Then show the correction: most of the detail was never in memory in the first place, so there was nothing to lose. Connect this to the anchor quote: \u2018We can't remember something if we never stored it in our memory in the first place.\u2019"}
{"task": "skin", "mode": "teach", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Blueprint vs instances. Clarify vocabulary: class, object, instance. Then focus on the two key rules: (1) each object built from a class is an instance, and (2) each instance carries its own copy of the attributes while the methods are shared by every instance. Use the provided worked example setup in words: a Dog class defines the blueprint; creating a = Dog() and b = Dog() makes two instances with each instance having its own name attribute."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Importantly, the loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again. Each comparison needs its own operands joined by and: age > 13 and age < 19, because the bare < 19 has nothing on its left. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Not in Python. The first element is at index 0, so lst[0] is the first item and lst[1] is the second. This is the off-by-one error to watch for when reading or writing list code."}
{"task": "skin", "mode": "teach", "source": "Warm start (readiness check): remind students of the defining equation for eigenvectors/eigenvalues: Ax = \u03bbx with x nonzero. Connect to the characteristic polynomial: \u03bb comes from det(A \u2212 \u03bbI) = 0. Emphasize the \u201cnonzero vector\u201d requirement now (we\u2019ll use it immediately in practice later).", "reference": "Warm start (readiness check): remind students of the defining equation for eigenvectors/eigenvalues: Ax = \u03bbx with x nonzero. Connect to the characteristic polynomial: \u03bb comes from det(A \u2212 \u03bbI) = 0, and we\u2019ll use the \u201cnonzero vector\u201d requirement immediately in practice."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example: if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "blend", "mode": null, "source": "People who have given up looking are classified as out of the labor force, not unemployed. That is why \u201crecount the rate including discouraged workers and ask whose situation each definition makes visible\u201d matters: the measured unemployment rate can look lower when discouraged workers are excluded.", "reference": "This is exactly the boundary problem: \u201cdiscouraged workers who are excluded make unemployment look lower than the felt experience of joblessness.\u201d If discouraged workers stop looking, they are \u201cclassified as out of the labor force, not unemployed,\u201d so the official unemployment rate can fall even if true joblessness hasn\u2019t improved."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Classes and objects core model: a class defines a type of object with attributes (data) and methods (behavior). A class is a blueprint; you create instances from it (individual objects are instances). Each instance carries its own copy of attributes, while methods defined by the class are shared by every instance."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example walkthrough: the reading\u2019s Dog example. \u201cclass Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute.\u201d Show what this means: changing a\u2019s name should not change b\u2019s name, because each instance has its own copy of the attributes; the method definitions are what are shared."}
{"task": "blend", "mode": null, "source": "`read()` returns the entire contents as a single string, and the `with` block closes the file automatically. `readline()` gives only one line, and `readlines()` returns a list of lines, not one string. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.", "reference": "The with block opens the file, read() returns the entire contents as one string, and Python closes the file automatically at the end. readline() only gives one line, and read() does not open a file by itself. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Over the real numbers, once a linear system has two distinct solutions, it has infinitely many because the whole line through those solutions satisfies the equations. Also, pivot positions are dictated by the echelon structure during elimination, not by the size of the entries, and reversible row operations preserve the solution set\u2014exactly why elimination is valid. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must make progress toward ending\u2014specifically, the loop body should change the value that the condition tests. For a counter, that usually means updating n inside the loop (like n = n - 1 or n += 1) so the condition eventually becomes false; otherwise you risk an infinite loop. Also note the condition is checked at the top of each iteration, so the rest of the current pass runs before the next check."}
{"task": "blend", "mode": null, "source": "Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.", "reference": "In comparison, use == for equality; a single = is assignment, so a condition that tests whether two values are equal must be written score == 100."}
{"task": "blend", "mode": null, "source": "Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.", "reference": "Equality checks use == (two equals signs); a single = is assignment. So a condition that tests whether score equals 100 must be written as if score == 100: to compare values rather than assign."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop keeps going only if its condition stays true, so you must change something the condition depends on each iteration. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually becomes false. Note the condition is checked only at the top of each iteration, after the current pass runs."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "To keep a while loop from running forever, something the condition depends on must change inside the body, like `n = n - 1` or `+= 1`. The loop expression is checked only at the top of each iteration, so the current pass still finishes before the next test."}
{"task": "blend", "mode": null, "source": "`range(3)` yields exactly three values: 0, 1, 2. That matches the worked example: starting at 0 and stopping before 3 gives three lines, not 1 through 3.", "reference": "range(3) yields 0, 1, 2 \u2014 it starts at 0 and stops before the endpoint, giving exactly three values. The 1 through 3 choice matches the common wrong belief about range, but Python does not include 3 here."}
{"task": "skin", "mode": "worked-example", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling'). Explain exactly when each branch runs using the anchor quote: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed,\u201d then connect the same idea to how and/often decide the condition value before choosing a branch."}
{"task": "blend", "mode": null, "source": "In Python 3 the / operator always returns a float, so 7 / 2 is 3.5. The tempting integer-division belief is exactly what // is for: it gives the whole-number quotient 3.", "reference": "In Python 3 the / operator always returns a float, so 7 / 2 is 3.5. The tempting integer-division idea belongs to //, which gives the whole-number quotient 3."}
{"task": "skin", "mode": "reteach", "source": "Reteach for students who arrived cold: walk the same worked example again with extra emphasis on definitions. We start from span(v1, v2) = {\u03b1v1 + \u03b2v2}. Then we explicitly prove it is a subspace: (1) zero vector is included by choosing \u03b1=\u03b2=0, (2) if u=\u03b1v1+\u03b2v2 and w=\u03b3v1+\u03b4v2 then u+w=(\u03b1+\u03b3)v1+(\u03b2+\u03b4)v2 is still in the span, (3) for scalar k, ku=(k\u03b1)v1+(k\u03b2)v2 stays in the span. Finish by connecting this to why a \u2018line not through the origin\u2019 fails: scaling takes you out and/or it misses the zero vector.", "reference": "Reteach for students who arrived cold: we redo the same worked example with extra emphasis on definitions. We start from span(v1, v2) = {\u007f1 + \u007f2}. Then we explicitly prove it is a subspace: (1) the zero vector is included by choosing \u007f= \u007f=0, (2) if u=\u007f1+\u007f2 and w=\u007f1+\u007f2, then u+w=(\u007f+\u007f)v1+(\u007f+\u007f)v2 is still in the span, (3) for scalar k, ku=k(\u007f1+\u007f2)=(k\u007f)v1+(k\u007f)v2 stays in the span. Finish by connecting this to why a \u201cline not through the origin\u201d fails: scaling takes you out and/or it misses the zero vector."}
{"task": "blend", "mode": null, "source": "According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.", "reference": "The chapter directly challenges the claim \u201cAll prose narratives are novels.\u201d It argues we should distinguish types of prose fiction; \u201cmany earlier prose narratives fit romance conventions rather than the novel\u2019s realism.\u201d So earlier prose fiction does not automatically count as a novel."}
{"task": "blend", "mode": null, "source": "`range(3)` yields exactly three values: 0, 1, 2. That matches the worked example: starting at 0 and stopping before 3 gives three lines, not 1 through 3.", "reference": "`range(3)` yields `0, 1, 2` \u2014 it starts at 0 and stops before the endpoint, giving exactly three values. The \u201c1 through 3\u201d belief misses both the starting point and the stopping rule."}
{"task": "skin", "mode": "reteach", "source": "`with open('data.txt') as f: text = f.read()` reads the whole file into one string and closes it automatically. Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Reteach the same worked example to prevent two common bugs: using read() twice and forgetting to close. We\u2019ll work through: with open('data.txt') as f: text = f.read(). Then discuss what would happen if you tried f.read() again on the same open file: the reading cursor is already at the end, so the second read() returns ''. And we\u2019ll reinforce the safety of the with block: \u201cAlways close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.\u201d"}
{"task": "skin", "mode": "worked-example", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example from Week 3 (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d"}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body immediately."}
{"task": "blend", "mode": null, "source": "'Age: ' + 19 raises a TypeError because Python will not silently turn an int into a string inside a + with a str. Convert the number with str() first, or use formatting: 'Age: ' + str(19) builds 'Age: 19'.", "reference": "A str and an int are not added directly, so 'Age: ' + 19 fails. Convert the number with str() before joining it to text, or use formatting; Python will not silently turn an int into a string inside a + with a str. Verify the exact interpreter version and path used by the IDE and the command line; ensure both run the same Python version and dependencies. Use identical entry points (same script, same working directory, same arguments) and fix environment differences (interpreter selection, installed packages, and current working directory for relative paths)."}
{"task": "blend", "mode": null, "source": "This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.", "reference": "Keys in a dictionary are unique, so assigning to an existing key overwrites the old value instead of adding a second entry. That\u2019s why using a numeric index like you would for a list doesn\u2019t apply to grades['Ava']."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Python\u2019s zero-based indexing means the first element is at index 0, so lst[0] is the first item and lst[1] is the second. To reinforce it with examples, the first character of a string also lives at index 0 (e.g., 'cat'[0] == 'c')."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it doesn\u2019t execute the indented body right away."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword creates a reusable function block, but it doesn\u2019t execute the indented body right away. The body runs only when the function is called by name, such as greet()."}
{"task": "blend", "mode": null, "source": "`a` and `b` are separate instances, so each carries its own copy of the attributes. The tempting class-level idea is the misconception: a class is the blueprint; per-object data lives on each instance, not on the class.", "reference": "`a` and `b` are separate instances created from the class, so each carries its own copy of the attributes. The shared part is the methods, not the attribute values. A class is the blueprint; you create instances from it (e.g. d = Dog()), and per-object data lives on each instance, not on the class."}
{"task": "blend", "mode": null, "source": "`b.name` stays separate. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Because a and b are two separate instances, they keep separate name attributes. Each instance has its own data\u2014changing one Dog\u2019s name won\u2019t change the other\u2019s\u2014while only the class methods are shared, and the class itself is just the blueprint (e.g., created via d = Dog())."}
{"task": "skin", "mode": "teach", "source": "Launch: What a function is for. Read and ground in the quote: \u201cA function is a named, reusable block of code that performs a task when called.\u201d Connect to how the interpreter behaves: definitions with def set up code; execution happens only when you call the function.", "reference": "Warm-up: Today we connect two ideas\u2014(1) what a function actually does, and (2) what \u201cscope\u201d means. Start with the definition: \u201cA function is a named, reusable block of code that performs a task when called.\u201d Then immediately contrast define vs run: \u201cA function is defined using the def keyword.\u201d Emphasize that the body runs only when you call the function by name."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Work through the provided Dog example as the anchor: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Show the key consequence: each instance carries its own copy of the attributes, so changing a\u2019s name does not change b\u2019s name. Use this to reinforce: methods are shared; per-object data is not shared."}
{"task": "skin", "mode": "reteach", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Reteach (walk the same worked example again, step-by-step): revisit the Dog worked example explicitly by name. Start from \u201cclass Dog: defines the blueprint,\u201d then create a = Dog() and b = Dog(). Re-ask: if you change a\u2019s name attribute, what happens to b\u2019s name? Repair: \u201cA class is the blueprint; you create instances from it (e.g. d = Dog()), and per-object data lives on each instance, not on the class.\u201d and \u201cEach instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.\u201d"}
{"task": "blend", "mode": null, "source": "The Iliad centers on wrath, destruction, and death. The Odyssey emphasizes Odysseus\u2019s adaptability, intelligence, and return home to restore family life.", "reference": "The Iliad centers on heroic wrath, destruction, and death, while the Odyssey centers on Odysseus as adaptable, intelligent, and oriented toward homecoming and the restoration of family life."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; for a counter loop, `n = n - 1` or `+= 1` inside the body ensures the condition eventually becomes false. The loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "worked-example", "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "Demonstrate read(), readline(), and readlines() on the same file shape. read() gives the whole file as one string. readline() gives the next single line. readlines() gives a list containing every line. Point out that the return type changes the way the result is used later."}
{"task": "blend", "mode": null, "source": "The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.", "reference": "The loop expression is tested only at the top of each iteration; the rest of the body for that iteration runs before the condition is checked again. Because of this, changes inside the body influence the decision about the next iteration, not the current one."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "With 0-based indexing, lst[0] is the first item and lst[1] is the second. And since Python strings are immutable, \u201cchanging\u201d a string means building a new one (such as with slicing or replace)."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.", "reference": "Defining with `def` only stores the function definition; it doesn\u2019t execute the function body immediately. The body runs later when you call the function (like `greet()`), which is when any `print` statements inside actually happen."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (string + number): Put the anchor text on screen and walk the expression step-by-step: 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added. Emphasize that Python won\u2019t silently convert an int to a string inside + when the other operand is a str.", "reference": "Worked example: building text from numbers. Start from the anchor idea: 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added. Then connect to conversions: explicit conversion with str(); implicit conversion only happens where Python promotes types inside mixed expressions."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A correct counter loop has to update the same variable used in the condition. If the body never changes `n`, then `n > 0` won\u2019t become false and the loop won\u2019t terminate. That\u2019s why you should do something like `n = n - 1` (or `n += 1`) inside the `while` body. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): the Dog class. Show: class Dog: defines the blueprint; then create a = Dog() and b = Dog(). Connect it directly to attributes: each instance has its own name attribute. Clarify with a quick mental trace: if you change a.name, b.name does not change.", "reference": "Worked example: show the \u201cblueprint vs instance\u201d difference with the provided code idea. Use: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances. Have students predict: (a) where does a.name live? (b) where does b.name live? (c) if you change a.name, does b.name change? Tie back to the anchor quotes: \u201cA class is a blueprint for creating objects\u201d and \u201cEach instance carries its own copy of the attributes.\u201d"}
{"task": "blend", "mode": null, "source": "No. A vector is any object that fits into a vector space with addition and scalar multiplication satisfying the axioms, and examples include polynomials, matrices, and functions.", "reference": "A \u201cvector\u201d is anything that satisfies the vector-space axioms, not just a geometric arrow. So polynomials, matrices, and functions can all be vectors as long as they support addition and scalar multiplication satisfying the axioms."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Reteach worked-through example: tracing conditional execution with a real condition and a branch decision. Walk through the reading example: if temp > 100: print('boiling') else: print('not boiling'). Use two traces: (1) temp = 120 \u21d2 condition temp > 100 is True \u21d2 execute the 'boiling' branch only; (2) temp = 80 \u21d2 condition is False \u21d2 execute the else branch only. Reinforce the anchored quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d Then connect back briefly to types: if you need to compare numeric values, be sure you\u2019re using numbers (not text)."}
{"task": "blend", "mode": null, "source": "`b.name` stays separate. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Instance attributes are per-object, so changing `a.name` won\u2019t affect `b.name` even if the methods are shared. The confusion is often between shared methods (class-level) and per-instance data (instance-level). To prevent state mixups, pass values explicitly via parameters/return values, or use a shared object/module attribute with clear ownership and scope if sharing is truly required. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python lists are zero-based, the first element is at index 0. So lst[0] is the first item and lst[1] is the second, not position 1 being the first item."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "A while loop must change something the condition depends on; for a counter loop, n = n - 1 or += 1 belongs inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration, while the loop variable in a for-style pass is just a fresh binding and assigning to it does not edit the container, so change list contents by indexing the list directly."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the first option is not valid Python.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the chained version is not valid Python."}
{"task": "blend", "mode": null, "source": "`read()` returns the entire contents as a single string, and the `with` block closes the file automatically. `readline()` gives only one line, and `readlines()` returns a list of lines, not one string. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.", "reference": "The `with` block opens the file, `read()` gets the entire contents as one string, and Python closes the file automatically at the end. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Lists in Python use zero-based indexing, so the first element is at index 0 and lst[1] is the second element. That means lst[0] gives the first item, not \u201cposition 1.\u201d"}
{"task": "skin", "mode": "teach", "source": "Progress and infinite loops: tie correctness to termination. Use the kernel fact: a loop whose condition never becomes false runs forever\u2014an infinite loop\u2014so the body must make progress toward ending the condition. Contrast two fragments conceptually: one that changes nothing relevant vs. one that updates the variable used in the condition.", "reference": "Progress and infinite loops\u2014tying correctness to termination. Use the kernel fact: a loop whose condition never becomes false runs forever\u2014an infinite loop\u2014so the body must make progress toward ending the condition. Contrast two fragments conceptually: one that changes nothing relevant vs. one that updates the variable used in the condition."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system. Connect to the key idea that row reduction can reveal inconsistent vs consistent once we encode the system as an augmented matrix.", "reference": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system, and connect this to how row reduction reveals inconsistent vs consistent once we encode the system as an augmented matrix."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "`read()` reads the entire contents of a file and returns a single string. By contrast, `readline()` returns the next single line, and `readlines()` returns a list with every line of the file."}
{"task": "blend", "mode": null, "source": "This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.", "reference": "In a dictionary, grades['Ava'] = 97 updates the value stored for the existing key 'Ava'. Dictionaries don\u2019t use numeric positions like lists, and duplicate keys aren\u2019t allowed\u2014assigning the same key again replaces the old value. Keys are unique, so overwriting is expected."}
{"task": "blend", "mode": null, "source": "Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.", "reference": "Each comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so the condition is incomplete."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Once a linear system over the real numbers has two distinct solutions, it must have infinitely many: every point on the line through those two solutions also solves the system. So expecting only a finite number of solutions is mistaken. By contrast, equations like x + y = 1 and x + y = 3 are inconsistent because they represent parallel lines that never intersect. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly instead of rebinding `n`. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "A square matrix is invertible exactly when its determinant is nonzero, and -7 is nonzero. The sign does not block invertibility; only a zero determinant does. The determinant measures invertibility and volume scaling, not entry size, so a matrix of large numbers can have determinant zero.", "reference": "A square matrix is invertible exactly when its determinant is nonzero. Since \u22127 \u2260 0, that sign doesn\u2019t prevent invertibility; only a determinant of 0 would. Recall that the determinant reflects invertibility and volume scaling, not how large the entries look."}
{"task": "blend", "mode": null, "source": "Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.", "reference": "Each comparison needs its own operands joined by and, like age > 13 and age < 19. A standalone \u201c< 19\u201d is incomplete because it has nothing on its left to compare against."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop expression is tested only at the top of each `while` iteration, meaning the current pass completes before the condition is re-checked. A counter loop has to change something the condition depends on inside the body (e.g., `n = n - 1` or `n += 1`) so the condition can become false. Also, the loop variable binding doesn\u2019t modify the container; change list contents by indexing, and for local variables you must return values to use them outside the function. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "blend", "mode": null, "source": "Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.", "reference": "Comparison uses == (two equals signs); a single = is assignment. So only if score == 100: properly asks whether score is equal to 100."}
{"task": "blend", "mode": null, "source": "Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.", "reference": "The file object keeps track of where reading left off. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. If you need the contents again, reopen the file or reset the position before reading."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "`read()` reads the entire contents of a file and returns a single string. The list-of-lines result belongs to `readlines()`, while `readline()` gives just one line."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example (Week 3): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "blend", "mode": null, "source": "Not quite\u2014weathering breaks rock down in place; erosion is the separate step that carries the broken material away. A rock can weather without eroding if nothing transports the pieces, so the two are related but not synonyms.", "reference": "Weathering vs erosion must be separated: weathering breaks rock down in place; erosion is the separate step that carries the broken material away. A rock can weather without eroding if nothing transports the pieces. This directly confronts the misconception \u201cStudents use weathering and erosion as synonyms.\u201d"}
{"task": "blend", "mode": null, "source": "Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.", "reference": "read() consumes data from the file starting at the current position, so after the first read reaches the end, the next read returns an empty string. A second read isn\u2019t repeatable unless you reset or reopen the file. This happens because reading advances the file pointer through the file."}
{"task": "blend", "mode": null, "source": "No. The (i,j) entry of AB is the dot product of row i of A with column j of B, which is different from an entrywise product.", "reference": "The (i, j) entry of AB comes from the dot product of row i of A with column j of B. It is not entrywise, and order matters because AB and BA can differ or one may not be defined."}
{"task": "blend", "mode": null, "source": "Least squares finds the best approximate solution when Ax=b has no exact solution. It minimizes the sum of squared vertical gaps, and the line can miss every single data point.", "reference": "The least-squares line minimizes the residual (equivalently the sum of squared vertical gaps), so it may not pass through every data point. Being \u201cclosest\u201d does not mean equality, so the fitted values Ax\u0302 generally differ from b."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left. Repeating `age` makes both parts complete.", "reference": "Use and to join the two operands in a comparison, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so each comparison must have its own operands."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (name it explicitly): the example is `with open('data.txt') as f: text = f.read()`.\n- Show the code line-by-line: with open(...) opens the file, assigns the file object to f.\n- f.read() reads the entire contents into `text` as one string.\n- The with block closes the file automatically when the block ends (even if an error occurs).\nConnect to the anchor quotes: \u201cThe read() function reads the contents of a file and returns a string\u201d and \u201cPython supports the opening of a file using the open() function\u201d.", "reference": "Review file-read patterns using the anchor example: with open('data.txt') as f: text = f.read() opens the file, reads all of it into text, and closes it automatically. Connect this to what read() returns (a single string) and how readlines() would differ (a list of lines). Use the quote: \u201cPython supports the opening of a file using the open() function\u201d and \u201cThe read() function reads the contents of a file and returns a string.\u201d"}
{"task": "blend", "mode": null, "source": "Each comparison must be written with its own operands joined by `and`\u2014for example, `age > 13 and age < 19`. Leaving a fragment like bare `< 19` doesn\u2019t make sense because it has nothing on its left. Finally, in Python 3 `/` always produces floats (`7 / 2` is 3.5), so use `//` for whole-number floor division.", "reference": "In Python 3, the / operator always produces a float, so 7 / 2 equals 3.5. For whole-number results, use // for floor division, which gives 3 in this case. Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (name it explicitly): the example is `with open('data.txt') as f: text = f.read()`.\n- Show the code line-by-line: with open(...) opens the file, assigns the file object to f.\n- f.read() reads the entire contents into `text` as one string.\n- The with block closes the file automatically when the block ends (even if an error occurs).\nConnect to the anchor quotes: \u201cThe read() function reads the contents of a file and returns a string\u201d and \u201cPython supports the opening of a file using the open() function\u201d.", "reference": "Worked example walkthrough (use exactly this): with open('data.txt') as f: text = f.read(). Point out the three key effects: (1) the open() creates a file object, (2) read() reads the entire contents and returns a single string, and (3) the with block closes it automatically even if something goes wrong. Tie to the anchor quote: \u201cThe read() function reads the contents of a file and returns a string\u201d. Then contrast with reading line-by-line conceptually (readline/readlines) at a high level."}
{"task": "blend", "mode": null, "source": "Dictionaries use keys, and assigning to an existing key overwrites the previous value. They cannot hold duplicate keys, so the old `95` is replaced rather than preserved. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Assigning to an existing key replaces the old value; dictionaries do not keep duplicate keys. The first option sounds plausible, but keys are unique, so 95 is overwritten by 97. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0]."}
{"task": "skin", "mode": "reteach", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Reteach one worked example through the most common slip: object data sharing. Return to the Dog anchor: if you create a = Dog() and b = Dog(), changing a\u2019s name should not affect b\u2019s name. Then contrast with what is shared (methods) vs what is not (each instance\u2019s attributes). Close by linking it back to the class-as-blueprint quote: \u201cA class is a blueprint for creating objects.\u201d"}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "Each recursive call must move toward the base case by shrinking the problem, for example with n - 1. If the problem does not shrink, the base case is never reached. A class is the blueprint; you create instances from it (e.g. d = Dog()), and per-object data lives on each instance, not on the class. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared."}
{"task": "blend", "mode": null, "source": "Without a base case, the self-calls never stop. That is exactly why recursion must have a simplest input solved directly without another call. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.", "reference": "Recursion only works when each call shrinks the problem, such as using n - 1, so it moves toward the base case. A base case is the simplest input solved directly without another call; otherwise the chain of self-calls never stops."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Row reduction reflects the same contradiction as the original equations: x + y = 1 and x + y = 3 are parallel lines with no intersection, so the system is inconsistent. Meanwhile, if there are two distinct real solutions, there are infinitely many because the whole line through them satisfies the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (name it explicitly): the example is `with open('data.txt') as f: text = f.read()`.\n- Show the code line-by-line: with open(...) opens the file, assigns the file object to f.\n- f.read() reads the entire contents into `text` as one string.\n- The with block closes the file automatically when the block ends (even if an error occurs).\nConnect to the anchor quotes: \u201cThe read() function reads the contents of a file and returns a string\u201d and \u201cPython supports the opening of a file using the open() function\u201d.", "reference": "Worked example walkthrough (name it exactly): \u201cwith open('data.txt') as f: text = f.read()\u201d. We\u2019ll explain what the code does step-by-step: opening returns a file object, f.read() reads the entire contents into one string, and the with statement automatically closes the file even if something goes wrong. Then we connect this to what read() returns (a single string) and how readlines() differs (a list of lines, in order)."}
{"task": "blend", "mode": null, "source": "Orthogonality is defined by the inner product being zero, not by how the vectors look in a picture. For nonzero vectors, that also gives linear independence. The \u201cperpendicular lines in a picture\u201d idea is too narrow; it misses higher-dimensional and non-graph settings.", "reference": "Orthogonality is defined algebraically via the inner product being zero, not via a picture of \u201cperpendicular lines.\u201d This works equally well for functions and high-dimensional vectors. For nonzero vectors, inner-product orthogonality also implies linear independence."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "That loop prints 3, 2, 1 and stops because n changes inside the body, so the condition eventually becomes false. The \u201ccondition stays true forever\u201d mistake is what causes an infinite loop when the variable is never updated. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "`split(',')` breaks the string at each comma and returns a list of substrings. That is the right tool when you need pieces from a delimited string.", "reference": "`split(',')` breaks the string at each comma and returns a list of substrings. `join()` goes the other direction, and assigning to `text[0]` is the wrong belief that strings can be updated in place."}
{"task": "blend", "mode": null, "source": "Yes. A square matrix with determinant 0 can be nonzero yet have no inverse, such as a matrix with two equal rows.", "reference": "A square matrix with a zero determinant has no inverse, even when it is nonzero. The wrong belief says every nonzero matrix has an inverse, but a square matrix with a zero determinant \u2014 for example one with two equal rows \u2014 is nonzero yet has no inverse."}
{"task": "blend", "mode": null, "source": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container itself. To change list contents, index into the list directly, rather than trying to mutate via the loop variable. So nums remains [10, 20, 30].", "reference": "In a `for n in nums` loop, `n` is just a temporary binding to each element, so `n = n + 1` won\u2019t change the `nums` list. To actually update the list, index into it and assign to the element. Also, every while loop must change something its condition depends on, so a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before it\u2019s checked again. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "teach", "source": "Which operator always returns a float for division?\n/\n//\n==\n=\nTrue division uses / and always returns a float. Floor division // gives the whole-number quotient instead.", "reference": "Division operators and integer vs float results: explain that True division (/) converts numbers to floats before dividing and always returns a float, while Floor division (//) computes the whole-number quotient, discarding the remainder. Connect to the worked mental check: predict the type (float vs int) before you run it."}
{"task": "skin", "mode": "reteach", "source": "Reteach the worked example for students who arrived cold: revisit the inconsistency worked example 'x + y = 1 and x + y = 3'. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using course vocabulary: the system has zero solutions, so it is inconsistent. Then link forward: when we translate systems into augmented matrices and row-reduce, the reduced echelon form (or a contradiction row) is what tells us we\u2019re in this 'inconsistent' case.", "reference": "Reteach the worked example for students who arrived cold: revisit the inconsistency example 'x + y = 1 and x + y = 3'. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using course vocabulary: the system has zero solutions, so it is inconsistent. Then link forward: when we translate systems into augmented matrices and row-reduce, the reduced echelon form (or a contradiction row) is what signals this 'inconsistent' case."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (functions): use the provided example def square(n): return n * n, then connect to calls: square(5) runs the body and returns 25. Reinforce: defining stores the function; calling produces the output. Mention what happens if you try to use an internal variable name outside\u2014scope prevents access.", "reference": "Worked example with the anchor snippet: def square(n): return n * n. Emphasize the two actions\u2014(1) defining the function stores it, (2) calling square(5) runs the body and returns 25. Then connect to scope: the parameter n and any inside variable exist during the call; outside you use the returned value."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because lists use zero-based indexing, the first element is at index 0\u2014so lst[0] is the first item and lst[1] is the second. Python doesn\u2019t treat position 1 as the first element for lists."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (string + number): Put the anchor text on screen and walk the expression step-by-step: 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added. Emphasize that Python won\u2019t silently convert an int to a string inside + when the other operand is a str.", "reference": "Worked example: the classic string+number rule. Use the given anchor: \"'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added.\" Emphasize: the addition operator + requires compatible types, so we convert with str()."}
{"task": "blend", "mode": null, "source": "Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Dictionaries are accessed by key, not by position; use d['name'] to retrieve the value stored under the key 'name', not d[0]. Thinking dictionaries work like lists leads to the wrong lookup."}
{"task": "skin", "mode": "teach", "source": "Warm start (readiness check): remind students of the defining equation for eigenvectors/eigenvalues: Ax = \u03bbx with x nonzero. Connect to the characteristic polynomial: \u03bb comes from det(A \u2212 \u03bbI) = 0. Emphasize the \u201cnonzero vector\u201d requirement now (we\u2019ll use it immediately in practice later).", "reference": "Warm start (readiness check): remind students of the defining equation for eigenvectors/eigenvalues: Ax = \u03bbx with x nonzero. Then connect \u03bb to the characteristic polynomial via det(A \u2212 \u03bbI) = 0. Emphasize the \u201cnonzero vector\u201d requirement now, since we\u2019ll use it immediately."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): the Dog class. Show: class Dog: defines the blueprint; then create a = Dog() and b = Dog(). Connect it directly to attributes: each instance has its own name attribute. Clarify with a quick mental trace: if you change a.name, b.name does not change.", "reference": "Worked example: Dog. Show the idea from the reading\u2019s example: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Then do a quick trace: set a.name to 'Rex' and b.name to 'Milo' and point out that changing one instance\u2019s attributes does not change the other\u2019s; methods are shared."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "Correct. This directly addresses the misconception: \u201cThe loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.\u201d So if the body changes n, the change won\u2019t prevent the *current* pass from finishing\u2014it affects the decision for the *next* iteration. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): the Dog class. Show: class Dog: defines the blueprint; then create a = Dog() and b = Dog(). Connect it directly to attributes: each instance has its own name attribute. Clarify with a quick mental trace: if you change a.name, b.name does not change.", "reference": "Worked example with the provided anchor setup: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Walk through the transfer: if a.name changes, b.name should not change, because \u201cEach instance carries its own copy of the attributes.\u201d"}
{"task": "blend", "mode": null, "source": "Comparison uses == (two equals signs); a single = is assignment. A condition that asks whether two values are equal must be written score == 100.", "reference": "Use == for comparison, since a single = is assignment. A condition that tests whether two values are equal must be written score == 100."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must change something the condition depends on, so a counter loop needs n = n - 1 (or += 1) inside the body until the condition eventually becomes false. The loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.", "reference": "Defining a function only stores it; nothing in the function body runs yet. The code inside executes only when you call it by name, such as greet()."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must update something the condition depends on; for a counter, you typically do n = n - 1 (or += 1) inside the body so the condition eventually becomes false. Remember the loop expression is checked only at the top of each iteration, so the rest of the current pass still runs before the condition is evaluated again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from today\u2019s reading): use a matrix A and form the augmented system Ax=b. Do row reduction to decide whether b is consistent; then match the consistent b-values with the column space (span of columns). Finally, set b=0 to describe the null space as the solution set of Ax=0, and interpret rank as the dimension of the column space.", "reference": "Worked example (week 9, reading-based): start with a matrix A and form the augmented system Ax=b. Do row reduction to decide whether b is consistent; then match the consistent b-values with the column space (span of columns). Finally, set b=0 to describe the null space as the solution set of Ax=0, and interpret rank as the dimension of the column space."}
{"task": "blend", "mode": null, "source": "The loop does not interrupt the body halfway through. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The loop expression is tested only at the top of each iteration, and the rest of the current pass runs before the condition is checked again. That\u2019s why you can\u2019t expect the loop to stop immediately mid-body. Design the loop so the condition will be different on the next iteration."}
{"task": "blend", "mode": null, "source": "This opens with the worked example itself: the penny details were not encoded well, so there was nothing to forget. The point is: \u201cWe can't remember something if we never stored it in our memory in the first place.\u201d", "reference": "This penny example is the clearest encoding case. The lesson says, \u201cWe can't remember something if we never stored it in our memory in the first place,\u201d and the penny details were never encoded, so there was nothing to forget."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Open with the core story: a class defines a type of object with attributes (data) and methods (behavior). Emphasize the key vocabulary: class = blueprint; object built from it = instance. Anchor with the quotes: \u201cA class is a blueprint for creating objects\u201d and \u201cIndividual objects created of the class type are called instances.\u201d Explain the worked example header: class Dog defines the blueprint; a = Dog() and b = Dog() create two instances."}
{"task": "skin", "mode": "worked-example", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example (from the reading):\nif temp > 100: print('boiling') else: print('not boiling')\nDiscuss execution: only one branch runs. Then connect boolean logic: temp > 100 by itself is a single condition; later we\u2019ll combine conditions with and/or and invert with not. Use the quote \u201cWhen an if-else statement is executed, one and only one of the branches is taken\u201d to emphasize mutual exclusivity."}
{"task": "skin", "mode": "reteach", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example walkthrough (classes/objects): the Dog example. We use: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Step through: (1) the class sets up the type, (2) each instance gets its own copy of attributes, (3) methods are shared across instances. Then do a quick \u201cwhat changes if I update a?\u201d check: changing a\u2019s name should not change b\u2019s name\u2014only the method code is shared."}
{"task": "blend", "mode": null, "source": "Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.", "reference": "Write each comparison with its own operands and connect them with and, like age > 13 and age < 19. A standalone fragment like < 19 has nothing on its left, so it isn\u2019t a complete comparison."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop condition is evaluated only at the top of each iteration, after the body has already run for the current pass. The loop variable is a fresh binding to each element, so assigning to it doesn\u2019t modify the container. To change list contents, index into the list directly."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Python lists are zero-based, so the first element is at index 0: lst[0] is the first item and lst[1] is the second."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "The base case stops the self-calls, and n - 1 makes each call smaller so the problem moves toward that stopping point. Same-size self-calls never reach the base case, which is the mistake that causes recursion to run forever. Recommend iterative development: implement small pieces, test them immediately with simple and edge-case inputs, and then integrate; explain that early testing reduces debugging time and helps isolate failures to specific changes. Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "In a zero-based list, the first element is at index 0, so lst[0] is the first item and lst[1] is the second. It\u2019s not \u201cposition 1\u201d for the first element."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; for a counter loop, `n = n - 1` or `+= 1` belongs inside the body so the condition eventually becomes false. Also, the loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "In Python 3 the / operator always returns a float, so 7 / 2 is 3.5. The tempting integer-division belief is exactly what // is for: it gives the whole-number quotient 3.", "reference": "In Python 3 the / operator always returns a float, so 7 / 2 is 3.5. The tempting \u201c3, because the operands look like whole numbers\u201d belief belongs to //, not /."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet, so there is no solution. Over the reals, having two distinct solutions would instead force infinitely many, since every point on the line through them solves the system. Finally, each elementary row operation is reversible and preserves the solution set, which is why Gaussian elimination works."}
{"task": "skin", "mode": "teach", "source": "A sample contains mineral grains. A geologist reports a numerical age in years for those grains. Which type of dating does this describe?\nRelative dating\nAbsolute (numerical) dating\nOriginal horizontality\nSuperposition\nThis is absolute (numerical) dating because \"Absolute dating assigns specific time units, in this case years, to mineral grains within a rock.\" Relative dating only answers older vs younger, without knowing their specific ages.", "reference": "Geologic time + the two dating ideas. Anchor language: \"Relative dating is the process of determining if one rock or geologic event is older or younger than another, without knowing their specific ages\" and \"Absolute dating assigns specific time units, in this case years, to mineral grains within a rock.\" Stress what students must be able to distinguish in questions that ask for order vs a numerical age."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example (from the reading): class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Walk through what gets created: (1) Dog is the blueprint, (2) a and b are two separate instances, (3) each instance carries its own copy of attributes, (4) methods defined by the class are shared by every instance. Do a quick \u201cthought experiment\u201d: if a.name changes, b.name should not change."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Walk a worked example from the reading: if temp > 100: print('boiling') else: print('not boiling'). Ask students to test two cases verbally: when temp is 120, the condition is true so the if-body runs; when temp is 80, the condition is false so the else branch runs. Tie back to the quote: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "skin", "mode": "teach", "source": "A while loop runs its body as long as the loop expression stays true, and the condition is re-checked only at the top of each iteration. If the loop body never updates the variable (or state) that the loop expression depends on, the condition may never become false, which creates an infinite loop.", "reference": "Start with the big idea: a while loop keeps going only while its condition is true, and the condition is checked again at the top of each iteration. Use the anchor quote: \u201cA while loop is a code construct that runs a set of statements, known as the loop body, while a given condition, known as the loop expression, is true.\u201d Point out that infinite loops happen when the body never makes progress toward ending the condition."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python lists are zero-based, the first element is at index 0. So lst[0] is the first item and lst[1] is the second, not \u201cposition 1 means the first item.\u201d"}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "For linear systems over the reals, there are only zero, one, or infinitely many solutions, so \u201cexactly two\u201d or any other finite number cannot happen. For instance, x + y = 1 and x + y = 3 is inconsistent because the equations are parallel lines that never meet, and if a system ever has two distinct solutions, the whole line through them solves it. That is also why each elementary row operation is reversible and preserves the solution set, making elimination valid."}
{"task": "skin", "mode": "reteach", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Reteach worked example (non-reader path) using the same reading example: `if temp > 100: print('boiling') else: print('not boiling')`. Students answer: What prints when `temp = 101`? What prints when `temp = 100`? Then they explain which branch is taken and why, using the phrase \u201cthe body is executed only when the condition is true.\u201d End with a mini warning: `=` is assignment, so conditions that ask equality must use `==`."}
{"task": "blend", "mode": null, "source": "Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Always close the file when you are done. A with block does that for you automatically, even if an error occurs."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "In Python, lists (and other sequences) use 0-based indexing, so lst[0] is the first item and lst[1] is the second. For example, in 'cat', the first character is 'cat'[0] == 'c', not position 1."}
{"task": "blend", "mode": null, "source": "The issue is that each recursive call must move toward the base case by shrinking the problem (e.g. n - 1). As the corrective states: \u201cEach recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.\u201d", "reference": "You confront the misconception directly: \u201cEach recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.\u201d If the recursive call uses the same n, the process can repeat forever and never solve the simpler version."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "A recursive call must shrink the problem, such as `n - 1`, so the base case becomes reachable. Calling the same-size input again leaves the problem unchanged, so the chain of self-calls does not move toward the base case. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared. Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops."}
{"task": "blend", "mode": null, "source": "According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.", "reference": "The key correction is that the chapter does not collapse all prose fiction into one bucket. It says we should distinguish types of prose fiction; many earlier prose narratives fit romance conventions rather than the novel\u2019s realism."}
{"task": "skin", "mode": "reteach", "source": "Reteach for anyone who arrived cold: re-walk the exact same worked example trace `n = 3; while n > 0: print(n); n = n - 1` . Ask: (1) What is the loop expression? (2) When is it evaluated\u2014before the body or during the body? (3) What line makes progress? Then rewrite the loop with an intentional error (remove `n = n - 1`) and have students explain why the condition never becomes false (infinite loop).", "reference": "Reteach for cold arrivals using the same worked example: revisit n = 3; while n > 0: print(n); n = n - 1. This time, explicitly answer two questions before tracing: (A) What exactly is the loop expression, and when is it evaluated? (B) What statement ensures the condition eventually becomes false? Repeat the trace quickly (3, 2, 1, then stop) and close with the key line: \u201cAt each iteration... the loop expression is evaluated again,\u201d followed by \u201cthe loop body must make progress toward ending the condition.\u201d"}
{"task": "skin", "mode": "teach", "source": "Progress and termination: every while loop must make progress toward ending the condition, otherwise it can run forever (infinite loop). Introduce a simple mental checklist: \u201cWhat variable(s) does the condition depend on? Where in the loop body do those change?\u201d Tie directly to the reading\u2019s anchor: \u201cthe loop body must make progress toward ending the condition.\u201d", "reference": "Progress and termination: every while loop must make progress toward ending the condition, otherwise it can run forever (infinite loop). In this Week 14 introduction to algorithms, use a simple mental checklist: \u201cWhat variable(s) does the condition depend on? Where in the loop body do those change?\u201d Tie directly to our anchor: \u201cthe loop body must make progress toward ending the condition.\u201d"}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python lists are zero-based, the first element is at index 0, the second at index 1, and so on. Therefore lst[0] is the first item and lst[1] is the second."}
{"task": "blend", "mode": null, "source": "This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.", "reference": "In a dictionary, the value is looked up by the key. Assigning grades['Ava'] = 97 overwrites the previous value 95 for the key 'Ava', so it becomes 97. Keys are unique; assigning to an existing key overwrites its previous value rather than adding a second entry."}
{"task": "skin", "mode": "teach", "source": "The law of supply says higher price means higher quantity supplied, so the curve slopes upward. The phrase to keep in mind is that 'a higher price raises the quantity that producers are willing to supply.'", "reference": "Launch: what a supply curve represents (quantity supplied as a flow per period, not a fixed stock). State the law of supply: a higher price raises the quantity producers are willing to supply, so the supply curve slopes upward. Connect \u201cprice \u2192 willing to supply\u201d to an upward-sloping curve."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword defines the function, but it doesn\u2019t immediately execute the indented body. The body runs only when the function is called by name, like greet()."}
{"task": "blend", "mode": null, "source": "Indexes begin at 0, so the third item is at index 2. `names[3]` is out of range, and list items are not accessed by key the way dictionary values are. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0]. Indexes run from 0 to length minus 1, so a 5-item list's last index is 4; lst[5] is out of range.", "reference": "Dictionaries are accessed by key, not position; use `d['name']` to get the value stored under the key `'name'`, not `d[0]`. With lists, indexes run from 0 to length minus 1, so a 5-item list's last index is 4; lst[5] is out of range. The first element is at index 0, so lst[0] is the first item and lst[1] is the second."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword creates a reusable block, but it doesn\u2019t execute the indented body right away. That body runs only when you call the function by name, such as greet()."}
{"task": "blend", "mode": null, "source": "Every recursion needs a base case\u2014a simplest input solved directly without another call\u2014so the chain of self-calls eventually stops. A recursive function should call itself on a smaller input until it reaches that stop point. Without that shrinking toward a base case, the function can\u2019t terminate properly.", "reference": "A recursive function must have a base case that stops the self-calls. Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops."}
{"task": "blend", "mode": null, "source": "Magma is molten rock below the surface; once it erupts onto the surface it is called lava, and the two cool at very different rates. Keep the location in mind first, because that is what separates the terms.", "reference": "Magma is molten rock below the surface; once it erupts onto the surface it is called lava, and the two cool at very different rates. So the correct distinction is molten below the surface (magma) vs molten on the surface after eruption (lava)."}
{"task": "skin", "mode": "worked-example", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Work through the given example: if temp > 100: print('boiling') else: print('not boiling'). Ask: what happens when temp is 101? What happens when temp is 100? What happens when temp is 99? Use the anchor idea that one and only one branch is taken. Then connect this to the meaning of else: it runs when the if condition is false."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the first option is not valid Python.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so Python cannot read it as a complete comparison."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "If the loop body never changes a variable the condition depends on, the condition stays true and the loop runs forever. The condition is not re-checked in the middle of the body; it is checked again only at the top of the next iteration. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "The passage frames senses as \u201cnatural\u201d powers through which we receive information, and says reading processes words into mental images involving them. That wording matters: the senses are part of understanding, not a decorative metaphor.", "reference": "The passage frames senses as \u201cnatural\u201d powers through which we receive information, and says reading processes words into mental images involving them. So the senses are not treated as empty figures of speech; they are central to how reading works here."}
{"task": "blend", "mode": null, "source": "Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Don\u2019t treat dictionaries like lists: you can\u2019t use numeric positions such as `d[0]` to get values. Instead, access by key\u2014e.g., `d['name']` returns the value stored under the key `'name'`."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs `n = n - 1` (or += 1) inside the body so the condition eventually becomes false. In this loop, decreasing `n` each pass makes the stopping test turn false."}
{"task": "skin", "mode": "worked-example", "source": "Which code correctly builds the string \"Age: 19\"?\n'Age: ' + 19\n'Age: ' + str(19)\nstr('Age: ') + int('19')\n19 + 'Age: '\nConvert the number with `str()` before joining it to text, or use formatting. Python will not silently turn an int into a string inside a `+` with a `str`, so `'Age: ' + 19` raises a type error.", "reference": "Hands-on walkthrough of types inside expressions using the worked string example: 'Age: ' + str(19) builds 'Age: 19', while 'Age: ' + 19 raises a TypeError because a str and an int cannot be added. Connect this to explicit type conversion: \u201cexplicit type conversion to convert one data type to another.\u201d Then connect to implicit type conversion: \u201cThe Python interpreter uses implicit type conversion to automatically convert one data type to another,\u201d but point out that Python will not silently make a string for you in 'Age: ' + 19."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Classes and objects (c-19): Start with the anchor idea\u2014\u201cA class is a blueprint for creating objects.\u201d Define: a class provides attributes (data) and methods (behavior). Then distinguish: an object created from the class is an instance. Emphasize what is unique per object (its own attributes) vs what is shared (the class-defined methods)."}
{"task": "skin", "mode": "worked-example", "source": "Worked example: determinant of a product. Start with \u201cdet(AB) = det(A)det(B)\u201d and walk through the full computation workflow: (a) compute det(A); (b) compute det(B); (c) multiply the results to get det(AB). Then interpret: if det(A)=0 or det(B)=0, det(AB)=0 so AB is not invertible. Finish by checking consistency with the invertibility rule: a matrix is invertible iff its determinant is nonzero.", "reference": "Worked example: determinant of a product. Start with \u201cdet(AB) = det(A)det(B)\u201d and walk through the workflow: (a) compute det(A); (b) compute det(B); (c) multiply to get det(AB). Then interpret: if det(A)=0 or det(B)=0, det(AB)=0 so AB is not invertible. Finish by matching the invertibility rule: a matrix is invertible iff its determinant is nonzero."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "In a list, the first element is at index 0, the second at index 1, and so on. So lst[0] is the first item and lst[1] is the second. Python\u2019s indexing is 0-based for sequences, not \u201c1-based position for the first item\u201d (e.g., 'cat'[0] == 'c')."}
{"task": "skin", "mode": "teach", "source": "This is absolute (numerical) dating because \"Absolute dating assigns specific time units, in this case years, to mineral grains within a rock.\" Relative dating only answers older vs younger, without knowing their specific ages.", "reference": "Teach relative dating directly: it orders events as older or younger. Contrast that with absolute dating, which assigns numerical ages in years to mineral grains within a rock. Emphasize that relative dating does not give a number, only an order."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "readlines() returns a list with every line of the file, which matches the goal of storing all lines for later looping. If you only wanted the whole file as one string, read() would fit instead."}
{"task": "blend", "mode": null, "source": "The text notes the Odyssey is only a continuation in the loosest sense. Its heart lies beyond the war narrative, in homecoming, adaptability, and wisdom.", "reference": "Corrective: The text notes it is a continuation only in \u201cthe loosest sense,\u201d and that the poem\u2019s heart lies beyond the war narrative. The Odyssey\u2019s focus shifts to individuals and to homecoming/adaptability rather than the ongoing Trojan War plot."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "For a `while` loop to end, it must change something the condition depends on, so a counter loop needs `n = n - 1` (or `+= 1`) inside the body. The condition is tested only at the top of each iteration, and the rest of the current pass still runs before it is checked again."}
{"task": "skin", "mode": "activity", "source": "Nested data starter: students sketch a structure where a dictionary holds lists (or lists hold dictionaries), then write 2-3 lookups. Example target structure: roster = {'Ava': ['CS', 'Math'], 'Ben': ['Bio']} and then do roster['Ava'][0] style retrieval (emphasize that each step uses the correct rule: key access in dictionaries, index access in lists).", "reference": "To start our Week 9 nested data, have students sketch a structure where a dictionary holds lists (or lists hold dictionaries), then write 2-3 lookups. Example target structure: roster = {'Ava': ['CS', 'Math'], 'Ben': ['Bio']} and then do roster['Ava'][0] style retrieval (emphasize that each step uses the correct rule: key access in dictionaries, index access in lists)."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. Reassigning the loop variable inside a for loop does not change the underlying list it came from. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The condition is tested at the top of each iteration, then the whole body runs before the next check. That is why the loop ends after `n` reaches 0; the \u201cinstant re-check\u201d idea is wrong here. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword creates a reusable function block, but it doesn\u2019t run the code in the body immediately. The indented statements execute only when you later call the function by name, like greet()."}
{"task": "skin", "mode": "activity", "source": "Timing check: In a while loop, when is the loop expression evaluated relative to the body\u2019s statements?\nOnly after the body finishes running for that iteration\nOnly in the middle of the body immediately when a variable changes\nOnly before the body runs for each iteration (at the top)\nNever; it runs once at the start and then ignores it\nThe loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Condition-timing check: show a snippet where the update happens inside the body and ask students to choose which line is \u201cwhen the loop expression is tested.\u201d Students then rewrite the same snippet with comments marking: top-of-iteration test vs execution of the rest of the pass. Close by stating: \u201cThe loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.\u201d"}
{"task": "blend", "mode": null, "source": "Eruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions. Runny basaltic magma flows easily like syrup, matching gentle lava flows.", "reference": "\u201cEruptive style depends on viscosity: low-silica basaltic volcanoes ooze lava gently, while only high-silica, high-viscosity volcanoes build the pressure that drives explosive eruptions.\u201d Pyroclastic flows are tied to explosive high-silica behavior, not gentle low-silica oozing."}
{"task": "blend", "mode": null, "source": "Python does not silently turn an int into a string when you use + with text. Convert the number with str() first, or use formatting.", "reference": "For string output, Python will not silently turn an int into a string inside a + with a str, so convert the number with str() or use formatting. Also remind learners to save the .py file, run it using the configured interpreter/command, verify the working directory, and install any required packages so Python can find code and dependencies."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. The underlying list stays `[10, 20, 30]` unless you index into the list directly. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. Also, the loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "teach", "source": "Floor division (//) computes the whole-number quotient and discards the remainder: 7 // 2 is 3. True division (/) returns a float quotient: 7 / 2 is 3.5. That is why the \u201c/ gives integer division because the operands look like whole numbers\u201d belief is wrong in Python 3; / always returns a float, and // is the operator that gives the whole-number result.", "reference": "Division and floor division in Python: true division (/) converts numbers to floats before dividing and always returns a float; floor division (//) computes the whole-number quotient, discarding the remainder. Reinforce with the anchor quotes: \"True division (/) converts numbers to floats before dividing\" and \"Floor division (//) computes the quotient, or the number of times divided\"."}
{"task": "blend", "mode": null, "source": "It prints 3 times. The loop expression is checked only at the top of each iteration; the rest of the current pass still runs before the condition is checked again. So the body runs while n > 1 is true at the top, then n is reduced and printed during that pass. After the third pass, the next top-of-iteration check sees n is no longer > 1 and the loop ends.", "reference": "Even if the body prints two lines each time, the key count is the four iterations, so the total output is eight lines. A while loop still needs to change something the condition depends on, such as `n = n - 1` or `+= 1`, so it can eventually become false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Correct: \u201cEach instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.\u201d a and b are two instances, each with its own attributes copy.", "reference": "`a` and `b` are separate instances, so changing one Dog's name does not change another Dog's name. The shared part is the methods defined by the class; the attribute values belong to each instance."}
{"task": "skin", "mode": "worked-example", "source": "This is the same problem as the textbook example: a survey emailed only to repeat buyers can overstate satisfaction because its provenance excludes the unhappy customers who already left. The collection method and purpose can bias what the data can support.", "reference": "Work through the customer-satisfaction example: a survey emailed only to repeat buyers can overstate satisfaction because the unhappy customers who already left are missing. Show how the collection method and purpose shape what the data can support. Ask students what that dataset can and cannot say before any conclusion is made."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "First, while-loop correctness needs general reasoning or comprehensive proofs/tests with clear coverage, not just empirical testing, because test results don\u2019t prove formal correctness. Also, the loop expression is checked only at the top of each iteration, so the current pass still finishes, and every while loop must change the condition variable, like n = n - 1 or += 1, so it eventually becomes false."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop expression is tested only at the top of each iteration, so the rest of the body still runs. The condition is not re-checked in the middle of the body the instant the variable changes. range(5) yields 0, 1, 2, 3, 4 \u2014 it starts at 0 and stops before the endpoint, giving exactly five values. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "skin", "mode": "reteach", "source": "Reteach for anyone who arrived cold: re-walk the exact same worked example trace `n = 3; while n > 0: print(n); n = n - 1` . Ask: (1) What is the loop expression? (2) When is it evaluated\u2014before the body or during the body? (3) What line makes progress? Then rewrite the loop with an intentional error (remove `n = n - 1`) and have students explain why the condition never becomes false (infinite loop).", "reference": "Reteach one worked example from the reading for students who arrived cold: n = 3; while n > 0: print(n); n = n - 1. Repeat the trace, but now explicitly label the \u201ccondition check moments\u201d (before iteration 1, before iteration 2, etc.). Then ask: \u201cWhere does the loop expression get tested again?\u201d Students answer: \u201cOnly after the current body finishes, at the top of the next iteration.\u201d Finish by highlighting the exact repair for infinite loops: \u201cEvery while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false.\u201d"}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop expression is tested only at the top of each pass, not in the middle of the body, so the rest of the current iteration still runs before the condition is checked again. Also, the loop variable is a fresh binding to each element in turn; assigning to it does not edit the container, so change list contents by indexing directly."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling'). Walk step-by-step through two moments in time: (1) temp = 105 \u2192 temp > 100 is True, so the if branch runs and else is skipped; (2) temp = 90 \u2192 condition is False, so else runs. Re-quote the guiding idea: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed\u201d and connect it to \u201cone and only one of the branches is taken.\u201d"}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "A while loop must change something the condition depends on, so a counter loop needs n = n - 1 (or += 1) inside the body until the condition eventually becomes false. The loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again. Separately, the loop variable is a fresh binding to each element in turn; assigning to it does not edit the container, so change list contents by indexing into the list directly."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, so a counter loop needs `n = n - 1` (or `+= 1`) inside the body. The loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again. Clarify that Big-O describes an asymptotic upper bound on growth as n increases (e.g., it ignores constants and lower-order terms); teach learners to compare growth rates and distinguish Big-O from exact timing."}
{"task": "blend", "mode": null, "source": "Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "The value you want is stored under the key `'name'`, so the correct access is `person['name']`. Since dictionaries are keyed (not position-based), use `d['name']` rather than `d[0]`."}
{"task": "blend", "mode": null, "source": "Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.", "reference": "A file object keeps track of its current position. Reading advances the position marker, so after the first `read()` reaches the end, the next `read()` returns an empty string unless you reopen the file or reset the position. That is why repeated `read()` calls do not reread the same contents automatically."}
{"task": "blend", "mode": null, "source": "According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.", "reference": "The text argues we should distinguish types of prose fiction; many earlier prose narratives fit romance conventions rather than the novel\u2019s realism. Here, realism, plausible people, and believable settings mark the novel, while romance leans toward extravagance and supernatural intervention."}
{"task": "skin", "mode": "reteach", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked-example reteach (non-reader path): Use the reading example \u201cif temp > 100: print('boiling') else: print('not boiling')\u201d. Walk through two cases: (1) temp = 105 \u2192 condition temp > 100 is True \u2192 the if-body runs. (2) temp = 80 \u2192 condition is False \u2192 the else body runs. Reinforce the anchor quote: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "blend", "mode": null, "source": "Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Dictionaries use keys, not numeric positions. To read the value under a key, use something like d['name'], rather than d[0]."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Two distinct solutions force infinitely many solutions over the reals: the entire line through those two points also satisfies the linear system. The \u201cexactly two\u201d idea is a known trap, and inconsistency means no solution at all. Only lines and planes through the origin are subspaces; others miss the zero vector and are not closed under scaling. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. If you need that total later, return it and store the returned value in a variable outside the function.", "reference": "Local variables exist only during the call that created them; here `total` stays local, but its value is returned and captured in `result`. That is the way to use a function result outside the function."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Classes are the blueprint and objects are the instances you build from that blueprint. In this reading, a class defines a type of object with attributes and methods. Make the distinction concrete: the class describes what the objects should have and do; each instance is a separate object created from the class."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A system like x + y = 1 and x + y = 3 is inconsistent, since the equations describe parallel lines that never meet. Over the real numbers, once a linear system has two distinct solutions it has infinitely many, because the whole line through the two solutions solves the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "Inside a function, local variables disappear when the call ends, so the outside code must capture a returned result. A loop variable is only a fresh binding to each element, not a way to edit the container; use direct indexing for list contents.", "reference": "Here the list stays the same because the loop variable is only a fresh binding to each element, so assigning to `n` does not edit the container. Every while loop must change something the condition depends on, and the loop expression is tested only at the top of each iteration. range(5) yields 0, 1, 2, 3, 4 \u2014 it starts at 0 and stops before the endpoint, giving exactly five values. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value."}
{"task": "skin", "mode": "reteach", "source": "Reteach worked example for cold arrivals (Expressions): revisit the reading\u2019s division and string-join surprises as a single story. First: 7 / 2 -> 3.5 because / returns a float in Python 3; use // to get 3. Second: 'Age: ' + 19 fails because you can\u2019t add a str and an int; fix it with 'Age: ' + str(19). Students translate each fix into the exact operator/function they need.", "reference": "Reteach worked example for cold arrivals (Expressions): to extend those fixes, we\u2019ll treat the reading\u2019s division and string-join surprises as one story. First: 7 / 2 -> 3.5 because / returns a float in Python 3; use // to get 3. Second: 'Age: ' + 19 fails because you can\u2019t add a str and an int; fix it with 'Age: ' + str(19). Students translate each fix into the exact operator/function they need."}
{"task": "blend", "mode": null, "source": "Distinct eigenvalues give eigenvectors that are automatically linearly independent, so two of them in a 2\u00d72 matrix give the needed two independent eigenvectors. The zero vector is not an eigenvector at all; it is excluded because eigenvectors must be nonzero. Eigenvectors are required to be nonzero \u2014 A0 = \u03bb0 holds trivially and identifies nothing about the matrix. A zero eigenvalue only means some nonzero vector is sent to zero \u2014 the matrix is singular, not necessarily zero.", "reference": "Distinct eigenvalues give eigenvectors that are automatically linearly independent, so two such vectors already give diagonalization in 2\u00d72. The zero-vector option is wrong because eigenvectors are required to be nonzero, and the zero-matrix claim is unrelated. Eigenvectors are required to be nonzero \u2014 A0 = \u03bb0 holds trivially and identifies nothing about the matrix. A zero eigenvalue only means some nonzero vector is sent to zero \u2014 the matrix is singular, not necessarily zero."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Because Python lists are zero-based, the first element is at index 0. So lst[0] is the first item and lst[1] is the second."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.", "reference": "When you define a function with `def`, the definition only stores the function; the body runs later when you call it by name (like `greet()`). Any values you create inside the function don\u2019t automatically live outside, so you typically return them if you need them after the call."}
{"task": "skin", "mode": "reteach", "source": "Worked example walkthrough (Lists + indexing): Use the provided names list example. Write: names = ['Ava', 'Ben', 'Cy'] then evaluate: names[0] is 'Ava', names[2] is 'Cy', and names[3] raises an IndexError. Emphasize the rule: indexes run from 0 to length minus 1, so the last valid index for a 3-item list is 2. Then extend to a 5-item list mentally (last index 4) and practice choosing the right index for the \u201cfirst\u201d element.", "reference": "Reteach worked example (lists): use names = ['Ava', 'Ben', 'Cy'] and walk it step-by-step. We verify names[0] is 'Ava' and names[2] is 'Cy'. Then we test the boundary: names[3] is out of range, so it raises an IndexError. We explicitly restate the rule: indexes run from 0 to length minus 1, so a 3-item list has last index 2."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop variable is a fresh binding to each element in turn, so assigning to it does not edit the container; to change list contents, index into the list directly. Every while loop must change something the condition depends on, and the loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "This overwrites: the key 'Ava' is unique in a dictionary, so grades['Ava'] = 97 replaces the previous value. The rule is: assigning to an existing key overwrites its previous value rather than adding a second entry.", "reference": "Dictionary keys are unique: assigning to an existing key overwrites its previous value instead of creating a second entry. That\u2019s why updating grades['Ava'] replaces the old value rather than keeping both."}
{"task": "blend", "mode": null, "source": "You run this code:\n\ndef square(n):\n    return n * n\n\nresult = square(5)\nprint(result)\nWhat prints?\n25, because square(5) calls the function body and returns n * n.\n5, because the function body only echoes the input value.\nn * n, because return prints the expression instead of computing it.\nNameError, because result is inside square and cannot be read outside.\nLocal variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. Defining a function only stores it; the body runs when the function is called by name, such as greet().", "reference": "square(5) calls the function, so the body runs and returns 25. The NameError idea fits a variable created inside a function, but result is created outside after the returned value is captured. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. Defining a function only stores it; the body runs when the function is called by name, such as greet(). range(5) yields 0, 1, 2, 3, 4 \u2014 it starts at 0 and stops before the endpoint, giving exactly five values."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "Because the condition is tested only at the top of each iteration, the body always runs once before the next check. In for-each loops, the loop variable is a fresh binding to each element, and assigning to it won\u2019t edit the container\u2014so to change list contents you must index into the list directly. Separately, a while loop must update the variables its condition depends on (e.g., decrement n) so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "readlines() returns a list with every line of the file. readline() gives only the next single line, and read() returns the whole file as one string.", "reference": "`readline()` returns the next single line, not the entire file. `read()` returns all contents as one string, and `readlines()` returns a list of every line."}
{"task": "blend", "mode": null, "source": "Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.", "reference": "read() returns the whole file as one string and advances through the file contents. After the first call reaches the end, a second read() returns an empty string unless the file is reopened or reset."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "In Python, the first element of a list is at index 0, so lst[0] is the first item and lst[1] is the second. This is different from counting \u201cposition 1\u201d as index 1. Remember that sequences are 0-based, like s[0] being the first character (e.g., 'cat'[0] == 'c')."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "For linear systems over the reals, the only possibilities are zero, one, or infinitely many solutions, so \u201cexactly two\u201d and any other finite number are impossible. A system like x + y = 1 and x + y = 3 is inconsistent because the equations are parallel lines that never meet; meanwhile, each elementary row operation is reversible and preserves the solution set, which is why elimination works."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Linear systems have only three possibilities: zero, one, or infinitely many solutions. The \u201cexactly two\u201d and \u201cfinite number\u201d beliefs both fail for linear systems over the reals, and a system may also be inconsistent. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Over the real numbers, once a linear system has two distinct solutions it has infinitely many, because the whole line through the two solutions solves the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "The def keyword defines a reusable function block, but it does not execute the indented body immediately. You only see the body\u2019s effects when the function is called by name, like greet()."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example (from the reading): Dog. Use the exact framing: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Show two separate instances conceptually and emphasize: each instance has its own attributes, while methods are shared. Concretely discuss what happens if one instance\u2019s name changes\u2014focus on \u201cEach instance carries its own copy of the attributes.\u201d Close by mapping terms: class \u2192 blueprint/type; instance \u2192 object created; attributes \u2192 data per instance; methods \u2192 behavior shared by the class."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, so a counter loop needs n = n - 1 or += 1 inside the body to make the condition eventually false. The loop expression is tested only at the top of each iteration, which means the current pass still runs before the next check."}
{"task": "blend", "mode": null, "source": "Dictionaries use keys, and assigning to an existing key overwrites the previous value. They cannot hold duplicate keys, so the old `95` is replaced rather than preserved. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Keys are unique; assigning to an existing key overwrites its previous value rather than adding a second entry. That is why the old 95 is replaced by 97 instead of being kept alongside it. Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0]."}
{"task": "skin", "mode": "reteach", "source": "Reteach the worked example for students who arrived cold: revisit the inconsistency worked example 'x + y = 1 and x + y = 3'. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using course vocabulary: the system has zero solutions, so it is inconsistent. Then link forward: when we translate systems into augmented matrices and row-reduce, the reduced echelon form (or a contradiction row) is what tells us we\u2019re in this 'inconsistent' case.", "reference": "For students who arrived cold, reteach the inconsistency example: x + y = 1 and x + y = 3. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using course vocabulary: the system has zero solutions, so it is inconsistent. Then connect forward: once we translate systems into augmented matrices and row-reduce, a contradiction row is what signals this 'inconsistent' case."}
{"task": "blend", "mode": null, "source": "Orthogonality is defined by the inner product being zero, not by how the vectors look in a picture. For nonzero vectors, that also gives linear independence. The \u201cperpendicular lines in a picture\u201d idea is too narrow; it misses higher-dimensional and non-graph settings.", "reference": "Orthogonality is defined by the inner product being zero, not by how the vectors look in a picture. That definition extends to functions and high-dimensional data where no picture exists. For nonzero vectors, orthogonality also gives linear independence. The picture-based belief is too narrow and misses the actual definition."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). The def keyword creates the reusable block, but it does not execute the indented body right away.", "reference": "Defining a function only stores it. The body runs later, when the function is called by name\u2014like calling greet(). Simply defining it never executes the indented block."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the core idea: a class defines a type of object with attributes (data) and methods (behavior). Use the anchor quote: \u201cA class is a blueprint for creating objects.\u201d Walk through vocabulary: class vs object vs instance. Emphasize: each object built from a class is an instance, and per-object data lives on the instance while methods defined by the class are shared across instances."}
{"task": "blend", "mode": null, "source": "The loop does not interrupt the body halfway through. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Correcting the claim: \u201cThe loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.\u201d So changes inside the body won\u2019t interrupt the already-running iteration."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A `while` loop only stops when its condition becomes false, so the code inside must change something the condition depends on. For a counter loop, update `n` in the body (e.g., `n = n - 1` or `n += 1`) so the condition eventually fails. Also, the loop condition is checked at the top of each iteration, not in the middle of the body. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "In a while loop, the condition is checked only at the top of each iteration, so changes made in the body affect the next check, not the current pass. Make sure every while loop changes something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false."}
{"task": "blend", "mode": null, "source": "According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.", "reference": "Correct answer: B. The text argues we should distinguish types of prose fiction; many earlier prose narratives fit romance conventions rather than the novel\u2019s realism. That means you don\u2019t label \u201cnovel\u201d just because a story is prose."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example reteach (walk-through): temp boiling example.\nCode: if temp > 100: print('boiling') else: print('not boiling').\nRun it for temp = 101, then temp = 100, then temp = 42. For each value, explicitly answer: is temp > 100 true or false, and therefore which branch runs? Close by restating: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed,\u201d and where else fits."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A counter loop must update the value the condition depends on\u2014like using n = n - 1 (or += 1)\u2014so the condition eventually becomes false. A while condition is checked only at the top of each iteration, not while the body is running. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Worked example reteach (walk-through): using the reading\u2019s conditional example. Start with `temp = 120`. Evaluate the condition `temp > 100` (true), so Python executes `print('boiling')` and skips the else branch. Then change `temp` to 80 and re-run the same conditional: the condition is false, so it executes the else body `print('not boiling')`. Conclude by restating: one and only one branch is taken."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A row of the form 0 = 5 is a contradiction, so the system is inconsistent. The tempting answer about infinitely many solutions confuses a free variable situation with an outright contradiction. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Over the real numbers, once a linear system has two distinct solutions it has infinitely many, because the whole line through the two solutions solves the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method. It minimizes the residual; the fitted values Ax\u0302 generally differ from b \u2014 being closest is not being equal."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "In a while loop, the condition is checked again at the top of the next iteration, so something inside the body must change the value the condition depends on. For counter loops, update n (e.g., n = n \u2212 1 or n += 1) so n eventually makes the condition false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.", "reference": "The loop condition is true at the start, so the body runs once. Then finished becomes True, and the next top-of-loop check ends the loop. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, so a counter loop needs n = n - 1 (or += 1) inside the body. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly. So `x = x + 1` leaves `nums` unchanged.", "reference": "The loop variable is a fresh binding to each element in turn, so x = x + 1 does not change nums. To change list contents, index into the list directly; reassigning the loop variable is the mistaken belief here. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "reteach", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Re-teach the reading\u2019s core concept by re-walking the same provided worked example: class Dog is the blueprint; then create a = Dog() and b = Dog() as two instances. Stop specifically to correct the two common confusions: (1) \u2018The class itself as an object and expect to store data on it\u2019 is wrong\u2014per-object data belongs on each instance, not on the class (so each dog instance has its own name attribute). (2) \u2018Two instances of the same class to share their attribute values\u2019 is wrong\u2014each instance has its own attributes, so changing one Dog\u2019s name does not change another Dog\u2019s name; only the methods are shared."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "A class is the blueprint; you create instances from it (e.g. d = Dog()), and per-object data lives on each instance, not on the class. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared. Also, every recursion needs a base case\u2014a simplest input solved directly without another call\u2014so the chain of self-calls eventually stops. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example: Walk the reading\u2019s example exactly\u2014\"if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false.\" Simulate two runs: temp = 120 (condition true \u2192 print 'boiling'); temp = 90 (condition false \u2192 print 'not boiling'). Tie back to: \"If the condition is true, the body is executed. If the condition is false, the body is not executed\" and to how else is defined for the false case."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The body changes `n`, so the loop expression eventually becomes false and the loop ends. The condition is re-checked at the top of each iteration, not in the middle of the body. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Warm-up: what problems do classes solve? Define: a class is a blueprint (type) that includes attributes (data/fields) and methods (behavior/procedures). Ground with the reading\u2019s anchor ideas: \u201cA class is a blueprint for creating objects\u201d and \u201cIndividual objects created of the class type are called instances.\u201d Emphasize the mental model: define once (class), then create many (instances)."}
{"task": "blend", "mode": null, "source": "Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Use a `with` statement or close the file when you\u2019re done so Python cleans it up automatically. That still works even if an error occurs."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "To keep a while loop from running forever, it must change something the condition depends on, such as n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is checked only at the top of each iteration, so the rest of the current pass still runs before it is tested again."}
{"task": "skin", "mode": "worked-example", "source": "People who have given up looking are classified as out of the labor force, not unemployed. That is why \u201crecount the rate including discouraged workers and ask whose situation each definition makes visible\u201d matters: the measured unemployment rate can look lower when discouraged workers are excluded.", "reference": "Worked example from the reading idea: \u201cIn a weak job market the official rate can fall simply because discouraged workers stop looking and drop out of the labor force.\u201d Step through: if workers become discouraged and stop searching, they move from the labor force to out of the labor force, so the measured unemployment rate can drop even though joblessness is still \u201cfelt.\u201d Quote in-class to anchor: \u201cpeople who have given up looking are classified as out of the labor force, not unemployed.\u201d"}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example: temperature check. Use the given template exactly: if temp > 100: print('boiling') else: print('not boiling'). Emphasize the anchor: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d Then point out: the else branch runs only when temp > 100 is false."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here, greet() is the step that triggers the code inside the function.", "reference": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). Here that means calling add_two(5) triggers the body and returns the computed value."}
{"task": "blend", "mode": null, "source": "Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.", "reference": "Reading moves a file position marker forward; once `read()` reaches end-of-file, a subsequent `read()` returns `''` until you reopen the file or reset the position. Also distinguish passing data back via `return` versus modifying shared mutable objects, rather than relying on default `None` behavior."}
{"task": "blend", "mode": null, "source": "Relative dating uses ordering, not numerical ages. Using the principle of superposition in an undisturbed sequence, \"the layers on the bottom are the oldest, and the layers above them are younger.\" So the bottom sandstone is older than the shale above it.", "reference": "Superposition says that in an undisturbed sequence, \u201cthe layers on the bottom are the oldest, and the layers above them are younger.\u201d So Unit B (sandstone) is older than Unit C (shale)."}
{"task": "blend", "mode": null, "source": "Distinct eigenvalues give eigenvectors that are automatically linearly independent, so two of them in a 2\u00d72 matrix give the needed two independent eigenvectors. The zero vector is not an eigenvector at all; it is excluded because eigenvectors must be nonzero. Eigenvectors are required to be nonzero \u2014 A0 = \u03bb0 holds trivially and identifies nothing about the matrix. A zero eigenvalue only means some nonzero vector is sent to zero \u2014 the matrix is singular, not necessarily zero.", "reference": "Distinct eigenvalues give linearly independent eigenvectors, and a 2\u00d72 matrix with two linearly independent eigenvectors is diagonalizable. The other choices add claims the theorem does not give. Eigenvectors are required to be nonzero \u2014 A0 = \u03bb0 holds trivially and identifies nothing about the matrix. A zero eigenvalue only means some nonzero vector is sent to zero \u2014 the matrix is singular, not necessarily zero."}
{"task": "skin", "mode": "reteach", "source": "Reteach pivot/row-operation reasoning with a targeted walkthrough of what students often mix up: show that elementary row operations do not change the solution set (swap, scale by nonzero, add multiple). Then show how pivot positions come from the echelon structure, not from \u201clargest entry\u201d thinking. Finish with a quick re-read: once you have echelon/RREF, pivot positions tell you which variables are basic and which are free.", "reference": "For students arriving cold: row reduction uses elementary row operations to simplify a system without changing its solution set. Swap rows, scale by a nonzero constant, or add a multiple of one row to another; each move is reversible and preserves solutions. In echelon form, pivot positions come from the elimination structure, and those pivots tell you which variables are basic or free."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example (Dog): Show the exact pattern from the reading: class Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute. Then demonstrate the consequence: change a.name and observe that b.name does not change, because each instance has its own attributes while the methods are shared. Quote/ground with: \"Each instance carries its own copy of the attributes\"."}
{"task": "blend", "mode": null, "source": "`split(',')` breaks the string at each comma and returns a list of substrings. That is the right tool when you need pieces from a delimited string.", "reference": "`split(',')` breaks the string at each comma and returns a list of the pieces. Here the result is `['red', 'green', 'blue']`. Emphasize that strings are immutable; show that s remains unchanged and the method returns a new string (e.g., s2 = s.replace(...); print(s); print(s2))."}
{"task": "blend", "mode": null, "source": "The passage argues that pedestalizing literature is harmful access-wise: \u201cwhen we put anything on a pedestal, we remove it from everyday life,\u201d and literature becomes something \u201cto be honored and respected, but it is not to be read\u201d by ordinary people. It also says scholarly barriers and \u201cyears of study\u201d can make it seem inaccessible.", "reference": "The passage argues against pedestalizing and barriers: \u201cwhen we put anything on a pedestal, we remove it from everyday life \u2026 so the corollary is that literature is to be honored and respected, but it is not to be read, certainly not by any normal person with normal interests.\u201d The corrective is that making it seem unreadable \u201cremoves it from everyday life\u201d and discourages engagement."}
{"task": "blend", "mode": null, "source": "Use a with statement so Python closes it automatically even if an error occurs. The corrective is: \"Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.\"", "reference": "Use the with statement. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "When a recursive function lacks a base case, the self-calls never stop and the call stack overflows. In classes, each instance has its own attributes, so changing one Dog's name does not affect another Dog's name; only the methods are shared.", "reference": "Recursion works only when the function moves toward a base case on a smaller input. The class-object option is the class-blueprint confusion: a class is the blueprint; you create instances from it, and data lives on each instance, not on the class. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared. Every recursion needs a base case \u2014 a simplest input solved directly without another call \u2014 so the chain of self-calls eventually stops. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached."}
{"task": "blend", "mode": null, "source": "`text` is local to `make_message()`, so it does not exist outside that call. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.", "reference": "word is a local variable, so it exists only during the call to make_message(). After the function finishes, word isn\u2019t defined outside, which is why print(word) fails; any value you need must be returned and captured by the caller."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.", "reference": "Defining a function only stores its code under a name; nothing inside runs yet. The function body executes only when you call it (for example, greet()). Simply writing def doesn\u2019t produce the function\u2019s output automatically."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Reteach the reading\u2019s core idea using one worked example: the condition controls which block runs, and only one branch is taken. Re-run the example if temp > 100: print('boiling') else: print('not boiling') and explicitly narrate both cases: if temp is 110, the if body executes; if temp is 90, the else body executes. Then connect this to boolean logic by restating the condition\u2019s job: the condition must evaluate to True or False for the branch decision."}
{"task": "blend", "mode": null, "source": "Without a base case, the self-calls never stop. That is exactly why recursion must have a simplest input solved directly without another call. Each recursive call must move toward the base case by shrinking the problem (e.g. n - 1), or the base case is never reached.", "reference": "Without a base case, recursion has no simplest input solved directly and the chain of self-calls never ends. Each recursive call must move toward the base case by shrinking the problem, such as n - 1, or the recursion can overflow the call stack."}
{"task": "blend", "mode": null, "source": "Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. In this case, the file object is already at the end, so there is nothing left to return.", "reference": "Since return is the only way to pass values back, you should explicitly return the value or update shared mutable objects when needed. That also lets you distinguish a real returned value from the default None. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (name it explicitly): the example is `with open('data.txt') as f: text = f.read()`.\n- Show the code line-by-line: with open(...) opens the file, assigns the file object to f.\n- f.read() reads the entire contents into `text` as one string.\n- The with block closes the file automatically when the block ends (even if an error occurs).\nConnect to the anchor quotes: \u201cThe read() function reads the contents of a file and returns a string\u201d and \u201cPython supports the opening of a file using the open() function\u201d.", "reference": "Walk through the provided worked example: with open('data.txt') as f: text = f.read(). Explain each part: open('data.txt') opens the file and gives you a file object f; f.read() reads the entire contents into the variable text; the with block closes the file automatically when the block ends. Anchor the idea with the quote: \u201cPython supports the opening of a file using the open() function\u201d."}
{"task": "blend", "mode": null, "source": "A while loop tests its expression at the top of each iteration. The rest of the current pass still runs before the condition is checked again, so it does not react in the middle of the body.", "reference": "A while-loop condition is checked at the top of each iteration, not in the middle of the body. The current pass still finishes before the loop expression is tested again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling'). Walk through two concrete cases: temp = 120 (condition is true \u2192 only the if body runs) and temp = 80 (condition is false \u2192 only the else body runs). Tie back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d Finish by pointing out that the else branch is exactly the complement case."}
{"task": "skin", "mode": "teach", "source": "Lists overview: show lists as ordered collections written with square brackets. Emphasize the key syntax: square brackets [] with comma-separated values, and that each element has an index that begins at 0. Use the anchor quote: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d. Briefly preview: we\u2019ll do direct indexing, then iteration with a for loop.", "reference": "Lists as ordered collections: introduce list syntax with square brackets and comma-separated values. Emphasize that each element has an index starting at 0, and that you access elements directly using an index. Anchor with: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d and \u201cIndexes begin at 0 and end at one less than the length of the sequence.\u201d"}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Introduce classes as blueprints and objects as instances. Emphasize that a class defines a type of object with attributes and methods, and that each object built from it is an instance. Use the course language directly: a class is the blueprint for creating objects, and each instance carries its own copy of the attributes while methods are shared."}
{"task": "blend", "mode": null, "source": "`read()` returns the entire contents as a single string, and the `with` block closes the file automatically. `readline()` gives only one line, and `readlines()` returns a list of lines, not one string. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs.", "reference": "`read()` returns the entire contents as a single string, and the `with` block closes the file automatically. A list of lines comes from `readlines()`, not `read()`. Reinforce that integration can introduce new defects; require tests/checks for each component and end-to-end scenarios, plus documentation of inputs/outputs and how to run the program. Reading advances a position marker through the file; once read() reaches the end, a second read() returns '' until the file is reopened or the position is reset. Always close the file when done, or open it in a with statement so Python closes it automatically even if an error occurs."}
{"task": "blend", "mode": null, "source": "`=` is assignment, not equality. Python evaluates the right side first, then binds the result to the name on the left, so `x` becomes 5.", "reference": "Assignment is not equality. Python evaluates the right side first, then binds the result to the name on the left, so this statement increases x by one."}
{"task": "blend", "mode": null, "source": "In a dictionary, keys are unique, so assigning to an existing key overwrites its previous value rather than adding a second entry. Also, `str` methods return a new string, so use forms like `s = s.upper()` or `tmp = s.upper()` and verify them with prints.", "reference": "Dictionary keys are unique, so assigning a new value to an existing key overwrites the previous value rather than creating a second entry. Also, `str` methods like `upper()` return a new string, so use `s = s.upper()` (or assign to a variable) and verify with prints."}
{"task": "skin", "mode": "reteach", "source": "Reteach the worked example for students who arrived cold: revisit the inconsistency worked example 'x + y = 1 and x + y = 3'. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using course vocabulary: the system has zero solutions, so it is inconsistent. Then link forward: when we translate systems into augmented matrices and row-reduce, the reduced echelon form (or a contradiction row) is what tells us we\u2019re in this 'inconsistent' case.", "reference": "Reteach for students who arrived cold: revisit the inconsistency example 'x + y = 1 and x + y = 3'. Restate the argument: both equations require the same left-hand side x + y to equal two different numbers, which is impossible. Re-label the conclusion using our course vocabulary: the system has zero solutions, so it is inconsistent. Then link to row reduction: after we translate systems into augmented matrices and row-reduce, the reduced echelon form (or a contradiction row) is what signals this 'inconsistent' case."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. In Python, lists and other sequences use zero-based indexing, which means the first element is always at position 0 rather than 1. A simple example is s[0] for the first character, such as 'cat'[0] == 'c', which shows how indexing works across sequences."}
{"task": "blend", "mode": null, "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Treat a class as the blueprint\u2014create instances from it (e.g., d = Dog()), and keep per-object data on each instance rather than on the class. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared."}
{"task": "skin", "mode": "worked-example", "source": "`a = Dog()` and `b = Dog()` create two instances from the class blueprint. The worked example says each instance has its own `name` attribute. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "Worked example: Dog class + two instances. Start from the given anchor idea: \u201cclass Dog: defines the blueprint; a = Dog() and b = Dog() are two instances, each with its own name attribute.\u201d Show: set a.name = 'Rex' and b.name = 'Milo' and explain why one change does not change the other (attributes per instance; methods shared). Tie to misconception fixes for storing data on the class and sharing attribute values across instances."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example (for Week 3): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d"}
{"task": "blend", "mode": null, "source": "The midterm scope is based on the course\u2019s covered objectives and practice problems, not only brand-new topics. Review materials are aligned with prior homework/lab question formats and concepts, so the best plan is to practice those earlier problem types and topics.", "reference": "That belief does not match how the exam is set up. Point out that midterm scope is based on the course\u2019s covered objectives and practice problems; review materials are aligned with prior homework/lab question formats and concepts. So earlier course work matters here."}
{"task": "blend", "mode": null, "source": "Dictionaries are accessed by key, not position; use d['name'] to get the value stored under the key 'name', not d[0].", "reference": "Dictionaries are accessed by key, not by position. Use d['name'] to retrieve the value stored under the key 'name'; doing d[0] would refer to a different access pattern, not the 'name' entry."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A while loop must change something the condition depends on, so a counter loop needs `n = n - 1` or `+= 1` inside the body. Also, the loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the next check."}
{"task": "blend", "mode": null, "source": "Inside a function, local variables disappear when the call ends, so the outside code must capture a returned result. A loop variable is only a fresh binding to each element, not a way to edit the container; use direct indexing for list contents.", "reference": "Local variables exist only during the call that created them, so the caller cannot read them directly. To use a result outside, the function must return it and the caller must capture the returned value. Moving total outside would change the scope, but the clean fix is to return the value from the function and capture the returned value in the caller. Every while loop must change something the condition depends on; a counter loop needs n = n - 1 (or += 1) inside the body so the condition eventually becomes false. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly."}
{"task": "skin", "mode": "teach", "source": "Lists overview: show lists as ordered collections written with square brackets. Emphasize the key syntax: square brackets [] with comma-separated values, and that each element has an index that begins at 0. Use the anchor quote: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d. Briefly preview: we\u2019ll do direct indexing, then iteration with a for loop.", "reference": "Lists quick start: show a list written with square brackets and comma-separated values, then emphasize it\u2019s an ordered collection with an index starting at 0. Anchor with: \u201cA list is defined by using square brackets [] with comma separated values within the square brackets\u201d. Briefly contrast \u201ccounting\u201d vs \u201cindexing\u201d (0-based)."}
{"task": "blend", "mode": null, "source": "Always close the file when done, or use a with statement so Python closes it automatically even if an error occurs.", "reference": "Always close the file when you\u2019re finished, or use a with statement so Python closes it automatically even if an error happens. This matters for correctness and resource safety."}
{"task": "blend", "mode": null, "source": "Magma is molten rock below the surface; once it erupts onto the surface it is called lava, and the two cool at very different rates. Keep the location in mind first, because that is what separates the terms.", "reference": "\u201cMagma is molten rock below the surface; once it erupts onto the surface it is called lava, and the two cool at very different rates.\u201d Use location + eruption context to choose the term, not just \u201cmolten rock.\u201d"}
{"task": "blend", "mode": null, "source": "Each comparison must be written with its own operands joined by `and`\u2014for example, `age > 13 and age < 19`. Leaving a fragment like bare `< 19` doesn\u2019t make sense because it has nothing on its left. Finally, in Python 3 `/` always produces floats (`7 / 2` is 3.5), so use `//` for whole-number floor division.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19, because the bare < 19 has nothing on its left. Separately, Python 3 makes / return a float, so 7 / 2 is 3.5; use // for whole-number floor division, which gives 3."}
{"task": "blend", "mode": null, "source": "Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.", "reference": "Each comparison needs its own left and right operand, joined with and. The version with bare '< 19' leaves the second comparison incomplete."}
{"task": "blend", "mode": null, "source": "'Age: ' + 19 raises a TypeError because Python will not silently turn an int into a string inside a + with a str. Convert the number with str() first, or use formatting: 'Age: ' + str(19) builds 'Age: 19'.", "reference": "When you build text with +, Python won\u2019t automatically convert an int to a string. Convert the number first with str() or use formatting, so 'Age: ' + str(19) works while 'Age: ' + 19 raises a TypeError."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "To avoid an infinite while loop, the body must change something the condition depends on, such as n = n - 1 or += 1 for a counter loop. The loop expression is checked only at the top of each iteration, so the rest of the current pass still runs before it can become false."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system. Connect to the key idea that row reduction can reveal inconsistent vs consistent once we encode the system as an augmented matrix.", "reference": "Worked example (in-class): determine consistency by reasoning about two equations with the same left-hand side. Consider the system: x + y = 1 and x + y = 3. Show that it cannot have any solution because the same expression x + y would have to equal both 1 and 3 at the same time. Conclude: no solution \u2192 inconsistent system, and note that row reduction can reveal this consistent vs inconsistent distinction once we encode the system as an augmented matrix."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "A system like x + y = 1 and x + y = 3 is inconsistent because the equations describe parallel lines that never meet. Over the real numbers, if a linear system has two distinct solutions, then it actually has infinitely many, since the whole line through them also solves it. That is why each elementary row operation is reversible and preserves the solution set, making elimination valid."}
{"task": "skin", "mode": "teach", "source": "Consistency vs. inconsistency: define consistent as \u201cat least one solution exists,\u201d inconsistent as \u201cno solution exists.\u201d Then connect to augmented matrices: every linear system can be represented compactly as an augmented matrix and solved by row reduction.", "reference": "Close with systems of linear equations. A solution must satisfy every equation simultaneously. Consistent means at least one solution; inconsistent means none. A linear system has exactly zero, one, or infinitely many solutions, never exactly two, and every linear system can be represented as an augmented matrix and solved by row reduction."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "The loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again. That\u2019s why a body update to `n` affects the next iteration, and why every while loop must change something the condition depends on."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the core picture: a class is a blueprint, and each object built from it is an instance. Emphasize attributes (data) vs methods (behavior). Anchor on: \u201cA class is a blueprint for creating objects\u201d and \u201cEach instance carries its its own copy of the attributes.\u201d"}
{"task": "blend", "mode": null, "source": "Every comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is incomplete.", "reference": "Each comparison needs its own operands joined by and, as in age > 13 and age < 19. The bare < 19 has nothing on its left, so it is not a complete comparison."}
{"task": "blend", "mode": null, "source": "The check happens at the top of each pass, not in the middle of the body. So the loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again.", "reference": "The loop expression is tested at the top of each iteration. Changing a variable inside the body does not interrupt the current pass; the rest of that pass still runs before the next check."}
{"task": "blend", "mode": null, "source": "Indexes run from 0 to length minus 1, so in a 5-item list the last index is 4. That means lst[4] is valid, while lst[5] is out of range.", "reference": "List indexing starts at `0`, so the third item is at index `2`, not index `3`. That\u2019s why `names[3]` can be out of range here, and in general the last valid index is `length - 1` (for five items, that\u2019s `4`), so `lst[5]` is invalid."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "To keep a while loop from running forever, every pass must change something the condition depends on, such as n = n - 1 or += 1. The loop expression is tested only at the top of each iteration, so the rest of the current pass still runs before the condition is checked again."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on, so a counter loop needs `n = n - 1` (or `+= 1`) inside the body to make the condition eventually false. The loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before it is checked again."}
{"task": "blend", "mode": null, "source": "Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. If you need that total later, return it and store the returned value in a variable outside the function.", "reference": "Correct. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. That is the safe way to reuse the computed total later."}
{"task": "blend", "mode": null, "source": "`b.name` stays separate. Each instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.", "reference": "The corrective is: \u201cEach instance has its own attributes, so changing one Dog's name does not change another Dog's name; only the methods are shared.\u201d So updating a.name does not change b.name unless you explicitly modify b\u2019s attribute."}
{"task": "blend", "mode": null, "source": "An if statement runs its body only when the condition is true and skips it when the condition is false. The else clause is the part that runs when the if condition is false.", "reference": "Since the if condition is false, the body after if is skipped and the else clause runs instead. An if statement runs its body only when the condition is true, and the else body runs when the condition is false."}
{"task": "blend", "mode": null, "source": "`text` is local to `make_message()`, so it does not exist outside that call. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.", "reference": "This is a scope rule. Local variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value. Since `message` is created inside `make_message()`, `print(message)` outside the function cannot access it."}
{"task": "blend", "mode": null, "source": "Over the real numbers, if a linear system has two distinct solutions, then it has infinitely many, because every point on the line through those two solutions also satisfies the system. A system like x + y = 1 and x + y = 3 is inconsistent: the equations describe parallel lines that never meet. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method.", "reference": "Both equations have the same left-hand side but different constants, so the lines are parallel and never meet. That makes the system inconsistent, so it has no solution; the \u201cevery system of equations has a solution\u201d belief fails here. Over the real numbers, once a linear system has two distinct solutions it has infinitely many, because the whole line through the two solutions solves the system. Each elementary row operation is reversible and preserves the solution set \u2014 that is exactly why elimination is a valid solving method."}
{"task": "blend", "mode": null, "source": "Not quite\u2014weathering breaks rock down in place; erosion is the separate step that carries the broken material away. A rock can weather without eroding if nothing transports the pieces, so the two are related but not synonyms.", "reference": "The corrective is: Weathering breaks rock down in place; erosion is the separate step that carries the broken material away. A rock can weather without eroding if nothing transports the pieces. That is why \u201cWeathering and erosion are synonyms\u201d is not correct."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, so the first option is not valid Python.", "reference": "Each comparison needs its own operands joined by and, such as age > 13 and age < 19. A bare < 19 has nothing on its left, so it is not valid Python condition syntax."}
{"task": "blend", "mode": null, "source": "A while loop has to update the variable that the loop condition depends on; with a counter, do n = n - 1 (or n += 1) inside the body so the condition eventually becomes false. Also, the loop condition is checked only at the start of each iteration, so the rest of the current pass still runs. The loop variable is a fresh binding to each element in turn; assigning to it does not edit the container. To change list contents, index into the list directly.", "reference": "The loop expression is checked only at the top of each iteration, so the body for the current pass runs fully before the condition is evaluated again. Also, the loop variable is a fresh binding for each element, so assigning to it doesn\u2019t modify the original list; to change list contents, assign by index into the list."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "Python lists are zero-based, so the first element is at index 0 and the second element is at index 1. That\u2019s why `lst[0]` is the first item and `lst[1]` is the second."}
{"task": "skin", "mode": "teach", "source": "Worked example (from the reading): the Dog class. Show: class Dog: defines the blueprint; then create a = Dog() and b = Dog(). Connect it directly to attributes: each instance has its own name attribute. Clarify with a quick mental trace: if you change a.name, b.name does not change.", "reference": "Worked example: the Dog class. Use the reading\u2019s worked example: `class Dog:` defines the blueprint; `a = Dog()` and `b = Dog()` are two instances, each with its own `name` attribute. Show how you might set `a.name` and `b.name` and discuss what changes (per-instance attributes) and what stays shared (methods). Include the key anchor quote: \u201cEach instance carries its own copy of the attributes.\u201d"}
{"task": "skin", "mode": "activity", "source": "Matrix algebra quick-checks that feed invertibility: (a) identity on both sides matters, (b) order matters in products, (c) undoing composed operations reverses order. Students do short prompts: given a product AB, write the correct formula for (AB)\u207b\u00b9 when both inverses exist; and compare AB vs BA with a reason why they might differ.", "reference": "Quick matrix-algebra checks that support invertibility this week: (a) identity on both sides matters, (b) order matters in products, (c) undoing composed operations reverses order. Students do short prompts: given a product AB, write the correct formula for (AB)\u207b\u00b9 when both inverses exist; and compare AB vs BA with a reason why they might differ."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the core mental model: a class is a blueprint that defines a type of object with attributes (data) and methods (behavior). Use the anchor quotes to frame language precisely: \u201cA class is a blueprint for creating objects\u201d and \u201cIndividual objects created of the class type are called instances.\u201d Emphasize that the attributes live on each instance; the methods defined by the class are shared."}
{"task": "blend", "mode": null, "source": "According to the text, we should distinguish types of prose fiction. Many earlier narratives follow romance conventions, not the realism of a novel.", "reference": "This corrects the misconception using the course\u2019s key claim: \u201cThe text argues we should distinguish types of prose fiction; many earlier prose narratives fit romance conventions rather than the novel\u2019s realism.\u201d Therefore, earlier English prose narratives can be romance rather than novel, even if they\u2019re still prose narratives."}
{"task": "blend", "mode": null, "source": "Defining a function only stores it; the body runs when the function is called by name, such as greet(). So def greet(): sets up the function, but it does not print anything until you call it.", "reference": "Defining a function only stores it; the body runs when the function is called by name, such as `greet()`. So `def greet(): ...` just creates the function object, and the print happens later when you call it."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (name it explicitly): the example is `with open('data.txt') as f: text = f.read()`.\n- Show the code line-by-line: with open(...) opens the file, assigns the file object to f.\n- f.read() reads the entire contents into `text` as one string.\n- The with block closes the file automatically when the block ends (even if an error occurs).\nConnect to the anchor quotes: \u201cThe read() function reads the contents of a file and returns a string\u201d and \u201cPython supports the opening of a file using the open() function\u201d.", "reference": "Worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d\n- What the with block does (file closes automatically)\n- What f.read() returns (a single string with the entire contents)\n- Where the data ends up (assigned into text)\nAnchor: \u201cThe read() function reads the contents of a file and returns a string\u201d."}
{"task": "skin", "mode": "reteach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Quick re-teach (cold start friendly): What is a class vs an object? Anchor quotes: \u201cA class is a blueprint for creating objects\u201d and \u201cIndividual objects created of the class type are called instances.\u201d Use a tiny example: class Dog: ...; then a = Dog() and b = Dog(). Emphasize: instances carry their own attribute data; methods are shared."}
{"task": "blend", "mode": null, "source": "Because a while loop tests its condition only at the top of each iteration, the body must change something the condition depends on. For a counter loop, use n = n - 1 or += 1 inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "A `while` loop re-evaluates its condition before each new iteration, not right after a variable changes. The key is that the loop body must update something the condition depends on; otherwise the condition may never become false. In counter loops, that often means updating the counter (e.g., `n = n - 1` or `n += 1`) inside the body. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (from the reading): if temp > 100: print('boiling') else: print('not boiling') \u2014 the else branch runs only when temp > 100 is false. Trace it with two temperatures (e.g., temp = 101 and temp = 99) and explicitly state which branch is executed in each case, tying back to: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed.\u201d", "reference": "Worked example: step through the provided code using the reading\u2019s exact example: if temp > 100: print('boiling') else: print('not boiling'). Walk through both cases: if temp > 100 is true, the else body is skipped; if temp > 100 is false, the else branch runs. Anchor with: \u201cIf the condition is true, the body is executed. If the condition is false, the body is not executed\u201d and \u201cAn else statement is used with an if statement and contains a body of statements that is executed when the if statement's condition is false\u201d."}
{"task": "skin", "mode": "worked-example", "source": "Worked example (functions): use the provided example def square(n): return n * n, then connect to calls: square(5) runs the body and returns 25. Reinforce: defining stores the function; calling produces the output. Mention what happens if you try to use an internal variable name outside\u2014scope prevents access.", "reference": "Worked example: use the provided anchor example exactly: def square(n): return n * n. Show (1) the definition creates a reusable name, (2) square(5) runs the body and returns 25, and (3) the variable n is only meaningful during the call. Then connect to scope: if you try to use n outside the call, it doesn\u2019t exist there because local variables exist only during the call that created them."}
{"task": "blend", "mode": null, "source": "Calling read() consumes the file contents as it reads, so after the first read() reaches the end of the file, a second read() on the same open file can return an empty string until you reopen the file or reset the position.", "reference": "After the first `read()`, the file position advances to the end, so a second `read()` returns an empty string until the file is reopened or reset. The tempting wrong answer is that `read()` re-reads from the beginning automatically, but it does not."}
{"task": "blend", "mode": null, "source": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses zero-based indexing for lists, not position 1 for the first item.", "reference": "The first element is at index 0, so lst[0] is the first item and lst[1] is the second. Python uses 0-based indexing for sequences, so s[0] is the first character, as in 'cat'[0] == 'c'. This example shows why the first position is accessed with 0, not 1."}
{"task": "skin", "mode": "reteach", "source": "For and, both parts of the compound condition must evaluate to True. Write each comparison fully, such as age > 13 and age < 19, rather than leaving the second comparison without its left operand.", "reference": "Reteach with a single worked correction: build the compound age condition carefully. Start with the incorrect student belief: \u201cif age > 13 and < 19 is valid.\u201d Then show the repaired logic step-by-step: you must repeat the left operand each time, so write: age > 13 and age < 19. Reinforce the corrective idea: \"Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left.\" Finish by running through one example value (age = 15 \u2192 true; age = 20 \u2192 false)."}
{"task": "blend", "mode": null, "source": "Those traits line up with romance in the chapter\u2019s distinction. The novel is tied to realistic people and situations; romance is the form with implausible characters, imaginative settings, extravagant action, and supernatural interventions.", "reference": "The chapter\u2019s romance yardsticks say romance \u201ctend[s] toward extravagant plots and rely on supernatural interventions.\u201d So when those features dominate (extravagant action + supernatural interventions), romance conventions provide the best match\u2014rather than the novel\u2019s emphasis on realistic people in realistic settings."}
{"task": "blend", "mode": null, "source": "x = x + 1 is the assignment form Python uses here. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one.", "reference": "The assignment uses the current value of x, adds 1, and stores the result back in x. In Python = is assignment, not equality: the right side is evaluated first and the result is bound to the name on the left, so x = x + 1 simply increases x by one."}
{"task": "blend", "mode": null, "source": "Every while loop must change the variable that the condition depends on, otherwise it may never become false. For a counter, update n in the body (e.g., n = n - 1 or n += 1) so the condition eventually fails. The loop expression is tested only at the top of each iteration; the rest of the current pass still runs before the condition is checked again.", "reference": "Every while loop must change something the condition depends on; a counter loop needs `n = n - 1` or `+= 1` inside the body so the condition eventually becomes false. The loop expression is tested only at the top of each iteration, and the rest of the current pass still runs before the condition is checked again."}
{"task": "skin", "mode": "reteach", "source": "Reteach reading core concept using the same worked example: \u201cwith open('data.txt') as f: text = f.read()\u201d. Then explicitly connect it to the position marker idea:\n- First f.read() consumes the file contents and returns a string.\n- A second call to read() at that point returns '' because the position has advanced to the end.\n- To see the file again, you\u2019d need to reopen the file or reset the position.\nAnchor corrective explicitly with the reading/position marker explanation.", "reference": "Reteach worked example: Use the same file workflow as earlier, but add the \u201cread twice\u201d situation. Students start with: with open('data.txt') as f: a = f.read(); b = f.read(). Walk through the position marker idea (first read consumes the contents; second read returns '') and then compare alternatives: reopen the file (or reset the position, if they\u2019re familiar) to get content again. Keep the anchor at the center: \u201cThe read() function reads the contents of a file and returns a string\u201d."}
{"task": "skin", "mode": "teach", "source": "Variables created inside a function have local scope. The corrective rule is: \u201cLocal variables exist only during the call that created them; to use a result outside, the function must return it and the caller must capture the returned value.\u201d", "reference": "Local vs global scope with direct comparisons: define a function that creates a variable inside it; show that accessing it outside fails, and explain why using the anchor quotes about scope. Reinforce the core corrective: Local variables exist only during the call that created them; to use a result outside, return it and capture the returned value."}
{"task": "skin", "mode": "reteach", "source": "Worked example from the reading (if-else): if temp > 100: print('boiling') else: print('not boiling'). Walk through what happens when temp = 101 (condition true \u2192 if branch) and when temp = 99 (condition false \u2192 else branch). Tie to the quote: \u201cWhen an if-else statement is executed, one and only one of the branches is taken.\u201d", "reference": "Reteach with the same worked example: if temp > 100: print('boiling') else: print('not boiling'). Run through it step-by-step for two cases: temp = 101 (condition is true \u2192 if body runs; else is skipped) and temp = 99 (condition is false \u2192 else body runs; if body is skipped). Re-ground the idea that exactly one branch is taken."}
{"task": "skin", "mode": "teach", "source": "A class is the blueprint for creating objects. Individual objects created from a class are called instances, and each instance carries its own copy of the attributes while the methods are shared by every instance.", "reference": "Start with the core picture: a class defines a type of object with attributes (data) and methods (behavior). Emphasize \u201cblueprint\u201d and \u201cinstance.\u201d Anchor the wording on the program idea: \u201cA class is a blueprint for creating objects.\u201d Then map terminology: attributes live on objects; methods are the behavior defined by the class and shared by instances."}
{"task": "blend", "mode": null, "source": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left. Repeating `age` makes both parts complete.", "reference": "Each comparison needs its own operands joined by and: age > 13 and age < 19. The bare < 19 has nothing on its left, and the single = option confuses comparison with assignment."}
